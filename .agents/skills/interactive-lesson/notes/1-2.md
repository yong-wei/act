# 1-2 课程实现笔记

## 课程信息

- 课次：1-2
- 标题：建模——从真实对象到可分析的系统
- 当前主线入口：`/interactive-learning/courses/unit-1-2-modeling-from-object-to-system`
- 当前预设键：`unit-1-2-modeling-from-object-to-system-v1`
- 当前资源键：`unit-1-2-modeling-from-object-to-system`
- runtime 真源：`course-content/runtime/lessons/1-2/interactive-manifest.json`
- 设计源：
  - `course-content/authoring/lessons/1-2/design/1-2-interactive-page.md`
  - `course-content/authoring/lessons/1-2/design/1-2-interactive-contract.yaml`
  - `course-content/authoring/lessons/1-2/design/1-2-interactive-design-acceptance.json`

## 当前状态

- 当前实现目标是新主线 14 步建模课程，不恢复旧版“系统结构图与化简”，不使用旧 17 步方案。
- 旧版 `unit-1-2-block-diagram-simplification` 仍保持不可解析，不应重新登记为 premium route、preset 或 AI context。
- 课程页面采用 manifest-first 模式：入口页、学生页、教师页、等待页、课程头部、步骤内容和活动面板都由 runtime manifest 派生。
- 缺失 `1-2-handout.pdf` 时入口页不得暴露破损静态 PDF 链接；当前依赖 runtime entry 的动态/可用资源策略。

## 14 步主线

| 步骤 | 阶段 | 标题 | 互动形态 |
|---|---|---|---|
| step-01 | B | 导入——不同对象的行为差异与共性 | 阅读 |
| step-02 | O | 课程目标 | 阅读 |
| step-03 | P1 | 前测——进入建模专题前的准备 | 题组提交 |
| step-04 | P2 | 建模的两条路径 | `visual.stage` 双路径舞台 |
| step-05 | P2 | 微分方程——物理对象的第一次翻译 | `visual.derivationStage` 教师显影 |
| step-06 | P2 | 传递函数——从微分运算到代数运算 | 单选 |
| step-07 | P2 | 方框图——系统的结构表达 | `visual.blockDiagram` 结构比较提交 |
| step-08 | P2 | 信号流图——变量间的决定关系 | `visual.signalFlowGraph` + 单选 |
| step-09 | P2 | 极点的几何来源——三维幅值曲面 | `static-surface-3d` |
| step-10 | P2 | 拖动极点看响应——行为地图的互动验证 | 共享控制工作台参数提交 |
| step-11 | P2 | 案例——三艘船，三种行为 | 共享控制工作台 + `visual.annotatedMedia` 与单选 |
| step-12 | P2 | 例题——从特征方程到行为判断 | `visual.derivationStage` 例题显影与单选 |
| step-13 | P3 | 后测——检验本次课程目标达成 | 题组提交 |
| step-14 | S | 总结——建模专题的局部地图 | `visual.stage` 总结地图与统计 |

## 实现文件

- 课程定义：`src/lib/unit-1-2-course.ts`
- AI 上下文：`src/lib/unit-1-2-ai-contexts.ts`
- 入口页：`src/app/interactive-learning/courses/unit-1-2-modeling-from-object-to-system/page.tsx`
- 教师端：`src/app/interactive-learning/courses/unit-1-2-modeling-from-object-to-system/teacher/[sessionId]/page.tsx`
- 学生端：`src/app/interactive-learning/courses/unit-1-2-modeling-from-object-to-system/student/[sessionId]/page.tsx`
- 等待页：`src/app/interactive-learning/courses/unit-1-2-modeling-from-object-to-system/teacher/[sessionId]/waiting/page.tsx`
- 课程组件：`src/features/interactive/unit-1-2-modeling-from-object-to-system/*`
- 教师预设：`src/features/teacher/preset-lessons/presets/unit-1-2-modeling-from-object-to-system.ts`
- 共享能力扩展：`src/features/interactive/shared/manifest-runtime/content-renderers.tsx`

## 视觉组件、3D 与控制工作台合同

- 第 4 步 `modeling-path-stage` 必须通过 `visual.stage` 渲染双路径舞台，呈现“机理建模路径”和“数据辨识路径”的空间对照，不得回退为方块条或静态截图。
- 第 5、12 步必须通过 `visual.derivationStage` 渲染非线性显影、公式分块、教师显影控制和 LaTeX 公式，不得把推导链降级为卡片堆叠或图片。
- 第 7 步 `closed-loop-structure` 必须通过 `visual.blockDiagram` 消费结构化节点、边、端口、求和点和显影计划；学生的结构比较必须形成共享提交证据。
- 第 8 步 `signal-flow-graph` 必须通过 `visual.signalFlowGraph` 消费节点、支路、路径、回路和梅森项映射；梅森公式项需要能回指图中路径或回路。
- 第 11 步 `ship-response-hotspots` 必须通过 `visual.annotatedMedia` 渲染响应曲线热点，热点标题和说明必须使用教学语义，不得暴露文件名、模块名或 payload key。
- 第 9 步 `magnitude-surface` 必须通过 manifest runtime 的 `compute.panel` + `static-surface-3d` 渲染。
- 浏览器或静态渲染验收 DOM 必须包含：`data-static-surface-3d-panel="magnitude-surface"`。
- 第 9 步曲面数据合同：`49x49` regular grid，`x=[-4,2]`，`y=[-3,3]`，`dx=dy=0.125`；极点标记为 `s=0` 与 `s=-B/J=-2`，标记高度高于截顶峰值。
- 第 9 步 WebGL 呈现需接近 MATLAB 读图方式：jet-like 色带、曲面网格线、底面坐标网格、三维坐标框和以曲面中心为目标的默认视角。
- 第 9 步 fallback 图片为：`/course-runtime/lessons/1-2/media/1-2-fig-08-magnitude-surface.png`。
- 第 10 步 `drag-pole-panel` 必须通过共享控制工作台 `compute.panel + capabilityRef="control-root-locus-design-map"` 渲染，DOM 标记为 `data-control-workbench-capability="control-root-locus-design-map"`。
- 第 11 步 `ship-simulation` 必须通过共享控制工作台 `compute.panel + capabilityRef="control-linked-comparison"` 渲染，DOM 标记为 `data-control-workbench-capability="control-linked-comparison"`。
- 第 10、11 步不得在 1-2 私有页面中绕过 manifest runtime，也不得降级为纯静态截图。
- `activity.panel` 等 activity runtime modules 不得在静态内容流中渲染成“互动任务”占位；活动入口由学生/教师 activity renderer 承接。
- 图片标题不得使用 module id、block key、panel id 或 spec key；合法英文标题如 `Root-Locus` 必须保留，不得被泛化清洗。
- `content.reveal` 的层级对象可能使用 `content` 字段承载正文，renderer 必须识别，避免 p12 类空白 reveal 模块。

## 验证记录

- `rtk python3 course-content/scripts/export_runtime.py 1-2`：通过。
- `rtk python3 .agents/skills/interactive-design/scripts/audit_interactive_manifest.py --lesson 1-2`：通过，14 steps、43 modules、0 issues。
- `rtk npm run test:unit -- src/features/interactive/__tests__/unit-1-2-course.test.ts src/features/interactive/__tests__/interactive-manifest-runtime.test.tsx src/features/interactive/__tests__/interactive-module-taxonomy.test.ts src/features/interactive/__tests__/interactive-module-registry-gate.test.ts src/features/interactive/__tests__/control-workbench-evidence.test.ts`：通过，5 files、149 tests。
- `rtk npm run test:course-data-quality-gates`：通过，4 files、88 tests。
- `rtk node artifacts/interactive-learning/unit-1-2-implementation-acceptance-2026-06-17/reports/browser-acceptance.mjs`：通过，覆盖入口页、学生 step-09/10/11、教师 step-10 控制、真实课堂学生提交、教师统计与结束课堂。
- `rtk npm run db:session-data-quality -- --session-id=cmqhiqwqg001cz7yfugvac2f8`：通过，qualityStatus green，requiredEvidenceCoverage=1，closure captured/materialized/summarized/cached 全为 true。
- `rtk python3 course-content/scripts/review_lesson_content.py --lesson 1-2 --skip-export --strict-implementation-contract`：通过。
- `rtk python3 .agents/skills/interactive-lesson/scripts/check_contract_alignment.py --lesson 1-2`：不适用于当前 1-2 manifest-first 主线；该脚本缺少 1-2 预设且面向旧式本地 `UNIT_*_PAGE_CONTRACTS` 常量，不能为通过脚本伪造私有页面合同。
- `rtk node artifacts/interactive-learning/unit-1-2-visual-contract-2026-06-18/reports/browser-visual-contract-audit.mjs`：通过，学生端覆盖 step-04/05/07/08/09/10/11/12/14，教师端覆盖 step-04/07/09/10/11/12，无阻断浏览器日志。
- 浏览器最终抽查：
  - 学生端：`artifacts/interactive-learning/unit-1-2-page-fixes-2026-06-17/student-final-dom-audit.json`
  - 教师端：`artifacts/interactive-learning/unit-1-2-page-fixes-2026-06-17/teacher-final-dom-audit.json`
  - 关键截图：`student-final-step-09.png`、`student-final-step-12.png`、`teacher-final-step-09.png`
  - 结果：p4/p6/p8/p9/p12/p14 学生端与 p4/p9/p12/p14 教师端均无 activity 占位、无渲染错误、无内部命名泄露；p9 WebGL 加载；p12 reveal 内容可见。

## 验收合同

- 实现验收合同：`course-content/authoring/lessons/1-2/notes/interactive-implementation-acceptance.json`
- 期望视觉稿位置：`course-content/authoring/lessons/1-2/design/1-2-interactive-page.md` 与 `course-content/authoring/lessons/1-2/design/1-2-interactive-contract.yaml`
- 当前现状截图位置：
  - 本轮视觉合同刷新：`artifacts/interactive-learning/unit-1-2-visual-contract-2026-06-18/screenshots/`
  - 本轮浏览器报告：`artifacts/interactive-learning/unit-1-2-visual-contract-2026-06-18/reports/browser-visual-contract-audit.json`
  - 早前完整课堂闭环：`artifacts/interactive-learning/unit-1-2-implementation-acceptance-2026-06-17/screenshots/`

## 当前剩余前提

- 本次实现不生成 PDF；缺失静态 `1-2-handout.pdf` 时入口页不得暴露破损下载链接。
- 外部预习资源 iframe/video 在浏览器验收中出现 cldisk 沙箱与 ORB 噪声，已归类为非阻断；真实教师端与学生端 live 上下文无浏览器错误。
- 后续新增 `interactive-figure` 规格时，应先扩共享 capability 合同与测试，不应在课程私有页面绕过 manifest runtime。
