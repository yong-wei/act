# 图形与缓存验证

ForceGraph `onZoom` 的 `x/y` 已是世界坐标中的视图中心，不能再次按原始 D3 平移换算。DOM 标签存在不证明 WebGL mesh 存在。相机回归应覆盖切换维度和改变视口后的恢复，必要时比较真实投影。浏览器标签验收检查与筛选栏等浮层的矩形交集；常驻控件从画布可用尺寸中排除。版本缓存的失败关闭验证覆盖构建、内存命中与磁盘命中的共同返回边界。

这些要点只适用于相应图形/缓存实现；使用前核实当前 API 与代码。
