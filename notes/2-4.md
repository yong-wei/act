# 2-4 互动课实现对照表

## 审计结论

- `course-content/authoring/lessons/2-4/design/interactive-contract.yaml` 与 `src/lib/unit-2-4-course.ts` 的 17 步页面契约已对齐，当前实现不需要整课重制。
- 本轮改动聚焦入口页 runtime-first 预习台、多媒体文案与课堂外资源追踪链路，而不是重写课堂步骤。

## 入口页与 runtime 资源

| 模块 | 人读稿 / runtime 真源 | 当前实现切入点 | 本轮处理状态 | 验证方式 |
|------|------------------------|----------------|--------------|----------|
| 入口页主视觉与课堂入口 | `design/interactive-page.md` 全课总览 + runtime `lesson.json` | `src/features/interactive/unit-2-4-nyquist-margin-entry/entry-page.tsx` | 严格实现 | 入口页源码断言 + 页面抽查 |
| 课前预习台多媒体 | `design/multimedia.md` + runtime `media/2-4-media.md` | `src/features/interactive/shared/lesson-entry-media-hub.tsx` 经 `entry-page.tsx` 接入 | 本轮已补齐 | Vitest 断言入口页必须渲染 `LessonEntryMediaHub` |
| 知识图谱 / 知识卡 / 讲义入口 | runtime `graph-overlay.json` + `handout.md` + `handout.pdf` | `src/features/interactive/shared/lesson-entry-runtime-sections.tsx` | 严格实现，讲义入口已改为共享预习台承接 | 入口页源码断言 `hideHandoutEntry` |
| 课堂外互动埋点 | 统一入口页资源追踪契约 | `useResourceInteractionTracking` 通过共享媒体中心与 runtime sections 承担 | 已接入共享链路 | 源码检索 `trackResourceOpen/play/progress/complete/download` 与知识追踪 |

## 双轨真源与实现主干

| 项目 | 真源 / 实现 | 当前状态 | 验证方式 |
|------|-------------|----------|----------|
| 步骤数量、顺序、标题 | `interactive-contract.yaml` ↔ `src/lib/unit-2-4-course.ts` | 严格实现 | Node 契约校验脚本全量通过 |
| 页面模板 / 区域 / 互动类型 | 同上 | 严格实现 | Node 契约校验脚本全量通过 |
| 教师聚合 / telemetry 摘要字段 / 预览路径 | 同上 | 严格实现 | Node 契约校验脚本全量通过 |
| runtime 媒体文案 | `course-content/runtime/lessons/2-4/media/2-4-media.md` | 已补导入视频标题说明并通过解析测试 | 单测断言 + 文档核对 |

## 统一约束

- 运行时代码真源为 `src/lib/unit-2-4-course.ts` 中的本地页面契约，并通过测试对齐作者态 `interactive-contract.yaml`。
- 真实学生页与 `/student/demo` 使用同一渲染链，不允许预览占位页。
- 入口页媒体资源统一消费 runtime `mediaResources` 与 `handoutPdfPath`，不在课程入口源码中硬编码外链或 `2-4-media.md` 路径。
- `2-4` 已通过入口页接入 `LessonEntryMediaHub`，并与 `LessonEntryRuntimeSections` 共同覆盖预习台、知识图谱、知识卡与讲义入口的课堂外资源行为。
