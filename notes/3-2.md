# 3-2 实现对照表

## 设计真源状态

- 双轨真源：`course-content/authoring/lessons/3-2/design/interactive-page.md` + `course-content/authoring/lessons/3-2/design/interactive-contract.yaml`
- runtime 审查：`course-content/runtime/lessons/3-2/review/*` 已齐备，可直接作为实现输入
- 当前发现的设计冲突：
  - `interactive-page.md` 的学生预览路径仍指向 `unit-3-2-routh-stability-feasible-region`
  - `interactive-contract.yaml` 的正式 `course_route_segment` 为 `unit-3-2-routh-stability-boundary`
  - 本轮先把 `interactive-page.md` 预览路径回对到契约真源，再按 `...stability-boundary` 落地

| 设计步骤 | 页面模板 / 区域 / 模块 | 契约互动类型 / 埋点 / 教师聚合 / AI | 当前实现位置或缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| step-01 | `map_hero_slide`；路径图、任务卡 | `none`；`viewed/timeOnStep/teacherFollowSync`；`view_count/sync_status`；AI 只做路径定位 | 缺 3-2 课程定义与静态首屏 | 严格实现 | 契约对齐测试 + 学生演示页手测 |
| step-02 | `figure_question_vote`；主图、三问、二选一 | `binary_choice`；`selectedOption/resultState/teacherRevealSeen`；`option_distribution/misconception_rate`；AI 只纠正“图像直觉已足够” | 缺 3-2 情境引入投票区 | 严格实现 | 契约对齐测试 + 学生/教师交互手测 |
| step-03 | `goal_boundary_slide`；目标卡、主线链、边界表 | `none`；`viewed/timeOnStep`；`view_count`；AI 只说明边界 | 缺静态承载组件 | 严格实现 | 契约对齐测试 |
| step-04 | `question_stack`；三题前测、提交条 | `quiz_group`；`attemptCount/resultState/errorBucket`；`question_distribution/top_misconceptions`；AI 只做错因归类 | 可复用 3-1/2-4 quiz 外壳，需换题目与误区 | 严格实现 | 契约对齐测试 + 提交链路手测 |
| step-05 | `table_construction_workspace`；对象卡、劳斯表骨架、结论卡、短答区 | `short_response`；`responseSubmitted/responseLength/timeOnStep`；`response_word_cloud/common_reason_tags`；AI 不代写短答 | 缺劳斯表静态承载与短答表单 | 严格实现 | 契约对齐测试 + 文本提交手测 |
| step-06 | `interval_workflow_workspace`；带参数对象、条件链、区间输入 | `interval_input`；`submittedRange/resultState/errorBucket`；`range_distribution/missing_condition_rate`；AI 只诊断漏条件 | 需新建区间输入表单和教师统计 | 严格实现 | 契约对齐测试 + 区间作答手测 |
| step-07 | `boundary_mapping_board`；极点迁移图、边界卡、对应区 | `triple_match`；`matchAttempts/resultState/timeOnStep`；`match_accuracy/boundary_confusion_tags`；AI 不进入根轨迹法则 | 可复用 triple match 外壳，需换成边界参数/根结构语义 | 严格实现 | 契约对齐测试 + 配对交互手测 |
| step-08 | `case_split_match`；双短例、方法卡、分类区 | `classification_drag`；`selectedBucket/resultState/retryCount`；`case_split_distribution/confusion_rate`；AI 只做“先辨识后处理”提醒 | 需新建分类拖拽/分桶表单 | 严格实现 | 契约对齐测试 + 分类交互手测 |
| step-09 | `method_card_with_completion`；辅助方程链、导数替换、填空区 | `formula_completion`；`completionState/resultState/timeOnStep`；`formula_error_hotspots/method_completion_rate`；AI 不把本页变成刷题器 | 需新建公式填空表单 | 严格实现 | 契约对齐测试 + 填空交互手测 |
| step-10 | `response_compare_board`；时域对照图、翻译表、配对区 | `triple_match`；`matchAttempts/resultState/timeOnStep`；`state_match_accuracy/critical_state_confusion`；AI 只做三域翻译 | 可复用 triple match 外壳，需换成状态/极点/曲线对应 | 严格实现 | 契约对齐测试 + 配对交互手测 |
| step-11 | `frequency_warning_board`；Bode 图、结论卡、判断区 | `reason_check`；`selectionState/resultState/teacherRevealSeen`；`reason_accuracy/boundary_to_frequency_confusion`；AI 不越界成 Nyquist 课 | 可复用 reason-check 外壳，需换成边界接近与峰值抬高语义 | 严格实现 | 契约对齐测试 + 交互手测 |
| step-12 | `constraint_transform_workspace`；竖线约束、平移链、流程图、区间输入 | `parameter_workspace`；`submittedRange/reasonTag/resultState`；`range_accuracy/constraint_reason_distribution`；AI 只讲约束收缩 | 可复用 parameter workspace 外壳，需换成变量平移语义 | 严格实现 | 契约对齐测试 + 工作区手测 |
| step-13 | `post_quiz_stack`；两题客观 + 一题解释 | `quiz_group`；`attemptCount/resultState/revisionCount`；`question_accuracy/explanation_keywords`；AI 不代写解释题 | 可复用 quiz 外壳，需支持解释题修正 | 严格实现 | 契约对齐测试 + 提交链路手测 |
| step-14 | `summary_infographic`；五条结论、信息图、去向卡 | `none`；`viewed/timeOnStep`；`view_count`；AI 只负责总结与后续去向 | 缺总结页静态面板 | 严格实现 | 契约对齐测试 |

## 统一接线

| 项目 | 设计要求 | 当前缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- |
| 入口页 | 统一预习台模板、runtime-first、课堂外资源埋点 | 缺 `unit-3-2-routh-stability-boundary` 入口页与路由 | 严格实现 | 单元测试 + 入口页源码断言 |
| runtime 媒体 | 只读 `course-content/runtime/lessons/3-2/media/3-2-media.md` | 需确认 `3-2-intro-video.mp4` 在无 URL 时仍被解析并以统一模板渲染 | 严格实现 | 运行时媒体解析测试 |
| 学生页预览 | 预览路径必须与契约一致 | 设计稿仍指向旧 route segment | 先修设计后实现 | 契约路径对齐测试 |
| 教师端 | 释放、揭示、汇总、结束课堂 | 缺教师页与 3-2 特定汇总摘要 | 严格实现 | 路由测试 + 手测 |
| AI 上下文 | 每步都有页内 AI 边界、问题与系统提示延伸 | 缺 `unit-3-2` AI 上下文注册 | 严格实现 | registry 测试 |
| 课程注册 | catalog / preset / classroom route / app routes 同步注册 | 缺全部 3-2 接线 | 严格实现 | 单元测试 |
