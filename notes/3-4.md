# 3-4 互动课实现整改记录

## 本轮范围

- 对齐 `course-content/authoring/lessons/3-4/design/interactive-page.md` 与 `interactive-contract.yaml` 已重写后的 16 步真源。
- 回修 `src/lib/unit-3-4-course.ts` 的步序、页面契约、媒体映射与教师控制语义。
- 重写 `src/features/interactive/unit-3-4-root-locus-reading-validation/step-panels.tsx`，重点处理 `step-04/05/06/08/11/12/13/14/15/16` 的新行为。

## 设计稿到实现稿对照

| 项目 | 当前实现结论 |
| --- | --- |
| `step-03` 对象与版本 | 运行时已取消旧配图依赖，只保留对象公式、版本表与前测。 |
| `step-04` 读图排序 | 排序区默认不再以正确顺序出现，运行时进入页内后会生成随机顺序，且不显示数字序号。 |
| `step-05` 关键节点证据板 | 已从旧文字热点题改为统一根轨迹面板；左侧统一面板、右侧三个问题按钮与当前问题提交区；判定采用容差，不联动其他极点。 |
| `step-06` 关键节点读图记录 | 已按两张卡分别提交，不再只有整页统一提交。 |
| `step-08` 增益换算链 | 已改成新版逐步显影：题面常显、默认首层、点击当前最下方已显影步骤继续展开、`重置步骤` 回到教师基线层；`student/demo` 不再硬编码满显影。 |
| `step-11` 频域代价 | 已切到讲义 `4.5` 的频域表 + `3-4-bode-compare.png`。 |
| `step-12` 连续跟踪对照 | 已同时嵌入 `3-4-turning-track-k06064.png` 与 `3-4-turning-track-k20.png`。 |
| `step-13` 广义根轨迹改写 | 已改为完整显影页，保留对象卡、结构图、题面卡和完整推导链；页顶对象公式与显影链公式均按 KaTeX 公式块渲染。 |
| `step-14` 非增益参数窗口 | 已改为根轨迹图 + 代表性极点表 + 参数区间窗口表 + 双卡作答。 |
| `step-15/16` 后测与总结 | 已分离：`step-15` 为后测，`step-16` 为总结并嵌入 `3-4-info.png`。 |

## 运行时实现要点

- `src/lib/unit-3-4-course.ts`
  - 删除旧 `step-17`，收敛为 16 步。
  - 将旧 `step-13/14/15/16/17` 映射到新的 `step-13/14/15/16`。
  - 调整 `UNIT_3_4_PAGE_CONTRACTS`，保持静态字面量前提下对齐作者态契约。
  - 更新新版媒体映射，移除旧 `step-03/05/08` 位图依赖，补入 `3-4-bode-compare.png`、`3-4-generalized-root-locus.png`、`3-4-info.png`。
- `src/features/interactive/unit-3-4-root-locus-reading-validation/step-panels.tsx`
  - `step-05` 新增 Rust/WASM 驱动的统一根轨迹提交区，拖点沿根轨迹吸附，并按题目容差判定。
  - `step-06` 改为双卡分别提交。
  - `step-08` 与 `step-13` 改为点击最下方已显影步骤继续展开，不再保留旧“显示下一步”按钮。
  - `step-11/12/14/16` 改为按讲义媒体和表格内容直接落页。

## 验证命令

- `rtk proxy npx vitest run src/features/interactive/__tests__/unit-3-4-course.test.ts`
- `rtk python3 .codex/skills/interactive-lesson-implementation/scripts/check_contract_alignment.py --contract course-content/authoring/lessons/3-4/design/interactive-contract.yaml --implementation src/lib/unit-3-4-course.ts --page-contract-const UNIT_3_4_PAGE_CONTRACTS --step-const UNIT_3_4_LESSON_STEPS`
- `rtk python3 course-content/scripts/review_lesson_content.py --lesson 3-4 --strict-implementation-contract`
- `rtk npm run lint`
- `rtk npm run test`
- `rtk npm run build`
