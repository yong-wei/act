# 3-5 课程实现笔记

## 课程信息
- 课次：`3-5`
- 标题：零点引入与动态改善——为什么改变结构后，轨迹和响应会一起变
- 设计真源：
  - `course-content/authoring/lessons/3-5/design/interactive-page.md`
  - `course-content/authoring/lessons/3-5/design/interactive-contract.yaml`
- runtime 真源：
  - `course-content/runtime/lessons/3-5/lesson.json`
  - `course-content/runtime/lessons/3-5/graph-overlay.json`
  - `course-content/runtime/lessons/3-5/handout.md`
  - `course-content/runtime/lessons/3-5/review/*`
- 当前实现入口：
  - `src/app/interactive-learning/courses/unit-3-5-zero-dynamic-improvement/*`
  - `src/features/interactive/unit-3-5-zero-dynamic-improvement/*`
  - `src/lib/unit-3-5-course.ts`
  - `src/lib/unit-3-5-ai-contexts.ts`

## 当前状态
- 本轮已把 `unit-3-5-zero-dynamic-improvement` 从旧的 `16` 步实现收束到作者态双轨真源规定的 `15` 步版本。
- 课程常量、页面契约、AI 上下文、媒体映射、学生总结面板触发点以及运行时知识分组已同步更新。
- 保留的统一框架能力包括：入口页预习台、runtime 媒体索引、课堂内 session/SSE、页内 AI、统一事件链、教师聚合与课堂外资源追踪。

## 设计稿到实现稿对照表
| 设计稿步骤 | 当前实现位置 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- |
| `step-01` 回到地图：从沿轨迹读图转向结构改变 | `src/lib/unit-3-5-course.ts` / `step-panels.tsx` | 已对齐 | 单测检查步骤标题与契约 |
| `step-02` 统一对象与观察框架：四个版本、三项产出、四个观察量 | 同上 | 已对齐 | 单测检查布局区域 |
| `step-03` 前测：三类起点误判先落地 | `workspace.ts` / `student-page.tsx` | 已对齐 | 单测 + AI 锁定逻辑 |
| `step-04` 二阶对象接入零点：先看哪条分支被拉走 | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查页面契约 |
| `step-05` 三阶对象接入零点：零点位置怎样改写主导分支 | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查步骤标题 |
| `step-06` 第一收束：左半平面零点改善趋势与右半平面问号 | `workspace.ts` / `unit-3-5-course.ts` | 已对齐 | 单测检查 telemetry 与教师聚合 |
| `step-07` 结构辨认：PD 与测速反馈不靠名字判断 | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查结构表述 |
| `step-08` 阻尼工作区：由目标阻尼反求 $K_d$ 与 $K_t$ | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查契约字段 |
| `step-09` PD 与测速反馈的三域对照：同样提阻尼，不等于结构相同 | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查媒体映射与核心表述 |
| `step-10` PD 单独装置的频域指纹：中高频抬升与高频代价 | `unit-3-5-course.ts` / `step-panels.tsx` | 已对齐 | 单测检查步骤与契约 |
| `step-11` 超前单独装置的频域指纹：相位峰、补角与作用频带 | `unit-3-5-course.ts` / `step-panels.tsx` | 已对齐 | 单测检查步骤与契约 |
| `step-12` 频域对照：什么时候优先想 PD，什么时候优先想超前 | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查场景矩阵常量 |
| `step-13` 非最小相入口：镜像零点、逆响应与“先反向动” | `unit-3-5-course.ts` / `step-panels.tsx` | 已对齐 | 单测检查媒体映射 |
| `step-14` 频域边界：右半平面零点为何限制带宽 | `workspace.ts` / `step-panels.tsx` | 已对齐 | 单测检查 `rule_check` 工作区 |
| `step-15` 后测与收束：四个观察量、风险边界与 3-6 入口 | `workspace.ts` / `student-page.tsx` / `step-panels.tsx` | 已对齐 | 单测检查收束步骤与信息图映射 |

## 本轮关键调整
- 删除旧实现中的 `step-16`，并把收束内容并入 `step-15`。
- 删除旧的 `sentence_rebuild` / `exit_reflection` 页面类型，引入 `rule_check` 以对应 `step-14` 的频域边界判断。
- 将媒体映射改为：
  - `step-09 -> 3-5-rl-03-pd-rate-compare.png`
  - `step-13 -> 3-5-rl-05-nmp-compare.png`
  - `step-15 -> 3-5-info.png`
- 将 runtime `lesson.json` 与 `graph-overlay.json` 的分组同步为 `3 + 3 + 3 + 3 + 3` 的 `15` 步结构。
- 进一步把 `step-04 / step-05` 的根轨迹工作区从 `基线 / 示例A / 示例B` 收束为 `基线 / 添加零点` 两模式；统一把新增零点改成图上可拖动的小型标注，并把根轨迹采样数提升到 `240`。
- 为 `step-07 / step-10 / step-11 / step-13` 重写静态证据区命名与顺序：不再使用“固定公式”等泛标题，而是改成讲义对应的对象名、公式名与分析顺序。
- 将 `step-13` 改为“公式在上、图与分析居中、5.4 对比表在下”的顺序；将 `step-15` 的四个观察量改成紧凑卡片。
- 已同步更新作者态 `interactive-page.md` 与 `interactive-contract.yaml`，避免实现与双轨真源继续漂移。

## 验证记录
- 已通过：`npx vitest run src/features/interactive/__tests__/unit-3-5-course.test.ts`
- 已通过：`node scripts/tests/test-interactive-contract-implementation-alignment.mjs --contract course-content/authoring/lessons/3-5/design/interactive-contract.yaml --implementation src/lib/unit-3-5-course.ts --pageContractConst UNIT_3_5_PAGE_CONTRACTS --stepConst UNIT_3_5_LESSON_STEPS`
- 本轮单测覆盖：
  - `15` 步步骤链与页面契约
  - YAML 契约解析
  - AI 上下文关键步快速问题
  - 运行时媒体映射
  - 页面工作区关键词与核心内容

## 后续建议
- 若需要进一步做课堂级验收，下一步应补跑 `check_contract_alignment.py`、`review_lesson_content.py --strict-implementation-contract`、`npm run lint` 以及必要的构建验证。
