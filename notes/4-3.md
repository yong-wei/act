# 4-3 实现对照

## 本轮范围

| 步骤 | 设计真源 | 当前缺口 | 本轮处理 |
|---|---|---|---|
| `step-05/06/07` | 方法页对象/控制器公式必须直接渲染为 KaTeX | 静态公式字符串仍保留双重反斜杠 | 改成标准转义字符串 |
| `step-09` | 五层显影，默认只见首层，支持点击最下方继续显影 | 当前先渲染五个条目标题 | 对齐 `4-2` 的 slice 显影模式 |
| `step-11` | 四层显影，默认只见首层，支持点击最下方继续显影 | 当前先渲染四个条目标题 | 对齐 `4-2` 的 slice 显影模式 |
| `step-13` | 边界案例使用时域/Bode 双栏对比，强调扰动通道重写 | 缺图例，轴域不稳，当前分析语义偏向普通闭环跟踪 | 改为专用扰动通道对比面板 |

## 本轮验收

- `step-05/06/07/13` 打开后不再出现 KaTeX 双反斜杠告警。
- `step-09/11` 首屏只显示第一层显影内容。
- `step-13` 必须显示“原系统 / 当前参数”图例。
- `step-13` 拖动参数后两张图都更新，且纵轴不裁切当前曲线。
