# 3-7 互动课程重构记录

## 本轮定位

- 本轮工作是把 `3-7` 互动课程从旧版 `12` 步实现重构到新版 `17` 步双轨真源。
- 实现必须同时服从：
  - 人读稿：`course-content/authoring/lessons/3-7/design/interactive-page.md`
  - 机读稿：`course-content/authoring/lessons/3-7/design/interactive-contract.yaml`
  - runtime 审查产物：`course-content/runtime/lessons/3-7/`
- 当前课堂代码主入口：
  - `src/lib/unit-3-7-course.ts`
  - `src/lib/unit-3-7-ai-contexts.ts`
  - `src/features/interactive/unit-3-7-steady-error-low-frequency-compensation/step-panels.tsx`
  - `src/features/interactive/unit-3-7-steady-error-low-frequency-compensation/workspace.ts`

## 当前缺口判断

- 课程契约、AI 上下文、页面内容映射与测试仍停留在旧版 `12` 步。
- runtime `lesson.json` 已更新到新版分组序列，但课堂代码尚未对齐 `step-13 ~ step-17`。
- 当前 `step-panels.tsx` 把时域设计、频域过渡与后测收束压缩为旧版综合页，未落实新版“方法页 / 比较页 / 后测页 / 收束页”拆分。
- 现有测试仍断言 “full 12-step lesson flow”，且对 `interactive-contract.yaml` 的解析方式不正确，必须一并回修。

## 设计稿到实现稿对照表（2026-04-16）

| step | 设计稿关键约束 | 当前实现位置 | 当前缺口 | 本轮状态 | 验证方式 |
|---|---|---|---|---|---|
| step-01 | `map_hero_slide`，路径图、主问题卡、边界卡同屏 | `unit-3-7-course.ts` + `step-panels.tsx` | 可复用，需只做标题/契约复核 | 待复核 | 定向测试 + 学生演示页 |
| step-02 | `goal_boundary_slide`，目标卡与边界表同屏 | `unit-3-7-course.ts` + `step-panels.tsx` | 可复用，需只做标题/契约复核 | 待复核 | 定向测试 + 学生演示页 |
| step-03 | `question_stack`，三题前测，允许一次重提 | `unit-3-7-course.ts` + `step-panels.tsx` | 基本可复用，需补 17 步上下文 | 待复核 | 定向测试 + 提交流 |
| step-04 | `formula_media_compare`，结构图 + 四式卡 + `hotspot_labeling` | `unit-3-7-course.ts` + `step-panels.tsx` | 基本可复用，需保留新版标题与证据顺序 | 待复核 | 定向测试 + 静态承载检查 |
| step-05 | `worked_example_workspace`，三步法与例题 1 分离 | `unit-3-7-course.ts` + `step-panels.tsx` | 可复用，但需补“题面完整显示、步骤默认隐藏”语义 | 待重构 | 定向测试 + 教师显影 |
| step-06 | `formula_table_workspace`，公式组 + 双表 + 作答卡 | `unit-3-7-course.ts` + `step-panels.tsx` | 当前模板与契约不一致，需从 `triple_match` 改到活动卡集合语义 | 待重构 | 定向测试 + 学生演示页 |
| step-07 | `worked_example_workspace`，例题 2 总误差式与结果 `0.4` 同页 | `unit-3-7-course.ts` + `step-panels.tsx` | 可复用，但需保留题面顺序与结构图位置 | 待重构 | 定向测试 + 教师显影 |
| step-08 | `contrast_summary_board`，增益 vs 型别对照卡 | `unit-3-7-course.ts` + `step-panels.tsx` | 基本可复用，需补新版判断卡文案 | 待重构 | 定向测试 + 教师揭示 |
| step-09 | `formula_figure_table_stack`，公式 -> 文案 -> 图 -> 表 -> 作答卡 | `unit-3-7-course.ts` + `step-panels.tsx` | 当前被压成旧版排序页，需改成总览页 | 待重构 | 定向测试 + 页面顺序核对 |
| step-10 | `method_reveal_page`，时域 `PI` 设计完整过程 | `unit-3-7-course.ts` + `step-panels.tsx` | 当前与滞后设计混在同一页，必须拆出独立方法页 | 待重构 | 定向测试 + 步骤显影 |
| step-11 | `method_reveal_page`，时域滞后设计完整过程 | `unit-3-7-course.ts` + `step-panels.tsx` | 当前被错误占用为旧版频域过渡页 | 待重构 | 定向测试 + 步骤显影 |
| step-12 | `comparison_board`，时域两法比较页 | `unit-3-7-course.ts` + `step-panels.tsx` | 当前被错误占用为后测收束页 | 待重构 | 定向测试 + 学生演示页 |
| step-13 | `method_reveal_page`，频域 `PI` 四步设计链 | 未实现 | 旧版课堂代码无此页 | 待新增 | 定向测试 + 步骤显影 |
| step-14 | `method_reveal_page`，`PD` 方案读取与核验 | 未实现 | 旧版课堂代码无此页 | 待新增 | 定向测试 + 步骤显影 |
| step-15 | `comparison_board`，频域 `PI/PD` 比较 | 未实现 | 旧版课堂代码无此页 | 待新增 | 定向测试 + 学生演示页 |
| step-16 | `assessment_card_grid`，四题后测，单卡提交 | 未实现 | 旧版后测与总结混页 | 待新增 | 定向测试 + 提交流 |
| step-17 | `summary_route_board`，规则表、信息图与 `3-8` 去向 | 未实现 | 旧版收束与后测混页 | 待新增 | 定向测试 + 学生演示页 |

## 本轮实施范围

- 更新 `src/lib/unit-3-7-course.ts` 为新版 `17` 步课程契约。
- 更新 `src/lib/unit-3-7-ai-contexts.ts`，覆盖 `step-01 ~ step-17` 的页面级 AI 上下文。
- 重构 `src/features/interactive/unit-3-7-steady-error-low-frequency-compensation/step-panels.tsx`，恢复方法页、比较页、后测页与收束页的分离。
- 视交互字段是否复用需要，更新 `workspace.ts` 的题卡与字段集合。
- 更新 `src/features/interactive/__tests__/unit-3-7-course.test.ts`，把 17 步契约、媒体映射、AI 问题与页面语义纳入测试。

## 收工门槛

- 3-7 定向测试通过。
- 课程步骤、标题、模板、交互类型、预览路径与作者态契约一致。
- 方法页不再被比较页替代；后测与收束不再混页。
