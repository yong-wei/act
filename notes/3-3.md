# 3-3 实现对照表

## 本轮结论

- 双轨真源仍以 `course-content/authoring/lessons/3-3/design/interactive-page.md` 与 `course-content/authoring/lessons/3-3/design/interactive-contract.yaml` 为准。
- 本轮已把 `unit-3-3-root-locus-rules` 从旧的 **13 步实现** 整改为与作者态一致的 **17 步实现**。
- 当前实现已对齐新增页面与互动类型：`activity_cards`、`sequence_sort`、`classification_cards`，并把 `step-10` 到 `step-17` 的内容链重新接回 `3-3` 真源。

## 关键差异已收口

| 项目 | 整改前 | 整改后 |
| --- | --- | --- |
| 步骤总数 | 13 步 | 17 步 |
| 步骤范围 | 停在旧版 `step-13` | 覆盖 `step-01` 到 `step-17` |
| 课堂主线 | “定义 -> 骨架 -> 广义 -> 后测” 被压缩 | “定义 -> 两大判据 -> 骨架 -> 三组例题 -> 读图顺序 -> 极点类型 -> 广义改写 -> 动态翻译 -> 后测 -> 收束” 完整落页 |
| 新互动类型 | 缺失 | 已补 `activity_cards` / `sequence_sort` / `classification_cards` |
| AI 上下文 | 只覆盖旧 13 步 | 已覆盖 17 步，`step-13` 回到广义改写，`step-16` 承接后测 |
| notes 记录 | 仍声称“13 步已完成” | 已改为 17 步对齐记录 |

## 已完成实现

- `src/lib/unit-3-3-course.ts`
  - 重写 `UNIT_3_3_PAGE_CONTRACTS` 与 `UNIT_3_3_LESSON_STEPS`，按作者态契约对齐 `step-01` 到 `step-17`
  - 新增 `activity_cards`、`sequence_sort`、`classification_cards` 页面类型
  - 调整 3-3 runtime 媒体映射到新步骤
- `src/lib/unit-3-3-ai-contexts.ts`
  - AI 页面目标、快捷问题与系统提示全部按 17 步重排
- `src/features/interactive/unit-3-3-root-locus-rules/step-panels.tsx`
  - 重写 17 步静态蓝图
  - 新增三类互动表单、教师参考锚点与汇总逻辑
  - 例题 1 / 例题 2 / 例题 3 改为各自独立配置，不再共用旧的单一 worked-example 语义
- `src/features/interactive/__tests__/unit-3-3-course.test.ts`
  - 测试口径从 13 步升级为 17 步
  - 契约校验改为覆盖全部 17 步

## 仍需人工补证

- 浏览器级手测尚未在本轮完成。
- 若后续需要验收截图，应重点补：
  - 学生演示页 `step-10 / step-11 / step-12 / step-16 / step-17`
  - 教师端释放 / 揭示链路
  - 例题页逐步显影与卡片提交联动

## 本轮验证

- `rtk npm run test:unit -- src/features/interactive/__tests__/unit-3-3-course.test.ts`
  - 通过（11/11）
- `rtk python3 .codex/skills/interactive-lesson-implementation/scripts/check_contract_alignment.py --contract course-content/authoring/lessons/3-3/design/interactive-contract.yaml --implementation src/lib/unit-3-3-course.ts --page-contract-const UNIT_3_3_PAGE_CONTRACTS --step-const UNIT_3_3_LESSON_STEPS`
  - 通过
- `rtk npm run lint`
  - 通过
- `rtk npm run build`
  - 通过
- `rtk python3 course-content/scripts/review_lesson_content.py --lesson 3-3 --strict-implementation-contract`
  - 通过，输出 `exported runtime for 3-3`
