# 3-3 实现对照表

## 设计真源状态

- 双轨真源：`course-content/authoring/lessons/3-3/design/interactive-page.md` + `course-content/authoring/lessons/3-3/design/interactive-contract.yaml`
- runtime 审查：`course-content/runtime/lessons/3-3/review/*` 已齐备，可直接作为实现输入
- 入口页约束：`3-3` 已具备 runtime `media/3-3-media.md`，本轮必须复用统一预习台模板、`LessonEntryMediaHub`、`LessonEntryRuntimeSections` 与课堂外资源埋点链路
- 当前实现缺口：
  - 缺 `unit-3-3-root-locus-rules` 的课程常量、页面契约、AI 上下文与 premium lesson card
  - 缺 `src/features/interactive/unit-3-3-root-locus-rules/*` 页面实现
  - 缺 App Router 路由、预置教案、课程目录与课堂码路由接线

| 设计步骤 | 页面模板 / 区域 / 模块 | 契约互动类型 / 埋点 / 教师聚合 / AI | 当前实现位置或缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| step-01 | `map_hero_slide`；路径图、任务卡 | `none`；`viewed/timeOnStep/teacherFollowSync`；`view_count/sync_status`；AI 只做模块定位 | 缺 3-3 课程定义与首屏静态面板 | 严格实现 | 契约对齐测试 + 学生演示页核对 |
| step-02 | `figure_question_vote`；主图、三问、二选一判断 | `binary_choice`；`selectedOption/resultState/teacherRevealSeen`；`option_distribution/reveal_correction_rate`；AI 只纠正“知道边界就够了” | 缺 3-3 引入投票区 | 严格实现 | 契约对齐测试 + 学生/教师交互手测 |
| step-03 | `goal_boundary_slide`；目标卡、主线链、边界表 | `none`；`viewed/timeOnStep`；`view_count`；AI 只说明目标边界 | 缺静态承载组件 | 严格实现 | 契约对齐测试 |
| step-04 | `definition_formula_figure`；定义卡、二阶对象、短答区 | `short_response`；`responseSubmitted/responseLength/timeOnStep`；`response_word_cloud/common_reason_tags`；AI 不代写标准短答 | 缺短答表单与词云汇总 | 严格实现 | 契约对齐测试 + 文本提交手测 |
| step-05 | `equation_to_condition_chain`；方程链、条件入口卡 | `none`；`viewed/timeOnStep`；`view_count`；AI 只解释条件来源 | 缺静态公式链展示 | 严格实现 | 契约对齐测试 |
| step-06 | `geometry_check_workspace`；几何图、条件卡、判断区 | `reason_check`；`selectedReason/attemptCount`；`reason_distribution`；AI 不替学生先判断 | 需复用 reason-check 外壳并换成“先资格后参数”语义 | 严格实现 | 契约对齐测试 + 交互手测 |
| step-07 | `rules_overview_board`；主图、法则卡、高亮区 | `region_highlight`；`highlightChoice/resultState`；`rule_confusion_heatmap`；AI 只围绕骨架法则 | 需新增区域高亮型工作区或基于既有热点类交互适配 | 严格实现 | 契约对齐测试 + 工作区手测 |
| step-08 | `keypoint_compare_board`；主图、三节点卡、配对区 | `triple_match`；`matchAttempted/matchCorrected`；`keypoint_mismatch_pairs`；AI 不混淆节点职责 | 可复用 triple match 外壳，需替换成 3-3 节点语义 | 严格实现 | 契约对齐测试 + 配对交互手测 |
| step-09 | `worked_example_workspace`；题面、三步法、工作区 | `worked_example_workspace`；`stepCompletion/attemptCount/errorBucket`；`example_bottlenecks`；AI 不一次性给全题 | 需新增三步分段校验工作区 | 严格实现 | 契约对齐测试 + 工作区手测 |
| step-10 | `equivalent_open_loop_chain`；改写链、等效开环卡、排序区 | `formula_ordering`；`orderAttempted/resultState`；`ordering_error_patterns`；AI 只讲等效开环 | 需新增公式排序交互 | 严格实现 | 契约对齐测试 + 排序交互手测 |
| step-11 | `compare_dual_root_locus`；时间常数例图、对照表、切换区 | `tab_switch`；`tabVisited/viewDuration`；`tab_attention_distribution`；AI 只比较 `0°/180°` 与一般参数 | 可复用 tab-switch 外壳，需换成 3-3 对照内容 | 严格实现 | 契约对齐测试 + 切换交互手测 |
| step-12 | `dynamic_translation_panel`；翻译表、动态图、映射区 | `mapping_highlight`；`mappingChoice/resultState`；`translation_error_map`；AI 只做“位置到结论”翻译 | 需新增映射高亮交互 | 严格实现 | 契约对齐测试 + 工作区手测 |
| step-13 | `postcheck_quiz`；三题后测、五点总结、下节预告 | `quiz_group`；`attemptCount/resultState/revisionCount`；`post_quiz_distribution/ready_for_next_lesson_rate`；AI 不代答后测 | 可复用 quiz 外壳，需换题目、总结卡和收束文案 | 严格实现 | 契约对齐测试 + 提交链路手测 |

## 统一接线

| 项目 | 设计要求 | 当前缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- |
| 入口页 | 统一预习台模板、runtime-first、课堂外资源埋点 | 缺 `unit-3-3-root-locus-rules` 入口页与路由 | 严格实现 | 单元测试 + 入口页源码断言 |
| runtime 媒体 | 只读 `course-content/runtime/lessons/3-3/media/3-3-media.md` | 需确认资源解析标题与 `handoutSummary` 正常进入入口页 | 严格实现 | 运行时媒体解析测试 |
| 学生页预览 | 预览路径必须匹配契约 | 缺 `student/demo` 实际渲染链 | 严格实现 | 契约对齐测试 + 演示页核对 |
| 教师端 | 释放、揭示、汇总、结束课堂 | 缺教师页与 3-3 特定聚合摘要 | 严格实现 | 路由测试 + 手测 |
| AI 上下文 | 每步都有页内 AI 边界、快捷问题与系统提示延伸 | 缺 `unit-3-3` AI 上下文文件与总注册 | 严格实现 | registry 测试 |
| 课程注册 | catalog / preset / classroom route / app routes 同步注册 | 缺全部 3-3 接线 | 严格实现 | 单元测试 |

## 当前状态

- 已完成：
  - `unit-3-3-root-locus-rules` 的课程常量、页面契约、媒体映射与 session adapter
  - `unit-3-3` AI 上下文注册、课程目录注册、课堂路由解析与预置教案注册
  - 统一入口页模板与 runtime-first 媒体 / 知识区接线
  - 3-3 App Router 页面与教师 / 学生页壳子接线
  - `src/features/interactive/unit-3-3-root-locus-rules/step-panels.tsx` 已按 13 步作者态设计重写：静态蓝图、`binary_choice / reason_check / region_highlight / triple_match / worked_example_workspace / formula_ordering / tab_switch / mapping_highlight / quiz_group` 均切回 3-3 语义，教师汇总与答案揭示不再沿用 3-2 劳斯内容
  - `src/features/interactive/unit-3-3-root-locus-rules/workspace.ts` 已改为 3-3 专用工作区常量：`RULE_HIGHLIGHT_OPTIONS`、`KEYPOINT_MATCH_GROUPS`、`WORKED_EXAMPLE_SECTIONS`、`FORMULA_ORDERING_SEQUENCE`、`TAB_SWITCH_OPTIONS`、`DYNAMIC_MAPPING_ROWS`
  - `student-page.tsx` 已切换到 `getUnit33StepAIContext`，学生端工作区选择会通过 `onWorkspaceParameterChange` 接入统一课堂事件链
- 当前仍待继续：
  - 浏览器级课堂手测尚未在本轮完成，后续若需要可补学生端/教师端联动演示证据与截图

## 验证记录

- `npx vitest run src/features/interactive/__tests__/unit-3-3-course.test.ts`：通过（11/11），新增守卫测试覆盖 `3-3` 关键术语、`workspace.ts` 专用常量与学生页 AI 上下文接线
- `python3 .codex/skills/interactive-lesson-implementation/scripts/check_contract_alignment.py --contract course-content/authoring/lessons/3-3/design/interactive-contract.yaml --implementation src/lib/unit-3-3-course.ts --page-contract-const UNIT_3_3_PAGE_CONTRACTS --step-const UNIT_3_3_LESSON_STEPS`：通过
- `python3 course-content/scripts/review_lesson_content.py --lesson 3-3 --strict-implementation-contract`：通过，输出 `exported runtime for 3-3`
- `npm run lint`：通过
- `npm run build`：通过；构建日志中仍有仓库既有 API 动态渲染告警，但未影响本次 3-3 新增页面进入生产构建
