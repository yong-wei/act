# 3-1 实现对照表

| 设计步骤 | 页面模板 / 区域 / 模块 | 契约互动类型 / 埋点 / 教师聚合 / AI | 当前实现位置或缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| step-01 | `map_hero_slide`；路径图、任务卡、边界卡 | `none`；`viewed/timeOnStep/teacherFollowSync`；`view_count/sync_status` | 缺 3-1 课程定义与首屏静态面板 | 严格实现 | `unit-3-1-course.test.ts` 契约对齐 + 学生演示页手测 |
| step-02 | `triple_model_vote`；三模型公式卡、时域对照图、二选一区 | `binary_choice`；选项分布、纠偏率；只讨论主导极点与附加模态 | 缺 3-1 本地活动表单与教师汇总 | 严格实现 | 契约对齐测试 + 学生/教师页交互手测 |
| step-03 | `goal_chain_slide`；目标卡、主线链、边界卡 | `none`；`viewed/timeOnStep`；`view_count` | 缺 3-1 静态承载组件 | 严格实现 | 契约对齐测试 |
| step-04 | `question_stack`；三题前测 + 提交反馈条 | `quiz_group`；误区聚合；允许重提一次 | 缺 3-1 quiz 表单和教师统计 | 严格实现 | 契约对齐测试 + 提交链路手测 |
| step-05 | `formula_plus_plane_check`；特征方程卡、半平面图、判断区 | `reason_check`；理由长度、词云；AI 不进入劳斯 | 缺 3-1 reason check 面板 | 严格实现 | 契约对齐测试 + 提交链路手测 |
| step-06 | `formula_explain_match`；展开式、模态解释条、配对区 | `triple_match`；错误配对统计 | 缺 3-1 配对区与教师聚合 | 严格实现 | 契约对齐测试 + 配对交互手测 |
| step-07 | `family_compare_switcher`；极点类型图、误区卡、切换区 | `tab_switch`；家族关注分布 | 可复用 2-2 家族切换模式，需换成 3-1 内容 | 严格实现 | 契约对齐测试 + 演示页切换手测 |
| step-08 | `triple_model_compare`；三模型公式区、时域对照图、记录栏 | `comparison_workspace`；对比记录与参数快照 | 需在 3-1 本地新增对比工作区 | 严格实现 | 契约对齐测试 + 工作区手测 |
| step-09 | `decision_table_with_reason`；指标表、准则表、解释区 | `reason_check`；判断理由聚合 | 需复用 reason-check 外壳并替换 3-1 内容 | 严格实现 | 契约对齐测试 |
| step-10 | `bridge_table_plus_prompt`；时域结论卡、追问清单、短答栏 | `short_response`；词云与回复列表 | 可复用 2-2/2-3 短答模式 | 严格实现 | 契约对齐测试 + 教师回复聚合手测 |
| step-11 | `bode_compare_workspace`；幅频公式区、Bode 图、三频率表 | `parameter_workspace`；参数变化与状态快照 | 可复用 2-2 工作区模式，需新建 3-1 Bode 对照工作区 | 严格实现 | 契约对齐测试 + 工作区手测 |
| step-12 | `compare_then_ai`；双域判断表、AI 对照区、证据清单 | `ai_compare_workspace`；页内 AI；先自判后对照 | 可直接复用 2-3/2-4 AI compare 模式 | 严格实现 | 契约对齐测试 + AI 上下文测试 |
| step-13 | `dual_media_reason_check`；卷积公式卡、双图对照、误区判断区 | `formula_pair_check`；概念纠偏聚合 | 可复用 2-2 `formula_pair_check` 模式，需替换为卷积/模态语义 | 严格实现 | 契约对齐测试 + 交互手测 |
| step-14 | `post_quiz_stack`；两题客观 + 一题解释 | `quiz_group`；后测统计与答案揭示 | 缺 3-1 后测表单 | 严格实现 | 契约对齐测试 + 提交链路手测 |
| step-15 | `summary_infographic`；四句出口判断、信息图、去向卡 | `none`；收束页埋点 | 缺 3-1 总结页静态面板 | 严格实现 | 契约对齐测试 |

## 统一接线

| 项目 | 设计要求 | 当前缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- |
| 入口页 | 统一预习台模板 + runtime-first + 课堂外资源埋点 | 缺 3-1 entry route / entry page；`3-1-media.md` 导入视频标题缺失 | 严格实现，并补 runtime 标题行 | 入口页测试 + 运行时解析测试 |
| 学生页预览 | 预览路径必须匹配 `interactive-contract.yaml` | 缺 `unit-3-1-pure-pole-stability-and-dynamics` 路由 | 严格实现 | 契约对齐测试 |
| 教师端 | 释放、揭示、汇总、结束课堂 | 缺 3-1 teacher page 与 preset | 严格实现 | 手测 + 路由测试 |
| 课程注册 | catalog / preset / classroom session route / AI contexts | 缺全部 3-1 接线 | 严格实现 | 单元测试 |
