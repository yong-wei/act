# 3-6 课程实现笔记

## 课程信息
- 课次：3-6
- 标题：零点作用与动态改善实验——从性能目标到校正设计
- 设计源：
  - `course-content/authoring/lessons/3-6/design/interactive-page.md`
  - `course-content/authoring/lessons/3-6/design/interactive-contract.yaml`
  - `course-content/runtime/lessons/3-6/lesson.json`
  - `course-content/runtime/lessons/3-6/handout.md`
  - `course-content/runtime/lessons/3-6/media/3-6-media.md`
  - `course-content/runtime/lessons/3-6/review/*`
- 当前实现入口：
  - `src/app/interactive-learning/courses/unit-3-6-zero-design-workshop/*`
  - `src/features/interactive/unit-3-6-zero-design-workshop/*`
  - `src/lib/unit-3-6-course.ts`
  - `src/lib/unit-3-6-ai-contexts.ts`
- runtime：
  - `course-content/runtime/lessons/3-6`

## 当前状态
- 已完成首轮实现：作者态双轨设计、runtime、review 与正式媒体已全部落到 `unit-3-6-zero-design-workshop` 课程代码、课堂路由、AI 注册、课程目录与教师预置教案。
- 统一框架已接入：课程入口页继续复用 `LessonEntryMediaHub` 与 `LessonEntryRuntimeSections`，课堂内继续走统一 session / SSE / 事件链，课堂外资源埋点保持统一 resource tracking 模板。

## 本次实现内容
- 建立 `3-6` 的课程常量、页面契约、媒体映射、AI 上下文、师生双端页面、步骤面板与专用工作区。
- 将 `3-6` 接入课程目录、预置教案、课堂码路由与课程级 AI registry，并补齐 App Router 入口、教师端和学生端动态路由。
- 追加守卫测试、契约校验与严格实现审查，确认入口页模板、互动契约、媒体索引与步骤级 AI 边界全部对齐。

## 设计稿到实现稿对照表
| 设计稿步骤 | 核心锚点 | 契约互动 | 当前实现位置 | 状态 | 验证证据 |
| --- | --- | --- | --- | --- | --- |
| `step-01` | 先判断“该先看目标还是先上结构” | `single_choice` | `step-panels.tsx` | 已实现 | 契约对齐 + 单测关键文案 |
| `step-02` | 模块路径、对象边界与主线定位 | `none` | `step-panels.tsx` | 已实现 | 契约对齐 |
| `step-03` | 五任务链与三项交付物 | `none` | `step-panels.tsx` | 已实现 | 契约对齐 |
| `step-04` | 三题前测，提交后才开放 AI | `quiz_group` | `step-panels.tsx` + `student-page.tsx` | 已实现 | 契约对齐 + AI gate 单测 |
| `step-05` | 指标入口分类：先按目标分类，再进入工具 | `categorize_and_confirm` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 + 单测关键文案 |
| `step-06` | 时域指标翻译：从目标到等效极点区域 | `workspace_builder` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 |
| `step-07` | PD 时域设计：先定等效极点，再求 `K_t` | `parameter_workspace` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 + 单测关键文案 |
| `step-08` | 测速反馈时域设计：顺序与等效极点解释 | `parameter_workspace` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 |
| `step-09` | 超前频域设计：从裕度目标到参数选择 | `parameter_workspace` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 |
| `step-10` | 同指标下 PD/超前并排比较 | `structured_compare` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 + 单测关键文案 |
| `step-11` | 非最小相边界：先改目标，再选结构 | `decision_submit` | `workspace.ts` + `step-panels.tsx` | 已实现 | 契约对齐 + 单测关键文案 |
| `step-12` | 三题后测，提交后才开放 AI 对照 | `quiz_group` | `step-panels.tsx` + `student-page.tsx` | 已实现 | 契约对齐 + AI gate 单测 |
| `step-13` | 三句结论、信息图与出口反思 | `exit_reflection` | `step-panels.tsx` | 已实现 | 契约对齐 |

## 统一接线
- 入口页：
  - 已复用统一预习台模板，`entry-page.tsx` 继续走 `LessonEntryMediaHub` 与 `LessonEntryRuntimeSections`，并从 `course-content/runtime/lessons/3-6/media/3-6-media.md` 解析课前资源。
- 课堂页：
  - `student-page.tsx` 与 `teacher-page.tsx` 继续复用统一课堂事件、会话同步与教师控制链路，`/student/demo` 与真实学生页共用渲染链。
- 课程注册：
  - 已接入 `src/features/interactive/learning-catalog.ts`、`src/features/teacher/preset-lessons/presets/index.ts`、`src/lib/classroom-session-route.ts`、`src/lib/course-ai-contexts.ts`。

## 设计差异与实现约束
- `UNIT_3_6_PAGE_CONTRACTS` 已按作者态 `interactive-contract.yaml` 的 13 步真源逐步回填，不再沿用 `3-5` 的语义壳。
- 类型系统当前仅接受 `C / X / D`，因此 `knowledgeType` 不能直接写 `X+C`，本轮保持为 `X` 以通过编译。
- 学生页页内 AI 的提交后解锁约束已明确落在 `step-04` 与 `step-12`，不提前开放。

## 复用基线
- `3-5`：师生双端会话框架、教师控制与课程接线骨架。
- `2-1`：统一预习台、runtime 媒体索引、课堂外资源埋点模板。
- `1-3 / 2-x`：步骤级 AI 上下文注册与课堂内页 AI 弹层机制。

## 媒体资源状态
- 已具备：
  - `3-6-cover-comic.png`
  - `3-6-design-map.png`
  - `3-6-pd-design.png`
  - `3-6-rate-feedback-design.png`
  - `3-6-lead-design.png`
  - `3-6-pd-frequency-design.png`
  - `3-6-rhp-boundary.png`
  - `3-6-info.png`
  - `3-6-intro-video.mp4` / `3-6-audio.m4a` / `3-6-slides.pdf` / `3-6-course.mp4`
- 缺失：
  - 当前未发现阻塞首轮实现的媒体缺口。

## 验证记录
- `npx vitest run src/features/interactive/__tests__/unit-3-6-course.test.ts`：通过。
- `python3 .codex/skills/interactive-lesson-implementation/scripts/check_contract_alignment.py --contract course-content/authoring/lessons/3-6/design/interactive-contract.yaml --implementation src/lib/unit-3-6-course.ts --page-contract-const UNIT_3_6_PAGE_CONTRACTS --step-const UNIT_3_6_LESSON_STEPS`：通过。
- `python3 course-content/scripts/review_lesson_content.py --lesson 3-6 --strict-implementation-contract`：通过，严格审查成功重新导出 `3-6` runtime。
- `npm run lint`：通过。
- `npm run test`：通过。
- `npm run build`：通过；构建日志中仍有仓库既有的 Next 动态路由 `headers` 告警，但未构成 `3-6` 回归。

## 下次优化建议
- 若需要对外展示更强验收证据，可补一轮浏览器级学生端 / 教师端联动截图与课堂事件流手测记录。
