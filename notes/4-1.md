# 4-1 互动课实现对照表

## 审计结论

- `course-content/authoring/lessons/4-1/design/interactive-page.md` 与 `interactive-contract.yaml` 已形成完整双轨真源，`course-content/runtime/lessons/4-1/lesson.json`、`graph-overlay.json`、`handout.md`、`media/4-1-media.md` 与审查产物均已齐备。
- 本轮目标不是补作者态，而是把 `4-1` 落成 `unit-4-1-design-task-expression` runtime-first 精品互动课。
- 入口页必须沿用 `2-1/2-4` 的共享预习台与课堂外资源追踪链路；课堂页必须沿用 `3-7/3-8` 的课堂会话骨架。
- 当前唯一新增交互类型是 `task_card_workspace`，其余 `quiz_group`、`triple_match`、`card_sort`、`binary_choice`、`structured_compare` 都可以沿现有课堂模式落地。

## 入口页与 runtime 资源

| 模块 | 真源 | 当前实现切入点 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- |
| 入口页课堂入口 | `runtime/lessons/4-1/lesson.json` | `src/features/interactive/unit-4-1-design-task-expression/entry-page.tsx` | 严格实现 | 源码断言 + 页面抽查 |
| 课前预习台多媒体 | `runtime/lessons/4-1/media/4-1-media.md` | `LessonEntryMediaHub` | 严格实现 | 单测断言共享媒体中心已接入 |
| 知识图谱 / 知识卡 / 讲义 | `graph-overlay.json` + `handout.md` + `handout.pdf` | `LessonEntryRuntimeSections` | 严格实现 | 单测断言 + 页面抽查 |
| 课堂外资源埋点 | runtime 入口页契约 | 共享 tracking hooks | 严格实现 | 源码检索共享埋点链路 |

## 双轨设计到实现映射

| 步骤 | 页面模板 / 区域 | 互动类型 | 当前实现位置或缺口 | 本轮处理状态 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| step-01 | `map_hero_slide` / `header+lead+summary` | `none` | `unit-4-1-course.ts` + `step-panels.tsx` | 严格实现 | 契约测试 |
| step-02 | `goal_boundary_slide` / `goals+boundary` | `none` | 同上 | 严格实现 | 契约测试 |
| step-03 | `question_stack` / `question-stack+submit-bar` | `quiz_group` | `workspace.ts` 前后测题库 | 严格实现 | 契约测试 + 表单提交测试 |
| step-04 | `case_study_dashboard` / `object+evidence+analysis` | `parameter_slider` | `step-panels.tsx` 原生重绘客船动态四联图，顺序固定为“框图 -> 传函 -> 动态图” | 已修正 | 契约测试 + 浏览器滑块联动验收 |
| step-05 | `case_study_dashboard` / `object+evidence+analysis` | `parameter_slider` | `step-panels.tsx` 原生重绘平台综合动态曲线区，并补回完整传函 | 已修正 | 契约测试 + 浏览器抽查 |
| step-06 | `contrast_summary_board` / `matrix+sorting+summary` | `card_sort` | 复用排序表单 | 严格实现 | 契约测试 |
| step-07 | `formula_table_match` / `formula+tables+interaction` | `triple_match` | 补全积分误差完整公式，并增加原生“指标角色矩阵”组件 | 已修正 | 契约测试 + 浏览器抽查 |
| step-08 | `comparison_panel_with_sort` / `rules+card-bank+sort-area` | `card_sort` | 复用分类选择器 | 严格实现 | 契约测试 |
| step-09 | `task_card_workspace` / `template+evidence+workspace` | `task_card_workspace` | 工作区补齐“最紧矛盾”字段，并增加原生“任务表达卡模板”组件 | 已修正 | 契约测试 + 浏览器抽查 |
| step-10 | `layered_region_board` / `formula+diagram+decision` | `binary_choice` | 补回 `\\mathcal{O}\\subseteq\\mathcal{S}\\subseteq\\mathcal{F}` 公式，并增加原生区域分层示意 | 已修正 | 契约测试 + 浏览器抽查 |
| step-11 | `misconception_board` / `cards+decision` | `binary_choice` | 复用判断题表单 | 严格实现 | 契约测试 |
| step-12 | `summary_quiz_board` / `quiz+summary+next-step` | `quiz_group` | 复用后测表单 | 严格实现 | 契约测试 |

## 接线清单

| 项目 | 文件 |
| --- | --- |
| 课程元数据 / 契约 / 会话 adapter | `src/lib/unit-4-1-course.ts` |
| 步骤级 AI 上下文 | `src/lib/unit-4-1-ai-contexts.ts` |
| 入口页 / 头部 / 学生页 / 教师页 | `src/features/interactive/unit-4-1-design-task-expression/*` |
| 预置教案 | `src/features/teacher/preset-lessons/presets/unit-4-1-design-task-expression.ts` |
| 学生 / 教师 / 入口路由页 | `src/app/interactive-learning/courses/unit-4-1-design-task-expression/*` |
| 课程目录与课堂路由 | `src/features/interactive/learning-catalog.ts`、`src/lib/classroom-session-route.ts` |
| 测试 | `src/features/interactive/__tests__/unit-4-1-course.test.ts` |

## 本轮回修记录（2026-04-13）

- `step-04`：去掉静态四联图截图，改为原生 SVG 动态曲线面板；控件栏下置折叠，滑块会即时驱动图形重绘。
- `step-04`：页面顺序回正为“对象框图 -> 开环传函 -> 动态四联图”，与讲义一致。
- `step-05`：补回稳定平台对象与开环传函，并把综合图改成原生动态曲线布局。
- `step-07`：补回 `ISE / IAE / ITAE` 完整积分表达式，并增加原生角色矩阵。
- `step-09`：原生重绘任务表达卡模板，补齐“最紧矛盾”字段。
- `step-10`：补回可行域/满意域/最优域完整公式，并增加原生分层示意图。
- `step-11`：修正 `ChoiceGroup`，内部枚举值不再泄露到学生前端。
- 技能回修：`.codex/skills/interactive-lesson-implementation/SKILL.md` 已显式加入“动态曲线必须真联动”“结构示意图必须原生重绘”“内部状态名不得漏到前端”等硬约束。

## 完整课件职责核对

- 关键公式已回到静态内容层，不再依赖提交后反馈或互动控件解释。
- 动态曲线步骤保持讲义阅读顺序，且默认状态对齐 handout 基线。
- 角色矩阵、任务卡模板、区域分层图均已改为原生组件承载，不再依赖位图示意。
- 学生作答区仍维持提交态、释放态与答案揭示闭环。

## 验证记录

- `npm run test:unit -- src/features/interactive/__tests__/unit-4-1-course.test.ts`：通过
- `npm run lint`：通过
- `npm run test`：通过
- `npm run build`：通过；存在仓库既有的 Next.js dynamic server usage 构建告警，但构建成功
- `python3 .codex/skills/interactive-lesson-implementation/scripts/check_contract_alignment.py --contract course-content/authoring/lessons/4-1/design/interactive-contract.yaml --implementation src/lib/unit-4-1-course.ts --page-contract-const UNIT_4_1_PAGE_CONTRACTS --step-const UNIT_4_1_LESSON_STEPS`：通过
- `python3 course-content/scripts/review_lesson_content.py --lesson 4-1 --strict-implementation-contract`：通过并导出 runtime
- 浏览器验收：
  - `step-04` 已确认滑块展开后可驱动 SVG 曲线路径变化
  - `step-07` 已确认完整积分公式与原生角色矩阵同页可见
  - `step-09` 已确认原生任务表达卡模板与“最紧矛盾”字段同页可见
  - `step-10` 已确认区域分层公式与原生分层示意同页可见
  - `step-11` 已确认按钮文案不再暴露 `stable_equals_done` 等内部键名
