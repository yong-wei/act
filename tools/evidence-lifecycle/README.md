# QA evidence lifecycle tool

Classifies tracked `artifacts/` entries, fail-closes product imports of those
paths, and emits a hashed deletion/retention receipt for run-specific outputs.

Commands: `npm run qa-evidence:check` and `npm run qa-evidence:write`.
