# Independent toolchain execution boundary

This directory is the explicit ACT tool-boundary entry. It classifies tracked
tool sources, binds each class to one owner and command identity, and fail-closes
product imports of tool implementations.

It does not migrate Teaching Projection, content/knowledge/runtime publishers,
migration scripts, or QA evidence in this change. Downstream OpenSpec changes
consume the revision-bound registry and denominator receipts.
