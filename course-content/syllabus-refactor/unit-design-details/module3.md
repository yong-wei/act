# 模块3单元设计细节

更新时间：2026-04-02

## 使用说明

- 本文件专门记录模块3的单元级设计细节：知识清单、能力清单、成文边界与前置/后续承接关系。
- 文件开头先给出模块3全部单元的边界详情表，后续再展开单元详细设计。
- 本文件与 [module-skeletons.md](/Users/YW/Documents/Site/act.just.edu.cn/course-content/syllabus-refactor/module-skeletons.md) 配套使用：前者看模块级骨架，后者看单元级落实。

## 本文件如何服务学生版讲义等产物

- 本文件是模块3的总体设计文稿，负责把“结构机理层”的单元边界、主线和收束方式固定下来。
- 本文件当前重点不是把每个知识点都讲成最终成稿，而是确保四类产物都围绕同一张“机制地图”展开。
- 本文件必须先钉住：
  - 哪些机理首次出现；
  - 哪些内容只允许调用不允许重讲；
  - 哪些实践只做验证、不再新增机理；
  - 哪些知识必须为模块4设计层留出清晰空间。

## 四类产物的使用边界

| 产物 | 主要职责 | 从本文件提取的内容 | 不在本文件展开的内容 |
|----------|----------|------------------|--------------------|
| 讲义 | 完整展开模块3的结构机理链，使学生可独立读懂“为什么” | 各单元首次机理、关键对比、必须保留的解释深度、与模块2/4的承接关系 | 最终全文推导、全部插图说明、完整习题解析 |
| 教案 | 作为课堂后台控制文稿，组织机理讲授顺序、页面切换与实践穿插节奏 | 单元核心问题、课堂重点、减载位置、页面切换节点、教师介入点、理论与实践衔接提醒 | 逐分钟时间轴、教师逐句台词、前台页面全文 |
| 互动课程设计 | 作为课堂前台页面化载体，统一承载机理主线展示与多域联动互动验证 | 平台验证点、对照版本、必须展示的图示/表格/推导骨架、学生提交产出、实践边界 | 平台前端实现代码、埋点字段、自动评分逻辑 |
| 媒体清单 | 服务页面化课程中的机理可视化、对比呈现与状态切换 | 根轨迹图、时域响应图、Bode/Nyquist 对照图、场景示意、互动前后状态素材 | 资源编号、规格参数、制作排期 |

## 单元边界详情表

| 单元 | 核心问题 | 必须首次出现的内容 | 可以调用但不重讲的内容 | 不应承担的内容 | 为后续学习保留的对象/语言 |
|------|----------|------------------|----------------------|--------------|------------------|
| `3-1（理论）` 纯极点视角下的稳定与动态基础 | 为什么高阶系统仍常能用低阶模型理解？ | 特征方程与闭环极点；稳定/临界/不稳定；极点对应运动模态；重根项；主导极点；纯极点分布与快慢/振荡性；高阶与近似低阶系统的 `Bode` 对比；卷积视角与模态叠加 | 模块2的时域指标、频域对象、基础响应语言 | 根轨迹定义与法则；增益驱动的完整迁移机制；零点；控制器联系；频域判稳 | 为 `3-2` 保留稳定边界与极点判读基础；为 `3-3` 保留纯极点出发点；为 `3-8` 保留双域近似的前置语言 |
| `3-2（实践）` 稳定边界可视化 | 高阶系统不显式求根时，如何用劳斯判据判断稳定、识别特殊情况并反推参数可行域？ | 普通劳斯判稳；右半平面根数；`首位为0` 与 `全零行` 处理；辅助方程；带参数劳斯表；区域约束与变量平移；劳斯结论与极点结构、时域/频域表现的跨域对应 | `3-1` 的稳定性语言、主导极点、纯极点动态解释 | 根轨迹法则系统训练；零点；控制器结构变化；频域综合解释 | 为 `3-3` 保留“参数继续变化会逼近或穿越稳定边界”的实感；为模块4保留“先保稳定可行域，再谈性能”的入口语言 |
| `3-3（理论）` 根轨迹机制与完整法则 | 增益为什么会推动闭环极点沿特定路径迁移，以及普通根轨迹有哪些必须讲全的完整法则？ | 根轨迹定义；开环增益与闭环极点迁移；幅值条件/相角条件；普通根轨迹完整法则（起点终点、分支/对称/连续、实轴区段、渐近线、分离点/汇合点、虚轴交点、出射角/入射角、根之和原则）；高阶系统中的迁移现象；单个极点类型对普通根轨迹的基本影响 | `3-1` 的纯极点语言与主导极点概念；`3-2` 的稳定边界感知；模块1中已有的增益直觉 | 广义根轨迹；`0°/180°` 根轨迹；非增益参数长例；零点引入；`PD/测速反馈/超前`；非最小相；频域判稳；完整设计题 | 为 `3-4` 保留“普通根轨迹读图 + 广义根轨迹迁移 + 主导极点近似验证”的理论底板；为 `3-5` 保留零点吸引作用和结构改变的前置语言 |
| `3-4（实践）` 广义根轨迹、主导极点近似与对象化验证 | 如何把 `3-3` 已讲的普通根轨迹法则继续推进到广义根轨迹、`0°/180°` 根轨迹以及主导极点近似前后对照中？ | 常用法则在主图读解中的应用；关键节点（分离点/汇合点、虚轴交点、主导极点候选、稳定窗口）的读图与验证；根轨迹增益到实际增益的换算；原系统与主导极点近似系统在时域/频域中的对照；广义根轨迹与 `0°/180°` 根轨迹；船舶案例中加入给定结构后非增益参数的影响 | `3-1` 的模态与主导极点近似；`3-2` 的稳定边界与参数可行域；`3-3` 的普通根轨迹完整法则；模块2的 Bode/Nyquist 图形对象 | 首次系统讲授普通根轨迹法则；零点机制的系统分类；完整 `PD/测速反馈/超前` 比较；非最小相；完整补偿设计 | 为 `3-5` 保留“零点机制与控制器联系”的系统展开；为 `4-1` 保留“结构改变后仍需用跨域证据判窗口”的前置经验 |
| `3-5（理论）` 零点引入与动态改善 | 为什么零点带来的变化不是简单增益放大？ | 零点对根轨迹的吸引作用；零点的时域影响；`PD` 与测速反馈的同源性；与纯增益调整的连续性与区别；超前校正与 `PD` 的联系与区别；非最小相系统概念；零点作用的频域解读 | `3-3` 的根轨迹语言；`3-4` 的极点实践基线；模块2的频率响应图形对象 | 完整超前校正设计；Nyquist 判据；Bode 判稳；模块4的选型与整定 | 为 `3-6` 保留零点实验框架；为 `3-7` 保留“结构改变不只限于极点”的过渡 |
| `3-6（实践）` 零点作用与动态改善实验 | 在零点机理已经清楚后，如何把时域/频域目标翻译为具体的零点设计动作，并比较不同结构的收益、代价与边界？ | 时域目标到目标极点区域的翻译；`PD`、测速反馈、简单超前的参数化设计链；同一频域指标下 `PD` 与超前的对照；右半平面零点的保守边界与附录示例 | `3-5` 的零点机理；`3-3/3-4` 的根轨迹与频域观察框架 | 模块4的约束排序、多目标权衡、整体方案选型与优化哲学 | 为 `3-7` 保留“动态改善不等于稳态改善”的对照；为 `3-8` 保留零点线的频域翻译素材；为模块4保留“整体设计理念高于单条零点设计链”的接口 |
| `3-7（理论）` 型别、积分环节与稳态改善 | 误差为什么会因系统结构与输入信号共同出现，又为什么不可能无代价地被消除？ | 误差的两类来源；输入作用下与扰动作用下的闭环传递函数；系统总输出与总误差；输入作用下与扰动作用下的误差传递函数；终值定理；系统类型（型别）；`Kp/Kv/Ka`；积分环节；`PI` 与滞后校正的机理、联系、区别与代价 | 模块2中的时域/频域对象；`2-1` 的结构表达工具；`3-5/3-6` 的动态改善线 | Nyquist 判据系统展开；Bode 判稳系统展开；完整频域校正设计；模块4选型整定；复杂多环结构的长链化简训练 | 为 `3-8` 保留低频补偿线的频域翻译对象；为 `3-9` 保留稳态改善路径 |
| `3-8（理论）` 频域判别与跨域综合语言 | 如何把前面学过的增益、极点、零点、积分全部翻译成一套频域判断语言？ | 增益、零点、积分/滞后在频域中的表现；Nyquist 判据；Bode 判稳；相位裕度、增益裕度、带宽；三种稳定语言的对应 | 模块2的 Bode/Nyquist 图基础；`3-1` 到 `3-7` 的机制线 | 图形绘制基础重讲；首次机理推导重讲；完整补偿器参数计算；模块4设计流程 | 为 `3-9` 保留综合映射框架；为模块4保留频域设计入口 |
| `3-9（实践）` 稳定—动态—稳态综合映射实验 | 模块3学过的几条机制线能否收束成一张可进入设计层的结构地图？ | 基准、积分、自选补强三类版本的跨域比较；结构变化与稳定/动态/稳态/频域后果的综合映射 | `3-1` 到 `3-8` 全部已建语言 | 模块4的完整设计流程；控制器选型与优化；唯一最优方案判断 | 形成模块4可直接调用的结构机理地图 |

## 模块3对四类产物的模块级成文约束

### 学生版讲义主线约束

**模块级组织原则**

- 讲义必须把模块3写成“结构机理地图”，而不是重新长成传统章节拼接版教材。
- 模块3讲义的主线必须稳定保持为：
  - 纯极点语言；
  - 增益推动的根轨迹机制；
  - 零点引入与动态改善；
  - 型别、积分与稳态改善；
  - 频域判别与综合翻译。
- 讲义最重要的任务不是把公式堆满，而是把“为什么这样变”讲透，并为模块4设计语言留出清晰空间。

**单元到学生版讲义约束表**

| 单元 | 讲义必须展开的内容 | 讲义可降位处理的内容 | 讲义禁止扩张的内容 |
|------|------------------|--------------------|------------------|
| `3-1` | 纯极点视角下稳定、模态、主导极点、`Bode` 近似对比与卷积实验的解释链 | 高阶对象的过多变体、判稳与设计技巧 | 根轨迹、零点和设计层内容 |
| `3-2` | 普通情形、特殊情况、参数可行域与跨域认识组成的完整劳斯链条 | 大量重复算表训练 | 根轨迹法则系统训练 |
| `3-3` | 根轨迹定义、幅角/幅值条件、普通根轨迹完整法则、出射角/入射角公式、根之和原则 | 关键节点精算与高强度手工绘图技巧 | 广义根轨迹长例、`0°/180°` 根轨迹长推导、完整设计题与零点补偿设计 |
| `3-4` | 关键节点读图、参数窗口验证、主导极点近似前后对照、广义根轨迹与非增益参数案例 | 个别对象的繁琐参数细节与高频指标细算 | 零点机制系统分类、补偿器和设计流程 |
| `3-5` | 零点引入、PD/测速反馈/超前校正的同源性与非最小相风险 | 单一控制器流派的枝节分类 | 完整超前设计和模块4选型整定 |
| `3-6` | 零点作用的跨域对照与非最小相风险记录 | 个别实验参数细节 | 精确参数推导和完整设计任务 |
| `3-7` | 给定/扰动作用下的闭环与误差传函、终值定理、型别、`Kp/Kv/Ka`、积分、PI、滞后校正及其代价 | 公式熟练训练细节 | 完整频域校正设计 |
| `3-8` | 频域翻译、Nyquist 判据、Bode 判稳、裕度与三种稳定语言对应 | 单独的图形绘制回顾 | 模块4的选型、整定和优化 |
| `3-9` | 跨域机制映射与模块3出口总结 | 单个版本的枝节表现 | 直接给出模块4最优设计答案 |

### 教师版教案约束

**模块级组织原则**

- 教案必须把模块3组织成“机理讲授 + 验证实践 + 收束出口”的链条，而不是理论课和实验课互相分离的两条线。
- 教师后台控制的重点应是：
  - 哪些机理是首次进入；
  - 哪些只允许调用不允许重讲；
  - 何时切到平台验证；
  - 何时明确收束到模块4的设计语言。

**单元到教师版教案约束表**

| 单元 | 教师主讲重点 | 教师介入点 | 课堂收束句 |
|------|-------------|-----------|-----------|
| `3-1` | 纯极点本身已经能解释很多动态现象 | 学生把极点只当坐标点、不当机理对象时 | 先看极点自己能说明什么 |
| `3-2` | 劳斯不是算表，而是高阶系统稳定分析与参数筛选工具 | 学生把 `首位为0` 误当成固定物理模式，或把临界边界看成“只是变慢”时 | 稳定边界和参数可行域是后续根轨迹与设计层的起点 |
| `3-3` | 增益变化为什么推动极点沿轨迹迁移，为什么先抓全局骨架就足以解释大趋势 | 学生把根轨迹误当纯画图规则，或急于陷入细节公式时 | 根轨迹先是增益作用的结构语言，再是读图工具 |
| `3-4` | 如何把关键节点、参数窗口和对象差异读成三域判断 | 学生只看一域变化、不做跨域对应，或把读图重新退回纯算点题时 | 图一旦读对，结构、参数和后果会一起出现 |
| `3-5` | 零点改善不是简单增益放大 | 学生把 PD/超前只当不同名字时 | 看零点，不只看控制器名称 |
| `3-6` | 零点线必须和极点线形成对照 | 学生只记“改善了”不说代价时 | 动态改善从来带条件 |
| `3-7` | 误差对象为什么必须按给定/扰动双通道分析，稳态改善为什么总伴随代价 | 学生把静态误差系数表当成所有问题通解，或把积分环节理解成“白送精度”时 | 先分清误差通道，再谈提高型别 |
| `3-8` | 前面所有结构变化都能被翻译到频域 | 学生把判据当新知识孤岛时 | 频域不是新章，而是统一翻译器 |
| `3-9` | 模块3出口是机制地图，不是最优答案 | 学生急于跳到设计结论时 | 先收束机制，再进入设计 |

### 互动页面约束

**模块级组织原则**

- 模块3前台页面必须优先承担“机理看见了没有、联动看见了没有、学生能不能给出解释”这三类任务。
- 页面化互动应围绕：
  - 结构变化可视化；
  - 三域联动对照；
  - 预测与验证；
  - 结构化解释提交。

**单元到互动页面约束表**

| 单元 | 页面静态骨架 | 优先互动化内容 | 不建议互动化的内容 |
|------|-------------|---------------|------------------|
| `3-1` | 纯极点分布、模态示意、主导极点提示、双域近似对照页 | 极点分布与动态特征配对、近似有效性判断 | 复杂根轨迹手绘与频域判稳交互 |
| `3-2` | 劳斯表骨架、特殊情况方法卡、参数可行域流程图 | 参数拖动下的稳定/临界/失稳判断，特殊情况识别与修正 | 机械表格填空竞速 |
| `3-3` | 根轨迹主图、法则总表、出射角/入射角公式卡、根之和原则提示卡 | 轨迹趋势预测与验证、法则分类、关键规则补全 | 高强度手工绘图步骤交互与关键节点机械求解 |
| `3-4` | 关键节点标注图、参数窗口读图页、原系统/近似系统对照页、广义根轨迹与 `0°/180°` 根轨迹示意页 | 三域联动观察、预测-验证记录、窗口判断、增益换算与非增益参数判断 | 过细的频域参数计算与完整设计求解 |
| `3-5` | 零点引入前后对照图、控制器同源性表 | 零点引入效果判断、非最小相风险识别 | 完整超前设计输入 |
| `3-6` | 四版本对照面板 | 版本比较、风险说明提交 | 参数调优竞赛式交互 |
| `3-7` | 给定/扰动双通道骨架图、终值定理流程图、型别/误差系数/积分作用骨架图 | 误差通道识别、稳态改善路径判断、代价识别 | 复杂公式反复演算 |
| `3-8` | 三种稳定语言对照页、Bode/Nyquist 收束页 | 频域判别、裕度与时域特征对应 | 图形绘制基础重练 |
| `3-9` | 跨域机制映射总览页 | 综合映射填表、结构化解释提交 | 直接跳到设计求最优 |

### 媒体准备约束

**模块级组织原则**

- 模块3的媒体必须优先服务“机理看得见、对照看得见、代价看得见”，不能退回静态公式墙。
- 所有媒体优先分成两类：
  - 静态页面媒体：推导骨架、根轨迹主图、响应曲线、Bode/Nyquist 对照图、结构比较表；
  - 互动状态媒体：预测态、验证态、标注态、对错对照态、版本比较态。

**单元到媒体准备约束表**

| 单元 | 静态页面媒体 | 互动状态媒体 | 媒体目的 |
|------|-------------|-------------|---------|
| `3-1` | 极点分布与模态对照图、主导极点近似对照图、高阶与低阶 `Bode` 对比图 | 极点-动态特征配对反馈态、近似有效性判断反馈态 | 让学生把极点当机理对象看，并理解近似何时可信 |
| `3-2` | 劳斯边界示意图、特殊情况方法卡、临界响应图、参数可行域流程图 | 参数拖动后的稳定区反馈态、全零行/临界振荡对照态 | 让稳定边界、特殊情况与参数筛选逻辑都真正可见 |
| `3-3` | 根轨迹主图、法则骨架图、全局趋势示意图 | 轨迹预测与修正反馈态 | 让增益迁移逻辑和全局读图骨架可见 |
| `3-4` | 三结构三域对照图、关键节点标注图、参数窗口对照图 | 预测/验证对照态、窗口判断反馈态 | 让极点结构的联动效应和工程读图结果都可见 |
| `3-5` | 零点作用对照图、控制器同源性图 | 零点引入前后差异反馈态 | 让零点不是“新名字”而是“新结构” |
| `3-6` | 四版本比较总览图、非最小相风险图 | 版本选择与风险记录反馈态 | 让动态改善与风险同时可见 |
| `3-7` | 给定/扰动作用下闭环与误差通道对照图、型别与误差系数关系图、积分/滞后代价图 | 误差通道识别反馈态、稳态改善路径判断反馈态 | 让“误差从哪来、改善为什么有代价”同时可见 |
| `3-8` | Bode/Nyquist/极点语言对照图 | 判据判断与对应关系反馈态 | 让频域成为统一翻译器 |
| `3-9` | 跨域机制映射总表 | 学生填表与标准对照反馈态 | 让模块3出口真正形成地图 |

### 成文与落地时的额外约束

- 讲义成稿时，不允许把模块3重新拆回传统章节顺序，破坏当前“结构机理层”主线。
- 教案成稿时，必须显式写出哪些内容首次讲、哪些只调用、哪些通过实践验证，不允许理论与实践各写各的。
- 互动课程成稿时，必须优先承担预测、验证、比较和解释任务，而不是只展示漂亮图。
- 媒体制作时，任何素材都必须明确对应以下至少一类教学动作：
  - 机理识别；
  - 结构对照；
  - 三域联动观察；
  - 收益与代价判断；
  - 模块出口收束。

## 单元详细设计

### `3-1（理论） 纯极点视角下的稳定与动态基础`

**定位**

- 在不引入零点、不引入结构改动的前提下，用纯极点语言建立高阶系统稳定、动态与低阶近似判断基础。
- 把“为什么高阶系统仍常能用低阶模型理解”讲成模块3的第一堂双域基础课。
- 为后续根轨迹、零点机制与频域综合语言提供统一的极点出发点。

**前置缺口与边界提醒**

- 前接 `2-2` 的低阶时域对象与指标语言，但不重复做一阶/二阶公式训练。
- 前接 `2-4` 的图形对象已可见状态，可调用 `Bode` 图做近似有效性的证据，但不在本课进入 Nyquist 判据或 Bode 判稳。
- 后接 `3-2` 的劳斯操作化与稳定边界可视化。
- 后接 `3-3` 的增益驱动根轨迹迁移机制。
- 后接 `3-8` 的频域综合语言，但不提前讲判稳与裕度。

**课次框架**

1. 从低阶对象回到一般高阶系统：
   提出“低阶系统可由参数和指标解释，那么高阶系统为什么仍然能抓住主要动态”的核心问题。
2. 用特征方程与闭环极点重建稳定底线：
   把稳定、临界稳定、不稳定重新压实为闭环极点位置问题，让左半平面、虚轴、右半平面的含义先稳定下来。
3. 用模态展开、共轭根与重根解释时域特征：
   把极点从“复平面坐标点”转成“运动模态”，说明无重根、共轭复根和重根怎样进入自由响应表达式。
4. 用主导极点与指标映射解释时域近似：
   说明快模态为何会退场，主导模态为何能决定主要超调、调节时间和振荡节奏。
5. 用高阶系统与近似低阶系统的 `Bode` 对比解释频域近似：
   说明被忽略极点在关注频段内影响为何可能很小，以及何时这种近似仍可信。
6. 用卷积数值实验与模态叠加收束：
   说明阶跃响应可以看成多个延时脉冲响应的叠加，并把“输入激发模态”的链条讲闭环。

**资源融入评审单**

| source_path | resource_type | use_position | usage_mode | adoption_level | boundary_note |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/8稳定_控制系统首要任务/README.md` | `pptx` | 导入 / 正文 / 练习 | 改写吸收 | `必融入` | 只服务稳定概念、左半平面条件、劳斯判据来源与稳定问题意识；不照搬整串劳斯例题 |
| `course-content/resource-library/pptx/7.2拉氏变换_工程直觉的数学实现/README.md` | `pptx` | 正文 / 附录 | 改写吸收 | `必融入` | 服务部分分式、重根项、卷积与“时域响应如何分解成模态”的解释链 |
| `course-content/resource-library/pptx/6性能指标_控制效果评价/README.md` | `pptx` | 正文 / 例题 / 总结 | 改写吸收 | `必融入` | 只服务模块2指标语言与主导极点近似的再挂接，不回到模块2指标定义课 |
| `course-content/resource-library/civics-cases/cases/03-数理交响启示录.md` | `civics` | 导入 / 总结 | 改写吸收 | `可选融入` | 只强化“稳定优先、数学服务工程”的价值收束，不替代理论主线 |
| `course-content/resource-library/ship-control-cases/sections/2.1-船舶航向控制建模实例.md` | `ship-case` | 导入 / 总结 | 改写吸收 | `可选融入` | 只服务稳定性与机动性的工程矛盾导入，不转成案例分析长段落 |
| `course-content/syllabus-refactor/homework-framework.md#t3-1-c-劳斯判据与稳定区间` | `exercise` | 练习 / 课后校准 | 作为边界校准 | `必对齐` | 只用于校准单参数稳定区间题的强度，不反向决定课堂内容组织 |
| `course-content/resource-library/ship-control-cases/sections/4.1-4.3` | `ship-case` | 正文 / 例题 | 暂不采用 | `排除` | 这些材料属于根轨迹对象对照，更适合 `3-3 / 3-4` |
| `course-content/resource-library/pptx/11放眼大局_根轨迹法/README.md`、`12根轨迹_基本形态/README.md`、`13根轨迹_细节修正/README.md`、`14参数根轨迹_广义定义/README.md`、`15.1根轨迹法_图形化思考/README.md` | `pptx` | 正文 / 互动 | 暂不采用 | `排除` | 避免本课提前滑入根轨迹机制、法则和手工绘图负担 |
| `course-content/resource-library/pptx/19稳定判据_频域的启示/README.md` | `pptx` | 正文 / 总结 | 暂不采用 | `排除` | 避免本课提前滑入 Nyquist 判据与 Bode 判稳 |

**知识清单**

- 特征方程与闭环极点。
- 稳定、临界稳定、不稳定的极点判据。
- 极点对应运动模态的基本效应。
- 无重根、共轭复根与重根进入时域响应的方式。
- 主导极点思想。
- 纯极点分布与响应快慢、振荡性之间的联系。
- 高阶系统与近似低阶系统的 `Bode` 对比。
- 时域响应的卷积视角及其与模态分解的关系。

**能力清单**

- 用极点分布解释系统稳定性与主要动态特征。
- 识别高阶系统中的主导模态。
- 结合时域与频域对比判断低阶近似是否可信。
- 从卷积与模态叠加角度解释响应形成机制。

**边界**

- 不进入根轨迹法则。
- 不进入增益变化导致的完整轨迹迁移机制。
- 不进入零点引入与控制器结构变化。
- 不进入频域判稳。
- 允许使用 `Bode` 图做近似有效性证据，但不进入 Nyquist 判据、Bode 判稳与裕度结论。
- 劳斯判据的操作化使用前移到 `3-2` 的前置理论段。

**对四类产物的直接约束**

- 讲义必须把“极点为什么能代表系统主要行为、为什么高阶系统常可低阶近似”讲透，重点是稳定底线、模态解释、主导极点、`Bode` 对比与卷积实验，而不是堆劳斯表格题。
- 教案必须把教师介入点放在“学生是否把极点当机理对象看”上，而不是放在复杂代数运算纠错上。
- 互动课程设计应优先做“极点分布 - 动态特征”配对、主导模态判断、近似有效性判断和模态叠加解释，不做根轨迹或控制器调参式互动。
- 媒体清单应优先准备左半平面/虚轴/右半平面稳定示意、极点与响应形态对照图、主导极点示意、高阶与低阶 `Bode` 对比图和模态叠加骨架图。

### `3-2（实践） 稳定边界可视化`

**定位**

- 作为模块3中唯一系统讲透劳斯判据的高阶系统稳定分析课，把 `3-1` 建立的极点语言转化为“不显式求根也能分析高阶系统稳定性与参数可行域”的方法。
- 核心不再只是“看见稳定边界”，而是把劳斯判据讲成一套完整链条：普通判稳、特殊情况处理、跨域解释、参数反推与区域约束。
- 把“稳定 / 临界稳定 / 不稳定”与极点结构、时域响应和频域特性建立起第一轮明确对应，同时为 `3-3` 的根轨迹机制提供越轴与临界振荡直觉，但不提前进入根轨迹法则。

**前置缺口与边界提醒**

- 前接 `3-1` 的稳定底线、左半平面条件和高阶纯极点语言，但不重复主导极点近似与双域近似主讲。
- 后接 `3-3` 的增益驱动迁移机制；本课负责把“参数推到哪里会碰到边界、碰到边界会发生什么”讲清，但不解释完整轨迹法则。
- 后接 `4-1 / 4-3` 的任务表达与初始方案形成；本课需先建立“参数先满足稳定可行域，再谈性能优化”的方法论入口。
- 与 `course-content/syllabus-refactor/homework-framework.md` 中 `T3-1` 对齐：闭题主任务仍以高阶系统稳定区间与临界条件为主，但课堂必须把特殊情况和参数设计升华讲透。

**组织方式**

- 采用一个统一高阶对象。
- 以高阶系统为默认对象位阶，不退回二阶模板。
- 采用“一个主对象族 + 两个短例”的组织方式：
  - 主对象族围绕单参数变化展开，负责普通情形、参数稳定范围与跨域对照；
  - 短例 A 负责 `首位为0且非全零行`；
  - 短例 B 负责 `全零行` 与辅助方程。
- 对象要求：
  - 闭环极点随单参数连续变化；
  - 能出现清晰稳定边界；
  - 临界点附近响应变化明显；
  - 结构上保持纯极点语言，不引入零点。
- 推荐对象结构：
  - `积分 + 双惯性环节`
- 该对象在增益增大后会演化出复平面上的共轭分支，可自然过渡到“二阶振荡 + 一阶惯性”的动态表现。

**课堂主线**

1. 用左半平面 / 虚轴 / 右半平面示意，回收 `3-1` 刚建立的稳定底线。
2. 用统一高阶主对象建立普通劳斯表，先讲清“第一列符号变化次数 = 右半平面根数”。
3. 在主对象上求稳定区间和临界参数，并对照极点分布与阶跃响应，说明“边界”不是“系统只是变慢了”。
4. 补入短例 A：讲清 `首位为0且非全零行` 时为什么要用极小正数替代，以及这更像算法奇点而不是一种固定物理模式。
5. 补入短例 B：讲清 `全零行`、辅助方程、虚轴根/对称根结构，以及临界振荡在时域和频域中的表现。
6. 在主对象上继续做典型参数的时域响应、极点位置和基础频率响应对照，形成“劳斯结论—极点结构—时域/频域表现”跨域映射。
7. 用带参数劳斯表把“判稳”提升为“反推参数可行域”。
8. 用变量代换与平移变换说明：如果要求极点落在某条竖线左侧，劳斯判据仍可继续服务参数设计。
9. 用一句最小收束提示后续学习：`3-3` 解释轨迹为何这样走，模块4再把可行域语言转成控制参数选择。

**资源融入评审单**

| source_path | resource_type | use_position | usage_mode | adoption_level | boundary_note |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/8稳定_控制系统首要任务/README.md` | `pptx` | 导入 / 正文 / 互动 / 练习 / 总结 | 改写吸收 + 直接复用图片 | `必融入` | 吸收稳定概念、劳斯骨架、两类特殊情况、参数稳定范围与平移变换的完整方法链；不照搬旧页序与纯例题串讲 |
| `course-content/questions/questions/AC-Q-0048.md` | `exercise` | 正文 / 互动 / 练习 | 改写吸收 | `必融入` | 作为“参数稳定范围 + 典型参数响应趋势验证”的主练习候选，服务劳斯到动态趋势的跨域对照 |
| `course-content/questions/questions/AC-Q-0047.md` | `exercise` | 例题 / 练习 / 课后校准 | 改写吸收 | `可选融入` | 作为从闭环特征方程到参数区间的辅助练习，不重复堆叠同类计算 |
| `course-content/questions/questions/AC-Q-0046.md` | `exercise` | 正文 / 课堂短例 / 讲义方法卡 | 改写吸收 | `必融入` | 服务 `首位为0`、`全零行` 与辅助方程处理原则，重点是方法辨识与根结构解释，不把它做成独立刷题段落 |
| `course-content/syllabus-refactor/homework-framework.md#t3-1-c-劳斯判据与稳定区间` | `exercise` | 边界校准 / 练习 / 考点收束 | 作为边界校准 | `必对齐` | 保持高阶系统、单主参数与参数可行域主线，并允许把特殊情况识别作为短小附问或题库储备 |
| `course-content/resource-library/civics-cases/cases/03-数理交响启示录.md` | `civics` | 总结 | 改写吸收 | `可选融入` | 只服务“数学工具最终要回到工程判断”的总结，不重复 `3-1` 的导入功能 |
| `course-content/resource-library/ship-control-cases/sections/4.1-4.3` | `ship-case` | 正文 / 对比 | 暂不采用 | `排除` | 这些材料属于根轨迹对象对照，更适合用于 `3-3 / 3-4`，不能在边界课里提前拉入对象差异 |
| `course-content/resource-library/pptx/11放眼大局_根轨迹法/README.md`、`12根轨迹_基本形态/README.md`、`13根轨迹_细节修正/README.md`、`14参数根轨迹_广义定义/README.md`、`15.1根轨迹法_图形化思考/README.md` | `pptx` | 正文 / 互动 | 暂不采用 | `排除` | 避免把本课变成根轨迹法则预讲或手工绘图准备课 |

**产出**

- 稳定范围判断卡。
- 特殊情况处理卡：
  - `首位为0且非全零行`；
  - `全零行 + 辅助方程`。
- 临界参数与参数可行域记录。
- 跨域对照表：
  - 劳斯结论；
  - 极点结构；
  - 时域响应特点；
  - 频域特性线索。
- 一段简短解释：
  - 为什么劳斯判据在计算机判稳便利的今天仍然服务参数设计。

**边界**

- 不进入根轨迹法则。
- 不要求学生画根轨迹。
- 不引入零点。
- 不引入 `PD/PI/超前/滞后`。
- 不做频域判稳，只允许把频率特性作为辅助观察对象，不作为本单元主任务。
- 可以讲基础频率响应变化与共振特征，但不把它升级为 Nyquist 判据、Bode 判稳或裕度计算。
- 不能把 `首位为0` 简化成某种固定物理模式；必须讲清它主要是算法奇点与连续性处理问题。

**对四类产物的直接约束**

- 讲义必须按“普通情形 -> 特殊情况 -> 跨域认识 -> 参数设计方法论”组织，不得退回成纯表格题单，也不得把特殊情况和区域约束全部删成附录。
- 教案必须把教师介入点放在三处：学生是否会把劳斯当作求根替代器、是否真正区分两类特殊情况、是否能从判稳走到参数可行域表达。
- 互动课程设计应优先做高阶对象参数拖动、普通情形/全零行对照、临界振荡观察、参数可行域标记和响应趋势验证，不做机械填表竞速。
- 媒体清单应优先准备左半平面条件图、劳斯表骨架图、特殊情况方法卡、临界振荡对照图、参数可行域流程图和典型参数的时域/频域对照图；船舶根轨迹图与根轨迹规则图不进入本课主媒体集。

### `3-3（理论） 根轨迹机制与完整法则：普通根轨迹的形成与关键法则`

**定位**

- 把“开环增益改变为什么会导致闭环极点沿特定路径迁移”讲清，并把普通根轨迹重新确立为模块3中唯一系统承担完整法则体系的理论课。
- 重点修补真实授课中最容易被挤掉却又必须讲全的规则：出射角/入射角公式、根之和原则，以及它们和分离点、虚轴交点之间的关系。
- 作为模块3前半段的转轴课，前接 `3-2` 的稳定边界意识，后接 `3-4` 的广义根轨迹、`0°/180°` 根轨迹和主导极点近似验证，以及 `3-5` 的零点语言。

**前置缺口与边界提醒**

- 前接 `3-1` 的纯极点语言与 `3-2` 的参数可行域意识，但不重复劳斯表与特殊情况处理。
- 后接 `3-4` 的广义根轨迹、参数方向变化、主导极点近似前后对照与工程读图实践。
- 后接 `3-5` 的零点吸引作用与结构改变语言。
- 与模块4的承接只保留在“趋势判断”和“关键法则读图语言”，不在本课进入设计选型。

**课堂主线**

1. 从 `3-2` 的问题出发：若参数已经知道会逼近边界，闭环极点究竟怎样连续地走向这条边界？
2. 用闭环特征方程和 `GH=-1` 把普通根轨迹重新定义成“正增益连续变化下的闭环极点集合”，强调它研究的是趋势而不是单点答案。
3. 用幅值条件与相角条件建立“点在不在轨迹上、该点对应多大参数”的通用判断框架。
4. 系统讲普通根轨迹完整法则体系：
   - 起点与终点；
   - 分支数、连续性与实轴对称；
   - 实轴区段；
   - 渐近线与重心；
   - 分离点 / 汇合点；
   - 虚轴交点；
   - 出射角 / 入射角；
   - 根之和原则。
5. 明确正文与附录的分工：
   - 常用法则进入正文主线；
   - 计算量较大但不可缺席的规则保留在正文中说明用途，并在讲义附录 / 拓展页中给出完整推导与例子。
6. 用一个纯极点基础对象和一个高阶对象说明：增益增大时，极点如何从“稳定而慢”逐步走向“更快 / 更振荡 / 逼近失稳边界”。
7. 把原点极点、实轴极点、共轭极点三类基本影响先讲成“趋势预告”，为 `3-4` 的对象化验证留出空间。
8. 以一句话桥接到 `3-4`：当变化参数不再只是正增益时，需要引入广义根轨迹与 `0°/180°` 根轨迹继续分析，但这部分不再占用本课主体时间。

**资源融入评审单**

| source_path | resource_type | use_position | usage_mode | adoption_level | boundary_note |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/11放眼大局_根轨迹法/README.md` | `pptx` | 导入 / 正文 / 总结 | 改写吸收 + 直接复用图片 | `必融入` | 负责“为什么研究轨迹而不是单点”“GH=-1 与幅角 / 模值条件”的机制入口，不照搬旧页序 |
| `course-content/resource-library/pptx/12根轨迹_基本形态/README.md` | `pptx` | 正文 / 互动 / 总结 | 改写吸收 + 直接复用图片 | `必融入` | 负责起点终点、实轴区段、渐近线与重心等普通根轨迹主规则 |
| `course-content/resource-library/pptx/13根轨迹_细节修正/README.md` | `pptx` | 正文 / 附录 / 拓展 | 改写吸收 + 直接复用图片 | `必融入` | 负责分离点、虚轴交点、出射角/入射角、根之和原则等关键补全规则，不能再被压缩到缺席 |
| `course-content/resource-library/civics-cases/cases/04-根轨全局观拓荒.md` | `civics` | 导入 / 总结 | 改写吸收 | `可选融入` | 只服务“先看趋势、抓主要矛盾”的全局观，不挤占方法主体 |
| `course-content/resource-library/pptx/14参数根轨迹_广义定义/README.md` | `pptx` | 过渡提醒 / 结尾桥接 | 改写吸收 | `可选融入` | 本课只保留一句桥接提醒，不把广义根轨迹推导重新压回正文主体 |
| `course-content/resource-library/pptx/15.1根轨迹法_图形化思考/README.md` | `pptx` | 正文 / 例题 | 暂不采用 | `排除` | 条件稳定、根轨迹增益换算和工程读图属于 `3-4` 的实践主负载，不应挤占本课 |
| `course-content/resource-library/ship-control-cases/sections/4.1-4.3` | `ship-case` | 正文 / 例题 | 暂不采用 | `排除` | 船舶对象更适合放在 `3-4` 做参数窗口、主导极点近似与广义根轨迹验证，不在本课拉成长例题 |

**知识清单**

- 根轨迹的基本定义与研究对象。
- 闭环特征方程与开环增益变化的关系。
- 幅值条件与相角条件。
- 普通根轨迹完整法则体系：
  - 起点与终点；
  - 分支数、连续性与对称性；
  - 实轴上的根轨迹区段；
  - 渐近线与重心；
  - 分离点 / 汇合点；
  - 虚轴交点；
  - 出射角 / 入射角；
  - 根之和原则。
- 单个极点类型对普通根轨迹的基本影响：
  - 原点极点；
  - 实轴极点；
  - 共轭极点。
- 增益变化与闭环极点迁移对稳定性、动态品质的影响。
- 高阶系统中增益变化的典型现象。

**能力清单**

- 根据开环结构判断普通根轨迹大致走向。
- 解释增益变化为什么导致稳定性、超调、响应速度同步变化。
- 用完整法则体系解释一张普通根轨迹主图的形成逻辑。
- 判断哪些规则必须进入正文，哪些推导可转为附录支持。
- 用根轨迹语言为后续关键节点读图和参数选择提供方向性判断。

**边界**

- 不把手工绘图训练作为重点。
- 不要求把所有法则都做成高强度刷题训练，但必须把普通根轨迹完整法则讲全。
- 计算量较大的规则允许在附录 / 拓展页展开，但正文必须正式提及其作用边界。
- 不展开广义根轨迹长例与 `0°/180°` 根轨迹推导。
- 不展开完整设计题求解。
- 不提前展开 `PD`、测速反馈、超前校正。
- 不转入频域判稳。

**对四类产物的直接约束**

- 讲义必须按“为什么研究轨迹 -> 条件从何而来 -> 普通根轨迹完整法则 -> 关键遗漏规则补齐 -> 增益迁移后果”组织；其中附录可承接较长推导，但不能替代正文中的正式出现。
- 教案必须把教师介入点放在三处：学生是否只把根轨迹当作画图规则，学生是否理解出射角/入射角与根之和原则的必要性，学生是否知道广义根轨迹已后移到 `3-4`。
- 互动课程设计应优先做法则分类、迁移趋势预测和关键规则补全，不做大量机械算点交互。
- 媒体清单应优先准备根轨迹概念图、条件框架图、完整法则总表、出射角/入射角公式卡和根之和原则提示卡；关键节点与工程窗口图保留到 `3-4` 主媒体集。

### `3-4（实践） 广义根轨迹、主导极点近似与对象化验证`

**定位**

- 承接 `3-3` 已建立的普通根轨迹完整法则，把根轨迹进一步升级为“能按法则读关键节点、能比较近似前后差异、能分析非增益参数”的工程判断工具。
- 把 `3-1` 的主导极点近似、`3-2` 的稳定边界意识和 `3-3` 的普通根轨迹法则联结起来，形成“法则 -> 图 -> 近似 -> 广义参数 -> 时域/频域后果”的综合认识。
- 让广义根轨迹、`0°/180°` 根轨迹、船舶案例与主导极点近似对照都找到正式落点，但不把课堂重新拉回完整补偿设计。

**组织方式**

- 第一段仍采用“同一对象族，三种典型工作点”的组织方式，用于完成关键节点读图、参数窗口判断和增益换算。
- 第二段显式回收 `3-1` 的主导极点近似：
  - 对比原系统与主导极点近似系统；
  - 先看时域差异；
  - 再看频域差异。
- 第三段采用“船舶航向控制 + 给定附加局部反馈结构”的工程对象迁移：
  - 默认令局部反馈系数作为非增益参数；
  - 用广义根轨迹分析该参数变化；
  - 在需要时引入 `0°/180°` 根轨迹解释参数方向变化。
- 船舶横摇减摇鳍对象仅作为高复杂度可选对照，不作为学生主工作区默认对象。

**任务**

- 在 `3-3` 已完成普通根轨迹法则讲授的前提下，先读取给定主图的关键节点：
  - 分离点 / 汇合点；
  - 虚轴交点；
  - 主导极点候选；
  - 稳定参数窗口。
- 对涉及中间量的案例，完成“根轨迹增益 -> 实际控制器增益”的换算。
- 再回收 `3-1` 的主导极点近似，用同一工作点比较原系统与近似系统：
  - 时域响应差异应保持“小而可解释”；
  - 频域比较必须显式指出低频与中频基本不变；
  - 同时给出一个高频明显改变的具体例子，说明近似不能替代高频细节判断。
- 在此基础上引入广义根轨迹与 `0°/180°` 根轨迹：
  - 说明参数不再只是正增益时，轨迹条件如何改写；
  - 解释参数方向变化为何会改变图形落点。
- 用船舶航向控制对象加入局部反馈结构的案例做“工程主图读法”迁移：
  - 读出非增益参数窗口；
  - 判断主导极点近似是否仍合理；
  - 解释参数变化为何会带来性能权衡。
- 完成“根轨迹—主导极点—时域—频域”对照记录与一句话机理解释。
- 课堂结束前必须收口为 3 项可提交产出：
  - `关键节点读图记录`
  - `参数窗口判断表`
  - `对象化验证记录`

**资源融入评审单**

| source_path | resource_type | use_position | usage_mode | adoption_level | boundary_note |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/13根轨迹_细节修正/README.md` | `pptx` | 正文 / 互动 / 练习 | 改写吸收 + 直接复用图片 | `必融入` | 负责把 `3-3` 已讲的关键规则转成主图读解与节点验证任务，不再承担首次系统讲法则 |
| `course-content/resource-library/pptx/14参数根轨迹_广义定义/README.md` | `pptx` | 正文 / 例题 / 总结 | 改写吸收 | `必融入` | 负责广义根轨迹、非增益参数与 `0°/180°` 根轨迹的正式落位 |
| `course-content/resource-library/pptx/15.1根轨迹法_图形化思考/README.md` | `pptx` | 正文 / 例题 / 总结 | 改写吸收 + 直接复用图片 | `必融入` | 负责参数窗口、条件稳定、增益换算与工程读图，不直接照搬旧例题串讲 |
| `course-content/resource-library/pptx/15.2根轨迹分析综合/README.md` | `pptx` | 对比 / 总结 / 近似验证 | 改写吸收 + 直接复用图片 | `必融入` | 负责主导极点近似前后在时域与频域中的对照例子，必须显式展示低中频近似不变与高频变化 |
| `course-content/resource-library/ship-control-cases/sections/4.1-船舶航向控制根轨迹分析.md` | `ship-case` | 正文 / 对比 / 互动 | 改写吸收 + 直接复用图片 | `必融入` | 负责工程对象下的主导极点选点、参数权衡与时域对照 |
| `course-content/resource-library/ship-control-cases/sections/5.1-船舶航向控制频域分析.md` | `ship-case` | 频域补证 / 总结 | 改写吸收 + 直接复用图片 | `可选融入` | 只服务低频/中频近似不变与高频差异的解释，不提前进入 `3-8` 的判稳语言 |
| `course-content/resource-library/ship-control-cases/sections/4.3-船载稳定平台控制系统根轨迹分析.md` | `ship-case` | 对比 / 总结 | 改写吸收 + 直接复用图片 | `可选融入` | 适合服务稳定窗口与虚轴附近读图，不必压成学生主工作区必做对象 |
| `course-content/resource-library/ship-control-cases/sections/4.2-船舶横摇减摇鳍控制根轨迹分析.md` | `ship-case` | 对比 / 拓展 | 仅作灵感 | `可选融入` | 复杂度较高，适合作为教师展示“多极点复杂对象”的补充，不强制纳入主线 |
| `course-content/resource-library/pptx/11放眼大局_根轨迹法/README.md`、`12根轨迹_基本形态/README.md` | `pptx` | 正文 | 暂不采用 | `排除` | 这两组资源已在 `3-3` 负责定义与普通根轨迹完整法则，不在 `3-4` 重复首次展开 |

**产出**

- 关键节点读图记录：
  - 分离点 / 虚轴交点 / 稳定窗口 / 主导极点候选。
- 参数窗口判断表：
  - 至少包含“根轨迹增益 -> 实际参数”换算栏；
  - 至少包含一个非增益参数窗口判断栏。
- 对象化验证记录：
  - 至少包含一个工程对象的主导极点近似判断；
  - 必须包含原系统 / 近似系统的时域与频域对照；
  - 必须写出低中频近似不变与高频变化的具体解释。
- 一段结论：
  - 为什么时域近似差异小是符合预期的；
  - 为什么低频与中频近似不变足以支撑主要动态与稳态判断；
  - 为什么高频变化仍必须被单独记录。

**边界**

- 不系统引入零点作用机理。
- 不展开 `PD`、测速反馈、超前校正的分类比较，只允许把附加结构当作非增益参数样例。
- 不进入非最小相系统。
- 不做 Bode 判稳。
- 不进入控制器设计。
- 不把船舶对象直接扩写成完整工程项目。
- 不把关键节点读图重新退回高强度手工绘图和纯代数算点竞赛。
- 不首次系统讲解普通根轨迹法则，而以应用、迁移和验证为主。

**对四类产物的直接约束**

- 讲义必须把 `3-4` 写成“图上关键节点 -> 参数窗口 -> 主导极点近似前后对照 -> 广义根轨迹与非增益参数”的实践课，而不是再写一份 `3-3` 的法则讲义。
- 教案必须把教师介入点放在四处：学生是否会从主图读出窗口、是否会做增益换算、是否会把主导极点判断翻译成时域/频域后果、是否理解广义根轨迹与 `0°/180°` 根轨迹为何后移到本课。
- 互动课程设计应优先做关键节点标注、窗口判断、原系统/近似系统对照和非增益参数预测-验证，不做无教学价值的滑块试玩。
- 媒体清单应优先准备关键节点标注图、窗口判读图、对象对照图、船舶主图裁剪版和换算示意图。
- 作者态起草前，必须先对齐 [authoring-brief.md](/Users/YW/Documents/Site/act.just.edu.cn/course-content/authoring/lessons/3-4/design/3-4-authoring-brief.md) 中的一句话主线、三项学生产出与资源采用级别，不再重复讨论单元属性。

### `3-5（理论） 零点引入与动态改善：PD、测速反馈、超前校正与非最小相的多视角审视`

**定位**

- 从根轨迹、频域和控制器结构三个视角讲清零点为什么会带来纯增益调整所没有的新变化。
- 以“零点”而不是“控制器种类”作为组织主轴。

**知识清单**

- 零点对根轨迹的吸引作用。
- 零点引入后闭环极点迁移趋势为什么改变。
- 零点引入后动态改善趋势的时域解释。
- 零点引入带来的相位提前趋势与带宽变化趋势。
- `PD` 与测速反馈的同源性。
- `PD/测速反馈` 与单纯增益校正的连续性与区别。
- 超前校正与 `PD` 的联系与区别。
- 非最小相系统的概念及其基本影响。
- 零点作用的频域解读。

**能力清单**

- 解释零点引入为何不是简单增益放大。
- 区分纯增益调整和零点引入带来的动态改善差异。
- 说明 `PD`、测速反馈与超前校正之间的同源性和差别。
- 识别右半平面零点带来的基本风险。

**边界**

- 不展开完整超前校正设计。
- 不系统展开 Nyquist 判据与 Bode 判稳。
- 不进入模块4的控制器选型与参数整定流程。

**建议课堂组织主线**

- 从 `3-4` 的边界回收：只调增益为什么很快不够。
- 先固定同一基准对象，比较“无零点 / 引入零点 / 不引入零点但改善阻尼”三类变化。
- 再把 `PD`、测速反馈、简单超前统一翻译为“结构怎样变、零点在哪里、极点为什么跟着变”。
- 最后用右半平面零点做教师演示，明确“不是所有零点都带来直观改善”。

**本课必须压实的三组对照**

- `纯增益调整 vs 零点引入`：
  - 重点回答“为什么轨迹和动态变化不再只是原轨迹上的平移”。
- `PD vs 测速反馈`：
  - 重点回答“为什么两者都可能减小超调，但只有前者在前向通道显式增加零点”。
- `左半平面零点 vs 右半平面零点`：
  - 重点回答“为什么看上去都叫零点，却会带来完全不同的工程后果”。

**资源融入评审单**

| 候选资源路径 | 资源类型 | 计划落点 | 采用方式 | 采用级别 | 边界说明 |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/12根轨迹_基本形态/README.md` | `pptx` | 正文导入 / 对照图 | 改写吸收 + 直接复用图片 | 必融入 | 只吸收“增加零点后根轨迹终点与实轴分布怎样改变”的基础骨架，不回到根轨迹法则重讲 |
| `course-content/resource-library/pptx/14参数根轨迹_广义定义/README.md` | `pptx` | 正文过渡 / 例题 | 改写吸收 | 可选融入 | 只服务“参数变化可能改变等效极点零点关系”的提醒，不展开广义根轨迹推导 |
| `course-content/resource-library/pptx/10.1校正_实现控制的手段/README.md` | `pptx` | 正文主体 / 对比总结 / 互动 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收 `PD` 与测速反馈同源性、差异性和对比图；排除前馈补偿、扰动补偿和整套校正分类展开 |
| `course-content/resource-library/pptx/22串联校正/README.md` | `pptx` | 正文后段 / 收束 | 改写吸收 + 直接复用图片 | 必融入 | 只吸收“超前网络为何提供相位提前、为何属于零点动态改善线”的频域直觉，不引入完整超前设计步骤 |
| `course-content/questions/questions/AC-Q-0080.md` | `exercise` | 例题 / 课堂判断题 | 改写吸收 | 可选融入 | 只取“零点位置变动如何改变轨迹和动态”的代表性结论，不照搬原题的完整计算与旧式图题表述 |
| `course-content/resource-library/pptx/23滞后超前/README.md` | `pptx` | 无 | 排除 | 排除 | 该资源主价值在 `3-7` 与模块4的联合校正/PID 桥接，若提前进入 `3-5` 会稀释“零点动态改善线”的单一主轴 |
| `course-content/resource-library/civics-cases/indexes/unit-mapping.md` 中现有案例 | `civics` | 无 | 排除 | 排除 | 目前没有比专业主线更强的嵌入点，本轮明确不强行接入思政案例，避免把 2 学时切碎 |
| `course-content/resource-library/ship-control-cases/sections/6.1-6.3` | `ship-case` | 无 | 排除 | 排除 | 这些资源属于模块4设计链，`3-5` 只讲机理，不提前把课堂拉成工程整定案例 |

**增强后建议固定产出**

- 一张“零点动态改善线”对照表：
  - 基准 / `PD` / 测速反馈 / 简单超前；
  - 是否显式增加零点；
  - 根轨迹主变化；
  - 时域主变化；
  - 频域主变化；
  - 主要收益与主要代价。
- 一张“左半平面零点 / 右半平面零点”风险卡。
- 一句模块级收束语：
  - 动态改善不等于设计完成，下一步还要回答稳态与约束。

**本课成文与呈现约束**

- 讲义：
  - 必须围绕“同一对象的四种结构变化”组织，避免按 `PD / 测速反馈 / 超前` 分三段平铺。
  - 必须把“零点为何改变动态”讲成三域联动，而不是只给时域结论。
  - 非最小相只做边界提醒，不写成独立专题。
- 教案：
  - 教师提问应优先围绕“是不是增加了零点”“零点在哪里”“为什么会改善/恶化”，而不是先问控制器名称。
  - 必须预留学生最常见误判：
    - 把测速反馈误判为也会增加零点；
    - 把超前校正误判为“只是更强的 `PD`”；
    - 把右半平面零点误判为“零点越多越快”。
- 互动课程设计：
  - 前台页面应至少支持“先预测后验证”的比较任务。
  - 互动重心放在轨迹、阶跃、Bode 趋势三联对照，不做完整参数整定游戏。
  - 右半平面零点固定为教师演示或受控揭示，不做全员自由调参主任务。
- 媒体清单：
  - 必须准备基准 / `PD` / 测速反馈 / 简单超前的统一对照图。
  - 必须准备 `PD` 与测速反馈结构差异图，以及左半平面 / 右半平面零点对照图。
  - 若复用旧 `pptx` 图片，需统一改写标题口径为“零点作用”而非“控制器种类大全”。

### `3-6（实践） 零点作用与动态改善实验`

**定位**

- 用统一对象把 `3-5` 的零点机理推进到“时域目标 / 频域目标如何落到零点设计动作”的可计算实践。
- 在模块4整体设计理念展开前，给 `PD`、测速反馈、简单超前和非最小相边界提供正式的参数化落点。

**组织方式**

- 一个统一基准对象。
- 4 个学生主任务：
  - 时域目标驱动的 `PD` 设计；
  - 时域目标驱动的测速反馈设计；
  - 频域目标驱动的简单超前设计；
  - 同一频域指标下的 `PD` 设计比较。
- 1 个教师演示与附录延伸版本：
  - 右半平面零点对象的边界重审与保守设计示例。

**建议课堂组织主线**

- 先从 `3-5` 回收一句核心判断：
  - 动态改善不是“把增益调大”这么简单，而是结构改变后零点位置改变了极点迁移路径。
- 第一轮先做目标翻译：
  - 时域指标写成目标极点区域；
  - 频域指标写成相角裕度与截止频率目标。
- 第二轮分两条链完成参数化求解：
  - `PD` 与测速反馈完成时域目标驱动设计；
  - 超前与同指标下 `PD` 完成频域目标驱动设计。
- 第三轮统一比较四个设计版本：
  - 把“结构动作 -> 根轨迹 / 频域变化 -> 时域后果 -> 代价位置”连成完整解释。
- 最后一轮由教师受控展示右半平面零点版本与附录保守样例：
  - 明确“也叫零点，但目标、带宽和结构选择都必须先重审”。
- 收束语必须显式铺到 `3-7`：
  - 动态改善线回答“怎么变快、怎么减振”；
  - 稳态改善线才回答“为什么误差还能继续变小”。

**任务**

- 先将时域指标翻译为目标极点区域，并证明纯增益为何不能进入该区域。
- 分别完成 `PD` 与测速反馈的时域目标驱动设计、参数求解与阶跃验收。
- 分别完成简单超前与同指标下 `PD` 的频域目标驱动设计、`Bode` 验收与时域回查。
- 比较四个设计版本的结构抓手、主要收益与附带代价。
- 观察教师演示的右半平面零点版本，并通过附录保守样例识别非最小相边界。
- 形成“目标 -> 结构 -> 参数 -> 跨域验收”记录。

**本课必须压实的四组比较**

- `纯增益 vs 时域目标驱动的 PD`：
  - 重点回答“为什么必须先有目标极点区域，零点设计才有明确入口”。
- `PD vs 测速反馈`：
  - 重点回答“为什么两者都能改善阻尼，但设计抓手一个是零点位置，一个是等效极点位置”。
- `超前 vs 同指标下的 PD`：
  - 重点回答“为什么在相同频域目标下，二者的高频代价与时域副作用并不相同”。
- `左半平面零点设计 vs 右半平面零点边界`：
  - 重点回答“为什么‘都有零点’不等于‘都能继续提速’，以及为什么非最小相要先重审目标”。

**资源融入评审单**

| 候选资源路径 | 资源类型 | 计划落点 | 采用方式 | 采用级别 | 边界说明 |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/12根轨迹_基本形态/README.md` | `pptx` | 预测导入 / 根轨迹对照图 | 改写吸收 + 直接复用图片 | 必融入 | 只吸收“增加零点后根轨迹终点与实轴走向如何变化”的基础骨架，不回到根轨迹法则重讲 |
| `course-content/resource-library/pptx/10.1校正_实现控制的手段/README.md` | `pptx` | 正文主体 / `PD` 与测速反馈对照卡 / 结构差异图 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收“`PD` 增加零点、测速反馈不显式增加零点但同样改善阻尼”的比较框架，排除前馈补偿、扰动补偿与整套校正分类展开 |
| `course-content/resource-library/pptx/22串联校正/README.md` | `pptx` | 频域验证 / 收束总结 | 改写吸收 + 直接复用图片 | 必融入 | 只吸收超前网络为什么会带来中高频相位提前与带宽变化趋势，不引入完整超前设计步骤 |
| `course-content/resource-library/pptx/15.1根轨迹法_图形化思考/README.md` | `pptx` | 参数窗口提醒 / 主导极点解释 | 改写吸收 + 直接复用图片 | 可选融入 | 只服务“图上看趋势、用主导极点解释现象”的实践语感，不把课堂拉回参数换算训练 |
| `course-content/questions/questions/AC-Q-0044.md` | `exercise` | 课堂判断题 / 课后短反思 | 改写吸收 | 可选融入 | 只吸收 `PD` 与测速反馈的结构比较、动态收益与稳态代价提醒，不照搬原题完整参数求解 |
| `course-content/questions/questions/AC-Q-0075.md` | `exercise` | 教师补充示例 / 板书扩展 | 仅作灵感 | 可选融入 | 只用其“增加负实零点迫使根轨迹左弯”的现象结论，不把不稳定结构分析变成本课主对象 |
| `course-content/resource-library/civics-cases/indexes/unit-mapping.md` 中现有案例 | `civics` | 无 | 排除 | 排除 | `3-6` 的核心负载是统一对象四版本比较，本轮没有比专业主线更强的自然嵌入点，不强行接入 |
| `course-content/resource-library/ship-control-cases/sections/4.1-4.3` | `ship-case` | 无 | 排除 | 排除 | 船舶根轨迹资源更适合 `3-4` 的对象化迁移；`3-6` 需要压实同一标准对象下的零点比较，不引入场景噪声 |

**产出**

- 时域目标设计记录：
  - 目标区域；
  - `PD` / 测速反馈参数；
  - 阶跃验收结果。
- 频域目标设计记录：
  - 补角；
  - `a/T/K_c` 或 `T_d/K`；
  - `Bode` 与时域回查结果。
- 四版本跨域对照表与一句话解释。
- 非最小相风险记录卡与附录保守示例。

**增强后建议固定产出**

- 一张“四版本统一对照表”：
  - 基准 / `PD` / 测速反馈 / 简单超前；
  - 是否显式增加零点；
  - 根轨迹主变化；
  - 时域主变化；
  - 频域主变化；
  - 一句话解释；
  - 主要收益与主要代价。
- 一张“零点实现方式判读卡”：
  - 零点在哪里；
  - 是否改变开环增益表达；
  - 是否更可能影响噪声放大、稳态误差或高频行为。
- 一张“右半平面零点风险记录卡”：
  - 只回答异常现象、错误直觉和工程风险，不展开完整理论。

**边界**

- 允许在统一对象上做精确参数推导、参数求解与跨域验收。
- 允许把 `PD`、测速反馈、简单超前作为零点设计的预展开主线，但不把课堂推进为模块4的多目标整体方案比较。
- 非最小相版本可进入附录级保守示例与结构选择判断，但不作为全班等量必做长算题。

**本课成文与呈现约束**

- 讲义：
  - 必须按“统一对象 + 目标驱动设计链 + 四版本比较”组织，不允许退回成零散的控制器名词课。
  - 必须保留时域目标翻译、频域目标翻译、参数求解与跨域验收的完整过程。
  - 右半平面零点正文可写边界判断，附录可保留保守参数示例，但不得扩写成模块4式整体方案优化。
- 教案：
  - 教师追问必须同时覆盖“目标怎样翻译”“结构抓手在哪里”“为什么三域会一起变”，而不是只问控制器名称。
  - 必须预留三类常见误判：
    - 把测速反馈误判成也显式增加零点；
    - 把简单超前误判成“只是更强的 `PD`”；
    - 把右半平面零点误判成“零点更靠右所以更快”。
- 互动课程设计：
  - 前台页面必须提供统一对象下的目标翻译、参数求解、验收与比较链路，不做无教学价值的自由调参竞赛。
  - 学生主任务固定为“预测 / 翻译目标 -> 求参数 -> 验收 -> 对比解释”，教师演示版单独锁定。
  - 频域部分允许完整 `Bode` 设计任务，非最小相部分允许边界判断与保守参数示例。
- 媒体清单：
  - 必须准备统一对象下的四版本根轨迹图、阶跃响应图和 `Bode` 趋势图，且坐标口径一致。
  - 必须准备 `PD` 与测速反馈结构差异图，以及左半平面 / 右半平面零点对照图。
  - 若复用旧 `pptx` 图片，标题必须统一改写为“零点作用与动态改善对照”，不能保留“控制器分类讲解”口径。

### `3-7（理论） 型别、积分环节与稳态改善：PI与滞后校正的低频补偿机理`

**定位**

- 讲清误差为什么会在给定输入与外加扰动作用下出现，以及系统结构为什么会决定这些误差能否被根本消除。
- 作为模块3中“稳态改善线”的正式建立课，系统讲透闭环传递函数、误差传递函数、终值定理与静态误差系数，不再把它们散落在模块2的结构工具课或后续设计课里。
- 与 `3-5/3-6` 的“动态改善线”形成明确对照：前者回答“为什么还能更准”，后者回答“为什么能更快更稳”。

**建议课堂组织主线**

- 先从 `3-6` 回收一句核心结论：
  - 零点主导的动态改善并没有直接回答“误差为什么还能继续变小”。
- 第一段先立“误差两类来源”对象：
  - 一类来自系统结构本身，决定某类输入下误差能否被根本消除；
  - 一类来自输入信号形式，决定同一结构下误差会以何种方式暴露出来；
  - 对扰动问题，要显式说明它是通过扰动通道进入闭环，而不是被“输入型别表”直接替代。
- 第二段把“闭环对象”与“误差对象”都正式列出：
  - 输入作用下的闭环传递函数；
  - 扰动作用下的闭环传递函数；
  - 系统总输出与系统总误差的叠加表达；
  - 输入作用下与扰动作用下的误差传递函数。
- 第三段把“直接求”路径压实：
  - 必须先判定系统稳定；
  - 再基于输入/扰动通道写出闭环与误差传递函数；
  - 再用终值定理求稳态误差，并说明终值定理的适用前提。
- 第四段再把“快速判”路径压实：
  - 先把开环低频结构整理为型别与误差系数；
  - 再做 `Kp/Kv/Ka` 与输入类型匹配；
  - 最后明确“静态误差系数法主要解决给定输入下的快速判断，不能机械替代所有扰动分析”。
- 第五段明确“增益变大”和“型别提高”不是一回事：
  - 单纯增益只能在部分情况下减小误差；
  - 引入积分才是在结构上改变误差阶次。
- 第六段再比较 `PI` 与滞后校正：
  - 两者都站在“低频补偿线”上；
  - 但一个主要通过积分提高型别，一个主要通过低频增益重分配改善精度。
- 最后一段必须显式收束到 `3-8`：
  - 为什么积分和滞后主要影响低频；
  - 为什么改善稳态通常伴随动态代价与频域代价；
  - 后续学习才把这种代价翻译成完整频域判断语言。

**知识清单**

- 误差的两类来源：
  - 系统结构来源；
  - 输入信号来源。
- 给定输入与外加扰动在闭环中的进入方式。
- 输入作用下的闭环传递函数。
- 扰动作用下的闭环传递函数。
- 系统总输出与系统总误差的表达。
- 输入作用下的误差传递函数。
- 扰动作用下的误差传递函数。
- 终值定理及其适用前提。
- 系统类型（型别）的定义与物理含义。
- `Kp/Kv/Ka` 与稳态误差的对应关系。
- 静态误差系数法的适用边界。
- 积分环节如何提高型别。
- 积分环节为什么主要改善低频误差。
- `PI` 的作用机理与代价。
- 滞后校正的作用机理与代价。
- `PI` 与滞后校正的联系与区别。
- 动态性能与稳态性能的基本权衡。
- 稳态改善与动态改善两条路径的差异：
  - 零点主导的动态改善；
  - 积分/低频补偿主导的稳态改善。

**能力清单**

- 写出给定输入与扰动作用下的闭环传递函数和误差传递函数。
- 区分“输出通道分析”和“误差通道分析”。
- 用终值定理求给定输入或扰动作用下的稳态误差。
- 判断不同输入下稳态误差趋势。
- 解释 `Kp/Kv/Ka` 与稳态精度关系。
- 判断什么时候该用静态误差系数法、什么时候必须回到完整列式。
- 说明积分环节有效及其风险。
- 区分 `PI` 与滞后校正。
- 形成“积分还是滞后”的初步判断语言。
- 用“目标是稳态精度还是动态品质”区分不同补偿思路。

**本课必须压实的六组对照**

- `输入作用下闭环传递函数 vs 扰动作用下闭环传递函数`：
  - 重点回答“同一闭环结构下，不同信号从哪条通道进入、最后影响的是输出的哪一部分”。
- `系统总输出/总误差分解 vs 只盯单一路径`：
  - 重点回答“为什么总输出是多通道叠加结果，总误差也必须按通道来源拆开分析”。
- `终值定理直接求解 vs 型别/误差系数快速判断`：
  - 重点回答“什么时候该完整列式，什么时候可以先用结构判断快速估计”。
- `给定输入快速判断 vs 扰动问题完整列式`：
  - 重点回答“为什么 `Kp/Kv/Ka` 表主要服务给定输入，而扰动问题更需要回到误差传递函数和终值定理”。
- `增益增大 vs 型别提高`：
  - 重点回答“为什么前者通常只是减小误差，后者才可能根本消除某类输入下的稳态误差”。
- `PI vs 滞后校正`：
  - 重点回答“为什么两者都能改善稳态精度，但型别变化、动态代价和后续设计调用完全不同”。
- `动态改善线 vs 稳态改善线`：
  - 重点回答“为什么 `PD/超前` 主要回答快慢与阻尼，`积分/滞后` 主要回答精度与低频补偿”。

**边界**

- 本课系统讲透单回路负反馈系统在给定输入与扰动作用下的闭环/误差传递函数，但不承担复杂多环结构的长链化简训练。
- 若结构过于复杂，应先回到 `2-1` 的结构图、信号流图与梅森公式化简为等效对象，再回到本课做误差分析。
- 不系统展开 Nyquist 判据。
- 不系统展开 Bode 判稳。
- 不进入完整频域校正设计。
- 不展开模块4层面的控制器选型与参数整定。

**资源融入评审单**

| 候选资源路径 | 资源类型 | 计划落点 | 采用方式 | 采用级别 | 边界说明 |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/9稳态误差_准确性的度量/README.md` | `pptx` | 正文开场 / 计算主链 / 总结 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收误差来源、终值定理流程、静态误差系数和型别表；不把旧课件算例链整段搬进正文 |
| `course-content/resource-library/pptx/3方框图_控制系统结构/README.md` | `pptx` | 误差对象建立 / 输入-扰动双通道整理 / 教师板书工具 | 改写吸收 + 仅作灵感 | 必融入 | 只吸收负反馈比较点、前向通路、反馈通路与扰动通道的结构整理，不回到结构图化简技巧课 |
| `course-content/resource-library/pptx/23滞后超前/README.md` | `pptx` | 正文后段 / `PI` 与滞后对照 / 收束 | 改写吸收 + 直接复用图片 | 必融入 | 只吸收“低频补偿如何改善精度、为什么伴随代价”的机理直觉，不提前进入联合校正步骤与 PID 整定 |
| `course-content/resource-library/pptx/21三频段_各司其职/README.md` | `pptx` | 过渡图 / 收束语 | 改写吸收 + 直接复用图片 | 必融入 | 只服务“低频段主要决定稳态误差”的频域桥梁，不展开截止频率、相角裕度和闭环估算链 |
| `course-content/resource-library/pptx/13时域分析习题2025/README.md` | `exercise` | 例题 / 课后短练 | 改写吸收 | 可选融入 | 只吸收“零稳态误差条件”和“扰动下稳态误差”两类代表性训练，不照搬旧题全部技巧与算式铺陈 |
| `course-content/resource-library/pptx/4信号流图_控制系统拓扑结构/README.md` | `pptx` | 教师收束语 / 备课背景 | 仅作灵感 | 可选融入 | 只补“表达可变、关系不变”的拓扑意识，不在学生主线中重开信号流图 |
| `course-content/resource-library/civics-cases/integration-points.md` | `civics` | 总结 / 课程价值收束 | 改写吸收 | 可选融入 | 只取“高精度控制直接决定工程水平”的一小段专业化表达，不单独展开案例叙事 |
| `course-content/resource-library/ship-control-cases/indexes/section-map.md` 中现有船舶案例 | `ship-case` | 无 | 排除 | 排除 | 当前船舶案例主要服务时域、根轨迹和频域对象，不足以支撑 `3-7` 的型别与低频补偿主线，本轮不强行接入 |

**增强后建议固定产出**

- 一张“给定/扰动双通道分析卡”：
  - 输入通道闭环传递函数；
  - 扰动通道闭环传递函数；
  - 总输出表达；
  - 总误差表达；
  - 应优先看输出还是看误差。
- 一张“稳态误差求解路径判断表”：
  - 输入类型；
  - 是否含扰动；
  - 当前型别；
  - 直接求还是快速判；
  - 对应误差系数；
  - 是否需要提高型别。
- 一张“终值定理适用条件检查卡”：
  - 是否先判稳定；
  - 当前所求对象是输出终值还是误差终值；
  - 是否已写对通道表达。
- 一张“`PI` vs 滞后校正”对照卡：
  - 是否改变型别；
  - 主要作用频段；
  - 主要收益；
  - 主要代价；
  - 更适合在哪条后续设计链中调用。
- 一句模块级收束语：
  - 动态改善解决“怎么更快更稳”，稳态改善解决“为什么更准”，两者都不是免费午餐。

**本课成文与呈现约束**

- 讲义：
  - 必须按“误差两类来源 -> 双通道闭环/误差传函 -> 终值定理 -> 型别/误差系数 -> 补偿路径比较 -> 代价收束”组织，不允许写成“稳态误差计算公式大全”。
  - 必须把“输入作用下/扰动作用下”的闭环与误差表达显式并列，不能只写 `E/R` 而把扰动分析留成口头补充。
  - 必须把“直接求”和“快速判”两条路径并列呈现，避免学生只背 `Kp/Kv/Ka` 表而不知其来源与边界。
  - `PI` 与滞后只能写成“低频补偿线”的两种路径，不得提前滑入模块4的完整校正设计叙事。
- 教案：
  - 教师追问必须优先围绕“误差从哪来”“给定与扰动分别走哪条通道”“为什么先要稳定”“增益变大和型别提高有什么本质差异”。
  - 必须预留三类常见误判：
    - 把“增益调大”误判成“等价于提高型别”；
    - 把“给定输入的静态误差系数表”误判成“所有扰动问题都可直接套表”；
    - 把 `PI` 误判成“白送稳态精度且没有动态代价”；
    - 把滞后校正误判成“只是更弱的积分”。
- 互动课程设计：
  - 前台页面必须至少提供一处“输入/扰动双通道识别”任务、一处“输入类型与型别匹配”的快速判断任务，以及一处 `PI / 滞后` 的代价对照任务。
  - 互动重点放在“分析路径选择”“误差通道识别”和“低频补偿代价识别”，不做完整 Bode 设计或 PID 整定游戏。
  - 若展示结构图，只服务闭环/误差通道理解，不新增结构图化简训练目标。
- 媒体清单：
  - 必须准备输入作用下/扰动作用下闭环与误差通道对照图、型别与 `Kp/Kv/Ka` 对应关系图，以及“低频补偿为何改善精度”的过渡图。
  - 必须准备 `PI` 与滞后校正对照图，标题口径统一为“稳态改善路径比较”，不能沿用旧 `PID/校正器大全` 命名。
  - 如复用 `pptx/9`、`pptx/21`、`pptx/23` 中图示，需统一改写为当前单元的四问主线：
    - 误差为什么会在结构与输入共同作用下出现；
    - 给定与扰动为什么不能混成一条通道；
    - 型别为什么决定误差阶次；
    - 为什么低频补偿会改善精度；
    - 为什么改善稳态通常带来代价。

### `3-8（理论） 频域判别与跨域综合语言`

**定位**

- 作为模块3最后一个理论收束点，把 `3-1` 到 `3-7` 的机制线统一翻译成频域语言。
- 先做结构变化的频域翻译，再收束到频域判据与综合判断，不按“判据清单”顺序平推。

**建议课堂组织主线**

- 第一段先回收 `3-5/3-7` 的两条线：
  - 零点主导的动态改善线；
  - 积分/滞后主导的稳态改善线。
- 先回答“同样是结构变了，为什么在频域里看起来完全不同”，再进入判据。
- 第二段用 `pptx/16` 与 `pptx/21` 建立“频域不是新章节，而是统一翻译器”的总图：
  - 增益变化更像开环幅频曲线的整体抬升/平移；
  - 零点变化更多体现在中频相位与穿越位置改变；
  - 积分/滞后主要重塑低频与中低频段的代价分布；
  - 高频段则负责解释“为什么不是越快越好”。
- 第三段再进入 `pptx/19` 的正式判稳语言：
  - 奈奎斯特判据不是额外新规则，而是把闭环求根问题改写为开环曲线如何对待 `(-1,j0)`；
  - Bode 判稳也不是独立判据，而是对奈奎斯特穿越规则的对数图表达。
- 第四段最后用船舶频域案例把“开环图上怎么读”拉回“闭环性能会怎样”：
  - 航向控制例子负责把裕度、带宽与阶跃指标对上；
  - 稳定平台例子负责把谐振峰值、相角裕度、超调和调节时间对上；
  - 收束到模块4入口：频域语言的价值不止是判稳，而是帮助做任务表达与方案判断。

**知识清单**

- 增益变化在频域中的表现。
- 零点变化在频域中的表现。
- 积分/滞后补偿在频域中的表现。
- 奈奎斯特判据。
- Bode 判稳。
- 相位裕度、增益裕度、截止频率、带宽。
- 频域量与时域表现、极点/零点分布、稳态改善的近似映射。
- 增益、零点、积分/滞后在频域中的重新解释。
- 非最小相在频域中的代价表达。
- 三种稳定语言的对应关系：
  - 极点语言；
  - 根轨迹/结构语言；
  - 频域判据语言。

**能力清单**

- 用奈奎斯特或 Bode 图做基本判稳。
- 解释相位裕度、增益裕度、带宽所反映的动态品质。
- 把频域指标与时域表现、极点/零点分布建立联系。
- 用频域语言解释补偿收益与代价。
- 用频域语言统一解释前面学过的增益、零点、积分、滞后变化。

**本课必须压实的四组翻译**

- `增益抬升 vs 零点引入 vs 积分/滞后补偿`：
  - 重点回答“为什么这三类变化在 Bode/Nyquist 上留下的痕迹不同，因此不能只用‘增益变大了’一句话概括”。
- `奈奎斯特包围规则 vs Bode 穿越/裕度规则`：
  - 重点回答“为什么两者描述的是同一件事，而不是两套互不相干的稳定判据”。
- `开环三频段分工 vs 闭环带宽/谐振峰/调节时间`：
  - 重点回答“为什么频域图不仅用来判稳，还能反推快慢、超调和抗扰边界”。
- `最小相对象 vs 非最小相对象`：
  - 重点回答“为什么相位代价会改变我们对‘更快更大带宽’的直觉”。

**边界**

- 不重讲系统类型和误差系数推导。
- 不重讲 `PI`、滞后、`PD`、超前的首次机理。
- 不重讲 Bode 图和 Nyquist 图的基本绘制法则。
- 不展开完整补偿器参数计算。
- 不进入模块4的选型、整定与优化。

**资源融入评审单**

| 候选资源路径 | 资源类型 | 计划落点 | 采用方式 | 采用级别 | 边界说明 |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/16频率特性_换个角度看控制/README.md` | `pptx` | 导入回收 / 翻译起点 / 关系图 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收“为什么需要频域语言”“频率响应与时域正弦稳态如何对应”的入口，不回到模块2重新讲定义和典型环节作图 |
| `course-content/resource-library/pptx/19稳定判据_频域的启示/README.md` | `pptx` | 正文主体 / 判稳收束 / 对照图 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收“闭环稳定如何改写为开环曲线对待 `(-1,j0)`”以及 Bode 判稳与 Nyquist 的对应关系，不照搬完整页序和推导细枝末节 |
| `course-content/resource-library/pptx/21三频段_各司其职/README.md` | `pptx` | 过渡图 / 正文后段 / 收束总结 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收“三频段分工”“截止频率/相角裕度/带宽与性能估算”的判断链，不把 `M` 圆、尼克尔斯图扩写成新主体 |
| `course-content/resource-library/civics-cases/cases/06-频段强国策应器.md` | `civics` | 导入一句话 / 总结收束 | 改写吸收 | 可选融入 | 只服务“不同频段承担不同任务、不同阶段需要不同策略”的工程视角，不展开五年规划叙事主体 |
| `course-content/resource-library/civics-cases/cases/04-根轨全局观拓荒.md` | `civics` | 承前启后 / 总结 | 仅作灵感 | 可选融入 | 只借“全局观、趋势判断”呼应 `3-3` 与 `3-8` 的统一视角，不再重开人物史料 |
| `course-content/resource-library/ship-control-cases/sections/5.1-船舶航向控制频域分析.md` | `ship-case` | 正文案例 / 开环到闭环指标映射 | 改写吸收 + 直接复用图片 | 必融入 | 用于把相角裕度、幅值裕度、带宽频率与阶跃指标对上；不在本课推进到模块4的控制器整定结论 |
| `course-content/resource-library/ship-control-cases/sections/5.3-船载稳定平台控制系统频域分析.md` | `ship-case` | 正文案例 / 闭环特征量收束 | 改写吸收 + 直接复用图片 | 必融入 | 用于把谐振峰值、带宽、相角裕度与超调/调节时间联系起来，强化“频域量可反推动态品质”；不展开完整优化设计 |
| `course-content/resource-library/pptx/17近似叠加_绘制伯德图/README.md` | `pptx` | 无 | 排除 | 排除 | 该资源主价值在模块2的 Bode 作图入口；若在 `3-8` 重回手工绘图，会削弱本课“翻译优先”的主轴 |
| `course-content/resource-library/pptx/18幅相特性_换个角度看频域/README.md` | `pptx` | 无 | 排除 | 排除 | 该资源主价值在模块2的幅相图读法与特征点入门；`3-8` 只调用其读图基础，不再重讲极坐标图绘制流程 |

**增强后建议固定产出**

- 一张“频域翻译总表”：
  - 结构变化类型；
  - `Bode` 主变化；
  - `Nyquist` 主变化；
  - 闭环性能趋势；
  - 主要收益；
  - 主要代价。
- 一张“判稳语言对照卡”：
  - 极点语言；
  - 根轨迹/结构语言；
  - 奈奎斯特语言；
  - Bode/裕度语言；
  - 同一结论在四种语言中的表达方式。
- 一张“双船案例速读卡”：
  - 航向控制；
  - 稳定平台；
  - 读到的频域量；
  - 推回的动态表现；
  - 能支持的工程判断。
- 一句模块级收束语：
  - 频域不是把旧知识再讲一遍，而是把结构变化、稳定边界和动态/稳态代价放进同一张工程判断地图。

**本课成文与呈现约束**

- 讲义：
  - 必须按“结构变化翻译 -> 判稳语言统一 -> 闭环性能读回”组织，不允许写成“奈奎斯特一章 + Bode 判稳一章”的并列复习稿。
  - 必须显式保留至少一张“同一结构变化在四种语言中的对应表”，避免频域再次变成孤立知识点。
  - 船舶案例只能作为判断链验证，不得提前滑入模块4的方案排序、选型或整定叙事。
- 教案：
  - 教师追问必须优先围绕“你先看哪一个频段/哪一个点/哪一条穿越关系”组织，而不是直接问公式结论。
  - 必须预留三类常见误判：
    - 把“相角裕度更大”误判成“所有性能都更好”；
    - 把“带宽更大”误判成“无条件更快更优”；
    - 把“Bode 判稳”误判成与奈奎斯特无关的另一套规则。
- 互动课程设计：
  - 前台页面至少要有一处“先预测结构变化会怎样影响频域图，再验证”的任务。
  - 必须至少有一处“由频域量反推动态品质”的任务，不做纯图形识别练习。
  - 船舶案例页面只做读图、判断和一句话解释，不做完整校正参数搜索。
- 媒体清单：
  - 必须准备三频段总图、`(-1,j0)` 包围示意图、`Bode/Nyquist/极点语言` 对照图。
  - 必须准备航向控制与稳定平台两个案例的开环/闭环频域图与对应时域曲线。
  - 如复用 `pptx/19` 或 `pptx/21` 图示，标题口径统一改写为“频域判断链”，不能保留旧课件式章节标题。

### `3-9（实践） 稳定—动态—稳态综合映射实验`

**定位**

- 作为模块3的出口实践，把稳定边界、极点/根轨迹、零点动态改善、型别/积分稳态改善和频域判据统一收束为一张跨域机制地图。
- 不新增机制，但必须把“结构变化会优先解决什么问题、又会额外带来什么代价”收束成模块4可调用的第一轮任务判断。

**建议课堂组织主线**

- 第一段先从 `3-8` 回收统一语言：
  - 频域不是新章，而是把 `3-1` 到 `3-7` 的结构变化重新翻译成同一张判断图；
  - 本课不是再讲一个新控制器，而是检验学生能否把这张图真正用于同一对象的多版本比较。
- 第二段固定围绕同一工程对象展开：
  - 先看基准版本；
  - 再看积分引入版本；
  - 再看学生自选的极点线补强或零点线补强版本；
  - 先不给“最优方案”，只要求学生先打标签：更偏向改善动态、改善稳态，还是以牺牲裕度换取速度。
- 第三段把比较顺序固定为：
  - 先看极点/根轨迹给出的第一信号；
  - 再看阶跃响应确认动态与稳态后果；
  - 最后回到 `Bode / Nyquist` 检查三频段、带宽与裕度代价；
  - 重点回答“哪一域最先暴露收益，哪一域最先暴露代价”。
- 第四段由教师演示滞后版本：
  - 不把它当成“第四个学生必做版本”，而是专门用来对照“积分改善稳态”与“滞后改善稳态”的差别；
  - 重点压实“同样是在低频补偿，为什么频域代价和动态代价并不一样”。
- 第五段最后收束到模块4入口：
  - 学生只能给出“哪条机制线更适合先承担哪类任务”的判断；
  - 不能直接越级给出完整控制器选型、参数整定和唯一最优结论。

**组织方式**

- 一个统一对象，默认采用与 `3-4 / 3-8` 连续的船舶航向控制对象。
- 3个学生必做版本：
  - 基准版本；
  - 积分引入的稳态改善版本；
  - 学生自选补强版本：
    - 极点线补强版本；或
    - 零点线补强版本。
- 1个教师演示版本：
  - 滞后校正。

**本课必须压实的四组判断**

- `极点线补强 vs 零点线补强`：
  - 重点回答“二者都可能让系统看起来更快，但在根轨迹、频域和代价分布上为什么不是一回事”。
- `积分改善 vs 滞后改善`：
  - 重点回答“二者都指向稳态改善，但低频收益、中频代价和实现难度为什么不同”。
- `极点/根轨迹第一信号 vs 阶跃验证 vs 频域复核`：
  - 重点回答“同一个版本应先看哪一域得到初判，再用哪一域补足风险信息”。
- `任务标签 vs 机制标签`：
  - 重点回答“学生说‘我想让它更快/更准/更稳’时，究竟应该先想到哪条机制线，而不是直接跳成控制器名称”。

**任务**

- 先对三个学生必做版本做预判：
  - 稳定性趋势；
  - 动态品质趋势；
  - 稳态误差趋势；
  - 频域指标趋势。
- 先为每个版本填写“任务标签”：
  - 更偏动态改善；
  - 更偏稳态改善；
  - 更偏综合折中；
  - 当前主要风险点。
- 在平台中逐个验证：
  - 极点/根轨迹；
  - 阶跃响应；
  - Bode / Nyquist 关键特征。
- 填写综合映射表：
  - 改了什么结构；
  - 极点/轨迹怎么变；
  - 时域怎么变；
  - 频域怎么变；
  - 稳态怎么变；
  - 主要收益；
  - 主要代价。
- 观看教师演示的滞后版本，补上“稳态改善的另一条路径”。
- 写出模块出口结论：
  - 哪类变化主要改善动态；
  - 哪类变化主要改善稳态；
  - 哪类变化更容易先吃掉裕度或带宽；
  - 为什么不同路径的代价不同。

**资源融入评审单**

| 候选资源路径 | 资源类型 | 计划落点 | 采用方式 | 采用级别 | 边界说明 |
| --- | --- | --- | --- | --- | --- |
| `course-content/resource-library/pptx/15.2根轨迹分析综合/README.md` | `pptx` | 统一对象引入 / 极点线版本回收 / 场景后果对照 | 改写吸收 + 直接复用图片 | 必融入 | 复用同一航向控制对象及“根轨迹 -> 主导极点 -> 场景后果”的连续链，只服务模块3出口验证，不重新展开完整根轨迹法则或主导极点设计流程 |
| `course-content/resource-library/pptx/21三频段_各司其职/README.md` | `pptx` | 频域复核 / 综合映射表 / 模块收束 | 改写吸收 + 直接复用图片 | 必融入 | 主吸收“三频段分工 -> 带宽/裕度 -> 动态品质”的判断链，服务本课“先打标签再验证”的出口逻辑，不扩写 `M` 圆或尼克尔斯图专题 |
| `course-content/resource-library/pptx/23滞后超前/README.md` | `pptx` | 教师演示 / 积分 vs 滞后对照 / 代价总结 | 改写吸收 | 必融入 | 只用于回收“另一条稳态改善路径”的代价对照，不进入滞后-超前联合校正、PID 整定或模块4设计调用 |
| `course-content/resource-library/ship-control-cases/sections/5.1-船舶航向控制频域分析.md` | `ship-case` | 统一对象验证 / 频域量回读 / 模块4入口桥接 | 改写吸收 + 直接复用图片 | 必融入 | 用航向控制对象把相角裕度、带宽、谐振频率和阶跃表现对起来，只做验证与解释，不输出完整设计结论 |
| `course-content/resource-library/pptx/20宽备窄用_稳定裕度/README.md` | `pptx` | 无 | 排除 | 排除 | 该资源主价值在模块4“裕度为什么影响选型与权衡”；若在 `3-9` 展开，会把出口实践拖回稳定裕度专题并提前侵入设计层 |
| `course-content/resource-library/pptx/10.1校正_实现控制的手段/README.md` | `pptx` | 无 | 排除 | 排除 | `3-9` 需要的是机制线收束，不是重新按控制器种类分类；若再次引入，会把学生注意力从“结构后果”拉回“器件名称” |

**增强后建议固定产出**

- 一张“结构机制 -> 任务标签”映射表：
  - 结构变化；
  - 极点/根轨迹第一信号；
  - 时域后果；
  - 频域后果；
  - 主要收益；
  - 主要代价；
  - 更适合承担的任务标签。
- 一张“积分 vs 滞后”对照卡：
  - 低频收益；
  - 中频/裕度代价；
  - 动态影响；
  - 适用语境。
- 一张“模块4入口判断卡”：
  - 若目标先是更快；
  - 若目标先是更准；
  - 若目标先是兼顾稳与快；
  - 学生应优先回看哪条机制线，而不是直接猜参数。
- 一段联动解释短报告：
  - 必须明确写出“自己选择的补强版本优先解决什么问题、代价出现在哪一域、为什么它还不是设计终点”。

**本课成文与呈现约束**

- 讲义：
  - 必须按“统一对象 -> 三个学生版本 -> 滞后教师演示 -> 模块4入口判断”组织，不允许写成松散的模块3大复习。
  - 必须显式保留“先打任务标签，再做跨域验证”的动作，避免学生只会做事后归纳。
  - 同一对象必须贯穿极点图、阶跃图和频域图，不允许每一域换一个案例。
- 教案：
  - 教师追问要围绕“你为什么先看这一域”“这一版最先暴露的风险是什么”组织，而不是直接追问结论对错。
  - 必须预留两类典型误判：
    - 把“积分能消除稳态误差”误判成“稳态改善路线都一样”；
    - 把“看起来更快”误判成“已经更适合进入设计层”。
- 互动课程设计：
  - 前台页面至少要有一处“先给任务标签，再拖动或切换版本验证”的动作。
  - 必须至少有一处“积分 vs 滞后”并列对照，不允许只保留文字总结。
  - 允许学生自选极点线或零点线补强，但必须要求其提交“为何选这条机制线”的一句话解释。
- 媒体清单：
  - 必须准备同一对象的根轨迹图、阶跃响应图、Bode/Nyquist 图成套素材。
  - 必须准备“结构机制 -> 任务标签”总表和“积分 vs 滞后”对照图。
  - 若复用 `15.2` 或 `5.1` 图示，标题口径统一改写为模块3出口判断，不保留旧课件/旧案例标题。

**产出**

- 跨域机制映射对照表。
- 联动解释短报告。

**边界**

- 不提前进入模块4的完整设计流程。
- 不做控制器选型与优化流程。
- 不要求形成唯一最优方案。
- 不把稳定裕度专题、串联校正专题或 PID 整定专题提前拉进来。
- AI 只辅助记录、对比或解释，不替代学生判断。
