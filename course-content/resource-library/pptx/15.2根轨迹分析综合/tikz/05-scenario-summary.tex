\documentclass[tikz,border=10pt]{standalone}
\usepackage{fontspec}
\usepackage{xeCJK}
\setmainfont{Times New Roman}
\setCJKmainfont{Hiragino Sans GB}
\setCJKsansfont{Microsoft YaHei}
\setCJKmonofont{STSong}
\usepackage{tikz}
\usetikzlibrary{matrix}

\tikzset{
  cell/.style={draw=black!70, line width=1.0pt, minimum width=4.9cm, minimum height=1.05cm, align=center, text width=4.4cm},
  head/.style={cell, draw=blue!70!black, line width=1.2pt},
  left/.style={cell, draw=green!50!black, line width=1.2pt}
}

\begin{document}
\begin{tikzpicture}
  \matrix[matrix of nodes, nodes in empty cells, row sep=-\pgflinewidth, column sep=-\pgflinewidth] {
    \node[head] {场景}; & \node[head] {本页验证内容}; \\
    \node[left] {回转}; & \node[cell] {按根轨迹选出的增益下，船舶可以完成改向，但航迹仍存在过渡滞后}; \\
    \node[left] {避障}; & \node[cell] {该增益能够让船舶平滑绕开障碍物，说明根轨迹选点并不只服务于阶跃输入}; \\
    \node[left] {$K=0.6064$ 的一分钟回转}; & \node[cell] {航向响应能够跟随指令，但航迹暴露出该控制器在实际回转圈上的偏差}; \\
    \node[left] {$K=20$ 的一分钟回转}; & \node[cell] {过大的增益会显著改变操纵轨迹，因此增益选择必须在任务空间中判断，不能只看极点图}; \\
  };
\end{tikzpicture}
\end{document}
