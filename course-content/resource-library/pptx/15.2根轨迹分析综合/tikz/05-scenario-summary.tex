\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{matrix}

\tikzset{
  cell/.style={draw=black!70, line width=1.0pt, minimum width=4.9cm, minimum height=1.05cm, align=center, text width=4.4cm},
  head/.style={cell, draw=blue!70!black, line width=1.2pt},
  left/.style={cell, draw=green!50!black, line width=1.2pt}
}

\begin{document}
\begin{tikzpicture}
  \matrix[matrix of nodes, nodes in empty cells, row sep=-\pgflinewidth, column sep=-\pgflinewidth] {
    \node[head] {scenario}; & \node[head] {what the slide verifies}; \\
    \node[left] {turning}; & \node[cell] {with the gain selected from root locus, the vessel can complete the heading change, but the path still shows transient lag}; \\
    \node[left] {obstacle avoidance}; & \node[cell] {the designed gain lets the ship bypass an obstacle smoothly, showing that the root-locus choice is not limited to a step input}; \\
    \node[left] {one-minute turning with $K=0.6064$}; & \node[cell] {the heading response follows the command, while the trajectory reveals the practical turning-circle bias of the designed controller}; \\
    \node[left] {one-minute turning with $K=20$}; & \node[cell] {a much larger gain changes the maneuvering trace noticeably, so gain selection must be judged in task space, not only on the pole map}; \\
  };
\end{tikzpicture}
\end{document}
