\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{matrix}

\tikzset{
  cell/.style={draw=black!70, line width=1.0pt, minimum width=5.3cm, minimum height=1.3cm, align=center, text width=4.9cm, fill=none},
  head/.style={cell, draw=blue!70!black, line width=1.2pt}
}

\begin{document}
\begin{tikzpicture}
  \matrix[matrix of nodes, nodes in empty cells, row sep=-\pgflinewidth, column sep=-\pgflinewidth] {
    \node[head] {departure angle at pole $p_0$}; &
    \node[head] {arrival angle at zero $z_0$}; \\
    \node[cell] {$\theta_{d0}=180^\circ+\sum \angle(p_0-z_j)-\sum_{i\ne 0}\angle(p_0-p_i)$}; &
    \node[cell] {$\theta_{a0}=180^\circ+\sum \angle(z_0-p_i)-\sum_{j\ne 0}\angle(z_0-z_j)$}; \\
  };
\end{tikzpicture}
\end{document}
