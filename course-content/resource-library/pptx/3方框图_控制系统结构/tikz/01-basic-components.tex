\documentclass[tikz,border=10pt]{standalone}
\usepackage{fontspec}
\usepackage{xeCJK}
\setmainfont{Times New Roman}
\setCJKmainfont{Hiragino Sans GB}
\setCJKsansfont{Microsoft YaHei}
\setCJKmonofont{STSong}
\usepackage{amsmath}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,positioning}

\tikzset{
  signal/.style={->, line width=1.2pt, draw=black!80, >=Stealth},
  block/.style={draw=blue!70!black, line width=1.2pt, rectangle, minimum width=2.4cm, minimum height=1.1cm, fill=none},
  sum/.style={
    draw=green!55!black,
    line width=1.2pt,
    circle,
    minimum size=0.58cm,
    inner sep=0pt,
    fill=none,
    path picture={
      \draw[green!55!black, line width=0.9pt]
        (path picture bounding box.south west) -- (path picture bounding box.north east)
        (path picture bounding box.north west) -- (path picture bounding box.south east);
    }
  },
  tap/.style={draw=orange!85!black, line width=1.2pt, circle, minimum size=4.5pt, inner sep=0pt, fill=none},
  note/.style={font=\small, align=left}
}

\begin{document}
\begin{tikzpicture}
  \draw[signal] (0,4.6) -- (2.8,4.6);
  \node[note, anchor=west] at (3.2,4.6) {信号线\\箭头表示信号方向};

  \draw[signal] (0,2.9) -- (2.8,2.9);
  \node[tap] (tap1) at (1.45,2.9) {};
  \draw[signal] (tap1) -- ++(0,-0.9);
  \node[note, anchor=west] at (3.2,2.9) {引出点\\分支信号保持同值};

  \node[sum] (sum1) at (1.45,1.0) {};
  \draw[signal] (0.05,1.0) -- (sum1);
  \draw[signal] (1.45,2.0) -- (sum1);
  \draw[signal] (sum1) -- (2.8,1.0);
  \node[font=\scriptsize] at (1.02,1.28) {$+$};
  \node[font=\scriptsize] at (1.84,1.28) {$-$};
  \node[note, anchor=west] at (3.2,1.0) {综合点\\输入信号作代数运算};

  \node[block] (g) at (1.45,-1.0) {$G(s)$};
  \draw[signal] (0,-1.0) -- (g.west);
  \draw[signal] (g.east) -- (2.8,-1.0);
  \node[note, anchor=west] at (3.2,-1.0) {方框\\表示环节传递函数};
\end{tikzpicture}
\end{document}
