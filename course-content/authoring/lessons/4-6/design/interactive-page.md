━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
互动页面设计 | 单元 4-6：场景迁移与方案比较：固定结构优化边界与结构编码入口
技术栈：Next.js 14 + TypeScript + Tailwind CSS + shadcn/ui
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 文档职责

- 本文件是 `4-6` 作者态互动页面蓝图，负责把讲义中的迁移任务重排、固定结构失配证据、驱逐舰专用代价函数、结构编码入口、四类方案比较、专项验证边界判断、后测与总结落成可直接实现的课堂页面。
- 同目录 `interactive-contract.yaml` 是机读真源；两者必须在步骤顺序、标题、模板、互动类型、教师控制和学生默认状态上逐项一致。
- 本轮设计以讲义顺序为硬约束：`4-5 可用解回收 -> 课程目标 -> 场景配置重排 -> 失配证据 -> 目标重排 -> 结构搜索引入 -> 统一编码与解码 -> 四类方案比较 -> 专项验证 -> 后测 -> 总结`。

## 表述规则

- 每一步都必须回答四件事：对象是什么、正在判断什么、关键证据在哪里、结论如何接回“固定结构边界与结构编码入口”。
- 图片、曲线图和收敛图都必须服从讲义证据顺序，不得抢到公式、题面或职责表之前。
- 后测与总结保持分离；后测只检查判断链，总结只负责收束与移交。
- 显影页可以隐藏的是步骤，不是题面或判断对象本身。

## 全课总览

| 步骤 | 标题 | 页面模板 | 互动类型 | 本页职责 |
|---|---|---|---|---|
| step-01 | 从场景配置对比到迁移问题：为什么这次不能只说“再调一调参数” | `migration_boundary_hero_board` | `none` | 按讲义表 1 对比客船与驱逐舰任务，并用驱逐舰失配图钉住本课问题 |
| step-02 | 本次课程目标：完成这轮迁移判断后应能做到什么 | `objective_chain_board` | `none` | 独立呈现本课布鲁姆能力目标 |
| step-03 | 迁移场景任务重排：客船与驱逐舰到底换了什么 | `scenario_reorder_compare_board` | `activity_card_set` | 固定对象、参考、筛选线与优先判断的重排 |
| step-04 | 固定结构迁移后的失配信号：拖尾、航迹偏离与职责过载如何同时出现 | `mismatch_evidence_board` | `teacher_reveal_only` | 承载失配图、三类失配信号与“旧问题写法失效”的结论 |
| step-05 | 驱逐舰专用代价函数：为什么比较对象必须重写 | `objective_rewrite_board` | `activity_card_set` | 把五项比较对象及其职责拆开写清 |
| step-06 | 何时把结构选择正式抬成问题本身 | `structure_entry_board` | `teacher_reveal_only` | 固定结构可用域收缩、结构搜索引入条件与入口判断 |
| step-07 | 五类结构的统一编码：先判结构，再解释参数 | `structural_encoding_workspace` | `teacher_reveal_only` | 承载六维编码、五类结构卡与两组最小解码示例 |
| step-08 | 四类方案比较矩阵：先问目标是否对题，再问结构是否够用 | `four_scheme_compare_board` | `teacher_reveal_only` | 用主比较图和问题矩阵拆开“目标错位”和“结构边界” |
| step-09 | 结构搜索证据与专项验证：总代价更低为什么仍不自动等于可交付 | `boundary_probe_board` | `teacher_reveal_only` | 放收敛图、组合结构专项验证、控制器表达式与边界判断 |
| step-10 | 后测：目标错位、结构边界与统一编码是否已经连成链 | `posttest_board` | `quiz_group` | 独立检查学生是否形成完整判断链 |
| step-11 | 总结：从任务重排到结构编码入口的完整判断链 | `summary_exit_board` | `none` | 收束本课并移交 `4-7` |

## 讲义核心内容映射

| handout_anchor | core_item_type | must_appear_content | target_step | page_mode | interaction_upgrade | media_or_table_ref | acceptance_note |
|---|---|---|---|---|---|---|---|
| ## 一、迁移场景中的任务重排 | 场景配置对比 + 问题钉住 | 讲义表 1、`4-5` 客船固定超前可用解、驱逐舰失配图、问题句“为什么不能继续只调参数” | step-01 | 入口证据页 | 静态证据，不放作答 | 表 1，`4-6-destroyer-mismatch-evidence.png` | 表 1 必须作为入口证据完整出现 |
| ## 二、课程目标 | 目标列表 | 五条本课目标，不暴露课程编排语言 | step-02 | 目标页 | 静态目标卡 | 无 | 只出现本课目标 |
| ### 3.2 驱逐舰快速机动任务的对象与约束 | 表后判断任务 | 对象模型、任务语义、参考轨迹、动作筛选线、优先判断的迁移解释 | step-03 | 对比判断页 | 双卡作答，单卡提交 | 表 1 | 不重复完整表 1，基于入口表格完成判断 |
| ### 3.3 固定结构迁移后的失配信号 | 失配图 + 显影链 | 旧权重、旧总代价、拖尾、航迹偏离、职责过载三类信号 | step-04 | 失配证据页 | 教师逐步显影 | 图 1，`4-6-destroyer-mismatch-evidence.png` | 图与三类信号显影必须联动 |
| ### 3.4 目标重排与固定结构可用域收缩 | 公式对照 + 职责表 | `J_p(θ)`、`J_d(θ)`、五项职责：切换段误差、全程跟踪误差、航迹偏离、峰值动作、控制能量 | step-05 | 目标重写页 | 双卡作答，检查职责理解 | 本节公式、职责表 | 不能退化成“权重改了”一句话 |
| ### 4.1 结构搜索的引入条件 | 引入条件 + 结构码本预览 | 固定结构可用域收缩链、三条引入条件、五类候选结构表达式、参数范围和解码例 | step-06 | 入口判断页 | 教师逐步显影 | 结构码本预览 | 先讲引入条件，再讲候选结构和解码入口 |
| ### 4.2 五类控制结构的统一编码 | 编码码本 + 解码示例 | 六维编码、五类结构卡、有效槽位、忽略槽位、两组解码示例 | step-07 | 编码工作页 | 显影 + 双卡作答 | 五类结构表、两组示例 | 不能只剩一张宽表 |
| ### 5.2 四类方案的方波跟踪与航迹证据 | 四类比较矩阵 | 四类方案定义、主比较图、每类方案回答的问题 | step-08 | 比较证据页 | 教师逐步显影 | `4-6-ship-heading-hybrid-search-comparison.png`、表 5 | 必须把“目标”和“结构”拆开判断 |
| ### 5.3 GA/PSO 的收敛图只承担结构选择证据 | 收敛图 + 专项验证 | 收敛图角色、专项验证图、控制器表达式、控制效果曲线、边界判断 | step-09 | 综合证据页 | 教师逐步显影 + 单卡作答 | `4-6-ship-heading-hybrid-search-convergence.png`、`4-6-ship-heading-combo-probe.png`、`4-6-ship-heading-combo-control-effects.png` | 不能把专项验证误写成“最终方案页” |
| ## 七、分层练习 | 后测题组 | 三题后测：问题写法、目标重排、编码解读 | step-10 | 后测页 | 题卡独立提交 | 练习 1-4 抽取题面 | 后测独立成页，不与总结合并 |
| ## 六、小结 | 信息图 + 收束链条 | 五条总结、本讲信息图、主线链条、`4-7` 去向 | step-11 | 总结移交页 | 静态收束，不放作答 | `4-6-info.png` | 总结页必须补充信息图 |

## step-01｜从场景配置对比到迁移问题：为什么这次不能只说“再调一调参数”

### 页面骨架
- 顶部依次放客船对象卡、`4-5` 固定超前可用解卡与迁移问题句。
- 中部完整放讲义第一章表 1，对比客船航向保持与驱逐舰快速机动航向控制。
- 底部放驱逐舰失配证据图、图后阅读口令与本页收束句。

### 模块清单
- 客船对象卡
- `4-5` 固定超前解卡
- 问题聚焦卡
- 场景配置对比表
- 驱逐舰失配图
- 图后阅读口令
- 桥接结论卡

### 静态承载内容
- 客船对象常显：`$P_p(s)=\\dfrac{0.01715}{s(s+0.1)(s+2.14375)}$`。
- 固定超前解常显：`$C_p(s)=1.7679\\dfrac{10.4676s+1}{2.6452s+1}$`。
- 问题句必须直接写出：同一对象族进入驱逐舰快速机动任务后，为什么“继续微调参数”已经不足以描述问题。
- 表 1 必须完整保留五行：
  - 对象模型：客船 `$P_p(s)=\\dfrac{0.01715}{s(s+0.1)(s+2.14375)}$`；驱逐舰 `$P_d(s)=\\dfrac{0.0216}{s(s+0.16)(s+2.85)}$`。
  - 任务语义：客船是单次航向保持，强调平顺和储备；驱逐舰是多段快速机动，强调关键时段跟踪与动作分配。
  - 参考轨迹：客船是单次保持参考；驱逐舰是分段机动参考，包括起始转向、加深机动、回收再调整。
  - 动作筛选线：客船只要求峰值动作和总能量不逼近边界；驱逐舰把峰值动作与控制能量都单独设线。
  - 优先判断：客船看速度、超调、稳健性、动作代价协调；驱逐舰把机动完成时间、跟踪误差、峰值动作、控制能量、稳健性并列比较。
- 图题固定写“客船代价函数和固定超前结构在驱逐舰对象上的失配证据”。
- 收束句固定写：本课不是先换算法，而是先检查原来的问题写法为什么失效。

### 混合证据顺序
- 先客船对象与 `4-5` 可用解 -> 再问题句 -> 再表 1 -> 再失配图 -> 最后图后口令与收束句。

### 互动升级点
- 本页只阅读，不放作答卡。

### 教师控制
- 无。

### 学生默认状态
- 页面全部可见；图与结论常显。

### 本页脱离讲稿后的自包含检查
- 学生仅凭本页即可知道：回收的旧解是什么、迁移后出现了什么新问题、为什么本课入口是“问题写法失效”。

### 预览口径
- 一个“表 1 对比 + 旧解回收 + 新证据钉住问题”的入口页，不允许先把算法名顶到前面。

## step-02｜本次课程目标：完成这轮迁移判断后应能做到什么

### 页面骨架
- 顶部目标标题卡。
- 中部五条课程目标纵向排列。
- 底部一条主线桥接句。

### 模块清单
- 目标标题卡
- 目标列表卡
- 主线桥接卡

### 静态承载内容
- 标题卡固定写“完成这轮迁移判断后，我们应能做到什么”。
- 五条目标必须逐条常显：
  1. 说明客船与驱逐舰任务中为什么会发生任务重排。
  2. 指出固定结构迁移后首先暴露出的失配信号。
  3. 解释驱逐舰专用代价函数为什么不是“旧指标换个名字”。
  4. 读懂五类结构的统一编码、有效槽位和逐槽映射。
  5. 用四类方案比较说明“目标是否对题”和“结构是否够用”是两道不同判断。
- 桥接句：本课承接 `4-5` 的固定结构可用解，正式把结构边界显性化。

### 混合证据顺序
- 先标题 -> 再五条目标 -> 最后桥接句。

### 互动升级点
- 本页只阅读，不放作答卡。

### 教师控制
- 无。

### 学生默认状态
- 页面全部可见。

### 本页脱离讲稿后的自包含检查
- 学生不需要依赖教师口播，就能知道本课要判断什么，不会把目标误读成“学一个新算法”。

### 预览口径
- 只展示本课目标，不暴露课程编排语言。

## step-03｜迁移场景任务重排：客船与驱逐舰到底换了什么

### 页面骨架
- 顶部放迁移判断式与导语。
- 中部放“表 1 后的迁移判断说明”和“优先判断对照卡”。
- 底部放两张判断卡。

### 模块清单
- 迁移判断公式卡
- 表后迁移判断说明
- 优先判断对照卡
- 收束结论卡
- 判断卡 `对象变化与任务变化`
- 判断卡 `动作边界为何要分开`

### 静态承载内容
- 迁移判断式常显：`$\\text{迁移判断}=\\text{对象变化}+\\text{任务变化}+\\text{筛选线重排}$`。
- 表后迁移判断说明必须回扣 step-01 的表 1：对象模型、任务语义、参考轨迹、动作筛选线和优先判断不是五个孤立变化，而是共同把问题从“客船平顺保持”改写成“驱逐舰快速机动下的多指标并列比较”。
- 优先判断卡必须把“客船的协调链”和“驱逐舰的并列比较链”并列写清。
- 收束句：驱逐舰不是“更快一点的客船”，而是对象、参考和筛选线同时改写后的新任务。

### 混合证据顺序
- 先迁移判断式 -> 再表 1 后的判断说明 -> 再优先判断卡 -> 最后两张判断卡与收束句。

### 互动升级点
- 两张作答卡题面常显：
  - 除了对象参数变化，本页还要求你至少指出哪两类任务语义变化？
  - 为什么驱逐舰场景必须把峰值动作和控制能量分开筛选？

### 教师控制
- 教师控制作答卡发放与参考答案显示；正文默认常显。

### 学生默认状态
- 正文可见，作答卡默认隐藏，待教师发放后单卡提交。

### 本页脱离讲稿后的自包含检查
- 学生只看本页就能回答“到底换了什么”，不会把任务重排误读成单纯加速。

### 预览口径
- 一个“表格对照 + 判断卡”的场景重排页，不引入曲线图抢占首屏。

## step-04｜固定结构迁移后的失配信号：拖尾、航迹偏离与职责过载如何同时出现

### 页面骨架
- 顶部放客船平衡权重卡与沿用旧结构说明。
- 中部左侧放失配图，右侧放三层失配信号显影链。
- 底部放“旧问题写法失效”的结论卡。

### 模块清单
- 旧代价函数说明卡
- 失配图
- 失配显影链
- 结论卡

### 静态承载内容
- 旧代价函数权重常显：`$B=[0.30,0.30,0.20,0.20]$`。
- 旧总代价常显：`$J_p(\\theta)=0.30\\bar t_s+0.30\\overline{\\mathrm{ITAE}}+0.20\\overline{\\mathrm{ITSE}}+0.20\\bar E_u$`。
- 旧总代价公式下方必须给出符号说明表：
  - `$\bar t_s$`：调节时间相对基准值的归一化结果，用来衡量保持任务的收敛速度。
  - `$\overline{\mathrm{ITAE}}$`：时间加权绝对误差相对基准值的归一化结果，用来惩罚后期仍拖尾的误差。
  - `$\overline{\mathrm{ITSE}}$`：时间加权平方误差相对基准值的归一化结果，用来压低较大的持续误差。
  - `$\bar E_u$`：控制能量相对基准值的归一化结果，用来约束持续动作代价。
- 图后口令必须写明：左图看方波切换段拖尾，右图看航迹偏离如何被积分放大。
- 结论句：这里暴露的不是某一组参数偶然失败，而是固定超前结构和旧判断链一起迁移后已经不再对题。

### 混合证据顺序
- 先旧代价函数与结构沿用说明 -> 再失配图 -> 再三层失配显影 -> 最后结论。

### 互动升级点
- 三层显影链依次写：
  1. 客船语言把首段收敛和总体能量看得比连续快速机动更重。
  2. 固定超前结构只有三项有效参数，低频恢复、中频提速和动作边界只能互相挤占。
  3. 当验证换成“方波跟踪 + 航迹积分”后，旧写法的缺口被持续放大。

### 教师控制
- 教师独立控制显影链；学生端默认不能自主展开。

### 学生默认状态
- 图与结论常显，显影链默认收起。

### 本页脱离讲稿后的自包含检查
- 学生只看本页即可说出三类失配信号，并知道为什么它们共同指向“问题写法失效”。

### 预览口径
- 一个“图先于显影链”的失配证据页，但图前必须先交代沿用的旧代价函数和旧结构。

## step-05｜驱逐舰专用代价函数：为什么比较对象必须重写

### 页面骨架
- 顶部放客船总代价与驱逐舰总代价对照。
- 中部放五项职责表，表中必须同时出现符号、计算方法和含义职责。
- 底部放两张职责判断卡和收束句。

### 模块清单
- 旧总代价卡
- 新总代价卡
- 五项职责表
- 判断卡 `哪一项直接回答船是否走到位`
- 判断卡 `为什么峰值动作和控制能量不能再糊成一项`
- 收束结论卡

### 静态承载内容
- 驱逐舰总代价常显：`$J_d(\\theta)=\\lambda_1\\bar E_{\\mathrm{trans}}+\\lambda_2\\bar E_{\\psi}+\\lambda_3\\bar E_{\\mathrm{traj}}+\\lambda_4\\bar u_{\\mathrm{peak}}+\\lambda_5\\bar E_u$`。
- 五项职责表完整保留五行：
  - `$\bar E_{\mathrm{trans}}$`：切换段误差相对基准值的归一化结果，统计方波切换后短时间窗口内的误差面积，衡量变向后能否尽快跟上。
  - `$\bar E_{\psi}$`：全程跟踪误差相对基准值的归一化结果，覆盖整个 `120s` 机动过程，防止只把少数切换段做好。
  - `$\bar E_{\mathrm{traj}}$`：航迹偏离相对基准值的归一化结果，将期望航向与实际航向按固定航速积分成平面轨迹后累计偏差，回答船是否真正走到位。
  - `$\bar u_{\mathrm{peak}}$`：峰值动作相对动作限值的归一化结果，检查执行机构瞬时最大操纵量是否越界。
  - `$\bar E_u$`：控制能量相对能量限值的归一化结果，统计全程控制量平方积分，检查持续动作代价。
- 收束句：驱逐舰场景改写的不是一组权重，而是整张比较对象清单。

### 混合证据顺序
- 先旧目标与新目标对照 -> 再五项职责表 -> 最后两张判断卡和收束句。

### 互动升级点
- 两张作答卡题面常显：
  - 哪一项指标直接回答“船是否真正走到位”？
  - 为什么峰值动作与控制能量必须继续分开接受比较？

### 教师控制
- 教师控制作答卡发放与参考答案显示；正文默认常显。

### 学生默认状态
- 正文可见，作答卡默认隐藏。

### 本页脱离讲稿后的自包含检查
- 学生只看本页即可解释五项量各自承担的判断职责，不会把它们理解成旧指标换名版。

### 预览口径
- 先对照公式，再落职责表，不允许把公式压到图后说明里。

## step-06｜何时把结构选择正式抬成问题本身

### 页面骨架
- 顶部放“对象变换 -> 任务变换 -> 筛选线重排 -> 固定结构职责过载”的链条。
- 中部放结构搜索引入条件三层显影。
- 底部放五类候选结构码本预览、一个解码映射例子与收束句。

### 模块清单
- 固定结构可用域收缩链
- 结构搜索引入条件显影链
- 五类候选结构码本预览
- 解码映射例子
- 结论卡

### 静态承载内容
- 入口链常显：`$\\text{对象变换}\\rightarrow\\text{任务变换}\\rightarrow\\text{筛选线重排}\\rightarrow\\text{固定结构职责过载}$`。
- 五类候选结构必须给出表达式和参数范围：
  - `PI`：`$C(s)=K\\dfrac{T_i s+1}{T_i s}$`，`$K\\in[0.90,2.20]$`，`$T_i\\in[8.00,28.00]$`。
  - `超前`：`$C(s)=K\\dfrac{T_z s+1}{T_p s+1}$`，`$K\\in[1.00,2.73]$`，`$T_z\\in[8.00,17.585]$`，`$T_p\\in[4.00,7.33]$`。
  - `PI+超前`：`$C(s)=K\\dfrac{T_i s+1}{T_i s}\\dfrac{T_z s+1}{T_p s+1}$`，`$K\\in[1.00,2.20]$`，`$T_i\\in[6.00,48.00]$`，`$T_z\\in[5.00,22.00]$`，`$T_p\\in[1.80,6.00]$`。
  - `滞后+超前`：`$C(s)=K\\dfrac{\\beta T_{\\mathrm{lag}}s+1}{T_{\\mathrm{lag}}s+1}\\dfrac{T_{\\mathrm{lead}}s+1}{T_p s+1}$`，`$K\\in[0.90,2.10]$`，`$T_{\\mathrm{lag}}\\in[6.00,18.00]$`，`$\\beta\\in[0.65,0.96]$`，`$T_{\\mathrm{lead}}\\in[6.00,18.00]$`，`$T_p\\in[2.50,6.20]$`。
  - 带微分滤波的 `PID`：`$C(s)=K_p\\left(1+\\dfrac{1}{T_i s}+\\dfrac{T_d s}{T_f s+1}\\right)$`，`$K_p\\in[0.90,2.50]$`，`$T_i\\in[8.00,22.00]$`，`$T_d\\in[0.50,3.80]$`，`$T_f\\in[0.04,0.20]$`。
- 解码映射例子必须常显：`z=[0.50,0.28,0.93,0.78,0.16,0.62]` 先由 `z_0=0.50` 得到 `ceil(5z_0)=3`，选择 `PI+超前`；随后只解释 `z_1` 到 `z_4`，得到 `$K=1.336$`、`$T_i=45.06$`、`$T_z=18.26$`、`$T_p=2.472$`，`z_5` 被忽略。
- 收束句：引入结构搜索不是因为算法更复杂，而是因为固定结构已经不能完整承接当前任务。

### 混合证据顺序
- 先可用域收缩链 -> 再引入条件显影 -> 再五类候选结构码本预览 -> 最后解码映射例子与收束句。

### 互动升级点
- 三层显影链依次写：
  1. 固定结构已无法同时解释速度收益、动作边界和稳健性余量。
  2. 同一组参数同时承担低频、中频和高频职责。
  3. 继续调参只会在同一小块可用域里交换代价。

### 教师控制
- 教师独立控制显影链；学生端默认不自主展开。

### 学生默认状态
- 结构码本预览和解码映射例子常显，显影链默认收起。

### 本页脱离讲稿后的自包含检查
- 学生只看本页即可知道“为什么此时才引入结构搜索”，而不是把它理解成任意扩题。

### 预览口径
- 一个“入口判断页”，不提前展示编码细节。

## step-07｜五类结构的统一编码：先判结构，再解释参数

### 页面骨架
- 顶部放六维编码定义卡。
- 中部先放五类结构卡，再放两组最小解码示例显影链。
- 底部放两张解码判断卡和结论句。

### 模块清单
- 六维编码定义卡
- 五类结构卡组
- 解码示例显影链
- 判断卡 `先判结构编号`
- 判断卡 `有效槽位与忽略槽位`
- 收束结论卡

### 静态承载内容
- 六维编码常显：`$[z_0,z_1,\\ldots,z_5]\\in[0,1]^6,\\quad k=\\max(1,\\lceil 5z_0\\rceil)$`。
- 五类结构卡每张都要写清：有效槽位、忽略槽位、参数允许范围、控制器表达式。
- 两组示例向量必须完整出现：
  - `z=[0.50,0.28,0.93,0.78,0.16,0.62]`
  - `z=[0.33,0.44,0.82,0.18,0.91,0.73]`
- 收束句：统一编码的关键不在“六个数”，而在“先判结构，再按当前结构解释哪些槽位真正生效”。

### 混合证据顺序
- 先六维编码定义 -> 再五类结构卡 -> 再两组示例显影 -> 最后两张判断卡与收束句。

### 互动升级点
- 解码显影链至少三层：
  1. 先根据 `z_0` 判结构编号。
  2. 再列出有效槽位与忽略槽位。
  3. 最后把有效槽位映射回工程参数。
- 两张作答卡题面常显：
  - 为什么不能把 `z_1` 到 `z_5` 永远当作同时生效的五个参数？
  - 给定一个编码向量时，哪一步必须先做，为什么？

### 教师控制
- 教师控制显影链与作答卡发放；学生端默认不自主展开显影。

### 学生默认状态
- 结构卡常显，显影链与作答卡默认隐藏。

### 本页脱离讲稿后的自包含检查
- 学生只看本页就能完成“结构编号 -> 槽位解释 -> 参数解码”的基本链路。

### 预览口径
- 一个“结构卡组 + 解码显影”的编码页，不能只剩一张宽表。

## step-08｜四类方案比较矩阵：先问目标是否对题，再问结构是否够用

### 页面骨架
- 顶部放四类方案矩阵卡。
- 中部放四行二列主比较图。
- 底部放表 5 与三层比较结论显影。

### 模块清单
- 四类方案矩阵卡
- 四类方案主比较图
- 表 5 问题矩阵
- 比较结论显影链
- 收束结论卡

### 静态承载内容
- 四类方案矩阵必须完整写出：
  1. 驱逐舰采用客船代价函数但是固定结构。
  2. 驱逐舰采用客船代价函数但是变结构搜索。
  3. 驱逐舰采用专用代价函数但是固定结构。
  4. 驱逐舰采用专用代价函数和变结构搜索。
- 图后口令必须写明：每一行左图看方波跟踪，右图看期望航迹与实际航迹。
- 收束句：四类方案存在的意义，是把“目标是否对题”和“结构是否够用”拆开，而不是只给出一个看起来最好的结果。

### 混合证据顺序
- 先四类矩阵卡 -> 再主比较图 -> 再表 5 -> 最后比较结论显影与收束句。

### 互动升级点
- 三层显影链依次写：
  1. 旧目标 + 旧结构：直接暴露目标错位。
  2. 旧目标 + 变结构：只放开结构无法弥补错误目标。
  3. 新目标 + 旧结构 / 新目标 + 变结构：进一步暴露结构边界与重新打开的可用域。

### 教师控制
- 教师独立控制显影链；学生端默认不自主展开。

### 学生默认状态
- 矩阵卡、主比较图和表 5 常显，显影链默认收起。

### 本页脱离讲稿后的自包含检查
- 学生只看本页即可知道四类方案各自回答什么，不会只报“最后一行最好”。

### 预览口径
- 一个“矩阵卡 + 主图 + 问题表”的比较页，主图不能脱离判断问题单独出现。

## step-09｜结构搜索证据与专项验证：总代价更低为什么仍不自动等于可交付

### 页面骨架
- 顶部放 GA/PSO 收敛图与读图口令。
- 中部放组合结构专项验证图、控制器表达式和控制效果曲线。
- 底部放三层边界判断显影链与一张判断卡。

### 模块清单
- 收敛图
- 收敛图读图表
- 组合结构专项验证图
- 组合控制器表达式卡
- 控制效果曲线
- 边界判断显影链
- 判断卡 `总代价更低为何仍不能直接交付`

### 静态承载内容
- 收敛图只服务于“结构选择证据”，不得被写成算法原理页。
- 组合结构控制器表达式必须完整出现：
  - `$u(s)=C_{fb}(s)(r(s)-\\psi(s))+F_r(s)r(s)-H_r(s)\\dot{\\psi}(s)$`
  - `$C_{fb}(s)=1.9727\\dfrac{47.0266s+1}{47.0266s}\\dfrac{20.6218s+1}{5.8952s+1}$`
  - `$F_r(s)=0\\cdot\\dfrac{0.9735s+1}{0.3286s+1}=0$`
  - `$H_r(s)=\\dfrac{0}{0.02s+1}=0$`
- 收束句：总代价更低、航迹误差更小，并不自动等于筛选线全部成立。

### 混合证据顺序
- 先收敛图与读图表 -> 再专项验证图 -> 再控制器表达式与控制效果曲线 -> 最后边界判断显影链和判断卡。

### 互动升级点
- 三层显影链依次写：
  1. 收敛图只说明搜索会把结果推向哪一类结构家族。
  2. 组合结构候选虽然总代价更低，但前馈与测速反馈最终收缩成零增益。
  3. 只要筛选线没有写成真正硬约束，优化器就会继续沿总代价允许的方向下降。
- 判断卡题面常显：为什么“总代价更低”与“工程可交付”不能直接画等号？

### 教师控制
- 教师控制显影链与判断卡发放；学生端默认不自主展开显影。

### 学生默认状态
- 图、公式和曲线常显；显影链与判断卡默认隐藏。

### 本页脱离讲稿后的自包含检查
- 学生只看本页即可知道专项验证的真正结论是“边界判断更清楚了”，而不是“组合结构已经可直接替代主方案”。

### 预览口径
- 一个“收敛证据 + 专项验证 + 边界判断”的综合页，但顺序必须先讲收敛图角色，再讲专项验证。

## step-10｜后测：目标错位、结构边界与统一编码是否已经连成链

### 页面骨架
- 顶部后测标题卡。
- 中部三题后测卡。
- 底部只保留一条提交说明。

### 模块清单
- 后测标题卡
- 后测题卡 1
- 后测题卡 2
- 后测题卡 3
- 提交说明卡

### 静态承载内容
- 标题固定写“阶段后测：把目标、结构与编码重新连成一条判断链”。
- 三题题面常显：
  - 为什么本课不能继续把问题写成“在原结构里微调参数”？
  - 驱逐舰专用代价函数比客船代价函数多回答了什么？
  - 给定一个统一编码向量时，为什么必须先判结构编号再解释参数？

### 混合证据顺序
- 先标题 -> 再三题后测卡 -> 最后提交说明。

### 互动升级点
- 三题均独立提交，不合并成整页大表单。

### 教师控制
- 教师控制后测发放与参考答案显示。

### 学生默认状态
- 标题常显，题卡默认隐藏，发放后可作答。

### 本页脱离讲稿后的自包含检查
- 学生能只凭本页题面回忆完整链路，而不是依赖教师再复述一遍。

### 预览口径
- 标准独立后测页，不与总结合并。

## step-11｜总结：从任务重排到结构编码入口的完整判断链

### 页面骨架
- 顶部放五条总结。
- 中部放本讲信息图总结。
- 信息图下方放本课主线链条。
- 底部放 `4-7` 去向卡。

### 模块清单
- 五条总结卡
- 信息图总结
- 主线链条卡
- 去向卡

### 静态承载内容
- 五条总结必须常显：
  1. 场景迁移会同时改写对象、任务和筛选线。
  2. 固定结构迁移的价值在于把结构边界暴露出来。
  3. 驱逐舰专用代价函数改写的是比较对象，而不只是权重。
  4. 统一编码让结构选择和参数搜索进入同一判断框架。
  5. 四类方案比较把“目标是否对题”和“结构是否够用”拆成两道独立判断。
- 主线链条固定写：`任务重排 -> 固定结构失配 -> 目标重排 -> 结构编码入口 -> 四类方案比较 -> 边界判断`。
- 信息图使用 runtime 媒体：`/course-runtime/lessons/4-6/media/4-6-info.png`，图题写“场景迁移、结构编码与边界判断总览”。
- 去向句：`4-7` 将把结构编码、目标函数、约束建立和结果解释并入完整工程设计闭环。

### 混合证据顺序
- 先五条总结 -> 再信息图 -> 再主线链条 -> 最后去向卡。

### 互动升级点
- 本页只阅读，不放作答卡。

### 教师控制
- 无。

### 学生默认状态
- 页面全部可见。

### 本页脱离讲稿后的自包含检查
- 学生只看本页就能说出本课真正完成的是哪条判断链，并知道下一课接什么。

### 预览口径
- 标准总结页，只负责收束与移交。
