\documentclass[tikz,border=8pt]{standalone}
\usepackage{ctex}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta,positioning}

\begin{document}
\begin{tikzpicture}[
  >=Stealth,
  signal/.style={->, line width=1.2pt, draw=black!80},
  block/.style={
    rectangle,
    draw=blue!70!black,
    line width=1.2pt,
    minimum width=2.25cm,
    minimum height=0.92cm,
    align=center,
    font=\footnotesize
  },
  sum/.style={
    circle,
    draw=green!55!black,
    line width=1.2pt,
    minimum size=0.48cm,
    inner sep=0pt,
    path picture={
      \draw[green!55!black,line width=1pt]
        (path picture bounding box.south west) -- (path picture bounding box.north east)
        (path picture bounding box.north west) -- (path picture bounding box.south east);
    }
  },
  tap/.style={
    circle,
    draw=orange!80!black,
    line width=1.2pt,
    minimum size=4.5pt,
    inner sep=0pt
  },
  labelbox/.style={font=\footnotesize, align=center},
  linelabel/.style={font=\scriptsize, fill=white, inner sep=1.5pt}
]
  \node[labelbox] (ref) at (0,0) {期望航向\\$\psi_d$};
  \node[sum, right=1.05cm of ref] (sumerr) {};
  \node[block, right=1.10cm of sumerr] (controller) {控制器\\$C(s)$};
  \node[block, right=1.15cm of controller] (servo) {舵机\\$\dfrac{1}{T_rs+1}$};
  \node[sum, right=1.05cm of servo] (sumd) {};
  \node[block, right=1.15cm of sumd] (hull) {船体艏摇\\$\dfrac{K_h}{T_hs+1}$};
  \node[block, right=1.15cm of hull] (integrator) {积分\\$\dfrac{1}{s}$};
  \node[tap, right=0.95cm of integrator] (tapout) {};
  \node[labelbox, right=0.78cm of tapout] (out) {航向输出\\$\psi$};

  \node[block, above=1.35cm of sumd] (dist) {扰动等效\\$\dfrac{K_d}{T_ds+1}$};
  \node[labelbox, above=0.45cm of dist] (dsrc) {风浪流扰动\\$d$};

  \draw[signal] (ref) -- node[linelabel, above] {给定} (sumerr);
  \draw[signal] (sumerr) -- node[linelabel, above] {误差 $e_\psi$} (controller);
  \draw[signal] (controller) -- node[linelabel, above] {舵令 $\delta_c$} (servo);
  \draw[signal] (servo) -- node[linelabel, above] {实际舵角 $\delta$} (sumd);
  \draw[signal] (sumd) -- node[linelabel, above] {合成输入} (hull);
  \draw[signal] (hull) -- node[linelabel, above] {艏摇角速度 $r$} (integrator);
  \draw[signal] (integrator) -- node[linelabel, above] {航向 $\psi$} (tapout) -- (out);
  \draw[signal] (dsrc) -- (dist);
  \draw[signal] (dist) -- node[linelabel, right] {等效舵角 $d_e$} (sumd);

  \draw[signal] (tapout) |- ++(0,-1.35) -| node[linelabel, left, near end] {$-$} (sumerr.south);

  \node[font=\footnotesize, align=center, below=0.95cm of hull, text width=8.6cm]
    {分段辨识只给出闭环设计所需的低阶主通道；扰动在舵机之后、船体艏摇之前注入。};
\end{tikzpicture}
\end{document}
