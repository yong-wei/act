\documentclass[tikz,border=8pt]{standalone}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta,positioning}

\begin{document}
\begin{tikzpicture}[
  >=Stealth,
  block/.style={rectangle, draw=blue!70!black, fill=none, line width=1.2pt, minimum width=3.1cm, minimum height=1.0cm, align=center, font=\small\bfseries},
  signal/.style={->, line width=1.2pt, draw=gray!80!black},
  note/.style={font=\small, align=center},
  tap/.style={circle, draw=orange!80!black, fill=none, line width=1.2pt, minimum size=4.5pt, inner sep=0pt}
]

\node[note] (formula) at (6.0,2.0) {$\hat T_k=\dfrac{\Delta t}{1-\hat a_k},\quad
\hat K_k=\dfrac{\hat b_k\hat T_k}{\Delta t},\quad
\hat d_k=\dfrac{\hat c_k}{\Delta t}$};

\node[block] (buffer) at (0,0) {Data buffer\\$\{r_j,\delta_j,r_{j+1}\}$};
\node[block] (identify) at (4.0,0) {Rolling least squares\\$r_{j+1}=ar_j+b\delta_j+c$};
\node[block] (project) at (8.2,0) {Projection + smoothing\\$\hat K,\hat T,\hat d$};
\node[block] (model) at (12.2,0) {Updated predictor\\$\hat x_{i+1}=\hat f_k(x_i,u_i)$};

\node[block] (optimizer) at (12.2,-2.3) {MPC optimizer\\$u_k^\star$};
\node[block] (plant) at (8.2,-2.3) {Plant};
\node[tap] (tap) at (4.0,-2.3) {};
\node[note] (output) at (2.5,-2.3) {$r_{k+1},y_{k+1}$};

\draw[signal] (buffer) -- (identify);
\draw[signal] (identify) -- (project);
\draw[signal] (project) -- (model);
\draw[signal] (model) -- (optimizer);
\draw[signal] (optimizer.west) -- node[above] {$\delta_k$} (plant.east);
\draw[signal] (plant.west) -- (tap) -- (output);
\draw[signal] (tap) |- ++(0,-0.85) -| (buffer.south);

\node[note] at (6.1,-3.45) {The optimizer keeps the same objective and constraints; only the predictor is refreshed.};

\end{tikzpicture}
\end{document}
