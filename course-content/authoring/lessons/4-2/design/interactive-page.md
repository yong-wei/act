━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
互动页面设计 | 单元 4-2：控制器选型原理：不同控制结构为何适合不同任务
技术栈：Next.js 14 + TypeScript + Tailwind CSS + shadcn/ui
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 文档职责
- 本文件只保留 4-2 当前审定后的 13 步课堂蓝图。
- 作者态与 `interactive-contract.yaml` 必须同名、同序、同互动类型。
- 本课唯一正式产物是“单结构首轮起步卡”；复合结构骨架一律留给 4-3。

## 本轮整改结论
- 删除原 `step-05` 的“课程总结信息图”步骤，后续整体前移编号。
- `step-03` 只保留判断链与参数方向提示，不再设置学生作答区。
- `step-04` 上方静态表至少复现讲义表 3 前三列，下方作答改为多选矩阵。
- `step-05` 与 `step-07` 改为 Rust/WASM 驱动的原生统一面板，四面板板式 + 底部增益控件 + 实时性能指标。
- `step-06`、`step-08`、`step-09` 采用逐步显影，作答区统一左右双栏，题面写在卡片标题中，不再在卡体重复文案。
- `step-10` 作答区同样改为左右双栏；结构图与四联图均读取 runtime 媒体。
- `step-11` 起步卡工作区简化为六字段最小卡片。
- `step-12` 后测与总结彻底分离，后测只检查判断链。

## 全课总览
| 步骤 | 标题 | 模板 | 互动类型 | 页面要点 |
|---|---|---|---|---|
| step-01 | 回到地图：4-1 的任务表达卡如何接到 4-2 的结构起步判断 | `map_goal_boundary_slide` | `none` | 路径、边界与本课角色 |
| step-02 | 前测：结构名字为什么不是答案 | `question_stack` | `quiz_group` | 三题误判前测 |
| step-03 | 单结构判断链：主矛盾先行，结构名称压后出现 | `formula_table_reasoning` | `none` | 开环表达 + 六步判断链 |
| step-04 | 控制结构工具箱：按作用机制重组，而不是按名字平铺 | `formula_table_match` | `multi_select_matrix` | 结构工具箱总表 + 多选矩阵 |
| step-05 | 案例 A 入口：客船航向保持先看低频保持能力，而不是先追更快 | `case_evidence_board` | `activity_card_set` | 客船原生统一面板 + 双栏作答 |
| step-06 | 案例 A 展开：为什么客船首轮更像 PI/滞后，而不是先上 PD | `worked_example_compare` | `worked_example_reveal` | 表5 + 逐步显影 + 双栏作答 |
| step-07 | 案例 B 入口：稳定平台先整理中频动态品质与储备 | `case_evidence_board` | `activity_card_set` | 平台原生统一面板 + 双栏作答 |
| step-08 | 案例 B 展开：为什么稳定平台首轮更像 PD/超前，而不是先补 PI | `worked_example_compare` | `worked_example_reveal` | 平台比较链 + 逐步显影 + 双栏作答 |
| step-09 | 按输入补偿前馈：它先改参考通道，不等于把 PD 改写了名字 | `worked_example_compare` | `worked_example_reveal` | runtime 结构图 + 公式链 + 四联图 |
| step-10 | 按扰动补偿前馈：它先削弱误差来源，不等于替代反馈保底 | `feedforward_compare_board` | `activity_card_set` | runtime 结构图 + 风险边界 + 双栏作答 |
| step-11 | 单结构首轮起步卡工作区：把结构、方向、收益与代价写成最小卡片 | `task_card_workspace` | `task_card_workspace` | 六字段最小起步卡 |
| step-12 | 后测：是否已经形成“先看主矛盾，再选结构”的判断链 | `assessment_card_grid` | `quiz_card_grid` | 四题后测 + 错因标签 |
| step-13 | 收束与去向：4-2 的出口只交给 4-3 的复合结构骨架阶段 | `summary_route_board` | `none` | 五句带走 + 去向 |

## 逐步说明

### step-03｜判断链静态页
- 必须完整保留 `L(s)=C(s)P(s)`。
- 必须完整列出六步判断链。
- 必须给出参数方向句式提示。
- 不设置学生作答区，也不显示“本页无需提交”类占位文案。

### step-04｜结构工具箱
- 上方先呈现“结构工具箱总表”，至少保留讲义表 3 的前三列：
  - 结构名称；
  - 最小作用语义；
  - 首轮使用边界。
- 下方学生区改为多选矩阵，允许同题多选。
- `PID` 只以“压后进入候选”的原则卡出现，不作为默认答案。

### step-05｜客船入口
- 图片区改为 Rust/WASM 原生统一面板，不再复用 4-1 静态 PNG。
- 板式保持四面板阅读逻辑，下方统一设置增益控件。
- 必须实时显示性能指标，如 `Mp`、`t_s`、`ω_c`、`PM`。
- 学生作答区保持左右双栏。

### step-06｜客船展开
- 题面不得隐藏。
- 分析过程必须逐步显影，不能只剩结论性文本。
- 页面必须出现讲义“表5”并作为对比表。
- 学生作答区左右双栏，题目写在卡片标题中，不再重复卡片文案。

### step-07｜平台入口
- 与客船入口一致，改为 Rust/WASM 原生统一面板。
- 保持现有图片对应的四面板信息结构。
- 底部统一放置增益控件，指标实时刷新。
- 学生作答区左右双栏。

### step-08｜平台展开
- 必须按讲义分析链逐步显影。
- 必须通过对比表说明为何先选 `PD/超前`，而不是先补 `PI`。
- 学生作答区左右双栏，题目写在标题中。

### step-09｜输入前馈
- 结构图与四联图都必须读取 runtime 媒体。
- 公式必须正确渲染，禁止双反斜杠误写。
- 逐步显影要围绕“通道差异—闭环结果—验证目标”展开。
- 学生作答区左右双栏，不加重复文案。

### step-10｜扰动前馈
- 结构图、公式链、四联图都必须正常显示。
- 强调“前馈削弱误差来源，反馈负责保底”的边界。
- 学生作答区左右双栏，不加重复文案。

### step-11｜起步卡工作区
- 只保留六字段：
  - 当前任务
  - 最紧矛盾
  - 首选单结构
  - 参数起步方向
  - 预期收益
  - 主要代价
- 删除“留给 4-3 的问题”等扩展字段。
- 工作区采用左右双栏。

### step-12｜后测
- 后测只检查判断链和边界意识，不与总结混页。
- 作答区维持简化布局，双栏展示题卡即可。

### step-13｜收束
- 只做五句带走与去向说明。
- 明确提示 4-3 才进入复合结构骨架。

## 媒体与公式口径
- 正式互动页面只允许读取 runtime 媒体路径，不得回读 authoring `media/processed`。
- React + KaTeX 场景下，公式字符串必须直接写 LaTeX 命令，禁止使用 `String.raw` 再叠加双反斜杠。
- 客船与平台入口页采用原生统一面板；输入前馈与扰动前馈仍保留 runtime 结构图与四联图。
