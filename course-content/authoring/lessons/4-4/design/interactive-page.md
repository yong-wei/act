━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
互动页面设计 | 单元 4-4：多目标权衡与控制器优化设计
技术栈：Next.js 14 + TypeScript + Tailwind CSS + shadcn/ui
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 文档职责

- 本文件是 `4-4` 作者态互动页面蓝图，负责把讲义中的对象引入、多目标拉扯、参数化入口、自由目标、数值优化直观印象、三组无约束候选、`Pareto front` 与横摇边界案例完整落成学生可独立阅读的页面内容。
- 同目录 `interactive-contract.yaml` 是机读真源，两者必须在步骤顺序、标题、页面模板、内容载荷、教师控制和学生默认状态上逐项一致。
- 本课只做到“固定结构下的无约束优化建模与候选族呈现”，不提前在页面里展开 `4-5` 的硬约束、罚函数或带约束求解细节。

## 表述规则

- 每一步都必须回答四件事：对象是什么、正在判断什么、关键证据在哪里、这一页的结论如何接回“固定结构下的无约束优化建模”。
- 讲义顺序优先。对象与问题先于目标卡，公式、表格和文字先于验证图，后测先于总结移交。
- 本课中“候选”“展示解”“Pareto 前沿点”都不是“最终可交付方案”的同义词，页面正文必须始终保留这层边界。

## 全课总览

| 步骤 | 标题 | 页面模板 | 互动类型 | 本页职责 |
|---|---|---|---|---|
| step-01 | 客船航向保持对象与多目标拉扯：为什么经典试凑会停住 | `map_goal_boundary_slide` | `none` | 先交代对象、矛盾和本课路径 |
| step-02 | 四组经典试凑结果：多目标拉扯先被证据看见 | `evidence_table_plus_chart` | `none` | 固定首轮证据与拉扯读法 |
| step-03 | 前测：先判断哪些边界还没有被正式写进模型 | `quiz_group_board` | `quiz_group` | 回收“候选不等于可用解”等前置判断 |
| step-04 | 参数化入口：起点、参数向量与参数范围从哪里来 | `parameterization_board` | `activity_card_set` | 固定起点与参数范围的职责边界 |
| step-05 | 自由目标与归一化：怎样把偏好写成同一套比较语言 | `formula_workspace_board` | `activity_card_set` | 固定四类自由目标和归一化来源 |
| step-06 | 数值优化到底在这门课里做什么 | `method_explanation_board` | `none` | 固定“评价-仿真-回读-更新”迭代链 |
| step-07 | 梯度下降最小例子：搜索为什么会沿代价面下滑 | `worked_example_reveal_board` | `teacher_reveal_only` | 给出梯度下降直观印象与示意图 |
| step-08 | 主案例总表：起始方案与三组无约束权重方案如何分化 | `results_evidence_board` | `single_choice` | 固定三组权重、传函和时域总表 |
| step-09 | 时域与频域对比：三组候选到底换来了什么 | `curve_evidence_board` | `activity_card_set` | 固定主案例综合图与频域回读 |
| step-10 | Pareto front：为什么会出现一族同样值得保留的设计 | `curve_evidence_board` | `single_choice` | 固定 front 图与二目标矛盾关系 |
| step-11 | 三个典型前沿点：它们为什么在 Pareto 意义下同样好 | `comparison_decision_board` | `single_choice` | 固定参数表、响应图和工程决策意义 |
| step-12 | 横摇边界案例：目标语言一变，数值响应也必须跟着变 | `boundary_case_board` | `task_card_workspace` | 固定横摇自由目标、数值表与验证图 |
| step-13 | 后测：判断链是否已经形成 | `quiz_group_board` | `quiz_group` | 检查本课核心判断链 |
| step-14 | 总结与移交：把候选族交给 4-5 做工程复核 | `summary_handoff_board` | `none` | 收束本课并把候选移交给 4-5 |

## 证据单元升级决策表

| evidence_unit_id | handout_anchor | target_steps | keep_elements | non_reducible |
|---|---|---|---|---|
| route_and_problem | 开头、第一节、课程目标 | step-01 | 对象 `P_h(s)`、四类拉扯、本课路径、本课七项目标 | 对象与拉扯先于目标卡 |
| classical_trials | 3.1-3.2 | step-02 | 四组试凑表、拉扯读法、图后结论 | 不得先讲求解器 |
| preassessment_gate | BOPPPS P₁ | step-03 | 三个前测判断题、题后过渡句 | 前测独立成页 |
| parameterization_entry | 4.1-4.2 | step-04 | 起始方案、参数向量、参数范围表、范围说明 | 必须写明“参数范围不等于输出复核条件” |
| free_objective_design | 5.1-5.3 | step-05 | 四类自由目标、归一化基准、`J_free` | 不得加入罚项 |
| optimization_method | 6.1-6.2 | step-06 | 数值优化五步迭代链 | 不得退化成算法名词列表 |
| gradient_intuition | 6.3 | step-07 | 一维例子、梯度下降图、图后解释 | 题面与显影分离 |
| unconstrained_family | 7.1 | step-08 | 三组权重、三类控制器、时域总表 | 传函与权重都要完整出现 |
| time_frequency_compare | 7.2-7.3 | step-09 | 综合图、频域表、三条回读结论 | 不得只保留单个候选 |
| pareto_front | 8.1 | step-10 | `Pareto front` 图、二目标定义、front 读法 | 必须出现可视化前沿 |
| pareto_points | 8.2-8.3 | step-11 | 三个典型点参数表、响应图、工程意义 | 不得退化成最小例题 |
| roll_boundary_case | 9.1-9.2 | step-12 | 横摇自由目标、数值表、验证图、三张工作卡题面 | 必须出现数值响应实验 |
| post_assessment | BOPPPS P₃ | step-13 | 三题后测、答题前过渡句 | 后测独立成页 |
| summary_and_handoff | 10、BOPPPS S | step-14 | 六条总结链、`4-5` 去向 | 总结与移交独立成页 |

## 逐步说明

### step-01｜客船航向保持对象与多目标拉扯：为什么经典试凑会停住
- 页面骨架：上半区先放对象卡与四类拉扯卡，中部放 `4-3 -> 4-4 -> 4-5` 路径图与角色说明，下半区放七项目标卡，不设置作答区。
- 页面正文内容：对象卡完整写 `$P_h(s)=\\dfrac{0.01715}{s(s+0.1)(s+2.14375)}$`，并写清“对象自带积分特性，当前最紧的矛盾不是稳态误差，而是多目标拉扯”。四类拉扯卡依次写“希望修航过程更快”“希望误差拖尾更短”“希望前中段误差强度不要被放大”“希望控制动作不要变得过于粗暴”。路径说明写“`4-3` 给出固定结构与问题清单，`4-4` 先产出无约束候选族，`4-5` 再做工程边界复核”。目标卡完整保留本课七项目标。

### step-02｜四组经典试凑结果：多目标拉扯先被证据看见
- 页面骨架：上半区放表 1，下半区先放读图口令，再放客船首轮对比图和图后结论。
- 页面正文内容：表格完整保留“比例基线、仅补低频、方向过激、最小修正”四组方案的设计意图和指标。读图口令固定写“先看哪一组更快，再看哪一组拖尾更短，随后看哪一组主要在收动作代价，最后判断为什么这些收益与代价还没有被写进同一套比较语言”。图后结论写“固定结构没有失效，真正暴露出来的是多目标拉扯还缺一套统一评价语言”。

### step-03｜前测：先判断哪些边界还没有被正式写进模型
- 页面骨架：顶部先放“前测名称 / 本页任务”标题卡，下半区放三题前测卡，题卡后放一条过渡说明。
- 页面正文内容：标题卡写“这一页不要求求参数，只要求先判断哪些话现在还不能说满”。三题题干固定写“只要一组参数让时间更短、拖尾更小，是否就已经能当最终答案”“参数范围和工程硬边界是不是同一种东西”“若任务从航向跟踪改成横摇抗扰，原目标函数能否直接照搬”。过渡说明写“若这三题里仍有判断不稳，后面的参数化、自由目标和边界案例就会被误读成‘优化已经替工程裁决’”。

### step-04｜参数化入口：起点、参数向量与参数范围从哪里来
- 页面骨架：上半区先放 `4-3` 起始方案卡，再放参数向量卡；下半区放参数范围表、范围说明卡和一张小卡提交。
- 页面正文内容：起始方案卡完整写 `$C_{h,0}(s)=2.796\\dfrac{10s+1}{4.06s+1}$`。参数向量卡完整写 `$\\theta=[K,T,\\alpha]^\\mathsf T$` 与 `$C_h(s)=K\\dfrac{Ts+1}{\\alpha Ts+1}$`。参数范围表逐项解释 `K,T,α` 的结构职责。范围说明固定写“参数范围只负责保持当前超前结构可解释，不负责替代输出过程的工程复核”。作答卡要求学生用一句话写出“为什么优化起点直接来自 4-3”。

### step-05｜自由目标与归一化：怎样把偏好写成同一套比较语言
- 页面骨架：上半区放四类自由目标项卡和归一化表，下半区放 `J_free` 公式卡、项义解释表和一张判断卡。
- 页面正文内容：目标项卡固定写“调节时间 `t_s`”“`ITAE`”“`ITSE`”“控制能量 `E_u`”。归一化表完整保留 `$t_s/40`、`ITAE/ITAE_0`、`ITSE/ITSE_0`、`E_u/E_{u,0}`` 的来源。公式卡完整写 `$J_{\\mathrm{free}}(\\theta)=w_1\\dfrac{t_s}{40}+w_2\\dfrac{ITAE}{ITAE_0}+w_3\\dfrac{ITSE}{ITSE_0}+w_4\\dfrac{E_u}{E_{u,0}}$`。解释表逐项写“能否更快”“拖尾是否更短”“中前段误差强度是否被压低”“动作代价是否被透支”。作答卡要求学生区分哪两项更像收益、哪一项最明显在记录动作代价。本页不出现罚项。

### step-06｜数值优化到底在这门课里做什么
- 页面骨架：上半区放“问题写成什么”卡，中部放五步迭代链，下半区放一张收束卡。
- 页面正文内容：问题卡完整写 `$\\min_\\theta J_{\\mathrm{free}}(\\theta), \\theta\\in[1,6]\\times[4,15]\\times[0.15,0.85]$`。五步链固定写“取参数 -> 跑闭环仿真 -> 回读 `t_s/ITAE/ITSE/E_u` -> 代回目标函数 -> 比较后更新”。收束卡写“本课里的数值优化不是神秘黑箱，而是把偏好表达变成可重复的证据迭代”。本页不设作答区。

### step-07｜梯度下降最小例子：搜索为什么会沿代价面下滑
- 页面骨架：顶部为完整题面卡，中部为逐步显影链，底部为梯度下降示意图和图后解释。
- 页面正文内容：题面卡完整写 `$f(x)=(x-3)^2+1$` 与“从 $x_0=0$ 出发，沿负梯度方向更新”。逐步显影链至少分成“当前位置的函数值”“局部梯度指向哪里”“更新后为什么更接近谷底”三层。图像固定使用 `4-4-gradient-descent-path.png`，图后解释固定写“这个例子不是在替主案例求参数，而是在给出一个最小直观印象：目标函数一旦写出，搜索就会在代价面上留下可读轨迹”。本页只允许教师逐步显影，不设置学生提交。

### step-08｜主案例总表：起始方案与三组无约束权重方案如何分化
- 页面骨架：上半区先放同一超前结构说明卡，再放三组权重表；下半区放表 4 与一张单选判断卡。
- 页面正文内容：结构说明卡完整写 `$C_h(s)=K\\dfrac{Ts+1}{\\alpha Ts+1}$`。权重表完整写三组权重 `A/B/C` 及其偏好说明，`C` 组必须与现讲义一致为 `[0.20,0.25,0.25,0.30]`。表 4 完整保留起始方案与三组控制器传函、调节时间、`ITAE`、`ITSE` 和控制能量。判断卡要求学生根据“若当前更担心动作代价继续抬高”判断更应优先保留哪一组。参考解释必须回到“权重代表偏好，候选不会只有一组”。

### step-09｜时域与频域对比：三组候选到底换来了什么
- 页面骨架：上半区放主案例四联综合图，中部放时域回读结论卡，下半区放表 5 和一张作答卡。
- 页面正文内容：综合图固定使用 `4-4-ship-heading-unconstrained-weight-compare.png`。时域结论卡完整写三条判断：“起始方案的主问题确实偏慢”“方案 A 与 B 都明显压短主过程，其中 B 更平衡”“方案 C 更保守，说明动作代价一旦被照顾，过程会回到更平缓的形态”。表 5 完整保留截止频率、相角裕度、超调和控制峰值。作答卡要求学生用一句话说明“为什么同样是无约束优化，时域收益、频域读数和动作代价不会朝同一个方向同步变化”。

### step-10｜Pareto front：为什么会出现一族同样值得保留的设计
- 页面骨架：上半区先放二目标定义卡，再放 `Pareto front` 图；下半区放 front 读法卡和一张单选判断卡。
- 页面正文内容：定义卡完整写二目标设计对 `$(ITAE(\\theta),E_u(\\theta))$`，并写清“任何一个目标再改善一点，就一定会让另一个目标变差”的非支配含义。图像固定使用 `4-4-pareto-front.png`。读法卡写“越往左走，控制能量更小，但 `ITAE` 会明显变差；越往上走，拖尾改善有限，但动作代价更容易被压低”。判断卡要求学生判断“为什么 Pareto front 不是再选一个绝对最优”。本页不能再退化成最小例题。

### step-11｜三个典型前沿点：它们为什么在 Pareto 意义下同样好
- 页面骨架：上半区先放同一超前结构说明卡和表 6，中部放典型点响应图与三条工程意义卡，下半区放单选判断卡。
- 页面正文内容：结构说明卡完整写 `$C_h(s)=K\\dfrac{Ts+1}{\\alpha Ts+1}$`。表 6 完整保留 `P_1/P_2/P_3` 的 `K/T/αT`、`ITAE`、`E_u`、调节时间和控制峰值。图像固定使用 `4-4-pareto-response-compare.png`。图后解释必须完整写出三条：“从 `P_3` 走向 `P_1`，可以显著压低 `ITAE`，但必须接受更高控制能量”“从 `P_1` 回到 `P_3`，可以大幅节省动作代价，但必须接受更长拖尾”“`P_2` 夹在中间，没有把另一边彻底碾压掉”。工程意义卡固定写“这条前沿先筛掉同时输给别人的候选”“它把偏好从口头争论变成可视化对象”“后续工程复核是在这组可解释候选上继续做筛选”。判断卡要求学生回答“它们为什么在 Pareto 意义下同样好，而不是谁彻底赢了”。

### step-12｜横摇边界案例：目标语言一变，数值响应也必须跟着变
- 页面骨架：上半区先放横摇对象与自由目标公式卡，中部放表 7 和横摇验证图，下半区放三张独立工作卡。
- 页面正文内容：对象卡完整写 `$P_r(s)=\\dfrac{1}{2.052s^2+0.3929s+1}$` 和 `$C_r(s)=k\\dfrac{2.052s^2+0.3929s+1}{s}, 0.5\\le k\\le 3.5$`。自由目标卡完整写 `$J_r(k)=0.60\\dfrac{RMS(\\varphi)}{RMS(\\varphi)_0}+0.25\\dfrac{\\max_\\omega|T_d(j\\omega)|}{\\max_\\omega|T_d(j\\omega)|_0}+0.15\\dfrac{RMS(u)}{RMS(u)_0}$`。表 7 完整保留起始方案与无约束优化结果的 `RMS(φ)`、横摇峰值、谐振峰值、谐振峰值 dB 与 `RMS(u)`。验证图后固定写两条回读结论：“目标语言改写后，搜索开始主动压低横摇整体强度和谐振峰”“收益没有白拿，`RMS(u)` 的上升说明动作代价被重新分配”。三张卡题面分别写“请写出横摇案例中，为什么原航向保持目标函数不能直接照搬”“请写出哪两项数值变化最能说明目标语言改了，搜索方向也改了”“请用一句话说明横摇案例里收益与代价是怎样重新分配的”，独立提交。

### step-13｜后测：判断链是否已经形成
- 页面骨架：顶部先放“后测名称 / 本页任务”标题卡，下半区放三题后测卡，不夹带总结句。
- 页面正文内容：标题卡写“这一页只检查判断链，不负责总结和移交”。三题题干固定写“为什么本课保留下来的只能叫‘候选族’，而不是最终可用解”“为什么 Pareto front 不是再选一个绝对最优”“为什么横摇案例不能继续沿用航向保持中的目标语言”。题后提示写“答题时必须同时回接本课中的证据页，而不是只重复名词定义”。

### step-14｜总结与移交：把候选族交给 4-5 做工程复核
- 页面骨架：上半区放“本课带走的六句话”，下半区放“`4-5` 去向”卡，不设置作答区。
- 页面正文内容：带走结论必须覆盖六点：多目标拉扯先由证据暴露、参数化入口来自 `4-3`、归一化基准来自任务书或首轮方案、数值优化把偏好变成迭代链、Pareto 把候选族显性呈现、边界案例说明任务语言变化会改写目标函数。去向卡只写“`4-5` 将接手这组候选，对输出侧工程边界做系统复核，再决定哪些候选还能继续保留”，不提前展开罚函数细节。
