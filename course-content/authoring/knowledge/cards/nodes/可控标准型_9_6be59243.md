---
node_id: 可控标准型_9_6be59243
name: 可控标准型
category: 程序性
bloom_level: 应用
chapter: 9
tags:
- 友矩阵
- 状态变量图
- 可控性
card_version: 1
source_docs:
- course-content/authoring/knowledge/base/knowledge_graph.json
---

## 首页

# 可控标准型

**一句话定义**：状态空间表达式中，A矩阵为友矩阵，B矩阵具有特定形式（如[0,0,...,1]^T），便于分析系统可控性。

**关键公式**：

$$
A = \begin{bmatrix} 0 & 1 & 0 & \cdots & 0 \\ 0 & 0 & 1 & \cdots & 0 \\ \vdots & \vdots & \vdots & \ddots & \vdots \\ -a_0 & -a_1 & -a_2 & \cdots & -a_{n-1} \end{bmatrix}
$$

$$
B = \begin{bmatrix} 0 \\ 0 \\ \vdots \\ 1 \end{bmatrix}
$$

## 详情

### 完整解释

状态空间表达式中，A矩阵为友矩阵，B矩阵具有特定形式（如[0,0,...,1]^T），便于分析系统可控性。

### 典型示例

- 例9-3中，由传递函数建立可控标准型动态方程。

### 关键词

友矩阵、状态变量图、可控性
