# 典型输入与响应九卡

九张作者卡已通过独立领域审核与受限复核。固定模型包括脉冲响应、部分分式、正弦、自然响应、卷积、阶跃、典型输入、单位冲激及参考输入，数值和因果/初态边界正确。

两卡无条件is_a分类的教学风险已通过排除该关系解决，不伪造替代关系、不改不可变来源。原关系保留供上游语境核对，详见relation-adjudication.json。21条保留关系仍可追溯到原邻域。

已接入本地v0.48-b22，累计165/165可执行卡、图谱检查器、实际生成路径选入、受保护页面与Markdown数学渲染通过。新目标路径包含本批全部9张，公式累计4161个，未知绑定端点/无效目标0。浏览器通过卷积卡与自检，主线程已查看截图。

退役4项旧资源并删除8文件。web/worker类型检查、16项相关测试通过。先修关系与学习事实未改变，未发布服务器。最终接受字节见review-acceptance.json。

相关ESLint、OpenSpec strict及git diff --check通过。
