# 瞬态与稳态性能指标十二卡

scope/source-inventory已捕获12个当前节点。verify-models.py先于正文，模型时间与变量均归一化。主要标准模型T=4/(s²+2s+4)，零初态单位阶跃y=1-exp(-t)[cos(sqrt3t)+sin(sqrt3t)/sqrt3]。不得自行更换参数或另造数值。

1. 稳定时间：来源采用终值2%带。本例首次进入带约1.1767445，但之后离开；最后穿越约4.0381745，才是持续保持的2%调节时间。常用近似4/(zeta wn)=4并非严格值。通过极值分段求全部边界交点，并以误差包络(2/sqrt3)exp(-t)证明4.055864以后永不越带，不只靠有限采样。具体交点见报告。
2. 振荡频率：来源指临界稳定增益处。使用L=K/[s(s+1)(s+2)]单位负反馈，特征s³+3s²+2s+K。K=6时分解(s+3)(s²+2)，极点-3与±j sqrt2，角频率sqrt2，普通频率sqrt2/(2pi)。K5稳定、K7有右半平面根。临界单位阶跃含持续正弦，完整式见报告，不存在有限常数终值；不是通常稳定欠阻尼wd定义，也不是非线性极限环证明。
3. 稳态响应：G=2/(s+1)，因果输入sin2t、零初态。长期分量.4sin2t-.8cos2t，幅值2/sqrt5、相位-atan2；与总响应之差.8exp(-t)趋零。稳态可以是周期运动，不要求y(t)趋向常数。
4. 峰值时间：主要标准二阶模型首次峰时pi/sqrt3≈1.813799，超调比例exp(-pi/sqrt3)。限定无零点、欠阻尼、零初态、输入起始时刻明确。
5. 调节时间：同一模型2%时间4.0381745，5%时间2.6445466；两者都要进入后保持。说明误差带幅度/归一化尺度与采样窗口，不能把首次进入当调节时间。包络尾界与极值分段避免有限时间误判。
6. 上升时间：本节点来源是0到100%首次达到终值，主要模型为2pi/(3sqrt3)=1.2091996。另列10%-90%口径：t10=.24411465,t90=1.06290112，区间.81878647。两口径不混用；单调渐近模型0-100可能无有限时间。
7. 峰值时间T_p：区分计时原点。同一标准闭环，参考在.5时刻施加H(t-.5)，相对施加时刻峰时仍1.813799，绝对时钟峰时2.313799。是参考起始平移，不是往反馈内加入延迟后声称极点不变。
8. 瞬态响应：复用第3卡因果sin2t总响应.4sin2t-.8cos2t+.8exp(-t)，暂态为.8exp(-t)，而零输入零初态响应为0。暂态可以来自零状态输入接通，不能都归因于初态储能。
9. 临界阻尼响应：T=4/(s+2)^2，y=1-(1+2t)exp(-2t)，导数4t exp(-2t)非负；90%时刻1.9448601，10-90区间1.6789543。任意有限t仍小于1，0-100没有有限首次到达。两个指标须明确区分。
10. 稳态过程：T=1/(s+1)，因果斜坡r=t，输出t-1+exp(-t)，长期行为t-1，误差r-y=1-exp(-t)趋1。系统极点-1稳定，但因输入无界，输出也增长；不能由输出增长直接判系统不稳定。
11. 过阻尼响应：T=4/(s²+8s+4)，慢/快根ps=-4+2sqrt3,pf=-4-2sqrt3，阶跃1+[pf exp(ps t)-ps exp(pf t)]/(ps-pf)。初值0、初斜率0、终值1、t90=4.4357097。只用慢极点的单位增益一阶近似会得到初斜率-ps不为0，不能称完全等效。
12. 动态过程：主要标准二阶阶跃初值0、初斜率0、初加速度4；首次经过终值1.2091996、峰时1.813799、2%保持时间4.0381745各不同。初斜率为0不代表纯延迟，第一次经过终值也不是动态过程已结束。

每张分别围绕其指标/分解/过程展开，不只换标题。明确稳态响应、稳态值、自然/零输入和暂态的差别；所有数字使用报告的精确式或适度有效数。主线程定稿并独立领域审核。

继承node_id导出前校验与pm/qquad词法检查。ID从scope复制，source_docs真实。关联只选择适合教学且真实的邻域子集；若分类缺少语境，记录后排除本卡无条件断言，不原地修改Authority。未接受不写runtime或学习事实。
