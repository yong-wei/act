请使用 GPT Image 2 / Codex 最新图片生成能力，制作一张横版中文教学信息图。

主题：优化前后证据比较表
所属课程单元：4-5《约束下的优化设计实践：参数约束翻译与带约束参数优化》
所属分组：统一证据比较
知识类型：D

事实真源如下，禁止添加未出现的事实、公式、术语或工程案例：

一句话定义：
把起始方案、无约束候选和带约束可用解放到统一证据里比较，解释约束改变了什么。

核心直觉：
比较的对象不是一列孤立数字，而是一整套收益与代价的再分配。

可用公式（内部依据，图面公式区标题只能写“公式”）：
\mathcal{E}_{cmp}=\{\theta_0,\theta_u,\theta_c\}\times\{M_p,t_s,ITAE,ITSE,E_u,u_{\max},\phi_m\}

本课关系：
- Pareto前沿与展示解取舍 --应用于--> 优化前后证据比较表
- 求解记录单 --引出--> 优化前后证据比较表
- 优化前后证据比较表 --引出--> 剩余风险说明
- 优化前后证据比较表 --相关--> 四类方案比较矩阵
- 求解记录单 --引出--> 优化前后证据比较表: 只有把求解输入和输出交代清楚，优化前后证据比较才真正可追溯。
- 展示解与Pareto最小取舍 --应用于--> 优化前后证据比较表: 三方案比较并不是逐列找最小值，而是回到当前偏好和可行域一起解释展示解为何变化。

建议视觉骨架：
用起始方案、无约束候选和带约束可用解的三列证据墙表达收益与代价再分配；用可行域边界和指标状态标记说明约束改变了哪些判断。

视觉资产 brief：
主视觉对象：选取源材料中的具体对象或可观察现象作为画面中心。
工程场景：把概念放入控制系统读图、仪表、曲线或物理对象中。
核心图示：至少包含一个数学/工程小图，不只使用图标和文本框。
构图方式：主视觉占画面一半以上，文字作为标注而不是正文。

结构化视觉架构：
visual_archetype: 工程评审板（把多个证据窗口组织成评审台，强调证据对照、任务匹配和结论读回。）
layout_contract: 16:9 横版；采用工程评审台或证据墙布局；3-4 个证据窗口按当前节点的曲线、轨迹、交点、微扰或工程波形组织；右下角给边界判断。
text_contract: 只允许围绕这些标签组织图面文字：优化前后证据比较表、证据比较、三方案对比、可行域、工程解释、结果解释；全图最多 12 个可见标签；不放正文段落。
technical_insets_contract: 1-3 个技术小窗，每个小窗必须承担读图、机理解释或边界判断功能。
negative_constraints:
- 不要生成电子讲义截图或白底多卡片堆叠。
- 不要把长定义、长例题或整段说明塞进图面。
- 不要用无关装饰替代工程对象、技术曲线或物理隐喻。
- 不要渲染 visual_archetype、layout_contract、text_contract 等提示词字段名。

图像要求：
- 横版信息图，适合放在互动课程入口的知识点详情中；画面应像一张教学视觉资产，而不是电子知识卡片截图。
- 必须包含一个具体主视觉对象或工程场景，并包含一个数学/工程图示嵌图；不要只画卡片、图标和箭头。
- 主视觉对象、示意图、公式和短标签要共同解释机制；不要用无关装饰填充画面。
- 中文为主，不使用英文大标题；英文只允许作为小号副标题。
- 不要把“课程单元、所属分组、知识类型、事实真源、可用公式、图像要求、visual_archetype、layout_contract、text_contract”等元数据或提示词字段画进图面；公式区域如需标题，只能写“公式”。
- 不要整句复刻“一句话定义”；把定义压缩成 3-5 个短标签或短判断。
- 全图可见中文标签控制在 12 个以内；单个标签尽量不超过 10 个汉字，不放解释段落。
- 图中的关系标签统一使用中文，不要显示 contains、leads_to、cross_domain 等英文关系类型。
- 采用“核心直觉 -> 机理路径 -> 边界提醒”的三段式视觉结构。
- 只使用少量短中文标签，优先使用这些词：优化前后证据比较表、证据比较、三方案对比、可行域、工程解释、结果解释。
- 右侧用简洁小面板表达判断结果或边界，不写长段落。
- 请准确渲染这些公式本身，放在留白充足的位置；公式区标题只能写“公式”：\mathcal{E}_{cmp}=\{\theta_0,\theta_u,\theta_c\}\times\{M_p,t_s,ITAE,ITSE,E_u,u_{\max},\phi_m\}
- 不要出现教师、课堂、流程说明、软件界面、二维码、水印。
- 字体清晰，文字少而准确，避免密集段落。
- 不要使用纯蓝或纯紫单色风格；使用白底、深墨色文字、青绿/橙色强调和少量红色边界提示。
