请使用 GPT Image 2 / Codex 最新图片生成能力，制作一张横版中文教学信息图。

主题：频域综合判断与工程读回
所属课程单元：3-8《频域判别与跨域综合语言》
所属分组：三频段分工与工程案例读回、后测与模块出口收束
知识类型：D

事实真源如下，禁止添加未出现的事实、公式、术语或工程案例：

一句话定义：
在同一工程对象上，用“频域指纹 -> 判稳余量 -> 三频段任务 -> 闭环读回”的顺序比较基线方案与校正方案，判断应优先改写哪一段频率，哪些收益值得保留，哪些代价必须控制。

核心直觉：
先定关键频带，再看离临界点多远，最后把这些变化翻译成速度、超调、稳态精度和高频代价。

关键公式锚点：
\Phi(s)=\frac{L(s)}{1+L(s)}；t_s\approx\frac{4}{\zeta\omega_n}

本课关系：
- 目标驱动超前校正 --应用于--> 频域综合判断与工程读回: 
- 三频段闭环性能回读 --引出--> 频域综合判断与工程读回: 
- 频域综合判断与工程读回 --引出--> 统一对象多版本综合映射框架: 
- 三频段闭环性能回读 --引出--> 频域综合判断与工程读回: 当三频段任务分工明确后，学生才有能力把基线方案与校正方案放进同一条工程判断链里比较。
- 目标驱动超前校正 --应用于--> 频域综合判断与工程读回: 3-8 的两个工程案例都把超前校正作为中频定向改写的代表动作。

建议视觉骨架：
用“频域指纹 -> 判稳余量 -> 三频段任务 -> 闭环读回”的链条表达工程判断，不堆叠公式。

图像要求：
- 横版信息图，适合放在互动课程入口的知识点详情中。
- 中文为主，不使用英文大标题；英文只允许作为小号副标题。
- 不要把“课程单元、所属分组、知识类型、事实真源、关键公式锚点、公式锚点、图像要求”等元数据或提示词字段画进图面。
- 不要整句复刻“一句话定义”；把定义压缩成 3-5 个短标签或短判断。
- 全图可见中文标签控制在 12 个以内；单个标签尽量不超过 10 个汉字，不放解释段落。
- 图中的关系标签统一使用中文，不要显示 contains、leads_to、cross_domain 等英文关系类型。
- 采用“核心直觉 -> 机理路径 -> 边界提醒”的三段式视觉结构。
- 只使用少量短中文标签，优先使用这些词：频域综合判断与工程读回、工程读回、航向控制、稳定平台、超前校正、综合判断、频域设计入口。
- 右侧用简洁小面板表达判断结果或边界，不写长段落。
- 不要绘制密集数学公式；不要让公式变形。若需要公式，只放关键公式锚点中的短符号：\Phi(s)=\frac{L(s)}{1+L(s)}；t_s\approx\frac{4}{\zeta\omega_n}
- 不要出现教师、课堂、流程说明、软件界面、二维码、水印。
- 字体清晰，文字少而准确，避免密集段落。
- 不要使用纯蓝或纯紫单色风格；使用白底、深墨色文字、青绿/橙色强调和少量红色边界提示。
