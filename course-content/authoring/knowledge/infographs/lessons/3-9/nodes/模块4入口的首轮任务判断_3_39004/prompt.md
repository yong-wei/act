请使用 GPT Image 2 / Codex 最新图片生成能力，制作一张横版中文教学信息图。

主题：模块4入口的首轮任务判断
所属课程单元：3-9《稳定—动态—稳态综合映射实验》
所属分组：综合映射与模块4入口
知识类型：D

事实真源如下，禁止添加未出现的事实、公式、术语或工程案例：

一句话定义：
在进入模块4之前，先把“更快”“更准”“兼顾稳与快”等口头需求翻译成需要优先回看的机制线与风险边界，明确当前只能做首轮任务判断，不能越级给出完整选型与整定结论。

核心直觉：
模块 3 负责把问题问对，模块 4 才负责把参数和设计流程做出来。

关键公式锚点：
\text{口头需求} \rightarrow \text{首轮任务判断} \rightarrow \text{后续设计问题}

本课关系：
- 任务标签与机制标签匹配 --引出--> 模块4入口的首轮任务判断:
- 任务标签与机制标签匹配 --引出--> 模块4入口的首轮任务判断: 当任务标签与机制标签对齐后，学生才能带着正确问题进入模块4，而不是直接越级给参数答案。
- 相角裕度 --跨域连接--> 模块4入口的首轮任务判断: 模块4入口判断要先问动态改善是否会过早吃掉中频余量。
- 带宽 --跨域连接--> 模块4入口的首轮任务判断: 模块4入口判断不能把带宽右移简单等同于“更优”，必须连同代价一起看。

建议视觉骨架：
用“频域指纹 -> 判稳余量 -> 三频段任务 -> 闭环读回”的链条表达工程判断，不堆叠公式。

图像要求：
- 横版信息图，适合放在互动课程入口的知识点详情中。
- 中文为主，不使用英文大标题；英文只允许作为小号副标题。
- 不要把“课程单元、所属分组、知识类型、事实真源、关键公式锚点、公式锚点、图像要求”等元数据或提示词字段画进图面。
- 不要整句复刻“一句话定义”；把定义压缩成 3-5 个短标签或短判断。
- 全图可见中文标签控制在 12 个以内；单个标签尽量不超过 10 个汉字，不放解释段落。
- 图中的关系标签统一使用中文，不要显示 contains、leads_to、cross_domain 等英文关系类型。
- 采用“核心直觉 -> 机理路径 -> 边界提醒”的三段式视觉结构。
- 只使用少量短中文标签，优先使用这些词：模块4入口的首轮任务判断、模块4入口、任务判断、约束表达、设计起点。
- 右侧用简洁小面板表达判断结果或边界，不写长段落。
- 不要绘制密集数学公式；不要让公式变形。若需要公式，只放关键公式锚点中的短符号：\text{口头需求} \rightarrow \text{首轮任务判断} \rightarrow \text{后续设计问题}
- 不要出现教师、课堂、流程说明、软件界面、二维码、水印。
- 字体清晰，文字少而准确，避免密集段落。
- 不要使用纯蓝或纯紫单色风格；使用白底、深墨色文字、青绿/橙色强调和少量红色边界提示。
