请使用 GPT Image 2 / Codex 最新图片生成能力，制作一张横版中文教学信息图。

主题：任务标签与机制标签匹配
所属课程单元：3-9《稳定—动态—稳态综合映射实验》
所属分组：综合映射与模块4入口
知识类型：X

事实真源如下，禁止添加未出现的事实、公式、术语或工程案例：

一句话定义：
把“更快、更准、更稳”等口头需求先翻译成动态改善、稳态改善或综合折中的任务标签，再对应回零点线、积分线、滞后线等机制标签，避免直接从口头目标跳到控制器名称或参数答案。

核心直觉：
任务标签回答“这次优先解决什么”，机制标签回答“准备动哪条结构机理线”。

关键公式锚点：
\text{任务标签} \neq \text{控制器名称}；\text{目标表达} \rightarrow \text{任务标签} \rightarrow \text{机制线选择}

本课关系：
- 统一对象多版本综合映射框架 --引出--> 任务标签与机制标签匹配:
- 积分与滞后的稳态改善分工 --supports--> 任务标签与机制标签匹配:
- 三频段闭环性能回读 --跨域连接--> 任务标签与机制标签匹配:
- 左半平面零点的动态改善机理 --跨域连接--> 任务标签与机制标签匹配:
- 任务标签与机制标签匹配 --引出--> 模块4入口的首轮任务判断:
- 统一对象多版本综合映射框架 --引出--> 任务标签与机制标签匹配: 只有先把跨域比较压成统一框架，学生才能稳定地先贴任务标签，再回到机制线。

建议视觉骨架：
用“频域指纹 -> 判稳余量 -> 三频段任务 -> 闭环读回”的链条表达工程判断，不堆叠公式。

图像要求：
- 横版信息图，适合放在互动课程入口的知识点详情中。
- 中文为主，不使用英文大标题；英文只允许作为小号副标题。
- 不要把“课程单元、所属分组、知识类型、事实真源、关键公式锚点、公式锚点、图像要求”等元数据或提示词字段画进图面。
- 不要整句复刻“一句话定义”；把定义压缩成 3-5 个短标签或短判断。
- 全图可见中文标签控制在 12 个以内；单个标签尽量不超过 10 个汉字，不放解释段落。
- 图中的关系标签统一使用中文，不要显示 contains、leads_to、cross_domain 等英文关系类型。
- 采用“核心直觉 -> 机理路径 -> 边界提醒”的三段式视觉结构。
- 只使用少量短中文标签，优先使用这些词：任务标签与机制标签匹配、任务标签、机制标签、动态改善、稳态改善、综合折中。
- 右侧用简洁小面板表达判断结果或边界，不写长段落。
- 不要绘制密集数学公式；不要让公式变形。若需要公式，只放关键公式锚点中的短符号：\text{任务标签} \neq \text{控制器名称}；\text{目标表达} \rightarrow \text{任务标签} \rightarrow \text{机制线选择}
- 不要出现教师、课堂、流程说明、软件界面、二维码、水印。
- 字体清晰，文字少而准确，避免密集段落。
- 不要使用纯蓝或纯紫单色风格；使用白底、深墨色文字、青绿/橙色强调和少量红色边界提示。
