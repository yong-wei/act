请使用 GPT Image 2 / Codex 最新图片生成能力，制作一张横版中文教学信息图。

主题：时域指标到目标极点区域
所属课程单元：3-6《零点作用与动态改善实验——从性能目标到校正设计》
所属分组：从 3-5 机理切到目标翻译
知识类型：C

事实真源如下，禁止添加未出现的事实、公式、术语或工程案例：

一句话定义：
把超调量和调节时间这类时域指标，压成阻尼比、衰减速度和主导极点可行域，后续 `PD` 与测速反馈设计都从这里起步。

核心直觉：
先把“更快、更稳”翻译成复平面上的区域约束，再讨论结构和参数。

可用公式（内部依据，图面公式区标题只能写“公式”）：
M_p=e^{-\frac{\zeta\pi}{\sqrt{1-\zeta^2}}}\times 100\%, \qquad t_s \approx \frac{4}{\zeta\omega_n}

本课关系：
- 时域指标到目标极点区域 --引出--> 目标驱动测速反馈校正
- 时域指标到目标极点区域 --引出--> 目标驱动PD校正
- 主导极点 --引出--> 时域指标到目标极点区域
- 主导极点 --引出--> 时域指标到目标极点区域: 3-6 先把时域指标翻译成目标主导极点区域，再进入具体校正设计。
- 时域指标到目标极点区域 --引出--> 目标驱动PD校正: PD 设计必须先建立目标区域，再通过零点位置和增益让根轨迹进入该区域。
- 时域指标到目标极点区域 --引出--> 目标驱动测速反馈校正: 测速反馈虽然不显式增零点，但同样要围绕时域指标建立设计目标。

建议视觉骨架：
用概念核心、证据位置和判断边界三块表达该知识点，不添加源材料之外的对象。

视觉资产 brief：
主视觉对象：选取源材料中的具体对象或可观察现象作为画面中心。
工程场景：把概念放入控制系统读图、仪表、曲线或物理对象中。
核心图示：至少包含一个数学/工程小图，不只使用图标和文本框。
构图方式：主视觉占画面一半以上，文字作为标注而不是正文。

结构化视觉架构：
visual_archetype: s 平面机理板（以 s 平面主图为核心，辅以响应曲线或阻尼区域小窗解释动态机理。）
layout_contract: 16:9 横版；左侧 55%-65% 为 s 平面或根轨迹主图；右侧上方为响应曲线小窗；右侧下方为判断边界或公式；极点/零点标签贴近图形。
text_contract: 只允许围绕这些标签组织图面文字：时域指标到目标极点区域、超调量、调节时间、阻尼比、目标极点区域、时域设计、目标翻译；全图最多 12 个可见标签；不放正文段落。
technical_insets_contract: 1-3 个技术小窗，每个小窗必须承担读图、机理解释或边界判断功能。
negative_constraints:
- 不要生成电子讲义截图或白底多卡片堆叠。
- 不要把长定义、长例题或整段说明塞进图面。
- 不要用无关装饰替代工程对象、技术曲线或物理隐喻。
- 不要渲染 visual_archetype、layout_contract、text_contract 等提示词字段名。

图像要求：
- 横版信息图，适合放在互动课程入口的知识点详情中；画面应像一张教学视觉资产，而不是电子知识卡片截图。
- 必须包含一个具体主视觉对象或工程场景，并包含一个数学/工程图示嵌图；不要只画卡片、图标和箭头。
- 主视觉对象、示意图、公式和短标签要共同解释机制；不要用无关装饰填充画面。
- 中文为主，不使用英文大标题；英文只允许作为小号副标题。
- 不要把“课程单元、所属分组、知识类型、事实真源、可用公式、图像要求、visual_archetype、layout_contract、text_contract”等元数据或提示词字段画进图面；公式区域如需标题，只能写“公式”。
- 不要整句复刻“一句话定义”；把定义压缩成 3-5 个短标签或短判断。
- 全图可见中文标签控制在 12 个以内；单个标签尽量不超过 10 个汉字，不放解释段落。
- 图中的关系标签统一使用中文，不要显示 contains、leads_to、cross_domain 等英文关系类型。
- 采用“核心直觉 -> 机理路径 -> 边界提醒”的三段式视觉结构。
- 只使用少量短中文标签，优先使用这些词：时域指标到目标极点区域、超调量、调节时间、阻尼比、目标极点区域、时域设计、目标翻译。
- 右侧用简洁小面板表达判断结果或边界，不写长段落。
- 请准确渲染这些公式本身，放在留白充足的位置；公式区标题只能写“公式”：M_p=e^{-\frac{\zeta\pi}{\sqrt{1-\zeta^2}}}\times 100\%, \qquad t_s \approx \frac{4}{\zeta\omega_n}
- 不要出现教师、课堂、流程说明、软件界面、二维码、水印。
- 字体清晰，文字少而准确，避免密集段落。
- 不要使用纯蓝或纯紫单色风格；使用白底、深墨色文字、青绿/橙色强调和少量红色边界提示。
