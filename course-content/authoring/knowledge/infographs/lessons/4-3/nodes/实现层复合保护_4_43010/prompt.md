请使用 GPT Image 2 / Codex 最新图片生成能力，重新制作一张横版中文教学信息图。

主题：实现层复合保护

核心事实：
实现层复合保护把微分滤波、输出微分、给定滤波、幅值限幅、斜率限制和抗积分饱和纳入首轮方案，使控制律能在执行器边界下恢复。线性模型里可行的控制律，可能在舵角贴边、舵速受限和积分累积时完全变样。

唯一允许显示的公式：
u_{\mathrm{act}}=\operatorname{rateLimit}\left(\operatorname{sat}(u_{\mathrm{raw}})\right)

视觉资产 brief：
主视觉对象：u_raw 从控制器输出后依次经过“限幅”“斜率限制”“执行器”三个模块，输出 u_act；从 u_act 与 u_raw 的差值引出一条回算箭头，回到“抗积分饱和”模块。抗积分饱和模块只允许写短标签“T_aw 回算”，不能写任何传递函数、分式或新增公式。
工程场景：客船舵角执行器；右侧三联小窗分别为“舵角幅值边界”“舵速边界”“饱和恢复趋势”。曲线可以用虚线/实线表达原始 u_raw 与实际 u_act 的差异。
构图方式：左侧控制器，中间保护层流程，右侧三联证据小窗，底部放唯一公式。

图面允许短标签：实现层复合保护、限幅、斜率限制、执行器保护、抗积分饱和、T_aw 回算、u_raw、u_act、舵角边界、舵速边界、饱和恢复。

硬性负面约束：
- 不要添加任何公式，除了唯一允许显示的公式 u_act=rateLimit(sat(u_raw))。
- 抗积分饱和模块禁止出现分式、传递函数、1/(T_aw s+1)、u/(T_aw s+1)、积分器公式或其他数学表达。
- 不要出现“课程单元、所属分组、知识类型、事实真源、visual_archetype、layout_contract”等提示词元数据。
- 不要出现教师、课堂、二维码、水印。
- 不要写长段落；全图中文标签控制在 12 个以内。
- 使用白底、深墨文字、青绿/橙色强调和少量红色边界提示，不要纯蓝或纯紫单色风格。
