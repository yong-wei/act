# Aggregate CourseCoverage active path

Production governance loads only:

`automatic-control.json`

That file must be a **reviewed** ledger binding:

- exact Projection/`knowledge_object` membership
- one accepted ReleaseSet Delta Receipt id
- inspectable ACT course-source evidence per object
- a truthful review identity

Heuristic generator output lives under `../candidates/` and is **not** authoritative.
Do not place unreviewed candidate JSON here.
