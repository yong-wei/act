# ACT legacy local-graph → Canonical one-to-one crosswalk (#1268).
# New authoring must not write legacyId values as Canonical endpoints.
# Rows are empty by default; fixtures and author decisions populate bindings.
