# 负责人抽样验收材料（任务 11.7，2026-08-25）

本文档从六个子类型与关系层各抽取真实样本，供课程负责人抽验。每个抽样面给出：样本内容、完整输入位置、需要你决策的问题。**请直接在文末"决策记录表"批注**（每项写 A/B/C 或修正意见），批注后我按决策执行修正并重跑受影响的验证。

---

## 抽样一：讲义原子切分（handout，5394 原子）

**样本**（2-1 单元前两段，BOUND 判定）：

| atomId | 标题路径 | 判定 |
|---|---|---|
| atom-a1c27f62d12e00797a5c60c5 | 单元 2-1 \| 建模与变换语言——从真实对象到统一分析对象 | BOUND |
| atom-5e9094110c2b6e3811005994 | 同上 | BOUND |

**NON_TEACHING 判定样本**（28 条，标题模式排除）：

| 资源 | 标题 | 依据 |
|---|---|---|
| handout-4-3 | 附录 B：参考文献 | 标题模式（参考文献） |
| handout-5-4 | 附录 A 参考文献 | 标题模式（参考文献） |
| handout-5-5 | 5.1 分层练习 | 标题模式（练习类标题） |

**输入位置**：
- 原子：`course-content/authoring/knowledge/formal-resource-remediation/resource-layer/text/text-atoms.json`（11056 行数组）
- 讲义真源：`course-content/authoring/lessons/<unit>/design/<unit>-handout.md`

**决策 1**：NON_TEACHING 把"分层练习"章节排除出教学原子是否可接受？

- A. 接受（练习节不是语义教学内容）

- B. 分层练习应作为教学原子保留（改为 BOUND）

- C. 其他意见

  回复：接受排除出教学原子。但是分层练习可以纳入习题绑定。

---

## 抽样二：卡片 exact-name 绑定（card，434/5662 原子绑定）

**样本**：

| 卡片 canonicalKey | 概念名 | 映射结果 |
|---|---|---|
| 传递函数_10_6c2d17e5 | 传递函数 | ✅ ctc:modeling-865eb1c8…（system-modeling 域"传递函数"） |
| 时域指标到极点参数映射_3_13003 | 时域指标到极点参数映射 | ❌ 未命中（31 概念映射表无此概念） |
| 结构参数联合搜索解码_4_47005 | 结构参数联合搜索解码 | ❌ 未命中 |

**输入位置**：
- 映射表（仅 31 概念）：`course-content/authoring/knowledge/formal-resource-remediation/20260823-asr-batch/course-to-authority-map.json`
- 投影绑定行：`…/teaching-projection/projection.json`（byModality.card=434）

**背景事实**：838 个唯一卡片键中仅 51 键（434 原子）命中映射；映射表只覆盖 31 个课程概念名。未命中卡片保持无绑定（如实体现在投影 limitations）。

**决策 2**：卡片绑定覆盖率现状（7.7% 原子）是否可接受为本轮验收基线？
- A. 接受（扩大映射是后续语义工作）

- B. 本轮需先扩充映射表再验收

- C. 其他

  回复：卡片本身作为资源是根据图谱节点制作的，为什么会和节点无法绑定？这个需要进一步查证。如果是说卡片中对应的实体节点无法完全绑定讲义的，接受现在的比例。

---

## 抽样三：ASR 转写与音频段绑定（audio，586/741 绑定行入投影）

**保留样本**（1-1 口语段，术语重叠 top-3）：

| 段 | 转写摘录 | 绑定 |
|---|---|---|
| 1-1 seg27 | "…所谓的根轨迹就是说如果我们改了系统的某个参数…" | root-locus 域"根轨迹"×2 个同名节点 + discrete-time 域"根轨迹"（后者被涵盖域过滤） |
| 1-1 seg30 | "…画出一条曲线告诉你面对低频的涌浪这艘船是会跟着一起剧烈摇摆还是能巧妙地稳住…" | 同上三节点 |

**被过滤样本**（口语提及课程外概念，模态事实保留在资源层、不入投影）：

| 段 | 转写摘录 | 被过滤目标 |
|---|---|---|
| 1-2 seg26 | "拉普拉斯变换能把微积分变成简单的代数运算…" | discrete-time-control-analysis 域"拉普拉斯变换" |
| 1-2 seg30 | "这是个角这是个角…"（ASR 复读段，质量异常） | 同上 |

**输入位置**：
- 段与绑定：`…/20260823-asr-batch/audio-semantic-segments/<unit>.json`（segments[].nodeBindings）
- 转写全文：`…/20260823-asr-batch/transcripts/<unit>.txt`

**两个子问题**：

**决策 3a**：口语提及课程外概念（如"拉普拉斯变换"节点仅在 discrete-time 域）→ 绑定被过滤。但"拉普拉斯变换"是建模基础概念，crosswalk 中无涵盖域版本——这是图谱概念覆盖缺口而非课程外内容。处理方式：
- A. 接受现状（缺口如实记录，后续图谱/映射批次处理）

- B. 此类基础概念应映射到最近涵盖域概念（给出清单我来修）

- C. 其他

  回复：接受现状。在同一个图谱文档中记录缺口以便跟踪，同时要在同一文档中跟踪更新已经被修复的问题。

**决策 3b**：ASR 复读段（seg30"这是个角"×15）等质量异常段当前仍参与绑定。处理：
- A. 接受（转写质量已过 holdout 验收，个别异常段无害）

- B. 增加复读/重复检测，异常段不参与绑定

- C. 其他

  回复：接受

---

## 抽样四：习题语义映射（exercise，26/33 保留 + 7 过滤）

**保留样本**（Codex 保守审核，gpt-5.6-sol）：

| 题目 | 题干摘录 | 映射目标 | 依据 |
|---|---|---|---|
| 1-2/step-08/sfg-quiz-1 | （信号流图题） | system-modeling 域"信号流图" | 题目核心是信号流图与方框图表征差异 |
| 5-4/step-03/first-order-parameter | 一阶对象参数变化对阶跃响应的影响 | robustness-sensitivity 域"参数变化" | 直接考查参数变化影响 |
| 1-4/step-03/feedback-denominator | 单位负反馈闭环传递函数分母结构 | root-locus 域"闭环传递函数" | 题干直接给出分母结构 |

**被过滤样本**（映射目标在 D1 排除域）：

| 题目 | 题干 | Codex 映射 | 问题 |
|---|---|---|---|
| 1-3/step-04/gain-role | "闭环中增益 K 为什么会改变系统极点？" | discrete-time 域"闭环特征方程" | **state-space 域存在精确同名节点** `ctkg:v3e-object-3a244e26b…`（闭环特征方程），Codex 未选中 |
| 1-5/step-03/feedback-characteristic | 单位负反馈的闭环特征方程 | discrete-time 域"闭环特征方程" | 同上 |

**输入位置**：
- 审核全量：`/tmp/remediation-run/review/exercise-mapping-review.jsonl`（455 行；已复制决策依据到仓库内 `…/resource-layer/exercises/exercise-node-bindings.json`）
- 题面真源：`course-content/runtime/lessons/<unit>/interactive-manifest.json` → steps.*.interaction_spec.activity_cards

**决策 4**：7 条被过滤映射中，"闭环特征方程"类（约 5 条）在涵盖域有同名/近名节点可修正：
- A. 修正：改指涵盖域同名节点（如 state-space 域"闭环特征方程"），恢复为有效绑定
- B. 保持过滤（宁缺毋滥，等后续语义批次）
- C. 其他

另：422 题 null（开放/操作类题）是否接受？ A. 接受 / B. 需补审

回复：A。开放/操作类题需要补审

---

## 抽样五：视频 cue 原子与绑定（intro-video，539/727 绑定行入投影）

**cue 原子样本**（1-1，mp4 轴锚点已核验：narration + 片头 10.0s）：

| cue | 文本 | mp4 时间锚 |
|---|---|---|
| 0 | 大家好，欢迎来到闲聊自控。 | 10.0–12.4s |
| 1 | 今天我们先看一个问题：一艘船想保持航向，为什么还会慢慢偏离？ | 12.4–18.2s |

**绑定样本**（含一个可疑绑定）：

| cue | 文本 | 绑定 | 评估 |
|---|---|---|---|
| 1-1 cue9 | "模型描述输入到输出的关系。" | "M_pω 与二阶系统阻尼比 ζ 的关系"（score 2）+ "高阶系统频域指标…"（score 2） | ⚠️ 术语重叠误绑：cue 讲建模概念，绑定到频域指标节点 |

**输入位置**：
- 原子：`…/resource-layer/intro-videos/intro-video-atoms.json`（1880 条）
- 绑定：`…/resource-layer/intro-videos/intro-video-node-bindings.json`（727 行全量）
- 视频真源：`course-content/runtime/lessons/<unit>/media/<unit>-intro-video.mp4`

**决策 5**：cue9 类术语重叠误绑（短 cue 的上下文不足）处理：
- A. 接受（top-3 模糊绑定，消费方可按 score/use 加权）

- B. 提高视频 cue 绑定阈值（如 score≥4 或仅 label 全词命中），降低误绑率（绑定数会下降）

- C. 其他

  回复：接受

---

## 抽样六：三族关系边（420 条）

**root-locus 域样本**（44 条边）：

| 源概念 | 关系族 | 目标 |
|---|---|---|
| 广义根轨迹 | containment | 根轨迹 |
| 等效开环传递函数 | containment | 参数根轨迹（"以非开环增益为可变参数绘制的根轨迹…"） |
| 参数根轨迹 | prerequisite | 等效开环传递函数 |
| A point on the root locus | containment | 根轨迹 |

**输入位置**：
- 域账本：`…/<domain>-closure/final-ledger.json`（15 个域，disposition=PUBLISHED_EDGE 的行）
- 边证据与决策：`…/<domain>-closure/decisions.jsonl`（Codex 域聚类审核+负责人原则决策）

**决策 6**：边语义方向（如"广义根轨迹 ⊂ 根轨迹"、"参数根轨迹 前置 等效开环传递函数"）是否接受？
- A. 接受

- B. 抽到具体异议边（列出即改）

  回复：接受

---

## 附：本轮治理原则确认

**决策 7**：投影绑定层统一涵盖域过滤原则（音频 155/视频 188/习题 7 条被过滤，完整绑定保留资源层）——接受 / 调整。

回复：接受

---

## 决策记录表（请批注）

| # | 决策项 | 决定 | 执行结果（2026-08-25） |
|---|---|---|---|
| 1 | NON_TEACHING 排除"分层练习"章节 | 接受；分层练习纳入习题绑定 | ✅ 新增 handout-exercise 通道：13 单元 46 题，其中 10 题获映射 |
| 2 | 卡片绑定覆盖率 | 要求查证 | ✅ 查证为映射构建缺陷（非概念缺失）：crosswalk 精确匹配 205 键可命中，card-name-index 替换后卡片绑定 434→1383 原子 |
| 3a | 概念缺口接受现状 | 接受+建档跟踪 | ✅ graph-issues.md G8（38 键仅排除域）/G9（595 键名差异）+修复跟踪表 |
| 3b | ASR 复读段 | 接受 | 无变更 |
| 4 | 闭环特征方程修正 | A 修正 | ✅ 2 条修正（stability 域同名节点）；5 条无涵盖域同名保持过滤（超调量/相角裕度等仅存在于 D1 排除域的带符号变体名） |
| 4b | 422 题 null | 补审 | ✅ Codex 二轮放宽口径：召回 29 卡 + 10 分层练习；习题映射 26→67 |
| 5 | 视频 cue 误绑 | 接受 | 无变更 |
| 6 | 三族边语义方向 | 接受 | 无变更 |
| 7 | 涵盖域过滤原则 | 接受 | 无变更 |

**执行后总态**：投影绑定 1585→2575（卡片 1383/音频 586/视频 539/习题 67）；信封 1058 资源/13522 原子（七子类型）；交接 handoff-9f3d13c0。
