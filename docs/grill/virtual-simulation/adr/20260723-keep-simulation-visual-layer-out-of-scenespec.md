# 仿真视觉层保持 SceneSpec 协议之外

本次场景视觉改造不改变模型身份、扰动策略、评估策略与遥测中的任何一项。环境预设、画质档位、相机与音景同相机角度一样属于呈现层偏好，不纳入 SceneSpec v1 / SimulationTrace v1；证据与回放语义不变。曾考虑借 assets 字段把视觉状态纳入协议以便完整重现启动时刻的视觉，被拒绝：那会把证据语义耦合到视觉 Chrome，扩大协议面并迫使回放校验处理与教学证据无关的状态。改造的 spec delta 将显式声明这条边界，防止后续审查把视觉层误读为协议变更。
