# 控灵错误在共享传输边界统一归一化

所有使用共享控灵聊天传输的学生端入口，在错误进入 React 展示层前统一解析为受限失败类别、学生安全文案和匹配的恢复动作；组件不得直接渲染 AI SDK 的 `Error.message`。AI SDK 会把非 2xx 响应正文原样变成异常信息，网络失败又产生英文浏览器错误，仅在各页面替换文案容易遗漏并造成恢复行为不一致。服务端继续记录脱敏诊断详情，公开响应可以保留稳定机器码，但学生界面只消费允许的产品投影；401、404、400/409 与 429/503/网络错误分别进入身份恢复、会话或任务修复、稍后重试等不同路径。
