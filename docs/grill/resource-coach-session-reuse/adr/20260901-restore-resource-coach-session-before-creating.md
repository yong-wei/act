# 教材资源辅导先恢复匹配会话再创建

教材资源辅导入口当前在会话库尚未加载、`activeAssistantBinding` 仍为空时立即调用 `createConversation()`；资源绑定只写入 `sessionStorage`，初始服务端会话又不包含可恢复 binding。页面每次刷新或重开都会把“尚未加载”误判为“没有匹配会话”，创建新的空会话并中断原连续问答。资源辅导应先完成当前用户会话库加载并按服务端持久化的完整资源身份查找匹配会话；没有匹配历史时保持未落库的待提问空白态，在首轮发送中建立并持久化绑定。这样既保留精确版本隔离，也避免会话导航持续积累孤立空记录。
