# 课件发布门禁只使用确定性检查

互动课件发布阻断只由可重复的结构校验、数据规则和固定视口无头浏览器测量产生，覆盖 BOPPPS 完整性、时间一致性、模块白名单、活动合同、学生信息隔离、来源与版本确认、AI 标识及 16:9 版式溢出。静态规则由纯脚本执行，字体换行、公式宽度和实际内容溢出由固定字体与视口的浏览器执行；两层结果形成与课件内容哈希绑定的门禁凭证，内容变化后必须重新校验。LLM 可以生成按需 AI 审核报告，但其判断具有不确定性，不能作为发布门禁，也不能替代可脚本化的失败证据。
