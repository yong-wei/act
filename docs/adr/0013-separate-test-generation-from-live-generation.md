# 将测试生成与真实模型生成分离

智能备课的自动化测试使用确定性本地测试 Provider，以稳定覆盖分阶段返回、失败续传、结构校验和发布门禁；赛题现场及生产生成则必须调用管理员配置的真实 LLM Provider。真实 Provider 失败时不得静默切换为固定输出。演示账号可以保留一份此前由真实 Provider 生成、审核并发布的备用版本，但必须明确标注为既有成果，不能计作现场生成验收。该分离使程序正确性可重复验证，同时保留对真实模型集成能力的真实性要求。
