# 生成任务采用可续传且幂等的后台执行

教案和互动课件生成作为后台分阶段任务执行，同一草稿同一时刻只允许一个活动任务。教师离开页面不会中断任务；取消或失败只停止未开始阶段并保留已完成结果，继续操作从最后完成阶段恢复。开始、继续和取消接口必须幂等，重复操作不能创建重复任务、覆盖结果或重复计费。不同单次课任务可以并行提交，由队列和 Provider 限流调度。该语义兼顾首轮响应速度、故障恢复和内容版本一致性。
