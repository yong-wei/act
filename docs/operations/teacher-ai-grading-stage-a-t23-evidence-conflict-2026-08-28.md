# 阶段 A T2-3 证据矛盾记录

日期：2026-08-28

## 结论

- 本记录覆盖阶段 A 中一份匿名 T2-3 提交，不扩展到其他 11 个逻辑项。
- 当前已发布结果为 `10/20`，包含 3 条中文扣分批注；成绩、AI 原始结果、审批快照和历史 PDF 均保持不可变。
- 该结果暂不能作为通过证据：批注与冻结作答图像存在直接矛盾，G.8 保持 `FAIL`，阶段 B 继续冻结。

## 证据

- 学生 PDF 第 2 页实际包含两幅手绘 Bode 图，并可见幅频、相频趋势及相关标注。
- 同一提交的 OCR/页面图像视觉证据块也识别出两幅 Bode 图。
- 但早先的视觉摘要块错误写成“该部分无任何图示或手写内容”，当前 3 条扣分批注和总体评价沿用了这一错误摘要。
- 因此根因是视觉证据摘要之间的不一致，不是 PDF 右侧栏锚点错位。

## 当前已验证

- 批注只保留在低于满分的评分项；满分项没有批注。
- 学生可见批注为中文，总体评价不含复核校验值、校验和、定位精度或 Provider 字段。
- 本地 T2-3 预览已显示右侧批注栏、与页面内容大致平行的布局、虚线连接和无内部字段的总评页。
- 该预览不是新的正式发布物；当前正式发布引用仍需在对象存储凭据恢复后重新物化并验证。

## 处置

1. 等待教师对该匿名 T2-3 提交的实际得分、扣分依据和批注文字作出明确裁定。
2. 若教师确认需要修订，只对这一份 `(匿名提交, T2-3)` 追加不可变复核版本，不重做已成功的其他项目，不覆盖历史版本。
3. 修订后仅定向生成该 PDF，重新执行教师确认、逐份发布、学生读取和该文件的下载/逐页核验。
4. 只有定向结果与作答证据一致、对象存储读取恢复且验证通过后，才重新评估 G.8；此前不得启动阶段 B。

## 失败经验

- 视觉摘要属于可失败的中间证据，不能在存在同页图像证据冲突时直接进入批准和发布链。
- 后续重试必须先比较同一页面的 OCR/文本块与视觉摘要；发现“有图/无图”冲突时应阻断并转教师复核，而不是修改分数或覆盖旧结果。

## 2026-08-28 定向重评分复核

- 针对同一匿名 `(submission, T2-3)` 仅创建新的不可变评分运行：`grading-batch:a72669bb-e3b8-4c08-90d0-165ca7bf1f2b` / `grading-run:ca9315e1b265986100c0b38657678e8d`。
- 新结果为 `18/20`，评分项得分为 `4、2、3、3、3、3`；仅保留两条中文扣分批注：转折频率应为 `5 rad/s`，以及应明确标注高频渐近斜率 `-40 dB/dec`。
- 新运行状态为 `AWAITING_REVIEW`，未创建教师审批快照，未改变旧的 `10/20` 正式发布结果；旧版本继续保留不可变历史。
- 已生成仅供教师查看的本地 PDF 预览 `output/pdf/T2-3-rerun-preview.pdf`：2 页作答页加 1 页总评页，仅有两处扣分批注，批注为中文，右侧栏使用虚线连接，总评不含内部复核字段。
- 预览已逐页渲染核验；本地预览不是正式学生发布物。下一步必须先由教师裁定新分数与两条扣分依据，再仅对该逻辑项追加审批、定向发布和下载复核；在此之前 G.8 保持 `FAIL`，阶段 B 不得启动。
