# 阶段 A G.8 独立审核证据基线

日期：2026-08-28

## 审核范围与边界

- 审核对象为本地专用阶段 A 作业的 3 份提交、4 题/提交，共 12 个逻辑项；不读取隐藏验收集，不启动阶段 B。
- 审核基线提交为 `cd218681d47839e4e92d7267ae30113dafc6bdbb`，当前工作区存在跨阶段的既有未提交改动；独立审核仅审阅阶段 A 的直接实现、测试、运行记录和发布证据。
- 所有学生可见批注、扣分依据、总体评价和 PDF 固定标签均为中文；量规 Provider 固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`。历史英文结果保留为不可变历史记录，未被覆盖或重新发布。

## 已冻结的阶段 A 证据

- 当前中文不可变结果、教师确认、治理证据和 PDF 派生均为 `12/12`；整份结果确认、逐份发布和学生读取均为 `3/3`。
- 学生发布验证器直接读取 `feedbackRelease.derivative`，而非以“最新 READY 派生物”替代真实学生下载引用；历史实测确认 3 份已发布结果、12 份 PDF、41 页、所有权与校验和一致。
- `9-chinese-font-sidebar-fallback` 派生版本已成为当前发布引用。12 个审批快照共有 19 条有效批注；7 份有扣分批注的 PDF 使用右侧栏，5 份无扣分批注的 PDF 保持原始 A4。
- `scripts/ops/inspect-stage-a-annotation-anchors.ts` 于本次冻结时重新执行成功：12 个当前审批、19 条批注；所有批注均有冻结页码并与答案证据块关联，当前发布引用与最新 READY 派生物一致。
- 阶段 A 定向回归记录为 18 个测试文件、`364/364` 通过，见 `.runtime/stage-a-regression-v9.log`；类型检查记录通过，见 `.runtime/stage-a-typecheck-v9.log`；`npx --yes openspec validate add-teacher-ai-grading-lab --strict` 于本次冻结通过。
- 本次冻结后重新执行全部当前阶段 A 受影响测试文件，得到 19 个测试文件、`378/378` 通过；`npm run typecheck` 和阶段 A 直接影响文件的 `git diff --check` 均通过。
- 阶段 A 直接影响文件的 `git diff --check` 于本次冻结通过。全局检查的既有无关尾随空白位于 `openspec/changes/add-teacher-ai-grading-lab/HANDOFF.md`，不纳入本次审核范围。

## 本次重验失败与处理经验

- 本次重新执行 `verify-stage-a-student-release-and-pdfs.ts` 时，先完成只读的锚点聚合检查；随后在对象存储读取第一份当前发布 PDF 时收到 `InvalidAccessKeyId`。失败发生在下载前，未修改数据库、评分、审批、发布或派生物。
- 该失败表明当前本机对象存储访问键已失效，不能将历史下载成功记录表述为本次重验成功。恢复有效的受限读取凭据后，应仅重跑同一只读验证器，不应创建批次、重跑评分或重派生 PDF。
- 对象存储地址和桶仍可响应；本机没有可用的容器运行时，且其它工作区未保存可恢复的对象存储凭据。不得猜测、伪造或替换凭据。

## G.8 待裁定事项

- 需要获授权的独立只读审核者审查阶段 A 实现、门禁测试、证据脚本、PDF 视觉材料与本文件。
- 审核 finding 必须由主执行线程裁定为 `ACCEPT`、`REJECT` 或 `DEFER`；审核者不编辑代码。
- 在独立审核结论为 `PASS` 前，`G.8` 保持 `FAIL`，阶段 B 与隐藏验收继续冻结。

## 独立审核结论与 finding 裁定

- 独立只读审核于 2026-08-28 完成，结论为 `PASS`；未发现 Provider 漂移、中文约束绕过、教师确认或发布越权、学生跨账户读取、批注锚点缺失、版本不可变性破坏或阶段 B 门禁遗漏。
- 唯一 finding：对象存储下载重验缺口，严重性为中等。证据为重跑验证器时收到 `InvalidAccessKeyId`；该错误发生在读取第一份 PDF 前，且已有成功运行日志证明 12/12 PDF、41 页、校验和与可重开性。主执行线程裁定为 `DEFER`，不阻断 G.8。
- G.8 现已裁定为 `PASS`；若恢复有效的受限对象存储读取凭据，后续只原样重跑 `scripts/ops/verify-stage-a-student-release-and-pdfs.ts`，不得重跑评分、创建批次、覆盖历史结果或重新派生 PDF。

## 2026-08-29 只读重验续记

- 已按既有命令原样发起只读验证器；在读取首个发布 PDF 前，对象存储端点 `127.0.0.1:9000` 返回 `ECONNREFUSED`。验证器未创建评分、批次、审批或派生版本。
- 此次环境失败不改变历史 12/12 PDF、41 页、校验和和学生读取的冻结证据，也不重开 G.8 的已裁定结论；对象存储读取重验的 `DEFER` 继续保留。恢复可用端点和受限读取凭据后，只能再次运行同一只读验证器。
