# 阶段 B 调优集结果（2026-08-29）

## 范围与冻结身份

- 数据集：`t2-formal-first-round-20260813` / `v1-half-point-20-20-20-40`
- 固定划分：28 个调优样本、12 个隐藏样本
- 配置：`experiment-config:9c35c1c283f399d3eae790a049f4524e`
- 批次：`experiment-batch:79a30bc5449b8fee6ac4b6f073491cc6`
- Provider：`siliconflow` / `Qwen/Qwen3.5-35B-A3B`
- 隐藏集状态：`SEALED`，未读取、未运行、未揭示

## 执行终态

- 预期 executions：336
- 成功：319
- 失败：17
- 最终成功率：94.94%
- Provider 调用：401
- 重试：65
- 首轮成功：292/336（86.90%）
- 重试后成功：27/336；最终成功：319/336（94.94%）
- 第 2 次调用：44；第 3 次调用：21；自动重试上限为 2 次
- 重试调用输入 / 输出 token：678,863 / 121,726；Provider 未提供费用遥测，重试费用明确记录为不可用
- 半分制合规：319/319
- 题目满分上限合规：319/319

失败项均保留在分母中。错误分布如下：

- `deduction-annotation-missing`：7
- `anchor-excerpt-mismatch`：5
- `anchor-bbox-outside-parent`：2
- `provider-no-output-generated`：2
- `schema-assessments.3.anchors-array-must-contain-at-least-1-element-s`：1

## AI—人工评分指标

- MAE：5.4013
- 平均偏差：-3.2445
- 完全一致率：36.36%
- 题目满分 10% 容差内一致率：51.72%
- 三次完全稳定率：65.69%
- 完整三次运行题组：102/112

逐题 MAE：

- T2-1：5.2500，平均偏差 -5.2500
- T2-2：4.1341，平均偏差 -3.5976
- T2-3：7.9605，平均偏差 -7.9605
- O2：4.3896，平均偏差 +3.9740

## 门槛裁定

调优集结论为 `FAIL`：

- 完全一致率低于 80%；
- 10% 容差一致率低于 95%；
- 三次完全稳定率低于 90%；
- 最终处理成功率 94.94%，低于 95%。

当前配置不得进入隐藏验收。隐藏集保持密封，不消耗一次性验收机会。能力边界暂定为“当前不适用”，不得宣称自动批改质量合格。

预检已有 9 条失败后，完整调优批次仍在此前启动。这一批结果仅作为失败诊断，不构成“预检通过后运行”的合规证据；因此任务清单的 10.2 和 10.3 保持未完成。

## 基准契约缺口

当前冻结数据目录中的 `baseline.json` 缺少 40 个样本的必填 `teacherTotalScore`。逐题人工分均存在，本记录中的人工总分由四题人工分确定性相加得到；原始数据集和 manifest 未被修改。

仓库当前 schema 与正式准备脚本均要求保存并校验 `teacherTotalScore`。因此核心报告入口会拒绝这份旧格式基准。该缺口不改变逐题指标和本轮 `FAIL` 结论，但意味着当前对比产物不能冒充完整核心报告。若以后存在经过审查的明确修复并需要创建新正式版本，必须创建新的不可变数据集版本、manifest 哈希与配置身份，不能覆盖本轮输入或结果。

人工基准还只保存逐题分数，没有可用于教师批注语义覆盖、错误归属或区域定位计算的教师批注内容与区域标注。相应指标必须报告为不可用，不能以 AI 自身批注替代人工证据；任务清单 10.5 因此保持未完成。

## 第二版本判断

17 项失败属于 Provider 输出契约：7 项扣分却没有批注、5 项证据摘录不连续、2 项锚点框越出父区域、2 项没有生成输出、1 项 schema 锚点为空。它们不是数据库、对象存储、转换、租约或 Provider 身份问题。

这些失败不能用确定性“补造”批注或篡改锚点坐标来消除。前者会伪造学生反馈，后者会把不可靠的精确定位伪装为有效证据。更重要的是，T2-1 的平均偏差为 -5.2500、O2 为 +3.9740，评分尺度偏差与输出格式失败相互独立。仅修补格式不可能使完全一致率、容差一致率和稳定率跨越冻结门槛。

因此当前没有一个同时具备明确根因和可验证效果的最小配置修复；不创建第二版本，也不消耗新的 Provider 调用。若以后要继续，须先提出并离线验证覆盖评分校准和输出契约的具体变更，再由负责人明确批准新的不可变配置和运行预算。

## 产物

## 提示词快照与重试策略复核（2026-08-29）

- V2 冻结配置为 `experiment-config:70b13c217cf523017a7eba0b17ba7c2f`。它将实际送入模型的静态提示词快照及内容哈希一并冻结，而不只记录版本标签。
- 该配置将 Provider 可用性和传输类失败冻结为 `until-result` 重试；结构化评分输出不合格则保留为终态失败，不能用重试、补造批注或改写锚点掩盖。
- 前置批次 `experiment-batch:2d434aacc35f135eb8e0556fa7eb56b9` 已终态完成：24 条 executions 中 15 成功、9 失败、0 条可重试。安全错误码汇总为 `schema-assessments.0-unrecognized-key-s-in-object-label` 6 条及 `schema-assessments.0.anchors-required` 3 条。
- 因此未启动 28 份调优样本的完整运行；隐藏集维持 `SEALED`，未读取、未运行、未揭示。该批次在“遇到首个终态失败即停止未领取执行”的实现加入前已完成，故仅作为 V2 输出契约失败的诊断证据。后续前置运行已具备该停止门禁。
- 结论仍为 `FAIL`。不得自动创建 V3 或继续消耗 Provider 调用；需先形成可离线验证的评分校准与输出契约改动，再取得负责人对新不可变配置和预算的明确批准。

## 未冻结的结构化输出候选（2026-08-29）

- 根因证据已限定到适配器与 Provider 能力边界：Qwen 3.5 请求被适配为 JSON 对象模式，Provider 文档只承诺 JSON 对象而非服务端 JSON Schema 约束。因此服务端无法阻止 `label` 这类额外字段，也无法保证每题都有非空 `anchors`。
- 已完成的唯一候选变量是静态输出契约：返回对象在每个嵌套层级必须为闭集，`label` 是输入元数据、不得输出；每个 assessment 返回前必须有可验证的非空 anchors。该改动未放宽校验、未改写结果、未改变模型、量规、数据或重试策略。
- 本地回归仅证明候选提示词内容被实际构造和冻结哈希覆盖，不证明 Provider 遵循效果。在该离线验证完成时，尚未创建 V3 配置或批次、未调用 Provider，也未触及隐藏集；其后执行结果见下一节。

## V3 前置终态（2026-08-29）

- 经负责人明确授权后，候选已冻结为 `experiment-config:5797f1475453f5c6f2ffb49157f4950b`，前置批次为 `experiment-batch:ea08b7f4cc73824dacd37016b425c243`。模型、量规、数据划分、处理器和 `until-result` Provider 可用性重试均保持不变；唯一候选变量仍是静态输出契约。
- 批次共有 24 条 executions，0 成功、24 失败、0 可重试。首个 Provider 终态失败为 `anchor-excerpt-mismatch`；`stopOnTerminalFailure` 随即将其余 23 条未领取 executions 结算为 `preflight-terminal-failure`。这些停止记录不是额外模型质量失败，也没有触发额外 Provider 调用。
- V3 未满足全 24 条成功的硬门槛，故未启动完整调优，隐藏集继续 `SEALED`。至此三个正式版本均已使用，按版本上限停止调参；能力结论维持 `FAIL` / “当前不适用”。

受控本地目录保存：

- `stage-b-tuning-report-v1.json`
- `question-comparison.csv`
- `sample-total-comparison.csv`

这些产物不进入 Git。仓库仅保存本聚合记录，不包含学生正文、教师自由文本、身份映射或凭据。

## 回归验证

受影响回归套件于 2026-08-29 通过：23 个测试文件、400 项测试全部通过。PDF 验证覆盖右侧可见反馈和 `/FreeText` 锚点标记；产品已移除的原生 `/Text` 批注图标不再作为要求。

阶段 B 直接验证另行通过：CLI、冻结配置、分割、三次运行、租约恢复、重试记录、隐藏密封、基准隔离及结果不可变性共 11 个测试文件、160 项；实验室操作/概览 Route Handler 与工作区页面测试共 4 个文件、24 项。
