# 作业批改任务阶段 0 基线记录

状态：待 G.0 审核
记录日期：2026-08-25

## 审核范围

本记录覆盖 [任务清单](assignment-grading-closure-and-comparison-tasks.md) 的阶段 0：分支基线、未跟踪运行产物、三条 OpenSpec 映射和本地验收运行依赖。它不包含学生身份、人工评分、样本正文、账号凭据或环境变量值。

## Git 基线

- 当前分支：`teacher-ai-grading-lab`。
- 当前 HEAD：`c2ee30a81c0e4f8236873a01a37f6cd1485d8fc8`。
- 与 `upstream/integration` 的合并基点：`681d2df61cd6c68a6d6c4ef0a7de2910d14f70f2`。
- 相对该基点的已跟踪实现差异：178 个文件，27,459 行新增，806 行删除。
- 当前未跟踪项包括两份本轮规划文档和三个本地运行目录；没有已跟踪文件的未提交改动。

## 本地运行产物边界

以下目录在检查时存在，但均为未跟踪本地运行产物：

- `.runtime/`：包含配置、证书、日志及本地辅助脚本。
- `artifacts/phase7-local-redis/`：包含本地 Redis 运行状态。
- `artifacts/teacher-ai-grading-lab/`：包含实验室 JSON、日志和本地辅助文件。

本轮已将上述三个精确目录加入 `.gitignore`。未读取、移动或删除其中内容。任何后续提交前都必须再次执行 Git 状态和忽略规则检查。

## OpenSpec 映射

| 本轮任务域 | 原 OpenSpec change | 当前任务状态 | 阶段 A 关系 |
| --- | --- | --- | --- |
| 已发布作业、提交、AI/人工批改、确认、发布、补交 | `add-assignment-ai-grading-loop` | 56/56 已勾选 | 提供真实业务主链，仍须做本轮真实验收 |
| 实验包、脱敏、独立运行、人工基准、指标和隐藏验收 | `add-teacher-ai-grading-lab` | 52/66 已勾选 | 提供阶段 B 与实验室边界 |
| 视觉证据、教师引导、PDF 批注和真实 Word 验收 | `improve-assignment-grading-visual-evidence` | 24/30 已勾选 | 阶段 A 主 change；4.3、5.3、5.4 等仍未完成 |

本轮的去重任务清单为跨 change 执行视图；每个任务完成后必须同步回写原始 OpenSpec 的任务和 handoff 证据。

## 本地验收依赖状态

| 依赖 | 当前事实 | 阶段 A 影响 |
| --- | --- | --- |
| Prisma schema | `npx prisma validate` 已通过 | schema 可作为后续本地验收起点 |
| LibreOffice | 固定安装路径存在，但不在 PATH | 启动批处理前需通过本地受控配置显式提供命令 |
| 本地开发服务 | 未监听预期端口 | 阶段 A 前需要启动并通过健康检查 |
| Redis | 未监听预期端口 | 阶段 A 前需要按本地运行契约恢复 |
| 可信扫描器 | 命令不在 PATH、预期端口未监听 | 阶段 A 前需要恢复并验证 |
| 对象存储 | 预期端口未监听 | 阶段 A 前需要恢复、验证私有存储与本地回环访问 |

上述状态只说明当前会话尚未启动本地验收依赖，不代表代码实现缺失。不得将依赖未启动误判为评分、转换或权限缺陷。

## 已有能力定向核对

- 教师批改编排、教师确认、逐份发布和学生受控读取均存在于现有领域层与 Route Handler。
- `REVIEWED_PDF` 已有独立派生规划、对象存储和学生下载授权路径；定向搜索确认其通过教师确认和发布状态关联。
- 教师 AI 评分实验室已有独立 PDF、运行记录、人工基准、报告和实验室页面实现。
- 定向 Vitest 覆盖批改编排、Word `REVIEWED_PDF` 派生及其对象存储，结果为 3 个测试文件、49 项通过。
- Prisma schema 校验通过；本轮未执行数据库写入、Provider 调用、上传、扫描或账号操作。

## G.0 审核记录

- 审核范围：`.gitignore`、本轮三份 operations 文档，以及阶段 0 的 Git、OpenSpec、依赖和定向测试证据。
- 审核方式：只读风险审查。当前会话未提供 code-review-graph 工具，按仓库技能回退为 Git diff、忽略规则、敏感字段扫描和定点源码/测试检查。
- 验证：`git diff --check` 通过；三条精确忽略规则均由 `git check-ignore -v` 命中；定向 Vitest 49/49 通过；Prisma schema 有效。
- Finding 裁定：未发现由阶段 0 变更引入的阻断问题。运行依赖未启动属于阶段 A 前置条件，记为残余风险，不阻断进入阶段 1 的代码与数据隔离工作。
- 结论：`PASS`。阶段 1 可以开始；阶段 A 的真实上传、批改和发布仍必须等待本地依赖恢复并通过独立健康检查。

## 阶段 0 结论

- 已确认本轮实施基线、三条 OpenSpec 映射和未跟踪运行产物的风险。
- 已修复本地运行产物未被忽略的 Git 边界。
- 已确认阶段 A 不能直接启动：开发服务、Redis、可信扫描器和对象存储均需先恢复并通过独立健康检查。
- 已完成既有能力定向核对和 G.0 只读审核；下一步进入阶段 1。
