# 阶段 B 版本预检记录（2026-08-28）

## 范围

- 数据集：`t2-formal-first-round-20260813` / `v1-half-point-20-20-20-40`
- 固定划分：`grading-lab-split:39c71b3e63237a1ec3bcba3343d88ca5`，调优集 28、隐藏集 12
- 配置：`experiment-config:9c35c1c283f399d3eae790a049f4524e`
- Provider：`siliconflow` / `Qwen/Qwen3.5-35B-A3B`
- 预检范围：8 个调优样本，96 条 executions，最多每项 3 次尝试

## 结果

- 批次：`experiment-batch:79f6181a4e08f3532a9661a1eaac5447`
- 终态：`PARTIAL`
- 成功：87/96
- 失败：9/96
- 可重试：0；失败项均已达到 3 次尝试
- 隐藏集：未读取、未运行、仍为 `SEALED`

## 失败分类

- `deduction-annotation-missing`：6 项。模型返回扣分分数但未返回对应扣分批注。
- `anchor-excerpt-mismatch`：3 项。模型返回的证据摘录不是对应证据块的连续原文。
- 所有失败均发生在 Provider 输出校验阶段；未观察到数据库、对象存储、文档转换、租约恢复或 Provider 身份漂移。

## 失败后的经验

1. 端口适配和完整划分哈希必须在冻结前以数据库真实记录为准；猜测短哈希会被门禁拒绝。
2. 预检批次被中断后必须恢复过期租约，不能留下 `RUNNING` 批次或创建重复批次。
3. 输出契约失败必须保留在分母；不能放宽扣分批注或锚点校验，也不能补写模型原始结果。
4. 预检失败不等于基础设施不可用；正式调优仍需记录同类失败的真实发生率，并单独形成质量结论。

## 后续

该建议已被后续事实取代：V1 完整批次虽已运行，但因预检失败而只作为失败诊断；快照化提示词和 `until-result` Provider 可用性重试的 V2 前置批次同样以 15/24 成功、9 个结构化输出终态失败结束，未启动完整调优。当前不得自动创建或运行 V3；隐藏集仍为 `SEALED`。现行结论、候选边界与后续授权条件见 `teacher-ai-grading-stage-b-tuning-result-2026-08-29.md`。
