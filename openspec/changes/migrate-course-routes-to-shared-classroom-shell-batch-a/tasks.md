## 1. Freeze the batch

- [ ] 1.1 Record the ordered batch-A lesson ids, route variants, feature
  modules, loader entries, direct session/evidence callers, and tests.
- [ ] 1.2 Capture route response, canonical identity, bundle/hash, access,
  waiting, progression, preview, submission, refresh, reconnect, and finish
  behavior for representative and exceptional lessons.
- [ ] 1.3 Mark the already-qualified predecessor pilot and exclude it from any
  second private implementation.

## 2. Migrate the runtime routes

- [ ] 2.1 Route batch-A entry pages through the C10 CourseBundle runtime reads
  and existing shared Classroom shell configuration.
- [ ] 2.2 Migrate student, teacher, and waiting routes to the existing session
  application use cases, preserving class access and role projections.
- [ ] 2.3 Migrate manifest media, knowledge-card, generated-courseware, and
  registered capability paths without reading authoring content.
- [ ] 2.4 Verify response-producing steps use durable submission evidence and
  progress drafts use live state; preview creates no student writes.

## 3. Retire private authorities

- [ ] 3.1 Re-run static, dynamic, route, and browser caller inventories for all
  batch-A members and record every remaining compatibility consumer.
- [ ] 3.2 Delete private route/component/session authorities only after their
  required caller denominator reaches zero; retain no permanent facade.
- [ ] 3.3 Record route behavior deltas, rollback boundary, and public URL
  compatibility in the qualification ledger.

## 4. Verification and handoff

- [ ] 4.1 Run focused batch-A route, identity, session-access, manifest,
  submission/evidence, live-state, and optional-resource tests.
- [ ] 4.2 Run affected Interactive/Classroom suites, typecheck, lint, build,
  strict validation, and browser acceptance for teacher/student journeys.
- [ ] 4.3 Bind the final evidence to one source revision and hand off the
  closed batch for Batch B planning.
