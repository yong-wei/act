# 验证记录

状态：应用代码已形成检查点；最终同修订候选、GitHub 审查及生产交付尚未完成。

## 范围与验证

- 本地工作树为 act-resource；当前应用代码检查点为 4b9b410f317bfa3c868d30078008259f0a067a2b，已包含集成分支 0bcf26f6842d882e4a438e2cd75fb7bae091aa57。
- 完整 ESLint 零错误零警告，Web/Worker typecheck 通过；Konling 脚本类型检查和画像使用门禁通过。
- 图谱相关测试 73 文件首次 965 项通过；两个旧快照断言改为验证当前 Authority 与目录一致性后，该文件 10 项通过。
- 课程生成、语义缓存、连续性与发布门禁回归 4 文件 65 项通过；课程生成器实际运行得到 466 个核心节点、571 条关系。
- 浮动控件、语言资格、控灵、PDF 及引擎复测 7 文件 335 项通过；WASM 重建后相关 14 文件 133 项通过。
- npm run test 通过，覆盖冒烟、Arena 路由和课程测试；回执 bb06f042eebff5dfb610b7ee43e5f7d088fbb09856a79a6b8553810dfe65fb0f。
- 完整单元测试已运行：12067 项通过、62 项失败、47 项 pending。失败按 WASM 构建身份、语言资格、工具清单、旧定位断言、超时及既存治理问题分组处理，不能报告为全量通过。
- tools 类型图存在 170 条其他文件诊断；本次修改文件的 7 条诊断已修复，重新编译后修改文件诊断为零。生产 Web/Worker 零错误门禁不受该结果替代。
- OpenSpec 严格验证通过。

## 浏览器证据

- 桌面真实二维画布 199 个节点在选择前后原有坐标变化为零。
- 控灵打开后显示“峰值时间”，继续选择“稳态误差”时同步更新。
- 三维节点范围约 1175 × 1180 × 1175；167 条自定义连线均有有效端点，实线、虚线与聚焦透明度均生效。
- 390 像素移动视口无横向溢出，领域与 2D/3D 可访问；控灵拖动、恢复默认、跨页面与刷新持久性已通过早前浏览器验证和客户端回归。
- 仿真与知识详情默认定位 CSS 改为只作用于未自定义的浮动位置，避免覆盖拖动结果；移动学习路径原有隐藏策略保留。

## 课程与资源

课程分母包括模块 1—5 的 31 个单元、143 个知识项，133 项映射到 193 个 Authority 对象，10 项明确保留缺口。保留 376 个既有资源核心节点，不用删除教学关系代替撤销资源资格。REQUIRED 无环，已映射课程节点无孤立点。

原始完整资源基线为 4038 个资源、21471 条绑定；初步候选为 4119 个资源、21633 条绑定，已逐项核验基线连续性。最终捕获后必须再次验证教学层、独立先修、侧车、分片、六消费者、Runtime 登记和语言包的组合身份。

时域资源筛选缺失已修复：筛选依据与当前 Authority 匹配的已发布绑定，不再把应用启动资格等同于资源存在性。真实接口恢复仿真 36 个节点、音频 31 个、视频及练习各 24 个、课程与讲义各 58 个、教材节 95 个。无独立启动入口的教材父级仍保留其已发布类型；新增反例确认 dirty capture 和 RegistryIndex 未拥有入口时仍禁止启动。完整启动恢复仍须完成冻结应用与匹配 Runtime 的发布。

## 审查与残余项

本地审查范围：26f0b87aa92019dafba4127bcf9f483219e21685 至当前检查点的本次变更，不重复审查合入的船模与其他任务代码。

ACCEPT：跨域教学关系遗漏、重复先修、统计不一致、选中同步、默认 CSS 覆盖拖动，以及新增发布脚本类型诊断；对应修复和定向验证已完成。本轮增量审查未发现新的 P0/P1 重大问题。此结论不等于 GitHub 当前 HEAD 清场或生产验收。

DEFER：完整测试中的既存工具路径读取边界、assessment 证据摘要漂移，以及智能教案源代码字符串断言。相关实现和测试与 origin/integration 内容一致，本次未扩大至其他业务治理整改。测试清单新增项目和本次发布工具登记通过既有清单生成机制同步。

生产未切换，最终清洁修订的完整验证与生产验收仍待执行。

## 节点资源完整可达验证

详情已复用系统已有的已发布资源索引与阅读器，按资源身份去重列出全部绑定，保留发布投影、Authority、范围与内容版本。可选资源索引异常只影响资源块，异步读取时的选择变化会被拒绝。

- 原有 10 张受阻卡片均已依据课程材料补全并定向导出；其他卡片、信息图与教学封装保持一致。数值例子经过 Octave 核验。
- 8 条教材占位引用已映射到现存、指定版次的教材单元；未知引用、缺失单元和错误版次继续拒绝。生产 blob-view 的链接只在文件字节匹配已验证 Runtime manifest 时读取。
- 本地索引 4038 项资源中有 2351 项实际绑定资源，未就绪后端为 0；这不是逐一执行全部资源交互的声明。
- 12 类代表资源的发布页面和实际入口请求均成功；终值定理抽屉显示 50 项资源、无禁用项，知识卡正文及 14 个公式渲染节点正常；Dorf 引用打开了教材阅读器。
- 资源索引、内容版本、媒体与受控链接测试 34 项通过；API 角色、可选失败和选择漂移测试 24 项通过；卡片导出相关 Python 测试 13 项通过。具体抽样见 resource-accessibility-verification.json。

生产仍未更新；本地已修复与生产交付状态分别记录。

## 最终应用代码检查点

`0f4e10c7e39c58862a9329e0cdfd948cd4d799bc` 的单 worker 全量 Vitest 完成：12145 项通过、6 项失败、47 项跳过，原始结果已汇总至 full-unit-verification.json。教材旧断言已更新为已审定章节可打开、未知章节仍不可打开，所在文件 6 项通过。其余涉及独立工具路径读取、assessment 旧证据摘要、智能教案旧字符串断言和资源审计命令入口；对应业务实现与集成基线一致，不计为本轮新增产品缺陷。资源审计命令回归单独复测 59 项通过、1 项失败，未宣称该项通过。

Web/Worker typecheck、推送门禁、194 项发布命令清单、部署脚本与 Docker 迁移就绪检查均通过。相关域 89 文件 1087 项与 Python 13 项通过。增量 GitHub 审查范围为 89bf86f6c05af4d737c81a1b117fe9f53b222bbf..0f4e10c7e39c58862a9329e0cdfd948cd4d799bc，尚待回复。

生产只读预检：Docker Desktop 24576 MiB 内存、8192 MiB Swap；远端可用空间约 34 GiB。生产 Runtime desired 为 9 月 6 日提交 24535a1922addb04c033f520aeb62eed8c049649 的已物化候选，active 仍为 runtime-150a505ac26b2130278fa269f41830f83a9d97658db4afd0aedddde。该历史候选的资源须纳入连续性核验，未覆盖生产选择记录。

## Codex P1 整改

增量审查 89bf86f6c05af4d737c81a1b117fe9f53b222bbf..0f4e10c7e39c58862a9329e0cdfd948cd4d799bc 报告资源异步读取遗漏课程投影、资源索引与 Runtime 最终身份复核，判定 ACCEPT。索引加载现保留捕获时的同步校验函数，覆盖 agreed course projection、完整分片 envelope、投影源文件及 Runtime 源状态；详情在返回前同时复核侧车投影与索引捕获，并沿用捕获的 teaching revision。明确的身份漂移返回 503，普通资源加载故障仍保留安全的基础详情。31 项直接回归已通过，复审仅覆盖该 finding 与整改直接风险。

资源审计命令入口的既存测试失败原因已复现：治理包装器返回 apply-rejected、executed=false 的标准输出回执，测试仍期待下游 Unsupported arguments 的 stderr。该命令、包装器与测试均未被本轮修改；保留拒绝执行行为，不绕过授权门禁以满足旧断言。

P1 整改相关 6 文件 64 项测试通过，变更文件 ESLint 通过；真实已登录浏览器请求终值定理详情返回 200、50 项资源，registry publication 与资源引用仍绑定同一课程投影。

## 冻结应用与完整 Runtime 候选

应用 0.7.6 已进入 main，冻结修订 3120f2fabafed823e6cccd9219539f20b419f61f。本机 Docker 正式构建成功，镜像包 SHA256 为 01ad8ac031ff296765adbb3388291b9bcc5373ec01eec1cdc1f14fd4ca996dfc，大小 2831406080 字节。冻结修订完整 Vitest 为 12162 项通过、4 项既存失败、47 项跳过；完整 lint、production typecheck、冒烟、Arena 及镜像内 Next 编译与类型检查通过。生产尚未更新。

Runtime 候选绑定同一应用捕获：Authority 0253d66d、课程投影 228afa64、领域教学层 0260a81c、独立先修 d8a57f75、分片集 14138b6c、六消费者 de00f069。全部 21471 条原绑定保留，新增 162 条；仅按冻结裁决移除 2 项没有绑定的信息图登记。4 份相关历史卡片/图像已按原路径归档，摘要与实际生产文件一致。

新绑定的 8 张卡片已补全并定向导出；78 个公式通过 KaTeX 解析，数值例子通过 Octave 核验。当前资源索引共有 4117 项、实际绑定 2513 项，无不可读的绑定资源。真实浏览器打开单位阶跃卡片，正文与 8 个渲染公式正常。

首次生成的三类教学关系治理覆盖 7472 个对象、22416 项处置；PR #2076 审查确认其中存在入边误归属，原闭合计数已撤回，原候选仅保留为审计输入。当前正式资源资格与历史语料分别记录：历史封装从原始提交 425ebc3bf00fa39f7da623d994f24eb091c35247 重开成功；当前发布绑定和阅读版本从真实 PublishedResourceFeatureIndex 派生并核验。

教材沿用已验证的七本 legacy 语料及 13292 条检索向量，原始修订 b167bb3c007da9d9c6eb28414d1a243db2e1308b，不将两本子集替代整套本地成果。应用与 Runtime 内容输入分开冻结，旧语料身份保持不变。

协调发布新增了精确承接已完成前任的校验，并保存/恢复候选挂载目录；同候选重放、前任 Authority/Runtime 不符及损坏回执均拒绝。对应事务测试、资源资格测试通过。旧 r4-c5 固定选择器测试与当前 r6 候选不相容，未修改旧夹具来冒充通过。


## PR #2076 源节点归属整改

审查基线为 b3e3b1607f962c4990408b14e1edee19a4429779。ACCEPT：先修边 A→B 只能关闭 A 的已发布关系处置，原重算器错误地同时关闭 B。修复按源节点选取当前边，保留目标的原始无关系裁决；未能匹配原已发布处置时拒绝闭合。唯一旧自环例外使用冻结应用中的 C6 projection-adjustments 明确排除记录，并核验历史边身份、同一源目标及证据，转换为有依据的无关系处置。原候选保持原字节，改用 graph-course-app-0-7-6-3120f2fa-source-owned 候选目录；资源分母、allocation 和资源资格证据复用不变。

直接回归 11 项通过；工程先修采纳 8 项通过，当前 23 条工程采纳边均逐项匹配证据与端点，不再使用旧的 1 条固定计数。Web/Worker typecheck 通过。b3e3b160 全量 Vitest 实际为 12154 通过、6 失败、47 跳过，其中新增关系的旧计数断言已修复；资源审计命令测试仍受治理包装器先行拒绝影响，与已记录基线一致。其余四项是旧选择器断言、独立工具路径读取和 assessment 证据摘要两项，不宣称全量通过。

新候选重算为 22157 项复用、259 项更新、12 项退役；258 项 PUBLISHED_EDGE 先修处置全部匹配真实源节点，误归属为零。逐文件对比确认仅三份治理工件改变；Runtime Git tree 仍为 60273282cfbcdc92bc2ce39b3c3965ddc056a03a，外部输入包无需重建。整改提交后的增量审查和生产验收仍待完成。


## 应用 0.7.6 生产更新与兼容性预检

应用和 worker 已通过现有 `4-deploy.sh --runtime-cutover-app-only` 更新至 3120f2fabafed823e6cccd9219539f20b419f61f；镜像内部摘要为 8aa2ee0dff70aa338bb6a6c54b26a779805947d172b7c036edecb5268bb4d3bb。新旧 schema/migrations 无差异，9 份选择器/回执、完整 Runtime 生命周期、挂载路径及数据库/Redis 容器身份前后一致，readyz 通过。旧镜像保留，Runtime 尚未切换。

正式兼容性预检发现生产 Podman 返回不带算法前缀的完整镜像摘要；捕获器原先将其拒绝。修复仅在读取 Podman 输出时规范化完整 64 位摘要，持久证明仍严格要求 OCI 格式，短 ID 与标签继续拒绝。5 项 Python 回归及部署/应用更新契约通过；修正后的只读程序在实际服务器确认应用修订、镜像摘要及 136 项迁移，迁移集合摘要 7ee088744b6ee48fba58b6cc77eb9c47bdf11ef0b47879052aba1bf856327bb0。

04a90448b9 的最终单元测试：12157 通过、5 项已披露失败、47 跳过；受默认 4GB 堆限制的资源治理退役 worker，其 31 项以 8GB 单独重跑全部通过。冒烟/Arena 通过，回执 dc719a79ad389a3756eb755f1ac9b26dbc872c968fa7bf93ddc8438383d584f6。该提交 Codex 增量审查未发现重大问题，原 P1 审查线程已解决；转换 PR 状态触发的平台自动同提交检查仍待结束。
