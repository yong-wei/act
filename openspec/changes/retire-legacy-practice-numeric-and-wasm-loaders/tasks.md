## 1. Freeze four retirement denominators

- [ ] 1.1 Verify qualified R3/R4/R5 identities, clean source revision, generated package, and complete caller graph.
- [ ] 1.2 Freeze the nine raw business-loader paths and classify direct, dynamic, worker, server, test, and compatibility callers; include `src/features/interactive/unit-5-5-policy-learning-entry-risk/rl-training-runtime.ts` and exclude it from facade/build exceptions.
- [ ] 1.3 Freeze the R5 TS numerical list, including controller/disturbance/model helpers and interactive learning paths; separate state/UI/telemetry adapters.
- [ ] 1.4 Freeze all `SimulationSession`/`SimulationLog` writers, readers, historical materializers, reports, seeds, actions, routes, and tests; mark the category protected for this change.
- [ ] 1.5 Freeze generated JS/types/WASM/.d.ts/hash and build-script inputs; mark generated artifacts explicitly non-deletable.

## 2. Retirement ledger and guards

- [ ] 2.1 Define the revision-bound retirement ledger with category, owner, replacement identity, all callers, delete condition, status, runtime surfaces, rollback commit, and receipts.
- [ ] 2.2 Add static guards for direct generated imports, duplicate `initSync`/loader paths, old TS numeric authorities, unknown dynamic imports, and permanent forwarders; assert zero direct business imports across all nine loaders, including Unit 5-5 RL training.
- [ ] 2.3 Add protected-entry guards for legacy persistence, historical evidence/readers, generated artifacts, and `src/resources/simulations/destroyer-simulation.tsx`.
- [ ] 2.4 Add a zero-caller report that distinguishes production/browser/server callers from tests, history, audit, and protected readers; ambiguity blocks deletion.

## 3. First deletion slice and numeric retirement

- [ ] 3.1 Select one R3-replaced server raw loader and prove facade-only import, route behavior, server runtime readiness/error, and reversible deletion.
- [ ] 3.2 Retire remaining raw loaders one at a time after R3-R5 final caller graphs show zero active consumers; include the Unit 5-5 RL training loader and remove aliases/forwarders only with explicit replacement identity.
- [ ] 3.3 Retire R5-migrated TS PID/Smith/DP/thruster/notch/gain-schedule numerics and wind/current/ice/sloshing/RK4 implementations by model family after tolerance/replay/browser/server receipts.
- [ ] 3.4 Preserve state adapters, unit conversion, telemetry, UI, legacy persistence, protected destroyer, generated artifacts, and unsupported capabilities unless separate proof authorizes deletion.

## 4. Runtime, evidence, and rollback verification

- [ ] 4.1 Run client/worker/browser smoke for migrated scenes and preview, including WASM unavailable, pause/reset, no-fallback, and generated identity checks.
- [ ] 4.2 Run server route/Control Odyssey/Arena official-boundary smoke, proving no hidden input leakage and no stale/TS result persistence; verify `/api/arena/virtual-simulation-runs` server-facade recompute/checksum/write and rejection of client trace/summary/checksum.
- [ ] 4.3 Run Rust/WASM build, property/baseline/abs-rel tolerance/benchmark/replay/evidence tests without full-array exact snapshots.
- [ ] 4.4 Verify preview `executor`/`authoritySource`, identity participation, surrogate `modelRelation`/`prohibitsMixedClaims`, and identified-model claims only when authorized parameters are consumed by Rust.
- [ ] 4.5 Execute the tested rollback commit for the first deletion slice and confirm historical SimulationRun/Arena/evidence records remain unchanged.
- [ ] 4.6 Run typecheck, affected unit/integration tests, static architecture guards, strict OpenSpec validation, and `git diff --check`.

## 5. Final disposition

- [ ] 5.1 Publish the zero-caller and runtime verification receipts and list every deleted file and reason.
- [ ] 5.2 Publish explicit retained/protected entries and their future removal conditions; do not claim `SimulationSession`/`SimulationLog` or generated artifacts were retired.
- [ ] 5.3 Confirm no permanent compatibility facade, Arena scoring/leaderboard change, DB migration, course manifest/plugin registry change, deployment, or selector mutation.
