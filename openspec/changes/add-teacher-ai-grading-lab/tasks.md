## 1. 仓库与数据安全基线

- [x] 1.1 在 ACT GitHub Fork 的功能分支继续实现，验证 `origin` 指向实验 Fork、`upstream` 指向 `yong-wei/act`，并保留与 `integration` 的可合并历史。
- [x] 1.2 定义本地评测数据根目录配置、负责人教师用户 ID 配置和拒绝式启动校验，不提供独立密码或认证旁路。
- [x] 1.3 增加真实评测数据、解压产物、身份映射和运行产物的 Git 忽略规则，并加入防止敏感文件进入提交的自动检查。
- [x] 1.4 建立不含真实学生内容的合成最小数据集，用于单元测试、集成测试和文档示例。

## 2. 评测包与题目量规

- [x] 2.1 定义并版本化 `manifest.json`、`baseline.json` 和评测包目录 schema，覆盖样本编号、文件引用、校验和、分层标签、逐题扣分与确认状态。
- [x] 2.2 实现安全 ZIP 导入，拒绝路径穿越、重复编号、未声明文件、校验和不匹配、缺失资源、非法扩展名和部分可运行状态。
- [x] 2.3 实现 `T1S.md` 严格解析，支持嵌套评分分组并保留题号、章节、评分项和源片段追溯。
- [x] 2.4 实现题号唯一、必填章节、题目满分、评分项合计和相对 `assets/` 完整性校验，使用稳定错误码报告失败。
- [x] 2.5 为包 schema、ZIP 安全、T1-4 嵌套量规、分值不一致、资源缺失和教师基准未确认增加测试。

## 3. 脱敏与本地数据治理

- [x] 3.1 实现文件名、文档属性、正文、页眉页脚和批注身份检查，并生成不修改隔离原件的脱敏副本。
- [x] 3.2 实现逐份人工脱敏确认状态机，确保网页和命令行都不能绕过确认调用云端 ACT AI。
- [x] 3.3 只向模型投影随机样本编号和评分所需内容，验证身份映射、绝对路径、原文和凭据不会进入日志、错误或报告。
- [x] 3.4 实现真实作业与身份映射删除清单，以及不可逆脱敏样本和聚合指标的保留判定记录。
- [x] 3.5 增加脱敏门禁、日志安全投影、未确认云端调用和数据根目录越界测试。

## 4. Word 与 PDF 双表示处理

- [x] 4.1 扩展现有文档处理链以接收 `.docx` 和 `.doc`，检测真实文件类型并拒绝 `.wps`、扫描件和扩展名伪装。
- [x] 4.2 提取 `.docx` 正文、题号上下文、OMML 公式和内嵌图片，并为 `.doc` 建立受控规范化与源谱系。
- [x] 4.3 复用 ACT 服务端转换生成固定版面 PDF，并建立源校验和、转换版本、页、块、区域或精确锚点映射。
- [x] 4.4 实现公式乱码、图片丢失、文字截断、题号失联和定位失真的内容完整性门禁，不以 PDF 可打开代替成功判定。
- [x] 4.5 为 Word 公式、MATLAB 截图、图文冲突、`.doc` 转换、局部题号映射失败和未作答待确认增加回归测试。

## 5. 共享评测核心与运行快照

- [x] 5.1 在 ACT 数据治理边界定义评测核心公开接口，统一导入、划分、冻结、运行、恢复、人工判定和报告操作。
- [x] 5.2 将评测核心接入现有证据、转换、批次、`GradingRun`、分项评分、结构化批注和 ACT AI Provider 契约，不复制评分实现。
- [x] 5.3 实现不可变实验配置版本，冻结数据、题目量规、模型、提示词、推理参数、处理版本、指标版本和随机种子。
- [x] 5.4 对每份第一轮样本创建三次独立运行，保留所有原始评分、证据、批注、置信信息和模型输出引用，禁止 rerun 覆盖旧结果。
- [x] 5.5 实现单样本失败隔离、限定自动重试、恢复执行、失败阶段记录和批次继续处理。
- [x] 5.6 为配置变更、新运行不覆盖、并发恢复、部分失败和重试耗尽计入评测增加数据治理测试。

## 6. 数据划分、隐藏集与指标

- [x] 6.1 实现按教师总分区间和主要错误类型的可复现随机分层，生成不可变的约 70% 调优集和约 30% 隐藏集划分版本。
- [x] 6.2 实现隐藏集状态机：冻结前不可运行或查看、冻结后只运行一次、揭示后永久消耗、失败后不得复用。
- [x] 6.3 实现逐题完全一致率、10% 容差一致率、三次评分稳定性和批注实质稳定性计算。
- [x] 6.4 实现批注定位、扣分原因、改正建议、严重误导和应批注扣分项覆盖情况的人工标注与独立统计。
- [x] 6.5 实现转换与 AI 处理成功率，以及耗时、调用量、重试和估算成本统计；失败实例不得从预期分母静默排除。
- [x] 6.6 实现调优集和隐藏集分别判定的门槛报告，并为边界值、分母、失败样本和合并统计禁令增加确定性测试。

## 7. 结构化复核与批注 PDF

- [x] 7.1 保存不可变 AI 原始结果，并以独立版本记录教师对分数、批注文字和位置的评判或修正、操作者与时间。
- [x] 7.2 基于所选结构化版本和固定版面锚点生成 PDF 页面短标记、详细原生注释和末尾评分汇总页。
- [x] 7.3 保证派生 PDF 保持原页面内容、顺序和可读性，并在精确定位不可靠时退化到声明的区域、块或页级定位。
- [x] 7.4 实现隐藏集全部 PDF 的逐份人工核验清单和阻断状态，不允许抽样核验代替。
- [x] 7.5 为派生物确定性、原生注释、汇总页、版面完整性、定位退化和 AI 原始结果不可变增加测试。

## 8. 命令行、API 与教师网页

- [x] 8.1 增加命令行入口，支持校验包、导入、划分、冻结、运行或恢复、计算报告和导出核验清单。
- [ ] 8.2 增加教师评测 Route Handlers，统一执行负责人授权、输入 schema、隐藏集门禁和安全错误投影。（已实现；定向回归通过 `37/37`；多次高推理独立审查均未在限定窗口产出报告，待获得可裁定审查结论后勾选。）
- [ ] 8.3 在现有教师作业区域增加评测页面，覆盖数据集、配置、批次进度、逐题三次结果、教师基准对比、批注盲评和 PDF 核验。（页面主体和录入控件已完成；定向回归通过 `37/37`；多次高推理独立审查均未在限定窗口产出报告，待获得可裁定审查结论后勾选。）
- [ ] 8.4 确保评测页面没有学生发布动作，且低置信度、题号映射失败、图文冲突和处理失败均有明确待复核状态。（已实现；定向回归通过 `37/37`；多次高推理独立审查均未在限定窗口产出报告，待获得可裁定审查结论后勾选。）
- [ ] 8.5 为命令行与网页等价性、负责人单用户授权、接口越权、隐藏信息泄露和页面关键状态增加集成测试。（定向覆盖通过 `37/37`；多次高推理独立审查均未在限定窗口产出报告，待获得可裁定审查结论后勾选。）

## 9. 第一轮验收与后续门禁

- 2026-08-12 运行记录：PostgreSQL 已恢复；Redis 服务配置指向不存在目录，已使用本机临时 Redis 并验证 `PONG`。v12 冻结已成功，运行上下文仅落在本机临时目录。v12 调优启动尚未通过数据库审计确认，原因是 Codex `exec` 工具在后台启动后发生调用层故障；恢复时先审计最近批次，禁止把旧 v11 或未确认 v12 状态作为 9.1 结果。
- 2026-08-12 两样本预检：`pilot` 未产生任何实验配置或批次；三样本受控输入包被仓库 `first-round` 最小 30 样本门禁拒绝。未创建批次、未调用 Provider、未运行隐藏集。若要保留两样本端到端预检，必须另行实现隔离的 preflight 契约，不能放宽 `first-round` 门禁。
- 2026-08-12 两样本端到端预检：新增 `preflight` 契约，要求恰好 2 份已确认脱敏样本，只创建 `TUNING` 成员，不创建隐藏验收记录，并拒绝隐藏运行、揭示、报告和 PDF 核验。该运行只验证生产链路，不构成调优或首轮验收结果。

- [ ] 9.1 前置适配：评测包、数据集存储、脱敏确认与转换运行均已改为 `(sampleId, questionId)` 的独立答案文件契约；禁止拼接四份 Word 文件。合成包、导入、核心、脱敏、CLI、概览和操作路由定向回归共 `74/74` 通过；负责人已完成逐份核对，并已写入 160 份可审计的 `CONFIRMED` 签署记录。

- [ ] 9.1 使用 30 至 50 份 T1 作业完成调优集运行，记录配置变更、错误类型、质量指标和运行成本。（已采用显式 `teacher-score-only` 基准模式：40 个样本、160 份逐题 DOCX 已构建并导入，数据集内 160 条脱敏确认记录已迁移并通过逐题读取验证；固定种子划分为 28 个调优样本与 12 个密封隐藏样本。唯一启用的 `rubric-grading` Provider 策略为中国处理、审批版本 `2026-08-07-v1`、最长保留 90 天、不训练、可删除。LibreOffice 隔离配置和 `pdfjs` `Uint8Array` 边界已修复。第八版 336 条 Provider 失败已确认为 PDF `file` 附件协议冲突；第九、十版部分运行暴露 JSON schema、锚点质量和冻结身份问题并保留审计；第十一版旧提示词批次当前为 134 条成功、132 条失败、4 条运行、66 条排队，但因已接受的 anchor 提示词兼容性 P1 不得作为最终结果。已补策略漂移 fail-closed 校验和按证据块精度生成 anchor 的提示词，定向测试通过 `47/47`；待旧批次结束后重新冻结并运行。尚未完成三次逐题评测、盲评、报告和成本统计。）
- 9.1 审查记录：已有高推理独立审查确认 score-only 指标语义无缺陷；本轮独立审查接受 PDF Provider 契约冲突、OpenSpec 契约不一致和真实协议覆盖不足 finding。PDF 省略修复、OpenSpec 同步和回归测试已完成，待新的高推理独立复审；审查时已告知 RTK 绝对路径 `C:\Users\Oops\.cargo\bin\rtk.exe`。
- [ ] 9.2 冻结候选配置并执行一次隐藏验收，完成隐藏集全部批注 PDF 的逐份人工核验。
- [ ] 9.3 验证调优集和隐藏集分别达到评分、稳定性、批注、处理成功率及 PDF 门槛；任一失败时标记首轮失败并消耗隐藏集。
- [ ] 9.4 首轮通过后建立不同章节的第二套作业复测记录；在复测通过前不启动正式学生上传、截止任务、补交和发布功能开发。

## 10. 验证与交付

- [ ] 10.1 运行评测模块单元测试、文档批改数据治理测试、授权与 API 集成测试、CLI 合成数据端到端测试。
- [ ] 10.2 运行 `npm run typecheck`、`npm run lint` 和受影响范围的构建或页面测试，并记录命令、结果与未执行的更广测试。
- [ ] 10.3 使用合成数据验证仓库克隆后可复现导入、三次运行、指标报告和 PDF 生成，且不依赖实验机器上的未声明接口。
- [ ] 10.4 审查 Fork 相对 `upstream/integration` 的变更，确认没有平行应用、AI Provider、业务模型或真实评测数据，再准备回合并 PR。

## T2(max) 40 份正式实验交接任务（2026-08-13）

- [x] F.1 收到正式实验数据目录 `E:\CODE\脱敏样本\T2(max)`，确认存在 `sample-001` 至 `sample-040`，共 40 个样本目录。
- [x] F.2 确认正式实验题目契约为每个样本独立提交 `T2-1.docx`、`T2-2.docx`、`T2-3.docx`、`O2.docx`，并保留一份答题记录文件。
- [x] F.3 发现并复核 `sample-030` 的题目文件异常；负责人已补齐并标注为正确的 `T2-2.docx`，当前目录契约检查通过。
- [x] F.4 确认正式实验沿用独立量规 `T2S-20.md`、题目满分 `20/20/20/40` 和 0.5 分评分粒度；不得覆盖原始 `T2S.md`。
- [x] F.5 记录两样本 Qwen 预检经验：批次 24/24 成功；替换样本后 MAE 约 2.90，但 T2-3 仍存在人工基准与量规不一致及视觉证据波动。
- [x] F.6 记录正式实验规模：40 样本 × 4 题 × 3 次独立运行 = 480 executions；Codex 账户剩余额度不可由本地项目读取，启动前需由用户自行确认账户额度。
- [x] F.7 构建新的正式实验评测包；数据集 ID、版本、ZIP、manifest、baseline 和哈希不得复用任何 preflight 版本。（正式身份 `t2-formal-first-round-20260813` / `v1-half-point-20-20-20-40`；仓库验证器通过 40 样本、166 个声明文件。）
- [x] F.8 对 160 份 `(sampleId, questionId)` 题目提交执行脱敏扫描、负责人确认和 SHA-256 校验；任何未确认项均阻断导入与 Provider 调用。（负责人已明确逐项确认均为脱敏数据；160 项清单状态与 SHA-256 已绑定，答题记录未进入 ZIP。）
- [x] F.9 从 40 份答题记录解析四题人工总分，校验题目顺序、满分、0.5 分粒度和总分一致性；解析失败必须人工处理，不得猜测。（40 份记录均为固定 26 段结构，四题分数位于固定段落 8/13/18/23；已生成本地隔离 `baseline.draft.json`，答题记录不进入评测 ZIP 或模型上下文。）
- [x] F.10 导入正式数据集，创建固定 70/30 调优集与隐藏集划分，冻结 Qwen 配置、提示词、量规、处理器、指标和随机种子。（split `grading-lab-split:39c71b3e63237a1ec3bcba3343d88ca5`：28 调优/12 隐藏，HiddenAcceptance 保持 `SEALED`；当前配置 `experiment-config:0eb98e1a372f11ef86101d15da2b8c4a`。）
- [x] F.11 启动正式 Qwen 批次并记录 config ID、batch ID、Provider、模型版本、执行数量、成功/失败/重试状态；前台超时只允许恢复同一批次，不得新建重复批次。
- [x] F.12 等待并核验 336/336 调优集 executions 完成；逐项检查 Provider 成功、评分半分粒度、题目满分上限和原始结果不可变。40 样本总量 480 不包含仍密封的 12 个隐藏样本。
- [x] F.13 生成逐题对比表：样本、题目、满分、人工分、AI 第 1/2/3 次、AI 均值和差值；另生成每个样本人工分与三次 AI 总分。
- [x] F.14 计算 MAE、平均偏差、逐题完全一致率、三次稳定率、处理成功率、半分制合规率和满分上限合规率；T2-3 单独报告。正式调优报告状态为 `fail`，失败样本保留在分母。
- [x] F.15 将正式实验结果、错误记录、运行上下文位置和未解决风险追加到 `HANDOFF.md`，不得写入姓名、学号、原始答题正文或 API key。
- [ ] F.16 正式结果经负责人确认前，不得宣称首轮验收通过，不得启动隐藏集揭示、PDF 全量核验或 40 份结论发布。
- [x] F.17 阶段 1 数据与人工基准隔离修复：评测输入不再携带人工分数派生分层；报告、概览和受控对比报告只在独立运行终态后读取 baseline；评分总分以四题已验证分数派生并写入受控字段；归档识别和本地路径输出均已加门禁。
- [x] F.18 G.1 第二轮最小范围整改：数据集枚举不得读取 `baseline.json`；正式基准必须持久化并校验总分；教师批注原文改为固定类别，禁止任何自由文本进入基准。新增基准物理移除、总分必填与身份标识符回归；独立复审裁定 `F2/F3/F4=RESOLVED`，G.1=`PASS`。
- [x] F.19 阶段 2 评分标准与小题契约候选：首轮四题 100 分及逐项满分校验、稳定评分项 ID 到 AI/证据/PDF 的追溯、扣分批注门禁、总评结构化与内部术语过滤、PDF 汇总一致性均已实现并通过定向回归；待 G.2 独立只读审核裁定。
- [ ] F.20 G.2 限定复审：F2（总体评价持久化）与 F3（首轮题目/分值固定契约）已解决；F1（满分项仍可经持久化入口保留批注，且结构化复核调回满分不清除既有批注）和 F4（结构化复核新增批注未要求独立扣分原因）未解决。G.2=`FAIL`，阶段 3 暂停。
- [ ] F.21 G.2 设计级整改候选：持久化事务以冻结量规校验评分项、分值和批注；结构化复核在同一版本内重放并验证分数/批注终态；新增批注必须包含独立原因、学生反馈和位置；操作路由同步强制该输入，PDF 物化再次 fail-closed。150 项限定回归和类型检查通过，待独立只读复审裁定。

### 当前状态

正式实验已完成导入、28/12 固定划分和配置冻结。F.11 调优批次 `experiment-batch:38c20a090cd3f5c2c5a98ea992254417` 正在后台运行；隐藏集保持 `SEALED`，未运行、未揭示。
