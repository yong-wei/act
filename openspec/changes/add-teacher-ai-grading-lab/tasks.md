## 1. 仓库与数据安全基线

- [x] 1.1 在 ACT GitHub Fork 的功能分支继续实现，验证 `origin` 指向实验 Fork、`upstream` 指向 `yong-wei/act`，并保留与 `integration` 的可合并历史。
- [x] 1.2 定义本地评测数据根目录配置、负责人教师用户 ID 配置和拒绝式启动校验，不提供独立密码或认证旁路。
- [x] 1.3 增加真实评测数据、解压产物、身份映射和运行产物的 Git 忽略规则，并加入防止敏感文件进入提交的自动检查。
- [x] 1.4 建立不含真实学生内容的合成最小数据集，用于单元测试、集成测试和文档示例。

## 2. 评测包与题目量规

- [x] 2.1 定义并版本化 `manifest.json`、`baseline.json` 和评测包目录 schema，覆盖样本编号、文件引用、校验和、分层标签、逐题扣分与确认状态。
- [x] 2.2 实现安全 ZIP 导入，拒绝路径穿越、重复编号、未声明文件、校验和不匹配、缺失资源、非法扩展名和部分可运行状态。
- [x] 2.3 实现 `T1S.md` 严格解析，支持嵌套评分分组并保留题号、章节、评分项和源片段追溯。
- [x] 2.4 实现题号唯一、必填章节、题目满分、评分项合计和相对 `assets/` 完整性校验，使用稳定错误码报告失败。
- [x] 2.5 为包 schema、ZIP 安全、T1-4 嵌套量规、分值不一致、资源缺失和教师基准未确认增加测试。

## 3. 脱敏与本地数据治理

- [x] 3.1 实现文件名、文档属性、正文、页眉页脚和批注身份检查，并生成不修改隔离原件的脱敏副本。
- [x] 3.2 实现逐份人工脱敏确认状态机，确保网页和命令行都不能绕过确认调用云端 ACT AI。
- [x] 3.3 只向模型投影随机样本编号和评分所需内容，验证身份映射、绝对路径、原文和凭据不会进入日志、错误或报告。
- [x] 3.4 实现真实作业与身份映射删除清单，以及不可逆脱敏样本和聚合指标的保留判定记录。
- [x] 3.5 增加脱敏门禁、日志安全投影、未确认云端调用和数据根目录越界测试。

## 4. Word 与 PDF 双表示处理

- [x] 4.1 扩展现有文档处理链以接收 `.docx` 和 `.doc`，检测真实文件类型并拒绝 `.wps`、扫描件和扩展名伪装。
- [x] 4.2 提取 `.docx` 正文、题号上下文、OMML 公式和内嵌图片，并为 `.doc` 建立受控规范化与源谱系。
- [x] 4.3 复用 ACT 服务端转换生成固定版面 PDF，并建立源校验和、转换版本、页、块、区域或精确锚点映射。
- [x] 4.4 实现公式乱码、图片丢失、文字截断、题号失联和定位失真的内容完整性门禁，不以 PDF 可打开代替成功判定。
- [x] 4.5 为 Word 公式、MATLAB 截图、图文冲突、`.doc` 转换、局部题号映射失败和未作答待确认增加回归测试。

## 5. 共享评测核心与运行快照

- [x] 5.1 在 ACT 数据治理边界定义评测核心公开接口，统一导入、划分、冻结、运行、恢复、人工判定和报告操作。
- [x] 5.2 将评测核心接入现有证据、转换、批次、`GradingRun`、分项评分、结构化批注和 ACT AI Provider 契约，不复制评分实现。
- [x] 5.3 实现不可变实验配置版本，冻结数据、题目量规、模型、提示词、推理参数、处理版本、指标版本和随机种子。
- [x] 5.4 对每份第一轮样本创建三次独立运行，保留所有原始评分、证据、批注、置信信息和模型输出引用，禁止 rerun 覆盖旧结果。
- [x] 5.5 实现单样本失败隔离、限定自动重试、恢复执行、失败阶段记录和批次继续处理。
- [x] 5.6 为配置变更、新运行不覆盖、并发恢复、部分失败和重试耗尽计入评测增加数据治理测试。

## 6. 数据划分、隐藏集与指标

- [x] 6.1 实现按教师总分区间和主要错误类型的可复现随机分层，生成不可变的约 70% 调优集和约 30% 隐藏集划分版本。
- [x] 6.2 实现隐藏集状态机：冻结前不可运行或查看、冻结后只运行一次、揭示后永久消耗、失败后不得复用。
- [x] 6.3 实现逐题完全一致率、10% 容差一致率、三次评分稳定性和批注实质稳定性计算。
- [x] 6.4 实现批注定位、扣分原因、改正建议、严重误导和应批注扣分项覆盖情况的人工标注与独立统计。
- [x] 6.5 实现转换与 AI 处理成功率，以及耗时、调用量、重试和估算成本统计；失败实例不得从预期分母静默排除。
- [x] 6.6 实现调优集和隐藏集分别判定的门槛报告，并为边界值、分母、失败样本和合并统计禁令增加确定性测试。

## 7. 结构化复核与批注 PDF

- [x] 7.1 保存不可变 AI 原始结果，并以独立版本记录教师对分数、批注文字和位置的评判或修正、操作者与时间。
- [x] 7.2 基于所选结构化版本和固定版面锚点生成 PDF 页面短标记、详细原生注释和末尾评分汇总页。
- [x] 7.3 保证派生 PDF 保持原页面内容、顺序和可读性，并在精确定位不可靠时退化到声明的区域、块或页级定位。
- [x] 7.4 实现隐藏集全部 PDF 的逐份人工核验清单和阻断状态，不允许抽样核验代替。
- [x] 7.5 为派生物确定性、原生注释、汇总页、版面完整性、定位退化和 AI 原始结果不可变增加测试。

## 8. 命令行、API 与教师网页

- [x] 8.1 增加命令行入口，支持校验包、导入、划分、冻结、运行或恢复、计算报告和导出核验清单。
- [x] 8.2 增加教师评测 Route Handlers，统一执行负责人授权、输入 schema、隐藏集门禁和安全错误投影。（限域审查通过；Route/overview/核心回归 `46/46`、类型检查与 lint 通过。）
- [x] 8.3 在现有教师作业区域增加评测页面，覆盖数据集、配置、批次进度、逐题三次结果、教师基准对比、批注盲评和 PDF 核验。（限域审查通过；Route/overview/核心回归 `46/46`、类型检查与 lint 通过。）
- [x] 8.4 确保评测页面没有学生发布动作，且低置信度、题号映射失败、图文冲突和处理失败均有明确待复核状态。（限域审查通过；Route/overview/核心回归 `46/46`、类型检查与 lint 通过。）
- [x] 8.5 为命令行与网页等价性、负责人单用户授权、接口越权、隐藏信息泄露和页面关键状态增加集成测试。（限域审查通过；Route/overview/核心回归 `46/46`、类型检查与 lint 通过。）

## 9. 第一轮验收与后续门禁

- 2026-08-12 运行记录：PostgreSQL 已恢复；Redis 服务配置指向不存在目录，已使用本机临时 Redis 并验证 `PONG`。v12 冻结已成功，运行上下文仅落在本机临时目录。v12 调优启动尚未通过数据库审计确认，原因是 Codex `exec` 工具在后台启动后发生调用层故障；恢复时先审计最近批次，禁止把旧 v11 或未确认 v12 状态作为 9.1 结果。
- 2026-08-12 两样本预检：`pilot` 未产生任何实验配置或批次；三样本受控输入包被仓库 `first-round` 最小 30 样本门禁拒绝。未创建批次、未调用 Provider、未运行隐藏集。若要保留两样本端到端预检，必须另行实现隔离的 preflight 契约，不能放宽 `first-round` 门禁。
- 2026-08-12 两样本端到端预检：新增 `preflight` 契约，要求恰好 2 份已确认脱敏样本，只创建 `TUNING` 成员，不创建隐藏验收记录，并拒绝隐藏运行、揭示、报告和 PDF 核验。该运行只验证生产链路，不构成调优或首轮验收结果。

- [x] 9.1 前置适配：评测包、数据集存储、脱敏确认与转换运行均已改为 `(sampleId, questionId)` 的独立答案文件契约；禁止拼接四份 Word 文件。合成包、导入、核心、脱敏、CLI、概览和操作路由定向回归共 `74/74` 通过；负责人已完成逐份核对，并已写入 160 份可审计的 `CONFIRMED` 签署记录。

- [x] 9.1 使用 40 份正式作业完成调优集运行并记录配置、错误、质量指标和调用成本字段。新冻结配置使用 `siliconflow / Qwen/Qwen3.5-35B-A3B`；28 个调优样本的 336 条 executions 已全部进入终态，319 成功、17 失败，报告结论为 `FAIL`。Provider 未返回完整 token 与成本遥测，因此成本字段明确记录为不可用，不作估算补写。隐藏集保持 `SEALED`。聚合证据见 `docs/operations/teacher-ai-grading-stage-b-tuning-result-2026-08-29.md`。
- 9.1 审查记录：已有高推理独立审查确认 score-only 指标语义无缺陷；本轮独立审查接受 PDF Provider 契约冲突、OpenSpec 契约不一致和真实协议覆盖不足 finding。PDF 省略修复、OpenSpec 同步和回归测试已完成，待新的高推理独立复审；审查时已告知 RTK 绝对路径 `C:\Users\Oops\.cargo\bin\rtk.exe`。
- [ ] 9.2 冻结候选配置并执行一次隐藏验收，完成隐藏集全部批注 PDF 的逐份人工核验。
- [ ] 9.3 验证调优集和隐藏集分别达到评分、稳定性、批注、处理成功率及 PDF 门槛；任一失败时标记首轮失败并消耗隐藏集。
- [ ] 9.4 首轮通过后建立不同章节的第二套作业复测记录；在复测通过前不启动面向真实学生的上传、截止任务、补交和生产发布功能开发。

## 10. 验证与交付

- [x] 10.1 已运行评测模块、文档批改数据治理、实验室授权/API 与 CLI 测试：受影响回归 23 文件/400 项、阶段 B 专属 11 文件/160 项、实验室 API/页面 4 文件/24 项均通过。真实外部基础设施集成与仓库克隆合成 E2E 仍由 10.3 单列。
- [x] 10.2 已运行 `npm run typecheck`、`npm run lint`、`npx prisma validate`、OpenSpec strict validate、直接影响文件 `git diff --check` 以及实验室 API/页面测试，全部通过；中断开发服务留下的 `.next` 缓存已清除后复验 typecheck/lint 通过。未在本轮运行完整生产构建或全仓测试门禁，原因是尚无最终预期提交且工作区含大量既有未提交改动。
- [ ] 10.3 使用合成数据验证仓库克隆后可复现导入、三次运行、指标报告和 PDF 生成，且不依赖实验机器上的未声明接口。
- [ ] 10.4 审查 Fork 相对 `upstream/integration` 的变更，确认没有平行应用、AI Provider、业务模型或真实评测数据，再准备回合并 PR。

## T2(max) 40 份正式实验交接任务（2026-08-13）

- [x] F.1 收到正式实验数据目录 `E:\CODE\脱敏样本\T2(max)`，确认存在 `sample-001` 至 `sample-040`，共 40 个样本目录。
- [x] F.2 确认正式实验题目契约为每个样本独立提交 `T2-1.docx`、`T2-2.docx`、`T2-3.docx`、`O2.docx`，并保留一份答题记录文件。
- [x] F.3 发现并复核 `sample-030` 的题目文件异常；负责人已补齐并标注为正确的 `T2-2.docx`，当前目录契约检查通过。
- [x] F.4 确认正式实验沿用独立量规 `T2S-20.md`、题目满分 `20/20/20/40` 和 0.5 分评分粒度；不得覆盖原始 `T2S.md`。
- [x] F.5 记录两样本 Qwen 预检经验：批次 24/24 成功；替换样本后 MAE 约 2.90，但 T2-3 仍存在人工基准与量规不一致及视觉证据波动。
- [x] F.6 记录正式实验规模：40 样本 × 4 题 × 3 次独立运行 = 480 executions；Codex 账户剩余额度不可由本地项目读取，启动前需由用户自行确认账户额度。
- [x] F.7 构建新的正式实验评测包；数据集 ID、版本、ZIP、manifest、baseline 和哈希不得复用任何 preflight 版本。（正式身份 `t2-formal-first-round-20260813` / `v1-half-point-20-20-20-40`；仓库验证器通过 40 样本、166 个声明文件。）
- [x] F.8 对 160 份 `(sampleId, questionId)` 题目提交执行脱敏扫描、负责人确认和 SHA-256 校验；任何未确认项均阻断导入与 Provider 调用。（负责人已明确逐项确认均为脱敏数据；160 项清单状态与 SHA-256 已绑定，答题记录未进入 ZIP。）
- [x] F.9 从 40 份答题记录解析四题人工总分，校验题目顺序、满分、0.5 分粒度和总分一致性；解析失败必须人工处理，不得猜测。（40 份记录均为固定 26 段结构，四题分数位于固定段落 8/13/18/23；已生成本地隔离 `baseline.draft.json`，答题记录不进入评测 ZIP 或模型上下文。）
- [x] F.10 导入正式数据集，创建固定 70/30 调优集与隐藏集划分，冻结 Qwen 配置、提示词、量规、处理器、指标和随机种子。（split `grading-lab-split:39c71b3e63237a1ec3bcba3343d88ca5`：28 调优/12 隐藏，HiddenAcceptance 保持 `SEALED`；当前配置 `experiment-config:0eb98e1a372f11ef86101d15da2b8c4a`。）
- [x] F.11 启动正式 Qwen 批次并记录 config ID、batch ID、Provider、模型版本、执行数量、成功/失败/重试状态；前台超时只允许恢复同一批次，不得新建重复批次。
- [x] F.12 等待并核验 336/336 调优集 executions 完成；逐项检查 Provider 成功、评分半分粒度、题目满分上限和原始结果不可变。40 样本总量 480 不包含仍密封的 12 个隐藏样本。
- [x] F.13 生成逐题对比表：样本、题目、满分、人工分、AI 第 1/2/3 次、AI 均值和差值；另生成每个样本人工分与三次 AI 总分。
- [x] F.14 计算 MAE、平均偏差、逐题完全一致率、三次稳定率、处理成功率、半分制合规率和满分上限合规率；T2-3 单独报告。正式调优报告状态为 `fail`，失败样本保留在分母。
- [x] F.15 将正式实验结果、错误记录、运行上下文位置和未解决风险追加到 `HANDOFF.md`，不得写入姓名、学号、原始答题正文或 API key。
- [ ] F.16 正式结果经负责人确认前，不得宣称首轮验收通过，不得启动隐藏集揭示、PDF 全量核验或 40 份结论发布。
- [x] F.17 阶段 1 数据与人工基准隔离修复：评测输入不再携带人工分数派生分层；报告、概览和受控对比报告只在独立运行终态后读取 baseline；评分总分以四题已验证分数派生并写入受控字段；归档识别和本地路径输出均已加门禁。
- [x] F.18 G.1 第二轮最小范围整改：数据集枚举不得读取 `baseline.json`；正式基准必须持久化并校验总分；教师批注原文改为固定类别，禁止任何自由文本进入基准。新增基准物理移除、总分必填与身份标识符回归；独立复审裁定 `F2/F3/F4=RESOLVED`，G.1=`PASS`。
- [x] F.19 阶段 2 评分标准与小题契约候选：首轮四题 100 分及逐项满分校验、稳定评分项 ID 到 AI/证据/PDF 的追溯、扣分批注门禁、总评结构化与内部术语过滤、PDF 汇总一致性均已实现并通过定向回归；待 G.2 独立只读审核裁定。
- [x] F.20 G.2 限定复审：F2（总体评价持久化）与 F3（首轮题目/分值固定契约）已解决；F1（满分项仍可经持久化入口保留批注，且结构化复核调回满分不清除既有批注）和 F4（结构化复核新增批注未要求独立扣分原因）未解决。G.2=`FAIL`，阶段 3 暂停。（历史失败已由 F.21、F.22 整改并以限定复审裁定 `PASS`。）
- [x] F.21 G.2 设计级整改：持久化事务以冻结量规校验评分项、分值和批注；结构化复核在同一版本内重放并验证分数/批注终态；新增批注必须包含独立原因、学生反馈和位置；操作路由同步强制该输入，PDF 物化再次 fail-closed。152 项限定回归和类型检查通过，独立复审裁定 G.2=`PASS`。
- [x] F.22 G.2 复审阻断修复：新增或改位批注必须具有有效物理页码，防止合法持久化的复核版本在 PDF 物化时失败；路由和核心均增加拒绝式回归。仅针对该 finding 的复审裁定 `PASS`。

### 当前状态

（历史记录）正式实验曾在 F.11 阶段后台运行；该状态已由后续 V4/V6 终态记录取代，隐藏集仍按当前策略保持 `SEALED`。

## 11. 已授权扩展：阶段 A 正式闭环与阶段 B 重新调优

- [x] 11.1 按 2026-08-26 负责人授权修订 proposal、design 与 spec：阶段 A 只限本地专用验收任务和三个已核验测试账号，不接入真实学生、现有课程受众或生产成绩发布。（`openspec validate add-teacher-ai-grading-lab --type change --strict` 通过。）
- [x] 11.2 完成总控清单 6.2–6.9：教师实验室展示受控指标与修订信息；正式教师页展示当前建议分数、小题扣分、批注、总体评价和四份 PDF；学生已发布结果展示逐题分数、总分、总体评价和四份 PDF 下载，同时维持确认、发布与敏感实现信息的拒绝式门禁。代码级与组件级证据见最终交接报告。
- [x] 11.3 完成 G.6 独立审核，记录所有 finding 的 `ACCEPT`、`REJECT` 或 `DEFER` 裁决，并在 `PASS` 前不得启动 G.7。2026-08-27 限定复审结论：`PASS`；所有本轮 finding 已裁定，阶段 A 真实闭环和 G.7 仍未启动。
- [x] 11.4 冻结三个阶段 A 样本、测试账号映射和专用验收任务；仅记录匿名标识、校验和和验证结果，不记录账号凭据、真实身份或作业正文。2026-08-27 已发布 T2S-20 专用 revision，G.7=PASS。
- [x] 11.5 完成阶段 A 真实闭环：三个受控学生账号的 12/12 中文 AI 结果、教师确认、治理证据、逐份发布、学生结果读取和四份 PDF 下载均已实测；未发布、非本人和匿名读取的反向门禁均有定向测试。PDF 均可下载且校验通过，但题旁右侧批注要求的复审仍保留给 G.8。
- [x] 11.6 完成 G.7 与 G.8 独立审核；G.8 `PASS` 前不得执行阶段 B 的完整 28 样本重新调优。（G.7 已 PASS；G.8 独立只读审核于 2026-08-28 结论为 `PASS`。对象存储凭据导致的下载重验缺口裁定为 `DEFER`，不阻断门禁；阶段 B 仍须继续遵守 G.9–G.11。）
- [x] 11.7 以新冻结配置重新运行 28 个调优样本的三次逐题实验，保留旧失败批次，记录 Provider、策略版本、配置哈希、成功/失败、重试、成本和质量指标。新批次 336 条 executions 已终态：319 成功、17 失败，调优结论为 `FAIL`；Provider 成本遥测不可用已显式记录，未估算补写。
- [x] 11.7a 负责人覆盖原门禁后，以 V4 配置 `experiment-config:77e94ce4a770b95d426bb09259b3f5bb` 使用同一调优划分完成强制运行。批次 `experiment-batch:aacd7de5a39a67348678e2dd75c03f54` 的 336 条 executions 均为 `SUCCEEDED`；聚合对比、重试和人工基准边界见 `docs/operations/teacher-ai-grading-stage-b-v4-completion-2026-08-29.md`。该完成不构成隐藏验收授权。
- [ ] 11.8 仅在负责人对新的调优报告作出明确确认后，才可执行一次隐藏验收、全量 PDF 人工核验和后续门槛判定；不得把本次范围授权当作隐藏集揭示授权。

### 2026-08-28 阶段 A 恢复证据

- 12/12 题目级教师审批、治理证据和批注 derivative 已成功；整份结果确认因冻结截止时间尚未开放，11.5 与 11.6 不勾选。
- 新增确认快照投影与已发布后治理证据重试回归；定向测试 `87/87`、`npm run typecheck` 通过。
- 详细记录：`docs/operations/teacher-ai-grading-stage-a-recovery-2026-08-28.md`。
### 2026-08-28 批注语言复核

- 已确认历史阶段 A 快照中存在英文总体评价和英文批注；这些内容来自历史评分输出，不能通过直接改数据库或覆盖发布版本修复。
- 已增加简体中文提示词约束、英文-only 批注输出校验和中文 PDF 固定标签；相关定向测试 `69/69` 通过。
- 语言不合规的历史逻辑项必须创建新不可变版本，并重新执行教师确认、逐份发布、学生读取和 PDF 核验；在完成前 `G.8` 保持 `FAIL`。

### 2026-08-28 阶段 A 中文发布与 PDF 核验

- 当前发布链只引用新的不可变中文版本：12/12 题目级教师审批、治理证据和批注 PDF 均成功；三个整份结果均已确认并发布，三个受控学生账号均可读取自己的四题结果和总评。
- 量规 Provider 固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`。学生可见批注、扣分依据、总体评价和 PDF 固定标签均为中文；历史英文快照未被覆盖。
- 12 份 PDF 已重新派生、下载和校验，共 41 页。静态 Noto Sans SC 字体以非分片方式嵌入后，实际 PDF 渲染中的中文完整可见。
- 当前阶段 A 定向回归 `342/342` 通过；学生反馈文件路由新增并通过“非本人学生”和“整份成绩未发布”两项拒绝式门禁测试。真实数据读取验证确认 3 份发布结果、12 份 PDF、所有权和校验和一致。
- 题旁右侧批注验收已完成：`9-chinese-font-sidebar-fallback` 仅重派生 PDF，不改评分或批准快照；19 条批注均投影到对应页右侧栏，五份无批注 PDF 仍保持原始 A4。G.8 只待独立只读审核；阶段 B 继续冻结。

### 2026-08-29 阶段 B 调优终态

- 配置 `experiment-config:9c35c1c283f399d3eae790a049f4524e` 固定使用 `siliconflow / Qwen/Qwen3.5-35B-A3B`。
- 批次 `experiment-batch:79a30bc5449b8fee6ac4b6f073491cc6` 的 336 条 executions 全部终态：319 成功、17 失败，最终处理成功率 94.94%。
- 完全一致率 36.36%、10% 容差一致率 51.72%、三次完全稳定率 65.69%，均未达到冻结门槛；调优结论为 `FAIL`。
- 旧格式正式 baseline 缺少必填 `teacherTotalScore`，核心报告入口拒绝生成；对比产物只按四题已保存人工分确定性派生总分，不修改原始数据或 manifest。该缺口不改变逐题指标与失败结论。
- 隐藏集继续保持 `SEALED`，不得启动 11.8。聚合证据见 `docs/operations/teacher-ai-grading-stage-b-tuning-result-2026-08-29.md`。
- V2 前置批次的 24 条 executions 为 15 成功、9 个结构化输出终态失败；Provider 可用性/传输错误按 `until-result` 重试，结构化失败不重试。当时完成的单变量输出契约候选为闭集返回字段、禁止输出输入元数据 `label`、每个 assessment 的 anchors 非空；其后 V3 的授权执行结果见下一条。
- 负责人授权后，V3 配置与 24 条前置批次已终态完成：首个 `anchor-excerpt-mismatch` 结构化失败后，停止门禁将余下 23 条未领取 executions 结算为 `preflight-terminal-failure`，没有继续 Provider 调用；成功数为 0。V3 未启动完整调优，隐藏集仍为 `SEALED`。三个正式版本已经用尽，能力结论维持 `FAIL` / “当前不适用”。
