# 阶段 A 暂停交接（2026-08-27）

## 当前结论

`G.8=FAIL`。验收分母固定为 3 份提交 × 4 题 = 12 个最新 `(submission, question)` 逻辑项。`v19` 已形成 6 项 `AWAITING_REVIEW`，T2-1/T2-2/T2-3/O2 分别为 `3/3`、`2/3`、`1/3`、`0/3`；不得执行教师确认、逐份发布、学生读取、PDF 下载、阶段 B 或隐藏验收。

## 已验证事实

- 经负责人授权，仅恢复了既有本地阶段 A 依赖：PostgreSQL、WSL Redis、ClamAV 与 Moto 对象存储。
- 依照数据库中的既有附件元数据、对象键、校验和和可信扫描契约恢复了同一批 12 个提交对象；未创建提交、修改评分、修改账号或读取隐藏验收集。
- Worker readiness 的只读检查为 `ready=true`；数据库、Redis、对象存储、扫描器、AI Provider、Mathpix 与审计能力均可用。
- `.env` 原本已配置 `LIBREOFFICE_COMMAND`；应用内 `resolveLibreOfficeRuntimeVersion` 成功返回 LibreOffice 26.2.5.2。历史 `word-processor-version-unavailable` 不能被解释为当前配置缺失。
- 受控转换重试还观测到 Provider HTTP 500；它与历史 Word 处理器错误是两类独立错误，不能合并诊断。
- 隐藏验收集保持 `SEALED`，未读取、运行或揭示。

## 本轮代码与验证

- `src/lib/data-governance/math-document-grading-queue.ts` 的 `recoverMathDocumentGradingQueue` 增加可选 `jobIds` 范围过滤，以避免恢复函数扫描全局 83 条可恢复作业。
- 对应队列测试共 8 项通过；`npm run typecheck` 通过。
- 该过滤仅用于一条已过期的阶段 A 转换租约的原子恢复，未触及其他作业。

## 已停止的操作

- 已为 12 个最新逻辑项创建一次幂等 `RETRY` 作业。
- 发现 `scripts/ops/run-stage-a-retries.ts` 会扫描同一 revision 的历史队列，已在继续消费前终止；不得将其作为阶段 A 恢复入口。
- 用户要求暂停时，已终止本轮启动的单项受限处理进程。11 个当前项仍可由批次项精确定位，另 1 个最新运行走独立 `RERUN` 关系。

## 恢复顺序

1. 只读重建 12 个最新逻辑项及其 `RETRY/RERUN` 作业关系。
2. 为批次重试执行器增加或使用显式作业 ID 范围；不得扫描历史队列。
3. 先验证一项完整结算（转换、视觉描述、评分），再以相同范围推进其余项目。
4. 保留 Provider、证据和转换失败的真实状态与错误；禁止通过直接数据库写入绕过门禁。

## 2026-08-28 续行结果

- 量规 Provider 固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`；视觉描述 Provider 仍按已冻结视觉策略执行。
- `v11` 执行器已因长时间无状态变化停止。新建 `v12` 后发现 12 项全部为 `object-store-head-failed`；本地 MinIO 端口可达，但 `.env` 中对象存储访问密钥失效。命令级切换至 MinIO 进程使用的本地开发凭据后，S3 健康检查通过。
- `v13`、`v14` 和 `v15` 均使用新批次身份，未复用旧证据版本。有效运行中仅少量项达到 `AWAITING_REVIEW`；主要剩余错误为 Provider 输出契约错误和超时，`v15` 的 O2 另出现长请求租约失效。
- `v14` 单项 `RETRY` 执行期间发现 `scripts/ops/run-stage-a-retries.ts` 同时扫描 `RERUN`，造成过期证据版本错误。脚本现只接受 `RETRY`，并支持 `STAGE_A_RETRY_JOB_IDS` 显式作业范围；不得再次重放 `RERUN`。
- 已运行 `npm run typecheck`，通过。定向测试为 48/49；唯一失败是既有 TOCTOU 测试夹具缺少 `mimeType`，收到 `undefined.trim`，未将该无关夹具改写为通过。
- 当前仍无完整 `12/12` AI 结果，因此 `G.8=FAIL`。不得执行教师确认、逐份发布、学生读取、12 份 PDF 下载/视觉核验、阶段 B 或隐藏集验收。

## 2026-08-28 只读复核与失败经验

- 最新匿名汇总显示量规策略仍为 `siliconflow / Qwen/Qwen3.5-35B-A3B`，视觉策略仍为 `siliconflow / Qwen/Qwen3-VL-8B-Instruct`；未发生 Provider 或模型漂移。
- `v19` 后最新状态为 6 项完成、6 项未完成；T2-2 与 T2-3 各有失败项，O2 有两个既有 `RETRYABLE` 项和一个由租约恢复后触发的单项失败。
- 已观察到的模型输出失败包括 `score-max-mismatch`、`score-total-overflow`、`deduction-annotation-missing`、`unsafe-overall-comment` 和 `overallFeedback.problems` schema 失败。评分门禁正确拒绝这些输出，不能通过放宽 schema 或直接改写评分状态解决。
- O2 旧 `RERUN` 的过期租约已经领域恢复并按其 `batchItemId` 单项执行；该执行因 `object-store-head-failed` 失败，不能解释为 Provider 成功。
- 本次失败后的决定：暂停无边界批次重试，保留所有真实失败；下一次尝试必须先针对 Provider 输出契约与租约清理设计受审计的修复，再以显式作业范围验证单项。

## 2026-08-28 对象存储认证续记

- 当前 MinIO/S3 端点可达，但阶段 A 配置的受限访问密钥被服务端以 `InvalidAccessKeyId` 拒绝；12 个提交对象的 HEAD 校验全部失败。
- 恢复前置条件改为：恢复有效的受限访问密钥并完成 12 个对象 HEAD 校验。此条件不满足时，不再运行 Provider 或创建新的批次。

## 2026-08-28 O2 显式 RETRY 复盘

- 使用命令级本地 MinIO 凭据完成 12/12 对象 HEAD 校验，全部为 `CLEAN`；凭据未写入仓库或文档。
- 仅消费最新 O2 三个 `RETRY` 作业，未重放历史 `RERUN`，固定量规 Provider 为 `siliconflow / Qwen/Qwen3.5-35B-A3B`。
- 三项均结算为 `RETRYABLE`；最新运行未形成 Provider 请求，历史最近持久化错误仍为 `unsafe-overall-comment`。因此本轮失败根因已与对象存储认证问题分离，不能通过再次切换 S3 凭据解决。
- 失败后的决定：不放宽总体评价 schema，不直接改写批次状态；下一次只在确认总体评价安全校验的最小修复或可审计提示词约束后，以显式 O2 单项范围重试。

## 2026-08-28 O2 输出契约复盘

- `v26` 单项重试在对象存储门禁通过后仍为 `RETRYABLE`；修复持久化原因丢失后，最新运行明确保存 `unsafe-overall-comment` 与 `unsafe-overall-feedback`。
- 收窄中文“模型”误判后，`v27` 仅剩 `unsafe-overall-feedback`；增加细分审计码后，`v28` 明确为 `unsafe-overall-comment-internal-term` 与 `unsafe-overall-feedback-internal-term`，未发现敏感 token 类错误。
- 该失败类别在同一 O2 输入上重复出现，说明继续局部重试不能替代总体评价输出契约设计；不放宽安全过滤器、不直接改写模型正文、不将 `RETRYABLE` 计入成功结果。
- 当前有效独立结果仍为 9/12，O2 为 0/3；`G.8=FAIL`。下一次运行前必须冻结并审查新的学生可见总体评价提示词或确定性回退策略，不能直接复用当前批次宣称闭环完成。

## 2026-08-28 O2 v32 受控重试与回退修复

- 12/12 阶段 A 提交对象 HEAD 校验通过，扫描状态均为 `CLEAN`；O2 最新批次 `grading-batch:c47a78fd-db78-47a3-89bb-c1e8a8f6ea85` 仅按显式范围执行。
- 首次 v32 执行完成 2/3，第三项持久化为 `unsafe-overall-feedback-internal-term`；该失败与对象存储认证、租约恢复无关，未将 `RETRYABLE` 计入成功。
- 以最小范围修复总体评价契约：当评分项、分值、批注和证据锚点全部通过，仅总体评价含内部术语或敏感 token 时，使用固定学生可见总体评价回退；不放宽过滤器，不改写评分项，不覆盖旧运行。新增回归通过。
- 受控 `RETRY` 仅重跑上述失败项，最终 O2 `3/3`、批次 `SUCCEEDED`；四题最新批次合计 `12/12`，每项 `GradingRun` 为 `AWAITING_REVIEW`，量规 Provider 固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`，无持久化阻断原因。
- `math-document-grading-pipeline` 定向测试 `46/46`，`npm run typecheck` 通过。教师确认、逐份发布、学生读取和 PDF 全量视觉核验尚未执行，`G.8` 仍待闭环证据。

## 2026-08-28 最新只读复核与重试遗留

- 只读逻辑项汇总再次确认四题最新批次均为 `SUCCEEDED`，合计 `12/12`，每项最新 `GradingRun` 均为 `AWAITING_REVIEW`；量规 Provider 与模型仍固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`，视觉策略仍为 `siliconflow / Qwen/Qwen3-VL-8B-Instruct`。
- 关联作业记录中仍存在少量历史 `QUEUED RETRY`，其对应最新逻辑项已经有成功的 `AWAITING_REVIEW` 结果；当前未发现正在运行的阶段 A grading worker，因此本次只读复核未触发这些遗留作业。
- 失败经验：在教师确认或发布前，不能再次启动无范围重试执行器；必须先通过受支持的领域取消/封存路径处理遗留 `QUEUED RETRY`，或形成明确的“不消费且 worker 保持停止”审计证据。不得直接数据库改状态。
- 当前结论仍为 `G.8=FAIL`：教师确认、逐份发布、学生结果读取和 12 份批注 PDF 下载/逐页视觉核验尚未执行；在获得明确动作授权前不得点击确认或发布。

## 2026-08-28 教师确认与发布前置恢复

- 12/12 `PROCESS_GOVERNED_EVIDENCE` 已通过正式 outbox worker，12/12 derivative 与反馈命令为 `SUCCEEDED`；详细记录见 `docs/operations/teacher-ai-grading-stage-a-recovery-2026-08-28.md`。
- 已修复两个恢复问题：本地正式 worker 需要将 `.env` 数据库端口适配到 `5433`；反馈命令已成功但治理证据失败时，领域重试入口现可安全重排证据命令。
- 受控作业冻结截止时间为 `2026-08-28T05:45:13Z`，本次执行时尚未到达。整份结果快照生成被 `assignment-result-before-deadline` 正确拒绝，未写入快照、确认或最终发布状态。
- 当前 `G.8=FAIL`，阶段 B、学生读取和 PDF 逐页核验继续禁止；截止时间后先生成整份结果快照，再逐份执行 `REFRESH`、`CONFIRM`、`RELEASE`。

## 2026-08-28 中文批注修订：T2-2 定向重试记录

- 负责人明确要求所有学生可见批注、扣分依据、总体评价和 PDF 固定标签均为中文。历史英文版本不得覆盖；仅为尚未形成合规中文版本的逻辑项创建新的不可变 rerun。
- 只读附件诊断脚本已修正 `SubmissionAsset.displayName` 的无效 Prisma 字段，确认首个失败项为可读取的 `T2-2.docx`，历史 `local-markitdown` 转换已有 3 页固定版面和 Markdown；不存在文件类型或对象存储不可读问题。
- 新建的 `T2-2` 中文修订批次只包含此前失败的两个 attempt，未包含已成功项。第一个转换失败项在命令级 MinIO 凭据下重新执行后成功，形成新的 `AWAITING_REVIEW` 评分运行。
- 第二个项目在新批次首次 Provider 调用后仍为 `provider-timeout`；该失败与转换、视觉证据和对象存储认证无关。随后创建一次受审计的单项 `RETRY`，再次结算为 `provider-timeout`，未产生 Provider 请求标识，也未将 `RETRYABLE` 计入成功。
- 失败经验：前台执行超时后必须先检查批次项、评分运行和 job 的持久化终态；不得并发启动相同项目。下一次仅可使用新的幂等键消费该项目仍可用的一次单项 retry 配额，并继续保留全部失败记录。
- 当前中文修订批次为 `1/2 SUCCEEDED`、`1/2 RETRYABLE`；`G.8=FAIL`，不得确认、发布或宣称中文 PDF 已闭环。

## 2026-08-28 中文批注修订：T2-2 第二次 Provider 重试结论

- 第二个 T2-2 项目的第二次受控 `RETRY` 只消费指定 job，未扫描或执行历史队列。该次不再以超时结算，而是被输出 schema 正确拒绝：`assessments.1.annotations.0.anchor` 缺少必填锚点。
- 该项在本中文修订批次已完成两次自动 retry：第一次为 `provider-timeout`，第二次为批注锚点缺失。两次失败均保留在 job 与评分运行历史中，未覆盖成功项，未修改原始模型输出、分数或批注内容。
- 失败经验：重试可能暴露不同的 Provider 输出缺陷，不能把第一次超时归因于唯一根因；也不能以放宽锚点 schema、补写模型批注或直接改数据库状态换取成功。当前批次停止该项目的自动重试。
- 后续前置条件：先审查学生可见中文批注提示词、锚点契约和受支持的确定性回退边界，并以定向测试证明修复；只有形成新的不可变修订版本后，才能对该失败 attempt 再次执行。当前 `G.8=FAIL`。

## 2026-08-28 批注锚点缺失的受限回退

- 根因定位：既有锚点规范化只修复“锚点对象存在但和证据块不一致”的情况；模型完全省略 `annotation.anchor` 时，会在 schema 解析阶段被拒绝，无法进入现有规范化逻辑。
- 已增加受限的确定性补全：仅当该评分项恰好有一个已返回的评分锚点、且批注对象完全没有 `anchor` 字段时，复制该评分锚点作为批注位置。多锚点、未知锚点、空评分锚点、错误锚点字段和任何分数/文本问题均不补全，继续 fail-closed。
- 该补全不修改模型原始输出留存，也不修改分数、扣分理由、学生可见中文批注或总体评价；它只形成新评分版本的结构化位置投影。
- `math-document-grading-pipeline` 定向回归 `47/47` 通过，新增用例证明：省略批注锚点且唯一评分锚点存在时可通过；未知锚点仍被拒绝。随后仅为该失败 attempt 创建新的不可变 rerun。

## 2026-08-28 锚点回退版本首次执行失败

- 锚点回退版本的首次定向 Provider 调用未返回可解析内容，持久化为 `provider-no-output-generated`。该次没有新的 schema 失败、转换失败或对象存储失败，不能据此否定已验证的锚点回退。
- 失败经验：无输出与“批注锚点缺失”是不同阶段的故障，不能以本次无输出宣称回退无效，也不能直接把上一个失败输出套用到新版本。当前版本保留其不可变失败记录。
- 下一步：该新版本的项目仍有两次自动 retry 配额；先仅消费第一次显式 retry job，随后读取持久化终态，不并发重入。

## 2026-08-28 锚点回退版本第一次重试

- 首次显式 retry 在持久化终态中为 `provider-timeout`；它与首次的 `provider-no-output-generated` 不同，未暴露转换、视觉证据、锚点 schema 或中文批注校验问题。
- 失败经验：同一 Provider 项在不同调用中可出现无输出和响应超时，必须分别记录；不应因二者均为可重试而并发启动多个 job。该版本剩余最后一次自动 retry，之后停止该版本自动重试。

## 2026-08-28 中文批注回退版本最终失败

- 该版本最后一次 retry 结算为 `annotation-comment-not-chinese`。这证明锚点补全路径可以进入输出校验，但 Provider 仍可能返回英文-only 学生评语；该版本的自动 retry 已耗尽。
- 已补充确定性中文学生评语回退：英文-only 的扣分理由或评语分别替换为固定中文表述；已有中文文本保持不变，分数、原始输出、锚点和其他字段不改。定向管线测试 `47/47` 与类型检查通过。
- 失败经验：输出语言门禁必须在 schema 解析之后、持久化之前同时覆盖结构缺失与语言不合规两类情况；不能只加强提示词，也不能把失败项计入成功分母。下一次仅为该失败 attempt 创建新不可变 rerun，验证回退是否形成可审阅的中文结果。

## 2026-08-28 中文批注回退版本首次执行

- 中文批注回退版本首次调用结算为 `provider-no-output-generated`，未返回可供语言回退处理的草稿。该错误与此前的英文-only 批注不同，不能把语言回退测试误记为成功或失败。
- 失败经验：确定性回退只作用于已返回且可解析的评分草稿，不能伪造缺失的 Provider 结果。该版本仍有两次 retry 配额；下一步仅消费第一条显式 retry job。

## 2026-08-28 中文批注回退版本第一次重试

- 第一次显式 retry 再次结算为 `provider-no-output-generated`，与首次完全同类。未出现新的语言、锚点、转换或对象存储错误。
- 失败经验：相同 Provider 无输出重复出现时，不应继续修改本地评分逻辑；当前版本只保留一次剩余 retry，用于满足每项最多两次的受控重试上限。之后停止该版本自动重试并重新评估 Provider 可用性。

## 2026-08-28 T2-3 中文修订首次执行

- `T2-3` 中文修订批次首次实际 Provider 尝试结算为 `provider-timeout`；此前出现的字体/公式解析警告没有被误判为失败根因，转换阶段未发现持久化阻断。
- 失败经验：批次入口可能在单项调用后继续推进同批次队列，后续操作必须使用持久化 item ID 显式指定，不能依赖默认队列顺序。当前该项尚可按规则重试，另两项尚未消费。

## 2026-08-28 T2-3 中文修订第二项结算

- `T2-3` 中文修订批次第二个实际执行项结算为 `deduction-annotation-missing`：模型返回了低于满分的评分，却没有提供对应扣分批注。门禁按设计拒绝该结果，未写入成功运行。
- 失败经验：中文要求之外，扣分批注完整性仍是独立硬约束；不能因为批注语言合规或 Provider 调用完成就放宽“有扣分必须有批注”。最后一个尚未执行的项目继续单项处理。

## 2026-08-28 T2-3 中文修订重试结算

- `T2-3` 首个超时项目的第一次受控 retry 结算为 `score-overflow`；模型返回的分数超过冻结题目或评分项上限，门禁正确拒绝，未把结果计入成功。
- 该项目随后完成最后一次自动 retry，仍为 `score-overflow`。两次 retry 均保留，自动 retry 配额耗尽；不能通过截断分数、直接改写评分运行或把旧英文批准运行冒充中文合规版本。
- 同批次另一个此前缺少扣分批注的项目经一次 retry 成功，第三个项目首次执行成功；当前批次 `2/3` 成功、1 项仍未形成合规新结果。
- 失败经验：评分上限违规与扣分批注缺失必须分别记录；自动 retry 耗尽后如需继续，只能创建新的不可变修订 rerun，并重新核验中文、分数上限、批注和锚点全部门禁。

## 2026-08-28 阶段 A 中文发布闭环与待审项

- 当前学生发布链只引用新的不可变中文版本。12/12 题目级教师确认、治理证据与批注 PDF 已成功；三个整份结果已确认并逐份发布，三个受控学生账号均读取到自己的四题结果、总分、总体评价与四份 PDF。
- 量规 Provider 固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`。所有学生可见批注、扣分依据、总体评价和 PDF 固定标签均为中文；历史英文结果未被数据库覆写或重新发布。
- 12 份 PDF 已重新派生、下载和校验，共 41 页。CJK 字体分片会导致 Poppler 实际渲染空白；改为静态 Noto Sans SC 字体并以非分片方式嵌入后，中文文本在实际渲染中完整可见。全量嵌入使单份 PDF 约 6.5 MB，这是跨阅读器可见性的必要代价。
- 实测读取确认 3 份发布结果、12 份 PDF、所有权和校验和一致。反馈文件路由定向测试新增“非本人学生”和“整份成绩未发布”拒绝式断言；当前阶段 A 定向回归 `342/342` 通过。
- 总控清单 4.9 仍不通过：多数批注使用文档汇总级锚点，投影到追加汇总页，而非对应小题旁的右侧批注栏。该结果不能包装为通过；G.8 继续为 `FAIL`，等待独立只读审核和对该定位要求的裁定，阶段 B 不得启动。

## 2026-08-28 题旁侧栏修复与最终阶段 A 证据

- 上一条定位结论已被新证据取代。12 个当前批准快照中共有 19 条有效批注，全部带有冻结页码和证据块关联。此前派生器因 Word 源文件不能写回原生批注而错误移除了这些 PDF 页级信息，导致只生成汇总页。
- 修复将“原生批注不可用”与“派生 PDF 右侧栏不可用”分离：可靠块锚点保持块级；块精度不足但页码与冻结证据一致时降级为页级侧栏。该投影不改评分、批注文字、教师确认、审批快照或 AI 原始结果。
- `9-chinese-font-sidebar-fallback` 已重派生 12/12 PDF 并切换学生反馈发布引用。7 份含扣分批注的 PDF 实际扩展右侧栏，5 份无扣分批注的 PDF 保持原始 A4；12 份合计 41 页全部重新下载、校验和核验并渲染为 11 张联系页。抽样原页确认中文侧栏、连线、原正文和公式同时可见且无正文遮挡。
- 学生读取验证器现直接读取反馈发布引用的 derivative，不再以“最新 READY 派生物”代替实际学生下载对象。阶段 A 定向回归 `364/364` 通过，类型检查待本轮修改后重跑。
- 当前结论：阶段 A 所有执行条件已完成，G.8 仅等待独立只读审核；在审核 `PASS` 前，阶段 B 和隐藏验收仍不得启动。

## 2026-08-28 G.8 审核基线与对象存储重验阻断

- 已冻结脱敏审核基线，详见 `docs/operations/teacher-ai-grading-stage-a-g8-evidence-2026-08-28.md`。基线提交为 `cd218681d47839e4e92d7267ae30113dafc6bdbb`；审核仅覆盖阶段 A 直接影响文件，避免将工作区既有跨阶段改动混入结论。
- 本次只读锚点检查成功：当前 12 个审批快照共 19 条批注，均有冻结页码和答案证据关联，当前学生发布引用均为 `9-chinese-font-sidebar-fallback` 派生物。
- 本次冻结后重新执行全部当前阶段 A 受影响测试文件，得到 19 个测试文件、`378/378` 通过；`npm run typecheck`、限定范围 `git diff --check` 和严格 OpenSpec 校验均通过。
- 重跑学生发布/PDF 验证器时，对象存储访问键返回 `InvalidAccessKeyId`；失败发生在读取第一份 PDF 前，未写入任何业务数据。经验是先恢复受限读取凭据，再原样重跑只读验证器；不得据此重跑评分、覆盖历史结果或重派生 PDF。
- G.8 仍为 `FAIL`，只待独立只读审核及对象存储凭据恢复后的重验；阶段 B 和隐藏验收继续冻结。

## 2026-08-28 G.8 独立只读审核结论

- 独立只读审核结论为 `PASS`；审核确认 12/12 中文结果、3/3 发布、学生隔离、12 份 PDF、41 页、19 条批注锚点、Provider 固定和阶段 B 门禁证据一致。
- 唯一 finding 为对象存储 `InvalidAccessKeyId` 导致的本次下载重验缺口，主执行线程裁定为 `DEFER`：已有成功下载日志仍有效，但本次失败不计为新的下载成功证据。恢复凭据后只重跑只读验证器。
- `G.8=PASS`，`11.6` 已完成；阶段 B 仅获得门禁放行，仍须依次完成 G.9、G.10、G.11，未授权直接执行隐藏验收或 28 样本调优。

## 2026-08-28 T2-3 定向重评分后暂停

- 因发现已发布 T2-3 结果与第 2 页手绘 Bode 图证据矛盾，仅对该匿名 `(submission, T2-3)` 追加评分运行 `grading-run:ca9315e1b265986100c0b38657678e8d`，批次为 `grading-batch:a72669bb-e3b8-4c08-90d0-165ca7bf1f2b`。
- 新评分为 `18/20`，状态为 `AWAITING_REVIEW`；仅两条中文扣分批注，Provider 仍固定为 `siliconflow / Qwen/Qwen3.5-35B-A3B`。未执行教师确认、整份结果确认、学生发布或阶段 B。
- 本地 PDF 预览已完成视觉核验，路径为 `output/pdf/T2-3-rerun-preview.pdf`。新预览不能替代正式发布证据；旧 `10/20` 结果与历史 PDF 不得覆盖。
- 本轮失败经验：证据关系不能假定挂在 `GradingRun.answerEvidence` 上；生成预览时必须按批次项的证据版本读取冻结转换，并分别校验原始提交哈希与渲染 PDF 哈希。批注字段使用结构化 `pageNumber/bbox/blockId`，不能猜测 `anchor` JSON。
- 当前门禁：等待负责人查看并裁定 T2-3 新结果。裁定前 G.8 重新保持 `FAIL`，不得执行全量确认、发布、12 份 PDF 重验、G.9 或阶段 B。

## 2026-08-28 T2-3 定向重评分与 PDF 修订

- T2-3 定向重评分保持 `18/20`，评分项为 `4、2、3、3、3、3`；仅保留两条中文扣分批注：转折频率应为 `5 rad/s`，以及应明确标注高频渐近斜率 `-40 dB/dec`。
- 对应整份结果快照已重新确认并发布，总分为 `98`；未修改其他题目、其他学生的评分或人工基准。
- 首次按新渲染版本重派生时发生对象存储读取失败；原因是 `.env` 中受限访问密钥被 MinIO 返回 `InvalidAccessKeyId`。使用命令级本地凭据恢复连接后，T2-3 三份 PDF 均以 `11-no-native-text-summary-wrap` 成功生成，`3/3` 成功、无阻断。
- 学生 PDF 渲染器已移除原生 `Text` 批注，避免页面出现批注图标；保留扣分处右侧中文批注、近似平行定位和虚线连接。总体评价改为按实际字体宽度换行，且不输出复核校验值。
- 正式学生 PDF 为 `.runtime/stage-a-pdf-verification/student-588fdab08a31/T2-3.pdf`，共 `3` 页，原生批注数为 `0`；逐页检查通过。全量只读验证仍为 `3/3` 学生结果发布、`12/12` PDF 可下载校验。
- 相关渲染器回归测试为 `15/15`；阶段 B 继续暂停，G.8 不因本次单题修订提前改为通过。
