## 1. Define the closed acceptance matrix

- [ ] 1.1 Generate an exact-current-revision manifest covering fifteen domains, three levels, every supported type/family, 2D/3D, desktop/mobile, three roles and Chinese/English.
- [ ] 1.2 Define blocking request count/bytes, usable-time, main-thread, force-settle, frame, visible-label, DOM-node and KaTeX execution budgets.
- [ ] 1.3 Add automatic invalidation for stale source, shard, locale, formula, test or evidence hashes.

## 2. Verify hierarchy and data closure

- [ ] 2.1 Prove every root entry has exact default/search/neighborhood/detail closure and no ordinary response returns a complete heterogeneous domain.
- [ ] 2.2 Exercise one concept overview and selected one-hop path for every domain, including valid relation-empty states.
- [ ] 2.3 Verify no synthetic hierarchy edge, name inference, Legacy response or client-hidden full graph enters active presentation.

## 3. Verify integrated interaction and presentation

- [ ] 3.1 Capture behavioral 2D/3D force traces for movement, collision, settlement, drag, pin, unpin, reflow, camera fit and neighborhood reheat.
- [ ] 3.2 Verify default concept labels, Formula mathematics, search/hover/focus/detail identity and label visibility budgets.
- [ ] 3.3 Verify the dedicated multi-select filter panel, state preservation, mobile drawer, keyboard access and absence of a visible node directory.
- [ ] 3.4 Switch a fully loaded graph Chinese-to-English-to-Chinese and verify atomic visible/accessibility replacement with stable graph state.

## 4. Capture role and performance evidence

- [ ] 4.1 Capture student, teacher and administrator browser evidence on desktop and mobile from the same clean revision without production writes.
- [ ] 4.2 Record network, server verification, client render, force, frame, label, DOM and KaTeX metrics for the largest domain and representative one-hop network.
- [ ] 4.3 Run privacy/forbidden-surface scans and prove evidence contains no credentials, personal data, raw TeX, internal IDs or local absolute paths.

## 5. Final gates and independent decision

- [ ] 5.1 Run all child direct regressions, affected domain suites, typecheck, lint, full `npm run test`, build, data/UI governance and strict specs/changes validation.
- [ ] 5.2 Verify every acceptance row passes and forbid advisory downgrade, fixture substitution, source-string clearance or manual waiver.
- [ ] 5.3 Obtain independent exact-revision review of the full matrix, residual risks and all prior migration failure classes.
- [ ] 5.4 Mark the third migration complete only after the acceptance manifest, evidence hashes, tests and independent review all agree; otherwise leave tasks open.
