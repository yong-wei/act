## 1. 规划输入与状态快照

- [x] 1.1 在 `adaptive-learning-path-planner` 中定义新路径规划上下文，直接消费目标切片的权威学习者状态。
- [x] 1.2 在规划入口固定学习者状态版本、证据窗口、置信度和新鲜度，并将引用写入新路径结果。
- [x] 1.3 验证继续原路径时只读取持久化路径快照，不重新读取画像生成路径。

## 2. 个性化决策映射

- [x] 2.1 将薄弱知识点、掌握度和练习表现映射到路径重点、难度、练习比例和检查点。
- [x] 2.2 将资源偏好、可投入时间和学习节奏映射到资源组合、总时长和节点密度。
- [x] 2.3 为每项实际发生的路径调整生成证据引用、置信度和限制元数据。
- [x] 2.4 增加缺失、过期、低置信度和部分证据的 starter/fallback 规则，禁止伪造个性化结果。

## 3. 学生可见解释与兼容性

- [x] 3.1 将内部证据元数据投影为直接中文说明，隐藏服务字段、批次标识和模型术语。
- [x] 3.2 保持既有路径摘要、候选路径比较、继续原路径和路径执行接口兼容。
- [x] 3.3 确认个人中心继续读取同一权威状态来源，但路径生成不依赖个人中心页面渲染。

## 4. 验证

- [x] 4.1 增加不同画像导致路径重点、难度、资源或节奏差异的单元测试。
- [x] 4.2 增加状态快照一致性、继续原路径不重算和已有路径不变的回归测试。
- [x] 4.3 增加低置信度、过期、无证据和服务不可用时的降级测试。
- [x] 4.4 运行类型检查、相关单元/接口测试，并完成新建路径与继续原路径的浏览器验收。
