## 1. Freeze prerequisites and inventory

- [x] 1.1 Confirm C0 baseline and business waves are stable with source revision/tree identity and no unresolved owner blocker.
- [x] 1.2 Inventory package scripts, CI hooks, scripts/tools entrypoints, imports, docs, worker commands and operator aliases as active/compatibility/retired/unknown.
- [x] 1.3 Bind each entry to owner, authority, inputs, outputs, graph scope, verification command, receipt/privacy class and deletion condition.
- [x] 1.4 Run the code-simplification process for candidate aliases and record before/after command behavior, failure codes and side effects.

## 2. Consolidate canonical tooling

- [x] 2.1 Select existing canonical entries for TypeScript graphs, architecture/fitness, content/knowledge/runtime release, quality and database operations.
- [x] 2.2 Migrate active callers and CI/docs references to canonical entries without adding a universal shell or dispatcher.
- [x] 2.3 Delete zero-semantic aliases and retain only necessary thin compatibility adapters with explicit sunset conditions.
- [x] 2.4 Preserve `verify:commit`, `verify:push`, `typecheck`, independent tool/test graphs, portable receipts and release/rollback unique security validator.

## 3. Verify command authority

- [x] 3.1 Add inventory completeness, alias equivalence, unknown/dead command, graph boundary, receipt identity and privacy regressions.
- [x] 3.2 Add tests proving publication remains non-activating and no command mutates production selectors or business facts without the existing transaction.
- [x] 3.3 Run affected tooling/quality/release tests, typecheck, lint, diff checks and strict OpenSpec validation.

## 4. Handoff

- [x] 4.1 Record canonical command map, retired/compatibility/unknown entries and rollback mapping.
- [x] 4.2 Run `openspec validate inventory-and-consolidate-active-tooling-cli-surface --type change --strict` before C35.
