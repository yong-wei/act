# Implementation Tasks

本次仅提案，所有任务未执行；领取后按真实输出完成，不能将代码槽位/接口存在视为验收。

- [x] 1. 绑定真实renderer并接通合法的GL/GPU时间查询及自动计时降级。
- [x] 2. 拆分CPU/Worker/分项GPU与资源统计，纠正旧报告口径。
- [x] 3. 实现有限离屏批次/吞吐测量并校验实际工作和状态恢复。
- [x] 4. 校验CPU提交计时不能冒充GPU，测试计时缺失、量化和disjoint。
- [x] 5. 运行本机对照小样本，保留raw及测量开销，不录屏干扰计时。
