## 1. Prerequisite and denominator

- [x] 1.1 Verify a qualified pilot receipt and the upstream CourseBundle, Classroom application-service, live/evidence, and manifest-plugin contracts before any bridge deletion.
- [x] 1.2 Freeze the exact route, title-alias, session-producer, private-component, old-renderer, generated-courseware, and browser denominator; reconcile the observed 32 runtime-first families and approximately 96 private route surfaces.
- [x] 1.3 Characterize canonical/private links, title changes, session access and lifecycle, optional media/knowledge-card surfaces, and legacy renderer behavior before each batch.

## 2. Batch migration and route deletion

- [x] 2.1 Publish a batch plan naming each owner, canonical replacement, compatibility boundary, consumer count, deletion condition, and rollback/receipt location.
- [x] 2.2 Migrate canonical links, dynamic callers, tests, and browser fixtures from each selected private route to the shared classroom shell and public capabilities.
- [x] 2.3 Migrate direct session policy, title fallback, manifest-renderer, and generated-courseware callers before deleting the corresponding private authority.
- [x] 2.4 Run the zero-consumer gate, then delete each private route/bridge in the same revision; do not leave a permanent redirect or forwarding alias.
- [x] 2.5 Retain only bounded canonical ingress aliases during migration and remove title-based private aliases after their denominator reaches zero.

## 3. Uppercase renderer and final zero state

- [x] 3.1 Inventory every uppercase `ResourceRenderer.tsx` consumer, including tests, generated paths, and dynamic imports, and record the denominator.
- [x] 3.2 Migrate those consumers to the lowercase `resource-renderer.tsx` mainline or another approved public capability with characterization coverage.
- [x] 3.3 Delete the uppercase renderer only after its consumer count is zero and a removal receipt exists; do not disguise it with a forwarding export.
- [x] 3.4 Repeat qualified batches until the ledger reaches zero private routes, title bridges, direct private session producers, and uppercase renderer consumers.

## 4. Verification and ledger

- [x] 4.1 Run per-batch identity, route/no-redirect, access/lifecycle, plugin, old-renderer, and zero-consumer tests.
- [x] 4.2 Complete browser acceptance for canonical and retired links, title changes, teacher/student session journeys, refresh/reconnect, and optional-resource degradation.
- [x] 4.3 Run final Course/Classroom/Interactive domain suites, typecheck, lint, build as required by the changed paths, strict OpenSpec validation, and `git diff --check`.
- [x] 4.4 Publish the final route/alias/renderer removal ledger and receipts; keep deployment, activation, and production claims outside this change.
