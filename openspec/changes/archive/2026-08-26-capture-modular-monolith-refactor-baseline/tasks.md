## 1. Freeze the census contract

- [x] 1.1 Re-read the current `integration` revision, active OpenSpec inventory, build/test configuration, Prisma schema, package scripts, route conventions, worker entrypoints, and existing architecture-related specs; record any scope drift from the proposal evidence before implementation.
- [x] 1.2 Define a versioned deterministic census-core schema and a separate immutable measurement-receipt schema with capture identity, discovery manifests, stable observation ids, owner evidence, target-owner candidate state, trust and compatibility attributes, command scope/provenance, and denominator reconciliation totals.
- [x] 1.3 Add representative fixtures for App Router conventions, production and test imports, one-way cross-domain deep imports, bidirectional and longer dependency cycles, Prisma access, event and worker entries, registries, compatibility surfaces, gates, oversized files, ambiguous ownership, and explicit exclusions.

## 2. Implement read-only discovery

- [x] 2.1 Implement repository-relative discovery for pages, route handlers, APIs, server actions, UI entrypoints, feature/domain candidates, and framework convention files.
- [x] 2.2 Implement discovery for Prisma models and access sites, event contracts and dictionaries, workers and schedulers, package and repository scripts, tests, registries/plugins, and current and active OpenSpec capabilities.
- [x] 2.3 Implement the declared source dependency graph with complete forward and reverse edges, production/test/generated/compatibility/framework classifications, cross-domain public-boundary and deep-import observations, strongly connected components, and separate feature-to-App Router and Prisma coupling projections.
- [x] 2.4 Implement compatibility and deprecation discovery for facades, aliases, re-exports, old routes, feature flags, and declared migration exceptions without guessing deletion eligibility.
- [x] 2.5 Implement trust-gate discovery that records validators, protected boundaries and facts, threats or corruption modes, failure consequences, consumers, and unresolved classification evidence.
- [x] 2.6 Implement change-center observations for source size, change reasons, TypeScript scope, Vitest discovery, CI triggers, and build graphs without treating counts as findings, and capture bounded test/typecheck results only through immutable environment-provenanced measurement receipts.

## 3. Qualify deterministic evidence

- [x] 3.1 Add stable sorting and deterministic serialization for the source-derived census core, plus byte-identical projection checks over an explicitly frozen measurement-receipt set; keep wall-clock and variable runtime results out of the census core.
- [x] 3.2 Fail qualification on dirty or unresolved capture identity, mixed worktrees, duplicate stable ids, missing primary observations, denominator mismatch, unsupported schema versions, absolute paths, secrets, or forbidden learner/content payloads.
- [x] 3.3 Permit explicit ownership and gate-classification ambiguity while requiring each ambiguity to retain its conflicting repository evidence and reconcile into the relevant totals.
- [x] 3.4 Emit bounded failure fingerprints and aggregate command results rather than raw logs, copied source, screenshots, media, or run-specific evidence trees.

## 4. Prove the census behavior

- [x] 4.1 Test all discovery adapters against positive, exclusion, duplicate, ambiguity, dynamic-registry, framework-convention, cross-domain deep-import, reverse-edge, and multi-node strongly connected component fixtures.
- [x] 4.2 Test clean-source acceptance and dirty, detached-unresolved, mixed-worktree, denominator, duplicate-id, privacy, path, schema-version, and nondeterminism rejection paths.
- [x] 4.3 Test that production/test dependency observations remain distinct, every graph edge and SCC reconciles, and file size, Prisma access, failed tests, and readiness-like names are never auto-promoted to architecture findings or merged authority states.
- [x] 4.4 Test that independently rerun timing or memory commands produce new receipt identities while the deterministic census core and projections over the same frozen receipt identities remain byte-identical.
- [x] 4.5 Run the targeted census contract suite and validate the implementation checkpoint with `rtk npm run typecheck` and strict OpenSpec validation.

## 5. Capture and publish the baseline

- [x] 5.1 Create a clean implementation checkpoint containing the census schema, generator, tests, and documentation of its command contract before generating committed evidence.
- [x] 5.2 Run the census from that exact clean checkpoint, capture each declared environment-sensitive command once as an immutable receipt, inspect every unresolved or excluded record, and generate the normalized machine-readable baseline plus human-readable summary and ownership, dependency, cycle, gate, compatibility, and metric projections from the frozen input set.
- [x] 5.3 Verify entrypoint, Prisma, event, worker, script, test, registry, OpenSpec, dependency-edge, reverse-edge, deep-import, SCC, compatibility, gate, and change-center denominators reconcile and that the census core and same-input projections regenerate byte-identically.
- [x] 5.4 Confirm committed artifacts contain only repository-relative, privacy-minimized evidence and record the source commit/tree, frozen receipt identities, environment-sensitive limitations, commands, exit results, and current unresolved totals.
- [x] 5.5 Document that the baseline does not activate architecture rules or modify product, test selection, TypeScript, CI, database, runtime, OSS, GitHub, or production state, and hand its exact identity to the dependent modular-monolith charter change.
