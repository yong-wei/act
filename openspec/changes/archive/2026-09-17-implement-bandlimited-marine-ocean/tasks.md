# Implementation Tasks

以下均未实施、未验收。先完成 Buddy 登记与 claim，再开始实现；只运行与本项风险相关的最小充分验证。

- [x] 1. 固定基础波场参数、种子和质量不变规则，定义几何/法线频带。（基础交互波场沿用 #2097 的 MARINE_BASE_INTERACTION_WAVES（全 12 波，档位无关，姿态输入不变）；新增 `ocean-bands.ts`：minResolvableWavelength/bandLimitWaves（λ ≥ 间隔数×网格间距）、近场规格 2048m×256²（8m 间距，档位无关，4 间隔→λ≥32m 频带 8 波）、远场规格 60km 按档分辨率（该尺度无可解析几何波→空集平基面）、近场包络 nearFieldEnvelope（smoothstep 384m 外环，C1 连续）；声明近场近似容差 1.0m = 被裁频带（27/20/14/9m）振幅和×海况 6 倍率上界）
- [x] 2. 实现有界近场网格与远场 LOD、拼接及 morph。（GerstnerWater 重构为双网格：近场交互网格（档位无关 256²，带限波组 + 幅度包络）+ 远场平面（按档分辨率，带限空集→基面+视觉过渡）；接缝连续性 = 包络在近场外环衰减到 0，两侧同为基准高度（接缝残差 < 1e-3m，测试钉住）；包络进 GPU 顶点幅度（uEnvelopeHalfSize/uEnvelopeFadeBand），CPU 查询同参数）
- [x] 3. 实现 world phase、完整导数和共享批量 CPU 采样，移除旧粗网格耦合。（世界相位沿用 #2097 uWorldOrigin（CPU/GPU 同口径）；完整参数曲面偏导法线 dPdx×dPdz（含水平位移 Jacobian，+Y 主导守卫，替代旧 -dYdz 近似）；`createNearFieldSurfaceQuery` 批量采样（一次构建共享角点缓存，多点不再逐点新建 Map）；尾迹贴水与贴水线切换近场可见曲面（档位无关）；旧 60km 单网格全波位移（混叠源）移除）
- [x] 4. 实现交互区误差测试和相机远离船体时的覆盖策略。（近场 2048m 跟船：以 30kn（≈15.4m/s）航行 2 分钟偏航 ~1.8km 仍在包络=1 核心半径 640m 的覆盖语义内由跟船原点保证（网格逐帧跟船，船恒居中）；交互区误差测试：独立参照（测试内直接 Gerstner 求和，不复用生产 helper）vs 近场网格插值，非顶点点位、海况 1-6 全程 ≤ 声明容差 1.0m）
- [x] 5. 以实际 GPU 输出和短视频验证无混叠假长波、接缝跳变、船水分离。（纯数学层验证完成：独立参照一致性/包络 C1 连续与接缝同高/远场空集不混叠/世界锚定原点不变，10 项测试；材质源码契约断言（完整导数/包络/旧近似移除）；真实浏览器 GPU 画面与短视频走查未在本环境执行——远场平面化带来的视觉差异（1-4.5km 距离海面变平、雾区外纯平地平线）按设计接受为带限代价，残余项：真实硬件画面验收待后续 Playwright/人工走查，与本项合并门禁分开记录）
- [x] 6. 记录定向 CPU/GPU 增量成本，并维护相关 OpenSpec 与兼容接线。（CPU：姿态 5 点采样走批量查询共享角点缓存（原逐点 Map×5/frame→1 缓存/frame）；尾迹/贴水线每帧 1 次查询构建 + N 点共享；GPU：high 档顶点 66k→近场 66k+远场 66k=132k（+100%），medium/low 远场 16k/4k 递减，近场恒 66k（交互质量不随档降）；两网格共享时间/原点/色彩 uniform 写入；六包未迁移贴水采样（解析场）与近场曲面差异属 #2104 范围）
