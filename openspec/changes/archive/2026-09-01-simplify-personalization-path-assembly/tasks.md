## 1. Predecessor and characterization

- [x] 1.1 Consume the qualified C0 current-head delta and C2 adaptive-owner retirement evidence; confirm `PlanLearningPath` is the canonical owner and list the exact target/deletion files.
- [x] 1.2 Read the canonical path-planning public API, application, ports, plugin policy, and existing path specs; record behavior, trust-boundary, and non-deletable invariants.
- [x] 1.3 Capture before metrics for production bytes/LOC, public exports, functions, state variants, guards, validators, duplicate conversions, dependencies, and tests; record `assemble-plan.ts` at 250,111 bytes / 6,182 lines.
- [x] 1.4 Run existing path-planning characterization tests for ready/fallback/low-resource, cold-start, plugin-unavailable, eligibility, ranking, repair, prerequisites, terminal validation, policy bundles, serialization, execution, and feedback cases without rewriting expectations.

## 2. Bounded simplification

- [x] 2.1 After owner and characterization evidence is recorded, actively invoke `/Users/YW/.agents/skills/code-simplification/SKILL.md` for one bounded canonical path-assembly slice, supplying invariants, non-deletable boundaries, allowed deletions, baseline metrics, and the requirement for before/after reporting.
- [x] 2.2 Simplify one responsibility at a time (normalization/context, candidate eligibility, ranking/repair, assembly, explanation/serialization, or feedback projection); keep the single use case and stage order.
- [x] 2.3 Remove only proven duplicate guards/conversions/one-implementation wrappers or dead compatibility paths; do not delete identity, authorization, privacy, evidence, immutable revision, terminal, or path-safety checks.
- [x] 2.4 Run direct characterization tests after each pass and revert the pass if any output, error, ordering, side effect, provenance, privacy, or revision behavior changes.

## 3. Net-reduction evidence and delivery

- [x] 3.1 Measure after metrics using the same scope and compare production bytes/LOC, exports, functions, states, guards, validators, dependencies, tests, and semantic concept count; reject file-only splitting.
- [x] 3.2 Run related Personalization path unit/contract/route tests, `rtk npm run typecheck`, `rtk npm run lint`, and the architecture fitness/import checks.
- [x] 3.3 Run `ponytail-review-staged --run`, classify findings with evidence, and seal the final staged tree before independent review; do not use Ponytail to weaken path safety.
- [x] 3.4 Run `rtk openspec validate simplify-personalization-path-assembly --type change --strict` and record the exact result.
- [x] 3.5 Record the stable rollback revision, before/after report, deferred adjacent hotspots, and proof that no database, runtime, selector, or production behavior changed.
