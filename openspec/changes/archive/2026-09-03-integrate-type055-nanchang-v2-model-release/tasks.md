## 1. 接收并登记模型发布包

- [x] 1.1 为 `type055-nanchang-101` v2.0.0 编写失败优先测试，覆盖完整六文件分母、manifest/文件 SHA-256、大小、角色重复和部分接收拒绝。
- [x] 1.2 将上游 v2.0.0 发布包复制到 ACT 的版本化候选目录，生成只读包描述符和接收收据，并证明复制前后字节一致。
- [x] 1.3 把 LOD0/1/2、collision、payload、demo 的角色、坐标系、127/8 动画及 112/24 装填计数纳入 ACT 侧验证器。

## 2. 建立共享模型包加载边界

- [x] 2.1 扩展共享模型加载类型和 registry，使单文件模型与版本化多角色模型可以共存，且现有七模型解析结果保持不变。
- [x] 2.2 实现高/中/低质量档位到 LOD0/1/2 的唯一映射，切换时保留当前模型直到下一 LOD 就绪，并在失败时保持当前模型或进入旧模型候选链。
- [x] 2.3 实现 `forward=+X`、`up=+Y` 的唯一坐标适配器，集中转换航向、左右舷、水线、尾迹锚点和交互轴，并加入固定方向测试。
- [x] 2.4 让 payload、demo 和 collision 仅由明确消费者延迟加载，证明普通航向仿真首屏只请求一个 ship LOD。

## 3. 接入语义节点与动画

- [x] 3.1 建立按稳定节点名、动画名和 extras 查找的有界接口映射，禁止依赖 glTF 数组索引或 Blender 自动后缀。
- [x] 3.2 为主舰 127 个动画和 demo 8 个组合片段加入唯一性、默认层级可达性及目标节点验证。
- [x] 3.3 在浏览器中实际播放代表性的舵、桨、主炮、VLS、机库门、旗帜和武器 demo 动画，断言可见节点产生预期变换或形变。
- [x] 3.4 实现运行时弹药/特效的按需生成和寿命销毁，保持 payload 与主舰生命周期分离，并不得把演示轨迹解释为数值仿真输出。

## 4. 接入活动的驱逐舰场景

- [x] 4.1 在模块化 `src/resources/simulations/simulations/destroyer-simulation.tsx` 路径接入候选模型包，不修改受保护的旧式 `src/resources/simulations/destroyer-simulation.tsx`。
- [x] 4.2 保留现有 browser-delivery registry 为默认/回退，通过显式候选配置启用 v2.0.0；未启用时现有请求和渲染行为必须不变。
- [x] 4.3 添加驱动链回归测试，证明 `SimulationClock`、Rust/WASM stepping、控制器、扰动、遥测和 Arena 评分没有语义变化。

## 5. 验证与交接

- [x] 5.1 运行模型包验证、相关单元测试、`npm run typecheck` 和受影响测试套件，记录 exact-current-HEAD 证据。
- [x] 5.2 在候选开关下完成 destroyer 场景的加载进度、透明贴花、LOD 切换、坐标方向、动画、延迟请求和失败回退浏览器验收。
- [x] 5.3 记录每档主舰及按需 payload/demo 的请求字节和渲染预算，确认低档不会因模型切换破坏既有质量降档合同。
- [x] 5.4 形成接入收据和上游缺陷模板；若问题属于模型包，保持候选未激活并附版本、哈希和复现交回 3DModels 发布新版本。
- [x] 5.5 明确确认本变更未发布到 ESA/Delivery Bucket、未切换生产默认模型、未删除旧 GLB，并把生产激活保留为独立授权变更。
