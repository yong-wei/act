## 1. Native dependency and independent current subject

- [x] 1.1 Verify from live GitHub truth that the mapped `complete-current-repository-payload-eligibility-classification` Issue is closed with `status:archived` and this Issue's native `blockedBy` relationship is resolved; otherwise stop before claim-time execution, fixture changes, or adjudication.
- [x] 1.2 Read and digest-validate the archived #1876/#1883 subject, decision package, compact projections, ledger locator, and non-qualified blocker as immutable comparison evidence; do not reopen, rewrite, or inherit their owner/outcome results.
- [x] 1.3 In a separate clean checkout after the dependency resolves, freeze the then-current `origin/integration` commit/tree, current `src/lib/data-governance/**` member-set digest, caller-bundle digest, and subject digest; reject dirty, mixed, detached-unresolved, non-integration, or changing source state.
- [x] 1.4 Record a clean independent adjudicator checkpoint with `toolCommit`, `toolTree`, schema version, entry-bundle digest, and frozen evidence inputs; do not require or imply equality with the upstream payload subject.

## 2. Current residual member and caller closure

- [x] 2.1 Enumerate every current tracked member under `src/lib/data-governance/**` with stable path/blob identity, byte size, family evidence, historical comparison status, and exactly one member disposition or bounded unresolved reason.
- [x] 2.2 Enumerate forward and reverse production, test, tooling, dynamic, re-export, documentation, route, worker, scheduler, persistence, and generated-input callers and reconcile discovered, represented, justified-excluded, duplicate, and unresolved counts.
- [x] 2.3 Split families whenever members differ on caller class, owner evidence, authority, public boundary, payload, privacy, retention, recovery, rollback, or outcome; do not use directory placement, a closed Issue, or historical equality as owner evidence.

## 3. Full-ledger byte verification and compact projections

- [x] 3.1 Generate the complete current residual ledger as a reproducible local/CI artifact with logical locator, byte count, SHA-256, subject/tool/schema identities, member-set and caller-bundle digests, and normalized records.
- [x] 3.2 Independently read and hash the actual ledger bytes, reconcile them against member/caller denominators and compact projections, and index a verification receipt; reject missing, unreadable, truncated, substituted, summary-only, or cross-identity detail.
- [x] 3.3 Emit a compact decision matrix, owner/outcome summaries, future-slice register, blocker register, artifact index, and handoff with stable ordering and non-self-referential decision/package digests.

## 4. Current owner and outcome adjudication

- [x] 4.1 Re-adjudicate every current member or homogeneous family to exactly one owner from the existing charter catalog and exactly one orthogonal residual outcome; use archived decisions only as path-level comparisons that current evidence may confirm or reject.
- [x] 4.2 Keep `unresolved` blocking whenever owner, outcome, caller, authority, payload, privacy, trust, retention, recovery, or rollback evidence is incomplete or conflicting; do not force the previous 262-record result or a planned zero-kernel conclusion.
- [x] 4.3 Verify global exactly-one owner semantics for in-scope and out-of-scope surfaces and confirm that kernel, tooling, fixture/generated, and compatibility outcomes also have one accountable owner or steward.

## 5. Payload and protected-boundary rebinding

- [x] 5.1 Consume the archived upstream payload schema, policy, safe compact evidence, and qualification status, then rebind every applicable payload identity and condition to this change's independent subject or record a bounded drift/unresolved result.
- [x] 5.2 Preserve and evidence the sole online Learning Record writer, fact identity/deduplication/trusted-time/outbox/pointer/watermark rules, and keep operator/backfill paths isolated from online writes and current pointers.
- [x] 5.3 Preserve Assignment approved snapshots, CAS, idempotency, processing/derivative/outbox boundaries; ArenaSubmission official scoring and declared context-only LearningFacts; Portrait V2 primary/current-pointer behavior; and classroom authorization, redaction, and independent-learner suppression.
- [x] 5.4 Scan every record, evidence locator, ledger surface, receipt, and compact projection for credentials, cookies, user/learner identifiers, raw answers/events/context payloads, parser text, private locators, absolute paths, and other forbidden data; emit only safe IDs and bounded codes on violation.

## 6. Migration-input qualification without action

- [x] 6.1 Emit a migration-input slice only when the whole package and the slice's member/caller, owner/outcome, payload, privacy, trust, recovery, rollback, and deletion-condition evidence all qualify for the exact subject/tool identity.
- [x] 6.2 Require each emitted slice to contain exact paths, one owner, one outcome, public boundary, all caller classes, explicit not-touched paths, protected invariants, payload status, zero-consumer proof requirement, deletion condition, rollback/recovery, and required future authorization.
- [x] 6.3 Emit one non-qualified blocker package when any global or slice gate fails; do not create downstream Issues, move source, rewrite callers, delete compatibility, run backfills, or claim that a migration input authorizes implementation.
- [x] 6.4 Leave active baseline, charter, fitness budget, test qualification, production selectors, runtime/OSS state, database/Prisma, test commands, GitHub coordination, and all product behavior unchanged; reserve activation for N5.

## 7. Focused verification

- [x] 7.1 Extend focused adjudication tests for native dependency gating, independent upstream/current subjects, historical comparison-only behavior, complete current member/caller closure, family splitting, and exact-one owner/outcome.
- [x] 7.2 Cover missing or mismatched ledger bytes, locator/hash/subject/tool drift, omitted caller classes, owner/outcome conflicts, payload-subject drift, protected-invariant conflicts, privacy injection, deterministic replay, non-self-reference, and action-neutral output.
- [x] 7.3 Run the focused residual Data Governance adjudication suite and affected architecture-charter tests; run `rtk npm run typecheck` when TypeScript implementation changes.
- [x] 7.4 Run `rtk openspec validate requalify-current-residual-data-governance-owner-migration-inputs --type change --strict`, validate the affected canonical spec, and run `rtk git diff --check` before delivery.

## 8. Handoff

- [x] 8.1 Produce a digest-bound handoff containing the frozen current subject, immutable historical comparisons, independent tool/schema/frozen-input identities, reconciled member/caller totals, compact locators, actual full-ledger locator/byte-count/SHA-256/verification receipt, qualification state, and bounded blockers.
- [x] 8.2 Confirm the final diff contains only the adjudicator, focused tests, and compact decision outputs authorized by this change and no business file move, import/export rewrite, compatibility retirement, migration Issue, or authority activation.
