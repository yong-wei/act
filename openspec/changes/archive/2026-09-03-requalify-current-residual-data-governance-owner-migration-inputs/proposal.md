## Why

The archived residual Data Governance adjudication assigned an owner and outcome to every historical member, but its package remained explicitly non-qualified because the complete inventory bytes were not verified. Since `integration` has advanced, those historical decisions are useful comparison evidence but cannot safely authorize current source moves, caller rewrites, compatibility retirement, or an umbrella deletion.

## What Changes

- Require the upstream current repository-payload eligibility change to be archived before this change can be claimed, serializing the two census interpretations without treating their subjects as identical.
- Capture an independent clean claim-time `integration` subject and rebuild the complete current `src/lib/data-governance/**` member, caller, family, payload, and trust-boundary denominator.
- Re-adjudicate exactly one accountable owner and one orthogonal residual outcome for every current member from current evidence; preserve the archived 262-record decision package as comparison-only history.
- Independently read and verify the actual complete ledger bytes, then emit compact, digest-bound migration-input slices only when the package, callers, owners, outcomes, payload conditions, and protected invariants all qualify.
- Keep the output action-neutral. This change moves no file, rewrites no import, retires no compatibility surface, creates no downstream Issue, and activates no architecture or production authority.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `modular-monolith-refactor-charter`: support current-subject residual-owner requalification, actual full-ledger byte verification, independent payload-policy binding, and migration-input projections without inheriting historical owners or activating the charter.

## Impact

- Expected implementation surfaces: `scripts/architecture-residual-data-governance.ts`, `src/lib/architecture-charter/residual-data-governance.ts`, focused charter tests, and compact outputs under `docs/architecture/modular-monolith/post-convergence/residual-data-governance/`.
- Read-only evidence includes the archived #1883 package, the qualified output of `complete-current-repository-payload-eligibility-classification`, current Git/caller evidence, canonical domain public boundaries, and existing privacy/retention contracts.
- Protected boundaries include the sole online Learning Record writer, Assignment approved snapshots/CAS/idempotency, Portrait V2 pointers, ArenaSubmission official scoring, classroom authorization/redaction/small-sample suppression, and backfill isolation.
- No product code, import/export path, Prisma schema/data, backfill/replay, test command, active baseline, fitness budget, production selector, runtime/OSS state, or GitHub coordination is mutated by the implementation.
