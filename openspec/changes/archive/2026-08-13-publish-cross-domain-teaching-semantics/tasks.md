## 1. Boundary Review

- [x] 1.1 Collect explicit boundary candidates from accepted foundation, classical and modern domain worklists.
- [x] 1.2 Review only direct cross-domain REQUIRED or RECOMMENDED dependencies with ACT evidence. Record that no admissible new direct ACT_TEACHING candidate exists; do not mint edges from engineering, visual or transfer relations.

## 2. Composition

- [x] 2.1 Author the generation-3 empty cross-domain fragment (`coreNodes` / `relations` / denominator remain zero) and reseal foundation, classical and modern shards against the same complete 4891-node Authority envelope.
- [x] 2.2 Compose all resealed fragments plus the empty cross fragment with unrelaxed `composeDomainTeachingProjection`, using one `authoritySelection` for endpoint closure, duplicates and the global REQUIRED-edge DAG. Do not relax `nodeIndexDigest`.
- [x] 2.3 Publish only `generation-3/` artifacts, including source/worklist/coverage/authoring/fragment/manifest, conversion protocol and upstream pins (path, original byte digest, original published/semantic digest, resealed digest, snapshot binding). Leave published generation-2 / modern / foundation / classical bytes unchanged.
- [x] 2.4 Pin every worklist actually read by `collectCrossDomainBoundaryCandidates`. Shared empty cross-domain source/worklist identity may bind multiple reviewer paths; do not invent one-to-one reseals.

## 3. Verification

- [x] 3.1 Add cycle, duplicate, unchanged-evidence reuse, empty-cross, pin fail-closed and non-blocking coverage regression tests. Equivalence may differ only on the explicit identity allowlist.
- [x] 3.2 Run the focused teaching-projection tests, related lint/typecheck, published-byte digest check and strict OpenSpec change/spec validation.
