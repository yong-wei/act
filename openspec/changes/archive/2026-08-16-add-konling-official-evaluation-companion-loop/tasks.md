## 1. 正式评测证据与干预轮次

- [x] 1.1 为 Arena 正式提交建立受作用域约束的同任务历史读取与官方评测尝试适配器。
- [x] 1.2 以正式硬约束、连续两次失败和三次改参未改变失败约束集合实现有优先级的干预判定，移除通用阈值作为正式约束依据。
- [x] 1.3 在控灵干预记录中保存触发提交、任务身份和回访基线引用，并以正式提交引用实现幂等去重。
- [x] 1.4 实现基于下一次同任务正式提交的回访关闭与新一轮干预放行，保持用户和任务作用域隔离。
- [x] 1.5 保留控灵建议反馈记录，并确保该记录不构成掌握度、任务达标或学习路径变更。

## 2. 正式结果陪伴界面

- [x] 2.1 在支持的控制工作台 Arena 正式评测结果面板接入控灵建议状态，不影响原有评分、硬约束和排行榜呈现。
- [x] 2.2 实现可展开的控灵建议卡，区分正式评测证据与调参方向，并提供可选的“有帮助／需要改进”反馈。
- [x] 2.3 实现同任务回访对照，展示基线与当前正式评测的指标差值和硬约束状态变化，避免输出无依据的优劣结论。
- [x] 2.4 为无干预、无可用基线、重复评测请求及不支持页面提供明确且不伪造数据的界面状态。

## 3. 验证

- [x] 3.1 为正式评测尝试适配、规则优先级、提交级去重、回访关闭和冷却例外编写单元测试。
- [x] 3.2 为用户、课程、班级、资源和任务作用域隔离，以及反馈不改变学习事实或学习路径编写运行时测试。
- [x] 3.3 为 Arena 正式结果面板的建议卡、回访对照、无干预状态及预览结果隔离编写组件或路由契约测试。
- [x] 3.4 以生产路由 `/interactive-learning/control-workbench?arenaTask=task-third-order-block-diagram` 完成失败提交、展开建议、反馈与回访验证，并记录 1440/320 浏览器证据。
- [x] 3.5 在独立真实 PostgreSQL 上执行 `prisma migrate deploy`，核验 `AIIntervention` partial unique index 可部署，且同一 `userId + arena-official sessionId` 只保留一条记录。
