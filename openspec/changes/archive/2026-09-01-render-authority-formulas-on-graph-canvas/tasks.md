## 1. Freeze Formula coverage

- [x] 1.1 Generate an exact-release baseline for every reachable Formula object, formula-render record, locale projection, rich-title state and current canvas payload.
- [x] 1.2 Add failing tests proving all current Formula canvas nodes lack `mathematics` and prose-only labels do not satisfy formula presentation.
- [x] 1.3 Define complete Formula denominator and safe unavailable-disposition validation bound to the current Authority release and locale.

## 2. Extend bounded shard presentation

- [x] 2.1 Add a public governed formula projection to materialized Formula objects without exposing raw original TeX or global indexes.
- [x] 2.2 Bind formula render identity, hash, macro profile, display mode, locale and unavailable disposition into shard validation and manifest evidence.
- [x] 2.3 Include formula projections only in bounded search/neighborhood responses that contain the corresponding Formula node.
- [x] 2.4 Fail qualification on missing, unsafe, cross-release or unreviewed Formula presentation instead of substituting the prose title.

## 3. Render formulas across graph surfaces

- [x] 3.1 Carry formula mathematics through active view models, search, hover, keyboard focus, canvas labels and inspector without detail-request fan-out.
- [x] 3.2 Render the governed expression as primary Formula label with bounded human context in both 2D and 3D.
- [x] 3.3 Cache KaTeX DOM by immutable render key and locale so force/camera frames update only coordinates, visibility and opacity.
- [x] 3.4 Make formula and supporting context share one LOD, accessibility and locale decision.

## 4. Close safety and regression paths

- [x] 4.1 Add negative tests for delimiter scanning, prose-to-TeX guessing, raw source exposure, unsafe commands, unknown macros and cross-release fallbacks.
- [x] 4.2 Add bilingual real Formula fixtures for inline/block expressions, long labels, unavailable dispositions, zoom, drag and dimension switching.
- [x] 4.3 Prove Formula rendering does not alter topology, relation filters, object identity, hit testing or selected detail behavior.

## 5. Verify the change

- [x] 5.1 Run direct governed-math, shard, adapter, semantic-label and cache tests, then the affected knowledge graph domain suite.
- [x] 5.2 Run real browser 2D/3D formula disclosure, search, hover, focus, zoom, drag and locale checks with KaTeX execution counts.
- [x] 5.3 Run typecheck, lint, full `npm run test`, build, commercial UI governance and strict OpenSpec validation on the final clean revision.
- [ ] 5.4 Obtain independent review of complete Formula coverage, exact identity, raw-source safety and no prose-only substitution.
