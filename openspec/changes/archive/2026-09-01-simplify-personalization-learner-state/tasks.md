## 1. Predecessor and characterization

- [x] 1.1 Consume the qualified C0 current-head delta and C2 adaptive-owner retirement evidence; confirm Personalization learner-state is the canonical reducer/application owner and list exact target/deletion files.
- [x] 1.2 Read the learner-state public API, application, ports, adapters, plugin contracts, reducer specs, portrait/identity/privacy contracts, and no-data semantics; record all non-deletable invariants.
- [x] 1.3 Capture before metrics for production bytes/LOC, public exports, functions, state variants, guards, validators, duplicate fact conversions, dependencies, tests, and the 111,670-byte / 2,754-line `internal.ts` plus 85-byte `reducer.ts` entry.
- [x] 1.4 Run existing reducer, application, role/goal, route, portrait, source-identity, assessment-mastery, path-context, plugin-missing, stale/partial, no-evidence, privacy, and error characterization tests without rewriting expectations.

## 2. Bounded simplification

- [x] 2.1 After canonical ownership and characterization are recorded, actively invoke `/Users/YW/.agents/skills/code-simplification/SKILL.md` for one bounded learner-state slice, supplying invariants, non-deletable trust boundaries, allowed deletions, baseline metrics, and before/after reporting requirements.
- [x] 2.2 Make `reducer.ts` a real canonical pure entry (or otherwise make the pure boundary explicit), while isolating application/adapters for database, cache, portrait authorization, pagination, and writeback reads.
- [x] 2.3 Remove only proven duplicate fact normalization, source/enum/null guards, compatibility wrappers, and one-implementation interfaces; preserve identity, privacy, role, freshness, no-evidence, snapshot fencing, concurrency, and append-only protections.
- [x] 2.4 Run direct characterization tests after each pass and revert the pass if learner-state values, error semantics, source lineage, role projection, privacy, ordering, or side effects change.

## 3. Net-reduction evidence and delivery

- [x] 3.1 Measure after metrics with identical scope; compare production bytes/LOC, exports, functions, states, guards, validators, duplicate conversions, dependencies, tests, and semantic concept count, rejecting cosmetic file splitting.
- [x] 3.2 Run related Personalization/Learning Record/Assessment learner-state unit and route/contract tests, `rtk npm run typecheck`, `rtk npm run lint`, and architecture fitness/import checks.
- [x] 3.3 Run `ponytail-review-staged --run`, classify findings with evidence, and seal the final staged tree before independent review; do not weaken learner-state authority or privacy for a smaller diff.
- [x] 3.4 Run `rtk openspec validate simplify-personalization-learner-state --type change --strict` and record the exact result.
- [x] 3.5 Record the stable rollback revision, before/after report, deferred adjacent work, and proof that no schema, data, runtime, selector, or production behavior changed.
