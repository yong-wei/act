## 1. Contract and navigation

- [x] 1.1 Update the adaptive path return contract so path-launched resources return to the path center entry point with the goal preserved.
- [x] 1.2 Keep non-path resource return targets unchanged and ensure execution/candidate parameters are cleared.

## 2. Regression coverage

- [x] 2.1 Update journey-control tests for the goal-only center href and non-path behavior.
- [x] 2.2 Verify saved-path continuation and new-path candidate comparison remain distinct in the center flow.

## 3. Validation and evidence

- [x] 3.1 Run strict OpenSpec validation, targeted tests, typecheck, and diff checks.
- [x] 3.2 Capture desktop and 320px browser evidence for path execution → resource → return → path center.
