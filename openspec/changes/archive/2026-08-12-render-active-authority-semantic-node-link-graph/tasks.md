## 1. Authority presentation contract

- [x] 1.1 Add a browser-safe active Authority presentation adapter that maps supported object types, predicates, directions and governance states to human-readable visual contracts while preserving opaque topology keys.
- [x] 1.2 Add deterministic adjacency indexing, connected entry-scope selection, type/search lookup and one-hop materialization helpers without creating virtual Authority nodes or inferred edges.
- [x] 1.3 Add unit and structural tests for registered semantic mappings, unknown-value failure states, deterministic subgraphs and the prohibition on Legacy/server-only imports.

## 2. Semantic node-link canvas

- [x] 2.1 Reuse or extract the source-neutral force-graph canvas contract needed by current Authority without importing Legacy progressive loading, course roots, resource projection or teaching relation semantics.
- [x] 2.2 Replace the active object-card grid and relation-card list with typed semantic nodes, authoritative edges, direction/line-style encoding, pan/zoom, selection and visible-scope controls.
- [x] 2.3 Implement search, type filtering, connected overview, successive one-hop exploration, isolated-object presentation, return-to-overview and bounded large-graph materialization.
- [x] 2.4 Add component tests proving nodes and edges reflect exact Authority endpoints/predicates/directions, unsupported semantics never fall back to raw strings, and no visual connectivity relation is fabricated.

## 3. Safe detail and identity boundary

- [x] 3.1 Replace active header provenance strings with human-readable Authority status, total coverage and visible-scope information while retaining server-side identity validation.
- [x] 3.2 Rebuild active node detail to use semantic neighbor names, registered relation terms, readable governance labels and approved source citations; use controlled unavailable states when display metadata is absent.
- [x] 3.3 Remove object/relation IDs, Release/ReleaseSet, hashes, Snapshot/Activation/Projection identities, raw enums and internal source locators from visible text, ARIA, tooltips, copy output and fallback messages for all product roles.
- [x] 3.4 Preserve responsive inspector focus entry, Escape/close behavior and focus return to the originating semantic node or canvas, with desktop and mobile regression coverage.

## 4. Workspace integration and acceptance

- [x] 4.1 Verify active, historical Legacy and administrator candidate modes retain independent API/rendering state and that active failure never fetches Legacy or candidate data as fallback.
- [x] 4.2 Run focused presentation, active workspace and interaction tests, followed by typecheck, lint, affected commercial UI governance (strict `safe-api-evidence/v1` projector/gate with endpoint-class/status/request-count isolation, required active-node observed/verified state pairs, shared raw/encoded/decoded `SensitiveValueMatcher` scans, identity-match-only checks, real SVG edge geometry/endpoint resolution, role surface scans and focus evidence) and strict OpenSpec validation.
- [x] 4.3 On the final clean revision with real credentials, capture authenticated student/teacher/administrator `/knowledge` evidence for desktop, tablet and mobile, including graph topology, progressive exploration, isolated nodes, semantic detail/adjacency interaction, safe focus return and prohibited-string absence. Do not mark complete before the governance gate accepts the revision-bound evidence; raw API payloads and opaque tokens must remain capture-memory-only.
- [x] 4.4 Complete independent visual and current-diff review, resolve accepted blocking findings, run the final required test suite and record any non-blocking residual verification scope. The accepted search/type-filter reachability P1 was resolved with direct regressions; the only residual is mobile third-row anti-aliasing visibility, which is supported by SVG geometry and label evidence and is non-blocking.
