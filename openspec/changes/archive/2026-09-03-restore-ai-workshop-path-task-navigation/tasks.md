## 1. 服务端路径任务投影

- [x] 1.1 扩展已采用路径读取，保留服务端确认的 `goalId`、路径 ID、节点有序列表、当前节点和执行状态。
- [x] 1.2 重构路径任务导航描述，生成包含 `goal`、`pathId`、`nodeId`、`intent=path-execution` 的标准编码 URL。
- [x] 1.3 对缺少目标、失效节点或无效路径上下文的记录返回诚实的限制/恢复语义，不生成通用练习地址。
- [x] 1.4 增加任务投影回归，覆盖当前节点、未解锁节点、参数编码和无效上下文。

## 2. 路径执行上下文恢复

- [x] 2.1 让自适应学习中心解析完整路径执行导航，并按目标、路径和节点加载指定工作区。
- [x] 2.2 在服务端重新验证当前学生的路径归属、节点成员关系、路径状态和节点 readiness。
- [x] 2.3 为已完成、锁定、失效和未授权节点分别呈现真实状态与可达恢复行动，禁止静默回退到通用练习。
- [x] 2.4 增加路由/服务端回归，覆盖目标缺失、节点不属于路径、其他学生路径和路径状态变化。

## 3. 浏览器验收与交付门禁

- [ ] 3.1 在认证学生场景中从 `/ai` 点击当前路径任务，验证最终页面保留目标、路径、节点和 `path-execution` 语义。
- [ ] 3.2 在 1440px 和 320px 视口验证当前节点可继续、锁定节点可解释、失效上下文可恢复且无横向溢出。
- [ ] 3.3 在稳定且干净的代码 checkpoint 重新生成受影响页面的 Commercial UI Evidence，并记录可核验的 revision、来源哈希、时间和截图哈希。
- [x] 3.4 运行 focused tests、相关路径/AI 工坊测试、`npm run typecheck`、OpenSpec strict validation 与 `git diff --check`，记录最终结果。
- [ ] 3.5 合并后按仓库规范执行 `openspec archive restore-ai-workshop-path-task-navigation`，并在归档前重新验证主 spec 已同步且 Issue 生命周期完成。

