## 1. Ownership, baselines, and failing acceptance

- [ ] 1.1 Re-read the completed `coordinate-latest-authority-and-active-oss-cutover` provider receipts and its truthful GitHub coordination record; this change SHALL remain unclaimable while the native provider dependency is open.
- [ ] 1.2 Record an ownership matrix proving that this change owns only `/knowledge` runtime migration and consumer acceptance while the provider owns Authority capture, Teaching/resource generation, Runtime publication, selector mutation, transaction receipts, and rollback.
- [ ] 1.3 Re-read the main specs, the archived migration evidence, current active and legacy render paths, current production selectors through the bounded ECS route, and the provider's latest immutable candidate and predecessor identities.
- [x] 1.4 Add failing structural tests that reject an active root SVG, an active-specific fixed-height canvas, a reduced Force Graph wrapper, flattened active node types, and a second active layout or camera state machine.
- [x] 1.5 Add failing browser contracts for full-workspace bounds, wheel zoom, pan, drag and pin, reflow, 2D/3D switching, hover, selection, filter reversal, toolbar/title collision, and active/legacy session restoration.
- [ ] 1.6 Add failing coherent-envelope fixtures for matching predecessor, qualified successor, missing domain fragments, partial Teaching Projection, mixed hashes, and identity-matched rollback.

## 2. Shared Force Graph runtime orchestration

- [x] 2.1 Extract a source-neutral runtime controller from `KnowledgeGraphSystem` that owns layout, force coordinates, fitting, gestures, cameras, hover, selection, filters, presentation links, and drawer coordination.
- [x] 2.2 Define typed active-root, active-domain, and legacy adapters plus namespace-scoped 2D/3D session stores without a label- or type-based identity bridge.
- [x] 2.3 Move the legacy path onto the extracted runtime without changing its data loader, explicit formula fields, interaction behavior, or isolated persisted state.
- [x] 2.4 Move the active domain semantic view onto the same runtime with exact registered node types, relation endpoints, direction, layers, Teaching order, resources, cross-domain entrances, and rich-label metadata.
- [x] 2.5 Remove the active-owned empty layout state, fixed grid positions, inert drag handler, fixed relayout version, reduced camera contract, and duplicate fit ownership.
- [x] 2.6 Extend the shared runtime only where active decorations or semantic labels need backward-compatible presentation primitives, and cover legacy non-regression for every extension.

## 3. Root and domain canvas completion

- [x] 3.1 Replace the active root static SVG with line-free navigation presentation nodes hosted by the shared Force Graph runtime.
- [x] 3.2 Prove root navigation identities cannot become Canonical object IDs, relation endpoints, semantic drawer objects, or legacy mappings.
- [x] 3.3 Replace the active graph card and `70vh` maximum with a `min-h-0` workspace canvas that fills the shell's actual remaining width and height.
- [x] 3.4 Connect complete 2D wheel/touch zoom, pan, drag/pin, reflow, selection, hover, fitting, and dynamic relation effects to active root and domain sessions.
- [x] 3.5 Connect complete 3D orbit, zoom, pan, camera fitting, camera restoration, drag, selection, hover, and dimension-specific coordinates while preserving the active semantic view.
- [x] 3.6 Validate collision size, hit testing, edge endpoints, labels, and camera fitting against dynamic node geometry with simultaneous resource markers, Knowledge Card star, and cross-domain halo.
- [ ] 3.7 Validate the largest admitted domain against bounded shard loading, force stability, frame responsiveness, and supported desktop and mobile viewport sizes.

## 4. Unified controls, filters, and inspection

- [x] 4.1 Consolidate mode, dimension, return-to-root, fit/reflow, search, relation filters, and node-type legend into the shared shell toolbar with collision-free placement.
- [x] 4.2 Keep the explicit `新版` / `旧版` switch while proving that each mode restores only its own data, loaded shards, coordinates, cameras, filters, selection, hover, and drawer.
- [x] 4.3 Make every available active node type visible by default and independently reversible without resetting coordinates, viewport, relations, selection, or loaded shards.
- [x] 4.4 Make every published Teaching family visible by default and every Engineering presentation family independently reversible without rewriting predicates or directions.
- [x] 4.5 Preserve bounded search, directory materialization, hover preview, cross-domain drawer entrances, rich text and mathematics, resource launch anchors, and unavailable-name exclusion through the shared runtime.

## 5. Coherent active-envelope and Teaching readiness

- [ ] 5.1 Extend `readKnowledgeSurface` and the existing `act-knowledge-surface/v1` response identity with a read-only latest-cutover verifier over the final coordinated active receipt, Authority snapshot and manifest, domain catalog and shards, complete Teaching Projection and composed fragments, prerequisites, formal resources, consumer activation, and Runtime active identity; do not add another assembler or cache authority.
- [ ] 5.2 Reopen and hash-verify every envelope member and fail readiness on missing members, mixed versions, stale pointers, partial or unavailable Teaching state, mismatched resources, or absent final active receipt.
- [ ] 5.3 Keep candidate and predecessor states truthful without interpreting a general projection pointer, database `KnowledgeLink`, engineering edge, local tree pointer, or legacy graph response as active Teaching proof.
- [ ] 5.4 Feed only version-matched Teaching relations, resource descriptors, cards, information graphics, and cross-domain entrances from the coherent envelope into the active semantic view.
- [ ] 5.5 Expose sanitized internal and product readiness evidence from the common knowledge-surface envelope containing the exact coherent identities and reasons for failure without leaking private payloads, credentials, signed URLs, or local absolute paths.
- [ ] 5.6 Verify current predecessor, qualified successor, provider rollback, missing fragment, partial projection, tampered member, and unknown-state behavior through unit and integration fixtures.

## 6. Provider handoff, deployment, and product cutover acceptance

- [ ] 6.1 Require the provider to capture the execution-time latest complete compatible ActKG composite once; verify its public Schema and consumer contract and stop for a separate adaptation change on incompatibility.
- [ ] 6.2 Reopen the provider's qualified immutable candidate and prove that Authority, complete Teaching Projection, domain fragments, prerequisites, resources, shards, consumer activation, Runtime Release, predecessor, and rollback identities close under one receipt.
- [ ] 6.3 Before implementation claim, verify that the provider has completed its authorized stopped-service transaction, final coordinated active receipt, production readback, and GitHub achievement; do not write or repair its selectors from this change.
- [ ] 6.4 Build the application image locally through `scripts/build.sh`, preserve OCI revision and provenance, deploy only through the ECS route, and qualify the shared runtime against the already active coherent successor.
- [ ] 6.5 After application deployment, re-read production selectors and immutable artifacts and prove `/knowledge`, Teaching relations, resource markers, readiness, app, and workers all resolve the same captured latest combination.
- [ ] 6.6 Run authenticated browser acceptance on production for root and representative domains in 2D and 3D, including full-workspace geometry, old-engine interactions, filters, hover, drawer, rich mathematics, resources, and absence of “教学关系暂不可用” where qualified relations exist.
- [ ] 6.7 Use the provider's immutable predecessor evidence and an identity-matched rollback rehearsal to prove the shared runtime can resume on the coherent predecessor without legacy-data fallback or false latest-cutover success; any real selector rollback remains provider-owned.
- [ ] 6.8 Record candidate, deployment, activation, product acceptance, and rollback evidence as distinct receipts; do not equate image deployment, database import, candidate recency, or selector presence with completed cutover.

## 7. Verification and delivery

- [ ] 7.1 Run the focused shared-runtime, active adapter, graph presentation, Teaching Projection, domain-shard loader, readiness, and coherent-envelope unit tests.
- [ ] 7.2 Run the affected Playwright graph suites with explicit layout and interaction assertions rather than component-presence checks.
- [ ] 7.3 Run `rtk npm run typecheck`, `rtk npm run lint`, the affected domain suites, `rtk npm run test`, `rtk npm run test:unit`, and `rtk npm run build` on the final intended revision.
- [ ] 7.4 Validate this change and `coordinate-latest-authority-and-active-oss-cutover` strictly and prove their normative requirements and write ownership do not overlap.
- [ ] 7.5 Update `docs/ProjectDescription.md` and operational handoff evidence with the actual shared-runtime architecture, active identities, verification scope, and truthful production outcome.
- [ ] 7.6 Obtain one independent implementation review focused on runtime equivalence, Authority/Teaching identity integrity, release safety, rollback, and test coverage; resolve accepted blocking findings once.
