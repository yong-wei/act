## 1. Freeze Batch B

- [x] 1.1 Record the ordered Batch-B lesson ids, route variants, feature
  adapters, loaders, plugin sets, simulation adapters, and direct callers.
- [x] 1.2 Capture ordinary, compute, visual, simulation, response-producing,
  Cruise, preview, unauthorized, drift, refresh, reconnect, and finish behavior.
- [x] 1.3 Verify C11 qualification and bind the Batch-B membership digest to
  the migration ledger.

## 2. Migrate routes and capabilities

- [x] 2.1 Route Batch-B entry pages through C10 and the existing shared
  Classroom shell/session application service.
- [x] 2.2 Migrate student, teacher, waiting, and demo paths while preserving
  class authorization, role projections, and route-visible errors.
- [x] 2.3 Migrate compute, visual, generated-slide, control-workbench,
  interactive-figure, and simulation modules through registered plugins and
  the existing Rust/runtime boundary.
- [x] 2.4 Verify durable submission evidence, distinct attempts, live-state
  isolation, teacher projection, optional-resource fallback, and preview
  non-write behavior.

## 3. Retire private authorities

- [x] 3.1 Re-run static, dynamic, route, plugin, test, and browser inventories;
  record any remaining compatibility caller.
- [x] 3.2 Delete private Batch-B route/session/component authorities only after
  zero required callers and no hidden fallback remain.
- [x] 3.3 Record behavior deltas, rollback boundary, batch digest, and deletion
  proof.

## 4. Verification and handoff

- [x] 4.1 Run focused Batch-B route, identity, session, manifest/plugin,
  simulation-boundary, submission/evidence, and live-state tests.
- [x] 4.2 Run affected domain suites, typecheck, lint, build, strict
  validation, and browser acceptance across representative role journeys.
- [x] 4.3 Hand off the closed route migration to renderer retirement and the
  M4 workspace stream.
