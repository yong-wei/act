## 1. 目录工件与完整性基线

- [ ] 1.1 在 #1392 先交付只含受治理 `kn:` 主知识节点的最终选项级归因后，基于该输入和 #1391 的固定 practice 基线派生全部学习目标集合，并核验每个目标的唯一规范主节点；`cap:` 值必须作为上游输入无效而非兼容转换。
- [ ] 1.2 在 runtime 资源治理目录新增版本化目标—节点目录，记录显式别名、来源依据、节点投影修订和启用状态。
- [ ] 1.3 为目录加载增加结构、唯一性、节点活动状态、来源和版本漂移校验，拒绝不满足目录契约的工件。

## 2. 统一解析与编排接入

- [ ] 2.1 实现返回规范节点或稳定失败码的单一目标目录解析入口，覆盖规范标识、显式别名、未知和歧义输入。
- [ ] 2.2 将错误选项归因改为使用目录解析，并移除稳定裕度专用映射；当唯一 K/A/Q `kn:` 节点与目录不一致，或上游提供 `cap:` 节点时保持受控不确定状态。
- [ ] 2.3 将目录工件纳入覆盖审计的受提交输入捕获，并在资源和验证题查询前校验选项归因与目录节点的一致性。
- [ ] 2.4 为目录缺失、歧义、停用、漂移和选项归因冲突增加可定位审计原因，并使严格模式 fail closed。

## 3. 回归测试与审计验证

- [ ] 3.1 为目录解析补充合法目标、合法别名、未知目标、歧义别名、重复映射、停用节点和版本漂移的单元测试。
- [ ] 3.2 扩展错误归因与覆盖审计测试，验证目录冲突不能产生可用微辅导任务，且资源与验证题不会收到未核验节点。
- [ ] 3.3 在 #1392 合入且全部主节点均已核验为 `kn:` 的最终输入上运行离线和严格覆盖审计，核验全部 54 道合格 practice 题的目标映射与 108 个错误选项归因一致。

## 4. 变更门禁

- [ ] 4.1 运行受影响的 Vitest 用例、微辅导覆盖命令、`npm run typecheck` 和 `git diff --check`。
- [ ] 4.2 运行 `openspec validate add-micro-tutoring-goal-node-catalog --type change --strict` 并记录结果。
