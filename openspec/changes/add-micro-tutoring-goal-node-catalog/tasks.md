## 1. 目录工件与完整性基线

- [x] 1.1 基于 #1392 已审核目标集合和 #1391 的固定 practice 基线派生全部学习目标集合，核验每个目标的唯一规范主节点，并重生成只含 `kn:` 主节点的选项归因；`cap:`/`qual:` 值不做运行时兼容转换。
- [x] 1.2 在 runtime 资源治理目录新增版本化目标—节点目录，记录显式别名、来源依据、节点投影修订和启用状态。
- [x] 1.3 为目录加载增加结构、唯一性、节点活动状态、来源和版本漂移校验，拒绝不满足目录契约的工件。

## 2. 统一解析与编排接入

- [x] 2.1 实现返回规范节点或稳定失败码的单一目标目录解析入口，覆盖规范标识、显式别名、未知和歧义输入。
- [x] 2.2 将错误选项归因改为使用目录解析；当目录无效、目标不可解析或选项归因节点与目录不一致时保持受控不确定状态。
- [x] 2.3 将目录工件纳入覆盖审计的受提交输入捕获，并在资源和验证题查询前校验选项归因与目录节点的一致性。
- [x] 2.4 为目录无效、目标不可解析和选项归因冲突增加可定位审计原因，并使严格模式 fail closed。

## 3. 回归测试与审计验证

- [x] 3.1 为目录解析补充合法目标、合法别名、未知目标、歧义别名、重复映射、停用目标、节点域无效和版本漂移的单元测试。
- [x] 3.2 扩展错误归因与覆盖审计测试，验证目录冲突不能产生可用微辅导任务，且资源与验证题不会收到未核验节点。
- [x] 3.3 在全部主节点均已核验为 `kn:` 的最终输入上运行离线和严格覆盖审计，核验全部 54 道合格 practice 题的目标映射与 108 个错误选项归因一致。

## 4. 变更门禁

- [x] 4.1 运行受影响的 Vitest 用例、微辅导覆盖命令、`npm run typecheck` 和 `git diff --check`。
- [x] 4.2 运行 `openspec validate add-micro-tutoring-goal-node-catalog --type change --strict` 并记录结果。
- [x] 4.3 根据正式审核补充目录级与记录级来源身份核验、解析结果来源证据及来源漂移 strict-fail 回归，并重新运行 current-head 门禁。
- [ ] 4.4 根据首次 Codex Review 补充完整目录项—内容哈希—审核目标—规范节点—记录哈希绑定核验，阻断使用真实目录项伪造目标的来源记录，并重新运行 current-head 门禁。
