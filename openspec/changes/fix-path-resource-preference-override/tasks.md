# Tasks

- [ ] 1. 生成面板状态区分「未选择」与「用户已选择」（URL 参数视为已选择）；默认态生成请求不携带 `resourcePreference`
- [ ] 2. 服务端偏好解析链加入画像层：`request → profile → intent → system-default`，`resourcePreferenceSource` 扩展 `profile` 来源；画像不可用或证据未达门槛时下落并如实标注
- [ ] 3. 解析出的偏好接入资源排序消费点，画像偏好可观察地提升对应资源类型在候选路径资源组合中的占比
- [ ] 4. 生成结果与推荐依据携带偏好来源，页面展示「用户选择 / 画像推断 / 系统默认」来源说明
- [ ] 5. 回归测试：默认不提交、画像偏好生效、显式选择覆盖三场景 + 偏好切换对照的资源组合差异断言；运行 `typecheck` 与相关套件
