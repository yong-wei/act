## 1. Foundation Worklist

- [x] 1.1 Build bounded core-node and relation candidate worklists for system modeling, time-domain analysis and stability analysis. The fixed denominator is only the four live Authority nodes named in design.md; record source, target, strength, exact evidence path, directness, status and unpublished reason for every candidate.
- [x] 1.2 Review candidates from current objectives, primary resources, eligible cards and explicit teacher curation. Accept only the direct 2-2 REQUIRED edge 单位阶跃响应 → 调节时间; leave 增益 → 稳定性 and 稳定性 → 调节时间 unresolved.

## 2. Publication

- [x] 2.1 Author the accepted direct REQUIRED relation with immutable ACT evidence on an independent `foundation-three-domain-v1` fragment. Do not rewrite `first-fragment.authoring.json`, `first-fragment.json` or the first-only `composed-manifest.json`.
- [x] 2.2 Generate the three-domain fragment and non-blocking coverage report from the existing complete live Authority envelope helper and builder contract. Endpoints may be resolved only from that envelope.
- [x] 2.3 Validate endpoint closure, deterministic identity and REQUIRED-edge acyclicity. The two-fragment fixture is validation-only: rebuild retained #1370 authoring against the shared five-plus-four canonical union (eight unique live nodes after deduplicating shared stability) and compose it with the new fragment; do not compose or replace persisted #1370 artifacts or the first-only current manifest.

## 3. Verification

- [x] 3.1 Prove unresolved candidates remain unpublished and engineering relations are not converted to prerequisites.
- [x] 3.2 Run teaching-projection content tests and strict OpenSpec validation. Tests must also prove first-fragment bytes/hashes are unchanged and two-fragment composition keeps both derived fixture fragments/provenance without silent collision; no runtime/store/pointer/activation path is changed.
