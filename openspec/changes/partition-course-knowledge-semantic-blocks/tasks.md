## 1. Implement semantic partitioning

- [ ] 1.1 Add the new read-only manifest CLI consuming only validated identity-component and domain-candidate manifests.
- [ ] 1.2 Exclude course/module and navigation pollution, then partition by identity semantics, text, and relation connectivity.
- [ ] 1.3 Preserve each equivalence component intact with one owner; retain pending splits inside that component.
- [ ] 1.4 Emit the complete shared future-child schema, including typed exact items, independent `candidate_concept_count`, owner/applicable endpoints, change-ID dependencies, scope anchors, capacity exceptions, and all four digest layers.
- [ ] 1.5 Emit per-component card/visual work seeds and a split-invalidation rule that regenerates one `canonical_card_review` and one `visual_suitability_review` for every resulting canonical concept, including missing-card and visual-not-applicable cases.

## 2. Verify the deliverable

- [ ] 2.1 Add synthetic exact/alias leakage, ownership gap, duplicate ownership, pollution, missing-card/visual-work, capacity-exception, and prototype-rejection fixtures.
- [ ] 2.2 Add a fixed real-snapshot integration test without fixing block count.
- [ ] 2.3 Prove deterministic repeated runs and no writes.
