## 1. Teacher cumulative portrait delivery

- [x] 1.1 Select and project the latest valid native portrait v2 in the
  teacher student detail as the primary cumulative capability result.
- [x] 1.2 Retain recent scoped facts only for diagnostic evidence and preserve
  explicit no-evidence behavior when no native portrait exists.
- [x] 1.3 Keep revoked portraits suppressed and preserve missing class
  comparison values as unavailable.

## 2. Cumulative class insight isolation

- [x] 2.1 Prevent cumulative class-insights requests from building the recent
  compatibility portrait map.

## 3. Verification

- [x] 3.1 Add focused regression tests for cumulative teacher detail,
  no-evidence detail, and cumulative requests with scoped facts.
- [x] 3.2 Run OpenSpec strict validation, focused tests, typecheck, and the
  required review before the pull request.
