## 1. Shared manifest contract

- [ ] 1.1 Extend shared manifest types and schemas with BOPPPS stage/step timing, fixed 16:9 layouts, named slots, size variants, text budgets, and role-safe activity metadata.
- [ ] 1.2 Register the finite P0 layout-template and module-size catalog with deterministic occupancy and compatibility rules.
- [ ] 1.3 Register the generated content/activity allowlist and six canonical response kinds without dynamic module registration.

## 2. Rendering and compatibility

- [ ] 2.1 Implement the fixed 16:9 step canvas and whole-canvas responsive scaling in the shared lesson renderer.
- [ ] 2.2 Implement bounded font adaptation, suggested-length warnings, and explicit unfit states without clipping, scrolling, or hidden overflow.
- [ ] 2.3 Preserve existing preset lesson rendering and add manifest/renderer regression fixtures for legacy paths.

## 3. Deterministic validation

- [ ] 3.1 Implement the pure static validator with stable issue codes for hierarchy, timing, bounds, allowlist, schemas, occupancy, size, text, and role metadata.
- [ ] 3.2 Implement pinned-browser, pinned-font, fixed-viewport validation for bounds, overlap, clipping, scroll overflow, formula width, and minimum font size.
- [ ] 3.3 Add content-hash invalidation and machine-readable stage/step/module issue locations.
- [ ] 3.4 Run schema, registry, renderer, validator, legacy compatibility, typecheck, and strict OpenSpec tests; record AC evidence.
