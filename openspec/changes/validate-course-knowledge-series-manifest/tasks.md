## 1. Implement closeout validation

- [ ] 1.1 Add the new read-only manifest CLI and discriminated future-child schema for all six work kinds.
- [ ] 1.2 Validate algorithm/normalization versions, typed counts/items, namespace-aware deduplication, field applicability, owners, cross-block endpoints, required outputs, the closed work-kind acceptance-profile registry, semantic-profile/card/in-block-relation phase order, and scope anchors.
- [ ] 1.3 Validate separate governance-contract, source-snapshot, structured upstream-manifest, and per-source digests; emit expected/observed drift.
- [ ] 1.4 Validate change-ID `blockedBy` closure, including every endpoint semantic-block dependency for cross-identity, cross-relation, and resource-binding work, declared references, acyclicity, privacy, and no-waiver rules.
- [ ] 1.5 Emit only a deterministic report and frozen candidate future-series manifest.
- [ ] 1.6 Require stage-one card/visual seeds per component, then require one canonical-card and one visual-suitability review item per final canonical concept with split-triggered exact-closure regeneration.
- [ ] 1.7 Validate registry fields against Prisma DMMF, wholly synthetic path/event shape-equivalent fixtures plus controlled database-side historical shape validation, transaction/export snapshot proof, diagnosis/portrait/risk/growth/recommendation/class semantic invariants, five-person privacy suppression, and bidirectional full-root AST writer equality. Repository outputs are limited to shape/version/error codes and suppressed counts.
- [ ] 1.8 Require typed cutover items for history/path migration, immutable knowledge-truth revision generation and fact backfill, projection import, DB-only read-model switch, and every direct writer with one allowed disposition; validate polymorphic `sourceLogId` namespace resolution across InteractionLog, SimulationLog, UserAnswer, AIIntervention, PromptAssessment, SimulationRun, AgentToolRun, ArenaSubmission, AdaptiveAssessmentAnswer, and non-relational path event keys, governed source-ID precedence, UTC-millisecond event-time normalization, conflict blocking, and unknown/no-ID unresolved behavior for many-to-one evidence deduplication.

## 2. Verify the deliverable

- [ ] 2.1 Add synthetic valid, zero-queue, wrong-kind, missing-field, missing-card/visual, DMMF drift, JSON-shape, writer-gap, split-snapshot, same-name definition/relation/resource-binding revision changes, legacy-unversioned fact backfill, many-to-one evidence deduplication with source-ID/time/role/lineage conflicts and no-ID unresolved cases, unknown/mismatched acceptance-profile, semantic-phase bypass, missing endpoint-semantic-block dependency, namespace-collision, stale-digest, ownership gap, endpoint, cycle, 1–4-person privacy, and waiver fixtures.
- [ ] 2.2 Add a fixed real-snapshot full-series integration test.
- [ ] 2.3 Prove byte-identical repeated runs and no writes or stage-two object creation.
