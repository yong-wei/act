## 1. Freeze the active locale gap

- [ ] 1.1 Record the exact v0.37 Authority/snapshot/catalog/language artifacts, current v0.22-only registry, absent ACT locale manifest and historical runtime capability.
- [ ] 1.2 Add failing tests for missing v0.37 registration, disabled English, seven-domain denominator failure and the stale test that expects active English to remain unavailable.
- [ ] 1.3 Verify the upstream v0.37 locale, localized-content, rich-text, formula and accessibility artifacts and enumerate their exact hashes/contracts.

## 2. Build offline v0.37 qualification

- [ ] 2.1 Add an explicit versioned adapter from the exact upstream v0.37 locale artifacts to ACT's normalized manifest without following mutable `latest`.
- [ ] 2.2 Independently reconstruct the complete fifteen-domain presentation denominator and verify object, explanation, type, relation, direction, alias, source, rich-text, formula and accessibility coverage.
- [ ] 2.3 Register the exact v0.37 composite identity and seal Chinese/English receipts, manifest digest, interface-catalog digest and graph closure in one immutable qualification package.
- [ ] 2.4 Reject missing, duplicate, unsafe, excluded-source, cross-release, denominator-drift, slot-drift and partial-locale evidence.

## 3. Make runtime qualification bounded

- [ ] 3.1 Replace v0.22-specific published constants with exact active-identity qualification lookup that never mutates selectors.
- [ ] 3.2 Make ordinary requests verify only the small immutable qualification package and bounded requested projection.
- [ ] 3.3 Add cold-process I/O and latency gates proving root/domain requests do not traverse every neighborhood/detail or parse the complete locale corpus.

## 4. Implement atomic language switching

- [ ] 4.1 Refresh root and every currently loaded logical shard under one locale generation with bounded parallelism and identity checks.
- [ ] 4.2 Commit all localized display records atomically while preserving domain, topology, force coordinates, camera, filters, selection, inspector section/scroll and resource bindings.
- [ ] 4.3 Keep the prior complete locale on any failed or stale response and prevent mixed-language frames, Chinese fallback in English, raw keys and cross-release values.
- [ ] 4.4 Switch root, concepts, secondary nodes, relations, formulas, search, filter panel, hover, inspector, optional states and accessibility together.

## 5. Verify the change

- [ ] 5.1 Run direct adapter, qualification, receipt, request-gate, shard-projection, race and state-preservation tests plus the full locale domain suite.
- [ ] 5.2 Run real browser Chinese-to-English-to-Chinese checks on a loaded 2D/3D graph with Formula, filters and inspector across desktop/mobile and all roles.
- [ ] 5.3 Run typecheck, lint, full `npm run test`, build, commercial UI governance and strict OpenSpec validation on the final clean revision.
- [ ] 5.4 Obtain independent review of exact-version provenance, complete denominator, no runtime full scan, no fallback and no selector/learner-state mutation.
