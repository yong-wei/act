## 1. 可信能力贡献边界

- [x] 1.1 在 LearningFact 物化模块增加不可序列化的服务端授权标记，并使未授权事件写入空能力贡献。
- [x] 1.2 在已完成持久化、计分和治理核验的自适应测评路径授予能力贡献资格。
- [x] 1.3 确认教师审核和其他直接服务端物化路径不经过客户端授权边界。

## 2. 回归测试与验证

- [x] 2.1 增加伪造客户端贡献的事实与 Portrait v2 无增量回归测试，并保留正常互动字段断言。
- [x] 2.2 增加服务端已核验自适应测评仍产生能力贡献的回归测试。
- [x] 2.3 执行相关 Vitest、OpenSpec 严格校验和 TypeScript 类型检查；P2 复核已在当前提交重新执行原始 `npm run typecheck` 和 `npm run test:data-governance`，均通过。

## 3. 审查修正

- [x] 3.1 为通过历史来源、真实性与资格门禁的服务端记录增加显式授权，并持续拒绝 `InteractionLog`。
- [x] 3.2 覆盖服务端历史事实保留贡献与历史 `InteractionLog` 为空贡献的回归。
