## 1. Signal expansion

- [ ] 1.1 Add experiment-safety normative markers (`安全规范`/`安全规程`) to the shared normative query markers.
- [ ] 1.2 Add `教材` to the authoritative source terms and share the standard-number regex with the primary classifier's normative branch.
- [ ] 1.3 Add the code-fence × (resolution markers ∪ defect) composite signal to the debugging branch.

## 2. Regression

- [ ] 2.1 Add the V2 tiered-bank table-driven regression with per-intent/per-difficulty confusion matrix reporting.
- [ ] 2.2 Assert the 90%/80%/90% gates and guard against question-text hardcoding.
- [ ] 2.3 Re-run the frozen #1816/#1903/#1948 intent regressions without any case flips.

## 3. Verification

- [ ] 3.1 Run focused tests, typecheck, strict OpenSpec validation and `git diff --check`.
