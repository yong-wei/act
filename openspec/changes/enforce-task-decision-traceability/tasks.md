## 1. 任务决策交付合同

- [x] 1.1 将 Use-Grill-Me 包装技能、作用域解析器和其测试纳入项目技能目录，并规定任务目录的读取、决策写回、清单生成和 OpenSpec/本地计划分流。
- [x] 1.2 为 #995 创建符合合同的任务清单，关联 Grill 决策目录、本 OpenSpec change 和本次实施范围。

## 2. 校验实现与接入

- [x] 2.1 实现无第三方依赖的任务决策交付校验器，支持 `--staged` 与 `--base <ref>`，并报告无效清单、未覆盖文件和范围冲突。
- [x] 2.2 将暂存区校验接入 `verify:commit`，保持现有 managed hook 的调用链不变。
- [x] 2.3 新增轻量 PR workflow，以目标分支为基线运行完整范围校验，不改变现有发布型 CI 的触发范围。

## 3. 验证

- [x] 3.1 添加校验器测试，覆盖有效 OpenSpec 清单、无 OpenSpec 计划、未覆盖实施文件、重叠范围与跨提交 PR 差异。
- [x] 3.2 运行针对性测试、`verify:commit` 和 OpenSpec 严格校验。
- [x] 3.3 记录实现 PR 面向 `integration` 与使用已配置 Codex 审查请求的交付约束。
