## 1. 任务归一化投影

- [ ] 1.1 在既有 portrait v2 物化器中读取全局当前学生任务目录和受治理任务证据，计算 `simulationValidationEvidence` 的等权完成任务分子、分母、证据时间和限制说明。
- [ ] 1.2 以来源定义的结果排序先选择同一 artifact 的最高贡献结果，再处理证据层级同分；将部分进度、低层过程、重复产物、旧 `param_change`、已下线任务和无当前目录任务的情形明确为不贡献或无证据。
- [ ] 1.3 为任务比例、任务唯一贡献、同一产物多结果的顺序无关、全局目录分母和无证据结果补充定向单元测试。

## 2. 单学生增量物化与历史应用

- [ ] 2.1 将实时合格任务证据接入既有单学生 portrait 请求、事务锁和 generation fencing，不改变其他六个维度。
- [ ] 2.2 将已接受历史候选的应用接入同一单学生路径，并验证重复候选、重试与同一学生多条事实不会重复贡献。
- [ ] 2.3 将事实水位、目录摘要、计算版本和历史候选 plan 摘要纳入任务投影的输入身份、事务内复核和 current portrait 选择；输入漂移时 fail closed。
- [ ] 2.4 实现目录发布、下线或完成谓词变动对所有已有仿真任务投影或合格任务证据学生的增量更新，不重放来源产物或创建 LearningFact。
- [ ] 2.5 提供学生自更新与既有班级授权教师更新学生画像的受限入口，返回异步请求状态和目标 generation，并验证客户端不能传入任务或分数。

## 3. 读模型、班级聚合与页面组成

- [ ] 3.1 扩展学生 learner-state 和授权教师详情响应，返回第七维的完成数、全局总数、来源分组、计算版本和限制说明。
- [ ] 3.2 由当前 roster 的可用个人任务比例算术平均刷新累计班级仿真汇总，并返回 coverage；缺失个人或班级值时保持不可用，不填零或推断趋势。
- [ ] 3.3 在既有学生与教师画像页面展示可展开的 Odyssey、Arena、控制工作台和虚拟仿真二元任务组成，不显示过程进度或近似得分，也不重做页面视觉层级。
- [ ] 3.4 在学生成长页和教师学生详情页实现独立更新画像动作的已提交、处理中、失败重试和完成后刷新状态；保留既有刷新数据为纯读取。
- [ ] 3.5 为角色授权、隐私摘要、班级成员变化、缺失班级均值、无证据、异步更新状态及展开控件的键盘/读屏行为补充 API 或浏览器回归测试。

## 4. 验证与回填演练

- [ ] 4.1 在代表性历史候选上演练应用、单学生 portrait 更新与当前 roster 班级聚合，核对任务数、分母和幂等结果。
- [ ] 4.2 运行任务投影、learner-state、教师画像、数据治理和页面定向测试。
- [ ] 4.3 运行 TypeScript 类型检查与 `openspec validate surface-task-normalized-simulation-portraits --type change --strict`。
