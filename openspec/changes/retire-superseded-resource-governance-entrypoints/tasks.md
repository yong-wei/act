## 1. Preconditions and candidate denominator

- [ ] 1.1 Verify the completed R1 RegistryIndex, R2 eligibility, and R3 knowledge read-contract identities and their affected-domain verification.
- [ ] 1.2 Inventory every superseded identity, aggregate readiness/eligibility, registry-read, and knowledge-resource projection entrypoint with owner, source path/export, semantic role, and replacement candidate.
- [ ] 1.3 Close production/test/generated/compatibility/framework, route/API, model, script, browser, reverse, dynamic, historical, and rollback caller denominators at one clean revision.
- [ ] 1.4 Mark protected Legacy, history, snapshot, crosswalk, audit, rollback, Runtime Release, Teaching Projection, and #1498/#1503/#1509/#1515/#1543 surfaces.

## 2. Retirement contract

- [ ] 2.1 Define `ResourceGovernanceRetirementManifest` with candidate, denominator, replacement, migration revision, zero-caller, protected-surface, rollback, and reviewer identities.
- [ ] 2.2 Implement `verifyResourceGovernanceRetirement` to recompute all identities and reject missing, mixed, stale, unauthorized, or expanded inputs.
- [ ] 2.3 Define exact deletion and post-delete receipts; prohibit directory/glob deletion, unknown callers, permanent facades, and selector/release writes.
- [ ] 2.4 Define monotonic comparison for the architecture allowlist and deprecation ledger; reject additions, broadening, or unexplained state reversal.

## 3. Revision-bound migration proof

- [ ] 3.1 Migrate every candidate caller to the R1 RegistryIndex, R2 dimensioned evaluator, or R3 composite knowledge read contract as applicable.
- [ ] 3.2 Compare old/new identity, source ownership, role, authorization, scope, revision, optional degradation, formal failure, cache, and response behavior at the captured revision.
- [ ] 3.3 Re-run static, string/config, route/framework, generated, reverse, dynamic, test, and rollback scans and prove zero callers to each candidate.
- [ ] 3.4 Rehearse digest-verified restoration of the exact pre-delete revision and verify protected surfaces remain reachable.

## 4. Narrow deletion and evidence

- [ ] 4.1 Delete only the exact entrypoints named by a fully valid manifest; retain candidates with any caller or unresolved protection condition.
- [ ] 4.2 Run post-delete import/build/route/unit/browser checks and record the reduced edge/entry count and ledger identity.
- [ ] 4.3 Prove no Legacy display, historical evidence, rollback archive, immutable Runtime/Teaching Projection reader, or protected issue contract was removed.
- [ ] 4.4 Publish the deletion, zero-caller, rollback, and monotonic-ledger receipts without claiming production activation or broad cleanup.

## 5. Verification

- [ ] 5.1 Add manifest, denominator, replacement-parity, zero-caller, protected-surface, race, no-facade, monotonicity, rollback, and post-delete tests.
- [ ] 5.2 Run affected resource/knowledge API and feature suites plus `rtk npm run typecheck`.
- [ ] 5.3 Run `rtk openspec validate retire-superseded-resource-governance-entrypoints --type change --strict` and `git diff --check`.
