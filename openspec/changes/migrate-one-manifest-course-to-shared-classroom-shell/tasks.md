## 1. Upstream and denominator

- [ ] 1.1 Verify the qualified CourseBundle, Classroom application-service, live/evidence, and manifest-plugin dependencies and record coordination with `add-default-teacher-class-launch-selection` and `unify-interactive-lesson-component-style`.
- [ ] 1.2 Freeze the `unit-1-2-modeling-from-object-to-system` runtime, entry, student, teacher, waiting, route, component, session, submission, projection, media, knowledge-card, and direct-caller denominator.
- [ ] 1.3 Characterize canonical links, session creation/join/access, submit/resubmit, reveal/review, refresh/reconnect, media/card fallback, accessibility, generated publication revision, and manifest hash.

## 2. Shared shell integration

- [ ] 2.1 Define the pilot shared-shell composition inputs and bind course runtime identity, bundle revision, manifest plugin set, and session application use cases through public capabilities.
- [ ] 2.2 Migrate teacher, student, and waiting views to the shared create/join/read/advance/end/access/stream mechanisms without changing the established access policy or duplicate-session behavior.
- [ ] 2.3 Bind every pilot runtime read to the captured bundle identity; remove authoring reads and title guessing from the classroom path.
- [ ] 2.4 Wire the pilot to the plugin, live-state, evidence, and common submission contracts without adding direct route-owned session policy.
- [ ] 2.5 Preserve DB BOPPPS, generated-courseware publication binding, optional media/knowledge-card soft degradation, and the common evidence format.

## 3. Private authority deletion

- [ ] 3.1 Produce a zero-consumer inventory for the pilot private route, private classroom components, direct session calls, title aliases, old renderer imports, and test/browser callers.
- [ ] 3.2 Remove the pilot private route and component authorities after caller migration; retain no permanent redirect or duplicate policy implementation.
- [ ] 3.3 Publish the pilot qualification ledger and revision-bound removal receipt as input to private-route retirement; do not claim deployment or production activation.

## 4. Targeted, domain, and browser verification

- [ ] 4.1 Run identity, bundle/session, application-service, live/evidence, plugin, and pilot characterization tests at the focused scope.
- [ ] 4.2 Complete the teacher waiting, student join, submit/resubmit, reveal/review, refresh/reconnect, finish, and preview browser journeys, including optional-resource failure checks.
- [ ] 4.3 Run the affected Interactive/Classroom domain suites, typecheck, lint, build as required by the changed paths, strict OpenSpec validation, and `git diff --check`.
