# Tasks

## 1. 修复共享相机竞态

- [ ] 1.1 编写失败优先测试：模拟控制器初始化后默认相机对象被替换，断言机位重新锚定到当前预设视角而非停留在替换后相机的默认位置。
- [ ] 1.2 在 `StayPutCameraController` 跟踪相机对象身份，身份变化且当前为预设视角时重新锚定一次；自由视角与已有用户偏移语义不变。
- [ ] 1.3 复跑全部管线挂载场景的相机停留语义与视角持久化测试，证明无回归。

## 2. 修复模型装配缺陷

- [ ] 2.1 编写失败优先测试：含 skins 的模型场景克隆后骨骼绑定保持完整（骨骼在克隆树内、skinned 网格绑定姿态正确）。
- [ ] 2.2 `DestroyerModelScene` 与 QA 装配改用 `SkeletonUtils.clone`；旧模型（无 skins）渲染行为不变。
- [ ] 2.3 QA 页按模型包围盒配置相机取景，首帧整舰完整可见。

## 3. 接收 v2.1.0 发布包

- [ ] 3.1 扩展接收脚本支持 manifest schema `type055-versioned-model-release/2` 与七角色分母（含 interactive-systems），覆盖逐文件 SHA-256、字节数、角色重复与部分接收拒绝的失败优先测试。
- [ ] 3.2 将 v2.1.0 包原子接收入 `public/assets/model-releases/type055-nanchang-101/v2.1.0/`，生成只读描述符与接收收据，证明复制前后字节一致。
- [ ] 3.3 按 v2.1.0 `runtime-interface.json` 与 `model-validation.json` 重新登记语义接口合同（动画、节点、装填计数），漂移即 fail closed。

## 4. 接入 v2.1.0 候选并视觉验收

- [ ] 4.1 候选路径切换为 v2.1.0 描述符；meshopt 解码与量化包围球处理（主模型关闭 frustumCulled）延续生效。
- [ ] 4.2 浏览器视觉验收：QA 页与 destroyer 场景高/中/低三档整舰可见（包围盒投影断言 + 截图矩阵），skinned 网格渲染正确。
- [ ] 4.3 回退路径视觉验收：候选全部失败时旧模型整舰同样可见；驱动链回归测试证明无语义变化。

## 5. 生产激活

- [ ] 5.1 按 browser-delivery 流程发布 v2.1.0 资产并更新 registry，destroyer 默认主候选切换为 v2.1.0 包，旧链降级为有序回退。
- [ ] 5.2 移除 `?model=type055-v2` 候选开关；默认路径请求账本只含当前档位 ship LOD。
- [ ] 5.3 浏览器流量与质量降档门禁复跑通过；v2.0.0 候选目录转为仅历史可审计，旧 `destroyer.glb` 保留为回退不删除。

## 6. 验证与交接

- [ ] 6.1 运行模型包验证、相关单元测试、`npm run typecheck` 与受影响测试套件，记录 exact-current-HEAD 证据。
- [ ] 6.2 输出激活收据：发布版本、registry 变更、视觉验收证据矩阵、回退验证结果。
- [ ] 6.3 若暴露模型资产缺陷，按既有模板记录版本、哈希与复现，返回 3DModels 发新版本，候选保持未激活。
