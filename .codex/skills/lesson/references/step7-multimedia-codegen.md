# Step 7A：代码直出媒体规格

> 适用范围：`Octave` 生成的控制图、`tikz-control-draw` 线框图、互动前端绘制需求。

## 适用资源类型

- 时域响应、频域响应、根轨迹、Bode/Nyquist、极点零点、稳定域、参数扫描图
- 方框图、信号流图、电路图、机械结构图
- 互动课中需要随参数实时更新的图表或工作区

## 输出格式

### 代码直出图（控制仿真/计算类）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
资源 {用途前缀}-{序号} | {简短描述}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件名：{单元编号}-{前缀}-{序号}-{描述}.m / .svg
存放：raw/{单元编号}-{前缀}-{序号}-{描述}.m → processed/{单元编号}-{前缀}-{序号}-{描述}.svg
引用于：{文档名} § {章节/步骤}

【图片内容描述】
{说明图片要呈现的内容、坐标轴、曲线含义、标注要求等}

【示例代码（MATLAB/Octave）】
```matlab
{可直接运行的示例代码}
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**硬约束：**

- 时域响应、频域响应、根轨迹、Nyquist/Bode、极点零点、稳定域、参数扫描等控制系统图，默认必须使用 `Octave` 完成计算、仿真与出图。
- 默认优先使用 `Octave` / control 包的原生高层函数完成出图和核验，例如 `step()`、`bode()`、`nyquist()`、`rlocus()`、`lsim()`、`margin()`；除非原生函数确实无法覆盖，否则不得手写底层数值积分、频率扫描或同类替代函数。
- 不得为阶跃响应、频率响应或性能指标等基础能力自造底层数值计算函数。
- 若讲义、互动课或信息图引用同一张控制图，优先复用同一产物，不要重复出图。
- 讲义正文或附录若提供参考代码，默认也应与这里保持一致，统一使用 MATLAB/Octave 形式的 `.m` 文件。

### TikZ 线框图

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
资源 {用途前缀}-{序号} | {简短描述}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件名：{单元编号}-{前缀}-{序号}-{描述}.png
存放：raw/{单元编号}-{前缀}-{序号}-{描述}.tex（或技能默认中间文件） → processed/{单元编号}-{前缀}-{序号}-{描述}.png
引用于：{文档名} § {章节/步骤}

【图片内容描述】
{说明结构组成、连线方向、标注要求、是否为反馈结构/并联系统/电路/机械结构等}

【生成要求】
- 调用 `tikz-control-draw` 技能
- 按对应图类读取其 reference 文件
- 输出符合该技能默认规范的 PNG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**硬约束：**

- 方框图、信号流图、电路图、机械结构图一律归入此类。
- 不得用普通 matplotlib、截图或手绘占位替代。

### 互动前端绘制需求

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
资源 ic-{序号} | {简短描述}（前端绘制）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
不产生媒体文件，在 interactive-page.md 对应步骤中标注。
引用于：interactive-page.md 步骤 {步骤ID}

【交互需求描述】
- 图表类型：{折线图 / 相平面图 / 极坐标图 / ...}
- 可变参数：{参数名，范围，控件类型（滑块/输入框）}
- 输出量：{Y轴含义，单位，初始值}
- 交互行为：{参数变化时图表如何响应}
- 推荐前端库：{Recharts / Canvas API / D3 / ...}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 代码直出媒体的额外提醒

- 若互动课中的图只是静态展示，优先沿用讲义已验证产物，不另起前端绘制。
- 每张导出图都必须检查坐标轴、图例、文字、箭头与留白，避免“内容对但版式拥挤”的半成品。
- 若信息图中需要嵌入控制图、结构图或参数图，仍按本文件对应类型分别制作，不要把信息图当成豁免区。
