# 3-7 讲义与媒体修订执行计划

> 目标：修订 `course-content/authoring/lessons/3-7/`，使讲义满足当前 Markdown/PDF 导出规范，并补齐本课所需图示与例题。

## 修订目标

1. 修复 `handout.md` 中不符合当前渲染链的公式编号与交叉引用写法。
2. 去除“引用资源库/课件”的行文口径，改成直接教学叙述。
3. 将直接引用资源库图片的内容迁移为本课自有媒体，并避免透明背景与非规范框图。
4. 在终值定理后补一个“多项式输入稳态误差”例题，并在静态误差系数法后再次用该方法求解同题。
5. 将 `PI` 与滞后校正改为本课内独立章节：先讲概念，再分别给出“给定时域指标设计”和“给定频域指标设计”的例题，并在相关位置配响应图/频率特性图。
6. 运行必要脚本与导出检查，确认讲义、媒体与渲染结果一致。

## 影响文件

- 修改：`course-content/authoring/lessons/3-7/design/handout.md`
- 修改：`course-content/authoring/lessons/3-7/design/multimedia.md`
- 修改或重写：`course-content/authoring/lessons/3-7/media/raw/3-7-steady-error-validation.m`
- 新增：`course-content/authoring/lessons/3-7/media/raw/` 下必要的绘图脚本
- 新增：`course-content/authoring/lessons/3-7/media/processed/` 下本课图示
- 如有必要，补充：`docs/ProjectDescription.md`

## 执行步骤

### 步骤 1：现状核对

1. 检查 `handout.md` 中所有 `\label`、`\ref`、资源库图片直链、透明背景风险与资源表述口径。
2. 检查 `multimedia.md` 与现有 `3-7-steady-error-validation.m` 能否支撑新例题与配图。
3. 列出需要保留、替换、新增的图示清单。

### 步骤 2：重构讲义结构

1. 保留“误差来源 -> 双通道传函 -> 终值定理 -> 型别/误差系数”的主线。
2. 把 `PI` 与滞后校正从原来的概念小节重构为独立章节。
3. 将不支持的交叉引用全部替换成直接编号表述，如“式（3-7-1）”“图 3-7-2”。

### 步骤 3：补例题与说明

1. 在终值定理后新增一个多项式输入稳态误差例题，先用终值定理求解。
2. 在静态误差系数法部分再次回到同一题，用型别与误差系数法快速求解。
3. 将后续单独例题融合进 `PI` 与滞后校正章节，减少重复。

### 步骤 4：补媒体与脚本

1. 为误差来源、双通道框图、终值定理流程图、本课例题时域响应图、Bode 图分别准备本课自有图片。
2. 线框图按 TikZ 规范生成；响应图与频率特性图用 `Octave` 脚本生成。
3. 统一命名为 `3-7-*.pdf/.png/.svg`，并将 `handout.md` 全部改为引用本课媒体。

### 步骤 5：验证

1. 运行本课 `Octave` 绘图/校验脚本，确认例题数值与图示一致。
2. 如导出链可用，运行讲义审查/导出命令，检查 Markdown 与 PDF 渲染。
3. 检查是否还残留 `\label`、`\ref`、资源库直链、作者态说明语气。

## 验收标准

- `handout.md` 中不再出现 `\label`、`\ref`、`图~\ref`、`式~\ref`。
- 本课正式插图均来自 `course-content/authoring/lessons/3-7/media/processed/`。
- `PI` 与滞后校正已成为独立章节，且各自包含时域指标设计与频域指标设计说明/例题。
- 终值定理与静态误差系数法都对“多项式输入稳态误差”给出成套求解。
- 时域响应处有时域图，频率特性处有 Bode 或等效频域图，结构分析处有规范框图。
