# 2026-03-30 `3-3 / 3-4` 中度重构执行计划

## 目标

- 在不推翻模块3总骨架的前提下，对 `3-3 / 3-4` 做中度重构：
  - `3-3` 压实为“完整根轨迹法则体系 + 广义根轨迹总视角”，并把开环增益根轨迹讲成广义根轨迹的一个特例；
  - `3-4` 承担“法则应用 + 关键节点读图 + 参数窗口验证 + 对象化验证”。

## 约束

- 不改 `3-3 ~ 3-6` 编号与模块3总体链条。
- 不把 `3-3` 重新做成旧式手工绘图与机械法则堆砌课，但必须保留完整法则体系。
- 不把 `3-4` 做成旧例题串讲或完整设计课。
- 资源采用必须写清必融入 / 可选融入 / 排除。

## 执行步骤

1. 在 `course-content/syllabus-refactor/` 内统一确认 `3-3 / 3-4` 当前口径冲突点。
2. 更新 `course-content/syllabus-refactor/decisions.md`，新增 2026-03-30 中度重构决策。
3. 更新 `course-content/syllabus-refactor/unit-design-details/module3.md`：
   - 重写 `3-3 / 3-4` 边界表项；
   - 重写 `3-3 / 3-4` 的详细设计；
   - 补资源融入评审单；
   - 同步讲义 / 教案 / 互动页 / 媒体接口。
4. 更新 `course-content/syllabus-refactor/blueprint.md` 与 `module-skeletons.md` 的单元任务描述。
5. 更新 `course-content/syllabus-refactor/main.md`，把最新模块3确认摘要写入总览。
6. 复查五份文档中 `3-3 / 3-4` 的表述是否一致。

## 验收标准

- 五份真值文档对 `3-3 / 3-4` 的角色分工一致。
- `3-3` 明确承担完整法则体系、广义根轨迹和 `0°/180°` 根轨迹主线，并至少含一个非增益参数示例。
- `3-4` 明确承接 `13/15.1` 与船舶根轨迹案例的主要落点。
- 资源采用单可追溯到具体路径与采用级别。
