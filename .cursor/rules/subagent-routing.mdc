---
description: Cursor 子代理模型路由（与 Codex TOML 分离）
alwaysApply: true
---

# Cursor 子代理路由

在 Cursor 中派发 `Task` 子代理时：

1. 先读 `.agents/skills/cursor-subagent-routing/SKILL.md`，按表选 `subagent_type` + `model`。
2. 不要使用 `.codex/agents/*.toml` 的模型名或独立 reasoning 档位；Cursor 无独立 effort 参数，强度写在 model slug 里。
3. 新开子代理默认禁止 `inherit`（仅 resume 续跑可 `inherit`）。
4. 禁止把所有子代理都派成 Composer 同构集群。
5. 用户显式点名模型时，以用户点名为准（须在当前允许列表内）。
6. 写任务串行；探索与审查可并行。
7. 不要主动调用 Claude 系列模型。审查默认 `gpt-5.6-sol-medium`。
